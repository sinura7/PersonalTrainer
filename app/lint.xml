<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Adaptive-icon XML is only valid in mipmap-anydpi-v26. AAPT
         ignores the same file in mipmap-anydpi. minSdk is 26, so the
         v26 qualifier is redundant but required. -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/res/mipmap-anydpi-v26" />
    </issue>
</lint>
