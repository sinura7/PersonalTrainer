package com.sinura.personaltrainer.domain

/** Caption so Body heat is not read as the History calendar ramp. */
object BodyHeatCopy {
    const val LEGEND_CAPTION = "Colour is muscle load in this window, not calendar sets."
}
