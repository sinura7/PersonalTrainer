package com.sinura.personaltrainer.ui.settings

/**
 * Focused Settings destinations. The tab itself stays Settings (ADR-014).
 * None of these is a sixth bottom tab.
 */
enum class SettingsPage {
    HOME,
    DISPLAY,
    REMINDERS,
    GENERATOR,
    REST,
    BODYWEIGHT,
    ACCOUNT,
    BACKUP,
    PLAN,
    DIAGNOSTICS,
    ABOUT,
    LOG,
    FOUNDATION,
}
