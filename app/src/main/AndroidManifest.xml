<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Held briefly by RestTimerAlarmReceiver so the completion cue actually plays when the
         alarm wakes a sleeping device. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Rest completion. Never USE_EXACT_ALARM — ADR-012. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PersonalTrainerApp"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalTrainer"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PersonalTrainer"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".timer.RestLockActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:theme="@style/Theme.PersonalTrainer"
            android:turnScreenOn="true" />

        <service
            android:name=".timer.RestTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Gym rest countdown that must stay accurate while the app is backgrounded during a workout" />
        </service>

        <!-- Fires the rest-complete alert even in Doze with the screen off.
             Exact delivery needs SCHEDULE_EXACT_ALARM; without the grant the
             scheduler takes an inexact path and does not call that reliable. -->
        <receiver
            android:name=".timer.RestTimerAlarmReceiver"
            android:exported="false" />

        <!-- Grant changes to SCHEDULE_EXACT_ALARM: the system force-stops the
             app on revoke and cancels its alarms; this is the documented hook
             to re-arm a persisted rest when the grant comes back. -->
        <receiver
            android:name=".timer.ExactAlarmStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".reminder.ReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminder.WorkoutAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminder.ReminderRebuildReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <!-- Intent.ACTION_TIME_CHANGED's string VALUE is TIME_SET; declaring the
                     constant's NAME here matched no real broadcast, so clock changes never
                     rebuilt reminders. -->
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
