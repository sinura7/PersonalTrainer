<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Temper Debug only. Gym-floor must never ship this permission. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <!-- Where Android answers an install session (audit RM-1). Not exported: only the
             session's own status reaches it, so no other app can hand it a screen to open. -->
        <activity
            android:name="com.sinura.personaltrainer.update.DebugInstallStatusActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.debugupdate"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/debug_update_paths" />
        </provider>
    </application>
</manifest>
