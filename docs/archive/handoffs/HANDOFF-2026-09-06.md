# Handoff — 6 September 2026, reliability stabilization (Fable)

This is the verification record for the reliability backlog handed over on
6 September 2026 (findings R01–R19, reviewed against trunk at
`22161c54f5b56d7b86294dc7f9490bb1a79f14fe`). It follows
[HANDOFF-2026-09-03.md](../archive/handoffs/HANDOFF-2026-09-03.md). Each batch below records
what changed, what was executed, and what is still owed. The current
program stays [FOUNDATION_PROGRAM.md](FOUNDATION_PROGRAM.md); this file
does not change any signed decision in [architecture/](architecture/README.md).

## 1. Baseline

| Thing | State |
|---|---|
| Starting commit | `22161c54f5b56d7b86294dc7f9490bb1a79f14fe` (`trunk`), clean working tree. Identical to the reviewed baseline, so every finding was checked against the exact reviewed source. |
| Work branch | `claude/read-zip-files-suok0y` (local commits only; nothing pushed, merged, tagged, or published) |
| Host | Linux 6.18 (x86_64), 4 CPU, 15 GB RAM, OpenJDK 21.0.10, Python 3.11.15, Gradle 8.14.3 system install, wrapper pinned to 8.11.1 |
| Android SDK | **absent**. `dl.google.com` and `maven.google.com` are refused by the egress policy (HTTP 403 on CONNECT), so neither the SDK nor the Android Gradle Plugin can be fetched. No `./gradlew` task can configure here. |
| Maven Central | reachable; the seven lane jars plus Gson were fetched into `build/test-jars` (`kotlin-compiler-embeddable` 2.0.21, `kotlin-stdlib` 2.0.21, `kotlinx-coroutines-core-jvm` 1.10.2, `junit` 4.13.2, `hamcrest-core` 1.3, `trove4j`, `annotations` 13.0, `gson` 2.11.0) |
| Executable gate here | `PT_JARS=build/test-jars tools/preflight.sh` — the 20 exit-code checkers, the four summary checkers, the parse-level syntax check, and the domain + backup JVM lanes. Robolectric, lint, `assembleDebug`, and every device lane are **not runnable** in this environment. |
| Emulator / device | none |

### 1.1 What the baseline preflight found on the untouched checkout

Run 1 (`preflight-baseline.log`): every exit-code checker up to
`check-still-pack.py` passed, then the gate died at
`tools/test_policy_move.py` with a `SyntaxError` — line 92 used a
backslash inside an f-string expression, which Python only allows from
3.12. On Python 3.11 (this host; also common on the owner's Windows setup)
nothing after that line ever ran, so "preflight green" was not
reachable on the reviewed commit. Fixed by hoisting the expression out
of the f-string (R19, tooling portability).

Run 2 (`preflight-baseline2.log`, after that fix): all 20 exit-code
checkers, the four summary checkers, and the syntax check passed. The
JVM lane then failed to compile its tests:

- `domain/GymFloorCopyTest.kt` imports `ui.library.LibraryTags` and
  `ui.navigation.LiveBarCopy` / `LiveBarKind`, which the lane never
  compiles. The test lives under `domain/` but is not a domain test.
- `domain/OccurrenceGeneratorTest.kt` uses
  `testutil.FrozenTime` (added by the J4 TimePort packet), which the
  lane's test list does not include.

Both are drift between recent packets and the no-SDK lane described in
`docs/DEVELOPMENT.md`. The "384 → 467 executed tests" figure in that file
is therefore historical: on the reviewed commit the lane executed zero
tests. See §3 for the fix and the current executed count.

## 2. Restore state machine (design before code, R01–R03)

The restore journal lives in `files/restore-journal/` as two files:
`incoming.json` (the decoded document to apply) and `state.json`
(`RestoreJournalRecord`). Room replacement is one SQLite transaction;
preferences (DataStore) and the bodyweight/block history tables are
written afterwards and are **not** atomic with it. Recovery bridges
those stores; it never pretends they commit together.

### 2.1 Phases

| Phase | Durable meaning | Room holds | Still owed | Workout start |
|---|---|---|---|---|
| (none) | No restore in flight. | consistent | nothing | allowed |
| `staged` | `incoming.json` + `state.json` written. Nothing touched. | old | everything | refused |
| `wiping` | Marked immediately before the Room transaction. | old **or** incoming; the witness decides | prefs, history tables, reconcile, reminders, cleanup | refused |
| `room` | Room transaction committed. | incoming | prefs + history tables (required), reconcile (required, idempotent), reminders (best effort), cleanup | allowed |
| `prefs` | Preferences and history tables applied. | incoming | reconcile, reminders, cleanup | allowed |
| `done` (new) | All required work finished. | incoming | delete `incoming.json`, delete `state.json` | allowed |

`staged` and `wiping` are the only phases in which a start could land on
tables about to be replaced, so they are the only ones that refuse a
start (`BackupRepository.restoreBlocksStart()`). In `room`/`prefs`/`done`
the training data is final and a workout may begin; recovery's remaining
work touches DataStore, the history tables, and the catalog under the
maintenance lock, which every start also takes.

### 2.2 Commit witness (R01)

The old witness was five row counts plus the lowest session id. Two
documents with identical counts and ids but different weights, notes,
activities, or preferences produced the same string, and recovery in
`wiping` could therefore mistake the original database for the incoming
one and apply the incoming preferences over untouched history.

`RestoreWitness` replaces it. It is a versioned SHA-256 (`w1:` prefix)
over a canonical JSON projection of exactly the tables `replaceRoom`
writes: exercises, exercise credits, routines, routine exercises,
finished sessions with their session exercises and set logs, schedule
slots, non-live activities and templates (nested blocks, sets, intervals,
muscles), schedule rules, occurrences, missed-work decisions, reminder
deliveries, and goals. Every list is sorted by a stable key at every
level, scalars are rendered as strings, nulls as JSON null, and the
projection carries no `exportedAt`, `version`, `app`, or preferences.
The same normalisation the mappers apply on write (null block fields
defaulting to `EXTERNAL`/`OTHER`/`false`/`0`) is applied on the document
side, so `witness(document) == witness(Room after replaceRoom(document))`
is a tested invariant. Unfinished sessions and live activities are
excluded on both sides — a backup never carries them and a workout
started after a crash must not make recovery undecidable.

The journal record stores `beforeWitness` (Room at stage time) and
`afterWitness` (the document). The legacy count fingerprints are kept so
a journal written by the previous build is still resolved on the old
rule.

### 2.3 Recovery decision table

| Journal phase | Decision |
|---|---|
| `staged` | Close the journal. Nothing changed. |
| `wiping` | `current = witness(Room)`. If `before == after` the content is the same either way: finish. Else if `current == after`: committed, finish. Else if `current == before`: rolled back, close the journal, nothing changed. Else (witness absent → legacy fingerprint rule; witness present but matching neither): **unresolved** — close the journal without applying preferences, record a durable recovery note naming the safety copy, never apply incoming preferences over an unproven database. |
| `room` | Apply preferences from `incoming.json`; on success mark `prefs`. If `incoming.json` is missing (only possible for a journal written by the old cleanup order or by external deletion): reconcile, close, record a note that settings could not be applied. |
| `prefs` | Reconcile the catalog, rebuild reminders (best effort), mark `done`, close. Does not need `incoming.json`. |
| `done` | Delete whatever cleanup left behind. Already finished. |
| unknown | Close the journal. |

Preferences failure after `room` (R02): the journal **stays** at `room`,
`incoming.json` is kept, the commit returns
`RestoreResult(preferencesRestored = false, settingsPending = true)`, and
Settings says the training data is in and settings will be finished on
the next launch or on **Finish restore**. Every later launch and every
Settings open retries; the retry is idempotent because
`setRestoredPreferences` writes every key unconditionally and replaces
the history tables wholesale. `CancellationException` is rethrown, never
converted into "settings could not be applied".

Cleanup interruption (R03): `done` is marked before the two deletes, so
a crash between them leaves a journal that recovery sweeps in one step.
Workout start is never blocked by `room`, `prefs`, or `done`.

### 2.4 Fault matrix

| # | Interruption point | Durable state | Recovery result |
|---|---|---|---|
| 1 | Before `stage()` | nothing | none |
| 2 | After `incoming.json`, before `state.json` | orphan incoming | swept on next `read()` |
| 3 | After `state.json` (`staged`) | staged | closed; nothing changed |
| 4 | After `mark(wiping)`, before the transaction | wiping, Room old | `current == before` → closed; nothing changed |
| 5 | Mid-transaction | SQLite rolls back | as 4 |
| 6 | Transaction committed, before `mark(room)` | wiping, Room incoming | `current == after` → prefs, reconcile, reminders, done, closed |
| 7 | After `mark(room)`, before preferences | room | prefs from incoming → continue |
| 8 | DataStore written, history tables not | room | prefs re-applied (idempotent) → continue |
| 9 | Preferences applied, before `mark(prefs)` | room | prefs re-applied → continue |
| 10 | After `mark(prefs)`, before reconcile | prefs | reconcile, reminders, done, closed |
| 11 | After reconcile, before `mark(done)` | prefs | reconcile again (idempotent) → continue |
| 12 | After `mark(done)`, before deleting incoming | done | both files deleted |
| 13 | After deleting incoming, before deleting state | done | state deleted (this was the permanent block in R03) |
| 14 | Old-build journal at `room` with incoming gone | room, no input | reconcile, closed, durable note "settings not applied" |
| 15 | Same counts/ids, different content (R01) | wiping | decided by witness, not counts |
| 16 | Coroutine cancellation at any point | journal as left | resolved on next launch; the cancellation propagates |
| 17 | Preferences write throws (disk) | room | journal kept; result says settings pending; retried on launch/Settings |
| 18 | Reconcile throws | prefs | journal kept; retried on launch; `seedCatalog` also reconciles because `catalogVersion` is 0 |
| 19 | Reminder rebuild throws | prefs → done | best effort; the boot receiver rebuilds later |

### 2.5 Export/import size contract (R04)

Two budgets, both in `BackupScaleBudget`:

- `DOCUMENT_BYTES_MAX` = 8 MiB of **UTF-8** plaintext JSON (the signed
  P3.7/P8.5 ceiling; `ENCODED_BYTES_MAX` keeps that name as an alias).
- `RAW_FILE_BYTES_MAX` = 12 MiB read from a picked file or a Drive
  download (`IMPORT_BYTES_MAX` keeps that name for the bounded reads).
  A protected envelope of an 8 MiB document is at most
  base64(8 MiB + 16-byte tag) plus a fixed header, about 11.2 MB, so the
  largest document the app will export still round-trips.

Every export path — plaintext file, protected file, Drive upload,
protected safety-copy export — measures the bytes it is about to write
and refuses with `TOO_BIG_TO_EXPORT` **before** writing or reporting
success. Import checks the raw file against `RAW_FILE_BYTES_MAX` and the
decoded document against `DOCUMENT_BYTES_MAX`. Sizes are UTF-8 byte
counts, never `String.length`.

Memory cost of the raised raw ceiling: about 70 MB transient at the
worst case (12 MiB raw bytes, its String, the decoded ciphertext, the
plaintext bytes and String) versus about 48 MB before. Acceptable on the
supported range; recorded here so it is a decision, not an accident.

## 3. Batch records

Batch records are appended below as each batch lands.

### 3.1 Batch B — restore/export integrity (R01–R04, plus the R11 half that shares these files)

| Field | Record |
|---|---|
| Findings | **R01 fixed with evidence** (content witness replaces the count fingerprint; activities are witnessed; same-count/same-id/different-content restores resolve correctly on both sides of the commit). **R02 fixed with evidence** (preferences failure keeps the `room` journal and its input; commit returns `settingsPending`; launch and Settings retry idempotently; cancellation propagates). **R03 fixed with evidence** (`done` phase marked before cleanup; a journal whose input is gone resolves to a named outcome; `room`/`prefs`/`done` no longer refuse workout starts). **R04 fixed with evidence** (UTF-8 document budget 8 MiB, raw-file budget 12 MiB, every export path checks the exact bytes before writing; import checks both). **R11 partially fixed**: `signIn` now runs on the IO dispatcher and the protected safety-copy export derives its key on the compute dispatcher; the remaining R11 items are tracked in Batch D. |
| Files (main) | `data/backup/RestoreWitness.kt` (new), `RestoreJournal.kt`, `RestoreJournalStore.kt`, `BackupScaleBudget.kt`, `data/repository/BackupRepository.kt`, `LocalBackupRepository.kt`, `PreferencesRepository.kt` (durable recovery note), `WorkoutRepository.kt` (`restoreBlocksStart`), `ActivityRepository.kt` (same refusal for confirms), `AppContainer.kt`, `ui/settings/SettingsViewModel.kt`, `ui/settings/SettingsScreen.kt` (Finish restore, recovery note) |
| Files (test) | `data/backup/RestoreWitnessTest.kt` (new), `BackupScaleBudgetTest.kt` (new), `RestoreJournalStoreTest.kt`, `BackupScaleEncodeTest.kt` (bytes, not chars), `data/local/RestoreRecoveryTest.kt` (new, Robolectric fault matrix), `RestorePrepareTest.kt`, `FakeAppDependencies.kt` (failing-DataStore seam, journal dir) |
| Files (tooling) | `tools/test_policy_move.py` (Python 3.11), `tools/run-domain-tests.sh` (FrozenTime, RestoreWitness), `app/src/test/.../ui/GymFloorCopyTest.kt` (moved out of `domain/`) |
| Environment | as §1: no SDK, JVM lane only |
| Commands run | `PT_STATIC_ONLY=1 sh tools/preflight.sh` (repeated until clean), `PT_JARS=build/test-jars tools/run-domain-tests.sh build/test-jars`, `PT_JARS=build/test-jars tools/preflight.sh` |
| Results | JVM lane: **166 classes, 1146 tests, 0 failures** (baseline on the reviewed commit: could not compile; after the lane fixes and before this batch: 164 classes, 1126 tests). Static gate: every checker at baseline, `required_args_mixed` held at 181 by writing new calls fully named. Full preflight result is in §3.1.1. |
| Regression proof | `RestoreWitnessTest.sameCountsAndIdsWithDifferentContentAreToldApart` pins the R01 collision on the legacy fingerprint and its absence on the witness. `BackupScaleBudgetTest.theLargestAllowedDocumentStillImportsAfterProtection` wraps an exact 8 MiB two-byte-character document and proves the envelope fits the raw budget and opens. `RestoreJournalStoreTest.doneSurvivesLosingItsIncomingCopy` is the R03 crash between the two deletes. `RestoreRecoveryTest` (Robolectric) covers fault-matrix rows 3, 4, 6, 13, 14, 17 and the witness-through-Room invariant; it is written and static-checked but **not executed here** (needs Gradle). |
| Data preservation | No schema change. Room v4 untouched. Backup document and envelope formats unchanged; import accepts everything it accepted before plus larger protected files. A journal written by the previous build (no witnesses) is resolved on the old count rule. IDs are never rewritten. |
| Runtime evidence | none — no device or emulator |
| Limitations | Robolectric and instrumented lanes unexecuted; the `Finish restore` row and the recovery-note banner in Settings are unrendered. Cancellation mid-transaction is reasoned from Room's `withTransaction` contract, not exercised. |
| Remaining | Run `./gradlew testDebugUnitTest` on a Gradle-capable machine (or let CI on a pushed branch do it) to execute `RestoreRecoveryTest` and `RestorePrepareTest`; render Settings on a device for the two new rows. |

#### 3.1.1 Full preflight for Batch B

`PT_JARS=build/test-jars tools/preflight.sh` on the Batch B tree: every
exit-code checker passed; `check-named-args` 0 mismatches over 357 main
and 253 test files; `check-when-exhaustive` 0 non-exhaustive, 47 skipped
(baseline 47); `check-unused-imports` 0; syntax check clean; JVM lanes
**166 classes, 1146 tests, 0 failures**; `preflight: OK`. Two ratchets
had to be held rather than raised: a new mixed positional/named call and
two `when` blocks the checker could not type were rewritten so the skip
counts stayed at their baselines.

### 3.2 Batch C — distribution continuity (R05) and candidate verification (R15)

| Field | Record |
|---|---|
| Findings | **R05 confirmed; mechanism fixed, closure blocked by a named dependency.** `debug-live.yml` restores one debug distribution keystore from the four `DEBUG_KEYSTORE_*` secrets, signs with it through a `debugLive` signing config that `app/build.gradle.kts` applies to the debug build type only when `debug-keystore.properties` exists, prints the signer's SHA-256 digest (non-secret), fails the drop when the `DEBUG_CERT_SHA256` repository variable is set and does not match, removes the material afterwards, and marks a drop without the secrets **THROWAWAY SIGNER** instead of implying it can update anything. A developer machine without the file keeps AGP's default debug keystore. Closing the finding needs owner-held input: the keystore that should sign Temper Debug (created once per SETUP.md §6), the four secrets and the digest variable. The reviewed source never restored any keystore and referenced no secret, so every past automated drop was signed by a key that runner generated. Which key signed the Temper Debug currently on the owner's phone cannot be determined from the repository. **R15 partially addressed**: this document is the fresh verification record for the JVM lane and the static gate on every batch; build, lint, Robolectric, emulator and device evidence stay pending because none can run here. Pushing the work branch triggers `ci.yml` (it runs on `claude/**`), which would produce the build/lint/full-unit-suite evidence; that push needs the owner's approval. |
| Also | `DriveAuthClient` names the installed package (`activity.packageName`) in its DEVELOPER_ERROR message instead of the release package (R19 item). `SETUP.md` §4 now creates one OAuth client per package (`com.sinura.personaltrainer.debug` for Temper Debug) and reads the debug SHA-1 off the installed APK; §6 documents the stable signer and the one-time move of an installed Temper Debug onto it (export, uninstall, install, import). `docs/RECOVERY.md` §4 says the same. `debug-keystore.properties.example` is the template; `.gitignore` covers the file and `debug-signing/`. |
| Files | `.github/workflows/debug-live.yml`, `app/build.gradle.kts`, `.gitignore`, `debug-keystore.properties.example`, `SETUP.md`, `docs/RECOVERY.md`, `app/src/main/java/com/sinura/personaltrainer/data/backup/DriveAuthClient.kt` |
| Commands | `PT_STATIC_ONLY=1 sh tools/preflight.sh` → `preflight: OK (static only)`; the workflow file parses as YAML; the Gradle script change is not executable here (no SDK). |
| Data preservation | No app data is touched. Signing changes affect only which drops can update which install; the transition instructions preserve history through export/import. |
| Limitations | The Gradle signing block and the workflow steps are unexecuted; the first real run with the secrets set is the proof (`apksigner` digest in the log, and an in-place update on the phone). |
| Remaining | Owner: create/choose the debug keystore, set the four secrets and `DEBUG_CERT_SHA256`, register the SHA-1 for the `.debug` package, run one drop and update in place. |

### 3.3 Batch D — daily correctness (R06–R12), with R13 and R14 integrated early

Three findings (R12, R13, R14) were implemented in parallel worktrees on top of
the Batch B commit and cherry-picked onto the work branch before the rest of
this batch, so their files were not edited twice: `86fd33e` (R12),
`ad6108b` (R13), `e7e8073` (R14). Their records are in §3.4. The merged
tree passed the static gate and the JVM lane (169 classes, 1168 tests)
before Batch D's own edits began.

| Field | Record |
|---|---|
| Findings | **R06 fixed with evidence.** `ActivityRepository.afterCommit` runs the reminder cleanup after the transaction and never lets it change the answer: a throw is logged and the `Accepted` write still returns; cancellation still propagates. `LiveSessionBarViewModel.finishFromBar`'s activity branch is wrapped the same way as the workout branch (it was an uncaught coroutine failure). No retry queue and no secondary warning: the verifier established that cleanup is idempotent and self-healing (`ReminderPolicy.decide` returns STALE for a non-planned occurrence; Skip and Move refuse a DONE row), so the honest outcome is "saved, cleanup logged", not a warning about a reminder that will be dropped at fire time anyway. **R07 fixed with evidence** (the verifier narrowed it: the Records section, summaries and totals already refreshed on edit through `FinishedWorkGeneration`; the horizon readout and the past-block reviews did not). `WorkoutRepository.observeFinishedWorkRevision()` exposes that fingerprint as an opaque token; `HorizonProgressKey` carries it instead of session count and newest id, and the past-block inputs carry it plus the whole bodyweight log instead of the last weigh-in. Left open, named: `PlanViewModel.completedBlockSessions` is keyed only on the block (the verifier's adjacent gap); it is not a History surface and is not touched here. **R08 fixed with evidence.** Product definition stated in code: History's Records are lifetime standing bests, one per lift, newest first, capped at three, independent of the horizon; the readout's PRs number stays the period-scoped count. Two read-only queries (`WorkoutDao.finishedWorkingSetRecords`, `ActivityDao.observeCompletedStrengthSetRecords`) project one row per working set with the lift's name and load type; `standingRecords` in `domain/StandingRecords.kt` ranks them and `prSummary` now delegates to it. The 32-day graph reads are gone from History. **R09 fixed with evidence.** `SavedStateComposerDraft` mirrors title, day, strength lines and cardio lines into `SavedStateHandle` on every change (one Bundle per line, every field the strength block needs, so a lift deleted from the library in between still saves as it was); the draft is cleared only by an accepted save or a deliberate leave. Live cardio mirrors type, indoor and the typed distance the same way and prefers them over the row's block after recreation. **R10 fixed with evidence.** `ActivityDetailUiState.failed` and `LiveCardioUiState.failed` are distinct from `missing`; both screens render a retry (`ActivityDetailTags.RETRY`, `CardioTags.RETRY`) and a way out; a failed live-cardio load no longer takes the process down; the timer-row reads in the tick loop are guarded. `ActivityRepository` gained `observeLiveHealth`, `observeCompletedSummariesHealth`, `observeRecordSetsHealth`; `observeLive` is now the values-only view of the health flow. History folds the activity summaries and both record reads as sidecars: a failed one marks the page stale instead of silently reading as "no activities" or "no records". **R11 fixed** (residue from Batch B): the safety-copy export's size check runs inside the compute hop; the Drive account lookup logs its failure and no longer swallows cancellation. The dispatcher-contract test the verifier asked for is **deferred with rationale**: `DriveAuthClient` is a final Play Services wrapper with no seam in the test graph, so a test could only pin `withContext` by reading the source; R13's `DriveHttp` seam covers the REST side. **R12 fixed with evidence** (worktree, `86fd33e`): both SharedPreferences persistences return `commit()`'s result; the rest controller arms only on a true save, publishes `FAILED` and stays in sync otherwise, retries a false clear once, and its IO scope carries an exception handler; the rest floor shows "Rest may not survive leaving the app." while a running rest's row is off disk. |
| Files (main) | `domain/StandingRecords.kt` (new), `domain/HistorySummaries.kt`, `domain/DataHealth.kt` (copy), `data/local/dao/RecordSetRow.kt` (new), `data/local/dao/WorkoutDao.kt`, `data/local/dao/ActivityDao.kt`, `data/mapper/RecordSetMappers.kt` (new), `data/repository/WorkoutRepository.kt`, `data/repository/ActivityRepository.kt`, `data/repository/BackupRepository.kt`, `ui/history/HistoryViewModel.kt`, `ui/activity/ActivityDetailViewModel.kt`, `ui/activity/ActivityDetailScreen.kt`, `ui/activity/LiveCardioViewModel.kt`, `ui/activity/LiveCardioScreen.kt`, `ui/activity/SavedStateComposerDraft.kt` (new), `ui/activity/ActivityComposerViewModel.kt`, `ui/activity/ActivityComposerScreen.kt`, `ui/navigation/LiveSessionBarViewModel.kt`, `ui/settings/SettingsViewModel.kt` |
| Files (test) | `domain/StandingRecordsTest.kt` (new, JVM lane), `testutil/FailingActivityDao.kt` (new), `FakeAppDependencies.kt` (`activityDaoDecorator`, `occurrenceCleanup` seams), `data/repository/ActivityRepositoryTest.kt`, `data/repository/WorkoutRepositoryInsightsQueriesTest.kt`, `ui/history/HistoryViewModelTest.kt`, `ui/activity/ActivityComposerViewModelTest.kt`, `ui/activity/LiveCardioViewModelTest.kt`, `ui/activity/ActivityDetailViewModelTest.kt`, `ui/activity/LiveCardioChromeTest.kt` |
| Environment | as §1: no SDK, JVM lane only |
| Commands run | `PT_STATIC_ONLY=1 PT_JARS=build/test-jars sh tools/preflight.sh` → `preflight: OK (static only)`; `PT_JARS=build/test-jars sh tools/run-domain-tests.sh build/test-jars` → `OK (1175 tests)` over 170 classes; `PT_JARS=build/test-jars sh tools/syntax-check.sh app/src/test/java` → `NO SYNTAX ERRORS` (the Robolectric files parse; they do not compile here). |
| Results | Static gate: every checker at baseline; `required_args_mixed` 180 (baseline 181, one below because the fake's `ActivityRepository` call is now fully named); `when_exhaustive` 47 (one new `when` was rewritten as a fold so the skip count did not grow); `state_members` 2; 0 unwired callbacks. JVM lane +7 tests, all of `StandingRecordsTest`. |
| Regression proof | Executed here: `StandingRecordsTest.anOlderBestOutlivesAWeakerRecentLift` is the R08 symptom (a 90-day-old 120×5 outranks a 2-day-old 100×5); `theNewestSetDecidesALiftsNameAndClass`, `aSessionGraphProjectsOnlyFinishedWorkingSets`, `prSummaryIsTheGraphShapedWayIn` pin the projection. Written, not executed (Robolectric): `ActivityRepositoryTest.aFailingReminderCleanupDoesNotUnsaveTheActivity` / `aFailingCleanupAfterALiveFinishStillFinishes` / `cleanupCancellationStillPropagatesAfterTheCommit` and `ActivityComposerViewModelTest.aFailingReminderCleanupStillSavesExactlyOnce` (R06: one row, receipt navigation, plan link kept); `WorkoutRepositoryInsightsQueriesTest.revisionMovesOnAFinishedEditNotOnALiveSet` and `HistoryViewModelTest.editingAFinishedSetRefreshesTheHorizonReadoutWithoutRelaunch` (R07: PRs go 0 → >0 after `updateSet` with no relaunch and no horizon change); `HistoryViewModelTest.recordsComeFromTheWholeLogNotTheLastMonth`, `recordSetsCarryTheLiftAndSkipLiveAndWarmUpWork`, `completedStrengthSetsFeedRecordsWithTheBlocksOwnSnapshot` (R08); `ActivityComposerViewModelTest.processRecreationRestoresTheTypedDraft` / `anAcceptedSaveClearsTheDraft` / `leavingClearsTheDraft`, `LiveCardioViewModelTest.recreationKeepsTheChangedInputs` (R09); `ActivityDetailViewModelTest.aFailedReadIsUnavailableNotMissingAndRetries`, `LiveCardioViewModelTest.aFailedReadIsUnavailableNotGoneAndRetries` (R10, via a delegating DAO whose one read throws on demand; recovery creates no second session). |
| Data preservation | No schema change; Room v4 untouched; the two new DAO methods are read-only queries over existing columns. No backup format change. Drafts live in `SavedStateHandle` only and are never exported. IDs are never rewritten; the activity block and set ids are still minted at save time. |
| Runtime evidence | none — no device or emulator |
| Limitations | Every Robolectric test above is unexecuted; the three new screen states (activity detail failed, live cardio failed, composer draft restored) are unrendered. Records and horizon behaviour on a real multi-year log are reasoned from the queries, not measured (that is R17). |
| Remaining | Run `./gradlew testDebugUnitTest` (or push and let `ci.yml` run) to execute the Robolectric tests; open History after editing a set and confirm the readout moves; kill the process under a half-typed composer and confirm the draft returns. `PlanViewModel.completedBlockSessions` keying is open. |

### 3.4 R13 and R14 — Drive listing pagination and durable crash diagnostics

Implemented in parallel worktrees on top of the Batch B commit and cherry-picked
as `ad6108b` (R13) and `e7e8073` (R14).

| Field | Record |
|---|---|
| Findings | **R13 fixed with evidence.** `DriveRestClient.listBackups` requests `nextPageToken`, follows it (URL-encoded) until the token is absent, blank or JSON-null, and stops at `MAX_LISTED_BACKUPS = 500` files or `MAX_LIST_PAGES = 20` pages, returning `DriveBackupListing(files, truncated)`; it is `suspend` and checks `ensureActive()` before every page so a refresh the owner backs out of stops at a page boundary. Settings says "Showing the newest N backup(s). Older ones exist in Drive." when the listing was cut, "No backups in Drive yet." when empty, "Found N backup(s)." otherwise; restore-by-id is untouched. The HTTP transport moved verbatim into a `DriveHttp` interface (`HttpUrlConnectionDriveHttp` is the old private `request` body), which is the seam the tests use. **R14 fixed with evidence.** `LastCrashStore` (pure JVM, `java.io` only) writes one redacted `DiagnosticEvent` as a line-based file under `files/diagnostics/last-crash.txt` with temp → flush → sync → rename, every string clipped to 200 chars with line breaks removed, frames capped at 16, and refuses files over 8 KB or of an unknown schema on read; it never throws on `load`. `PersonalTrainerApp.installDiagnosticCapture` loads the previous crash first and records it into the ring as kind `previous-crash`, then installs the handler; the handler is first-crash-wins, wrapped in `runCatching`, and always delegates to the previous handler. Settings → Diagnostics gains **Clear diagnostics**; the caption says the last crash is kept on the phone until cleared. The store is excluded from Auto Backup and D2D in both rules files, named in the threat model's store inventory, and pinned by `tools/check-backup-policy.py`. `docs/SUPPORT.md` documents retention and the `mapping.txt` / `retrace` workflow for a minified build (the release workflow already uploads the mapping). |
| Files | R13: `data/backup/DriveHttp.kt` (new), `DriveRestClient.kt`, `BackupDocument.kt` (`DriveBackupListing`), `data/repository/BackupRepository.kt`, `ui/settings/SettingsViewModel.kt`, `test/.../data/backup/DriveRestClientTest.kt` (new, 11 tests), `tools/run-domain-tests.sh`. R14: `diagnostics/LastCrashStore.kt` (new), `PersonalTrainerApp.kt`, `diagnostics/DiagnosticMetadata.kt`, `ui/settings/SettingsScreen.kt`, `res/xml/backup_rules.xml`, `res/xml/data_extraction_rules.xml`, `tools/check-backup-policy.py`, `docs/architecture/backup-threat-model.md`, `docs/PRIVACY.md`, `docs/SUPPORT.md`, `docs/DEVELOPMENT.md`, `test/.../diagnostics/LastCrashStoreTest.kt` (new, 8 tests), `tools/run-domain-tests.sh`. |
| Commands run | Each worktree: `PT_STATIC_ONLY=1 sh tools/preflight.sh` → OK, ratchets at baseline; JVM lane → `OK (1157 tests)` over 167 (R13) and 168 (R14) classes. After both cherry-picks on the work branch: static gate OK, JVM lane **169 classes, 1168 tests**. |
| Regression proof | Executed here (JVM lane): `DriveRestClientTest` — 50/50/1 pages in order with tokens on requests 2 and 3; a short first page with a base64-padded token that must travel encoded; an empty page with a token and a page with no `files` key; 21 token-bearing pages → truncated after exactly 20 requests; a failing second page throws the same `BackupException` after 2 requests; the first request carries `nextPageToken` in `fields`, no `pageToken`, `pageSize=50`, `orderBy` and the folder query; the 500-file cap (550 offered → 500 kept, truncated), exactly 500 with no token → complete, a blank/null token ends the listing, a cancelled scope stops after one request. `LastCrashStoreTest` — round trip; missing file; five corrupt shapes → null without throwing; overwrite; clear; no temp file left; oversized fields and 40 frames clipped and a smuggled `\nid=` neutralised; every `DiagnosticCanaries.ALL` string placed in message, cause, foreign frame and app frame is absent from the written file. |
| Data preservation | No schema change; no backup format change (the listing type is in-memory); the crash file is app-private and excluded from every backup channel. |
| Limitations | Settings' Clear diagnostics button and the truncated-listing status line are unrendered here; the crash handler path (a real uncaught exception in a debug build, restart, share) is untested on a device, as is a minified build's `retrace`. |
| Remaining | Device: crash a debug build, relaunch, share diagnostics, confirm the `previous-crash` event and no canaries; one Drive account with more than 50 backups to see the paging live. |

### 3.5 Batch E — accessibility evidence (R16) and docs/tooling reconciliation (R19)

| Field | Record |
|---|---|
| Findings | **R16 addressed in code, closure needs an emulator.** `ui/ProductionScreensPassInstrumentedTest.kt` (androidTest) mounts the real `HistoryScreen`, `ActivityComposerScreen`, `LiveCardioScreen`, `ActivityDetailScreen` and `HomeScreen` with their real ViewModels against the real `AppContainer` and Room, seeded with a populated fixture (a finished strength session, three backdated mixed activities one month apart carrying a bench heavier than the session's, so History has month groups, calendar marks, cardio totals and a lifetime record older than the insight window). Seven tests across 360 / 412 / 600 dp and font scales 1.0 / 1.3 / 2.0: History chips, readout, Records header and a clickable session row reachable by scrolling the real list at 360/2.0; horizon switching at 412/1.3; every chip named and clickable at 600/1.0; the composer at 360/2.0 with the IME open (title typed) keeping Save and Cancel displayed and Choose lift reachable, with the typed draft reflected in state; live cardio at 360/2.0 keeping the elapsed readout and the three dock actions; the activity receipt at 600/1.0 keeping Done named and clickable; Home at 412/1.3 naming the last-session tile and offering a start affordance. Fixtures are prefixed and removed afterwards (a leftover sweep runs first). `ActivityRepository.deleteCompleted` was added for that cleanup; no screen offers it (it is a row in the convergence matrix). The isolated-control tests stay. Physical TalkBack traversal and announcement checks remain a manual gate and are listed as such in the test's KDoc. **R19 fixed with evidence** for the items the review named: `tools/check-named-args.py`, `check-when-exhaustive.py` and `check-unused-imports.py` read source with `encoding="utf-8"` (no other `open()` without an encoding remains in `tools/`); `docs/architecture/backup-threat-model.md` derives the KDF count from `BackupEnvelope.DEFAULT_ITERATIONS` (600,000) instead of stating 210,000; `docs/DEVELOPMENT.md` gains a "Verification and distribution lanes" table that names each lane, where it runs and what it gates, labels the historical 384→467 count by era and points at this record for current counts, states that the merge gate is the JVM gate and the phone gates the gym-floor release (agreeing with `.cursor/rules/owner-loop.mdc`, which was left as the authority), and notes Python 3.11+ (`tomllib`) with explicit UTF-8 so `PYTHONUTF8=1` is no longer needed on Windows; `docs/foundation-program/evidence/P3.4-restore-journal.md` is labelled historical where it describes the pre-`done` phase set (the refute pass flagged it). SETUP.md's OAuth package text was already corrected in Batch C. |
| Files | `app/src/androidTest/java/com/sinura/personaltrainer/ui/ProductionScreensPassInstrumentedTest.kt` (new), `data/repository/ActivityRepository.kt` (`deleteCompleted`), `tools/check-named-args.py`, `tools/check-when-exhaustive.py`, `tools/check-unused-imports.py`, `docs/DEVELOPMENT.md`, `docs/architecture/backup-threat-model.md`, `docs/foundation-program/evidence/P3.4-restore-journal.md` |
| Commands run | `PT_STATIC_ONLY=1 PT_JARS=build/test-jars sh tools/preflight.sh` → OK (the doc-authority checker passes on the edited docs); `PT_JARS=build/test-jars sh tools/syntax-check.sh app/src/androidTest/java` → `NO SYNTAX ERRORS`. The three checkers were re-run through preflight after the encoding change with identical counts. |
| Limitations | The instrumented class is written and parsed, not compiled against the Android SDK and not run; its scroll helper assumes the page's `LazyColumn` is the first scrollable in the tree, and the 600 dp mounts use `assertExists` because the reference emulator is 411 dp wide. TalkBack is not covered by any automated test. |
| Remaining | `./gradlew connectedDebugAndroidTest` on the `temper-tests-api29` profile against `com.sinura.personaltrainer.debug`; a physical TalkBack pass over the five screens; run the documented commands once on the Windows host to confirm the UTF-8 note. |

### 3.6 Batch F — History computation (R17) and convergence plan (R18)

| Field | Record |
|---|---|
| Findings | **R17 partially addressed; measurement deferred with rationale.** The one structural change that needs no numbers is made: `HistoryViewModel.catalog` is `shareIn`'d within `viewModelScope` (`WhileSubscribed(5_000)`, replay 1), so `uiState` and `horizonProgress` share one upstream subscription and one assembly per change instead of two. R07's revision keys already remove the recompute-on-proxy problem, and R08 replaced the 32-day graph reads with flat projections. The full-history `sessionsBetween(0, end)` read behind the horizon readout and the per-block reads behind past-block reviews are left as they are: the backlog asks for a measured before/after, and no device, emulator or Gradle exists here to measure with. The measurement plan (fixtures of 500 and 15,000 sets, Room `QueryCallback` counts per open and per horizon switch, a macrobenchmark for p50/p95) and the intended replacement (the readout reads the bounded `RecordSet` projection plus a SQL prior-record baseline) are written down in the convergence record §5 so the next session with a device starts from numbers. **R18 fixed with evidence (a plan, as the finding asks).** `docs/architecture/completed-training-convergence.md` records the capability matrix across strength sessions, backdated strength, cardio-only and mixed activities for chronology, totals, records, horizon readout, block reviews, exercise detail, PR badge, insights, Home, detail, edits, repeat, drafts, read faults, backup/restore, plan links and one-live; names the drift (reads that still query the strength store alone; edits the activity path never had); defines the `CompletedTraining` read contract assembled over the two existing stores with no new table; orders the consumers that move onto it; lists the use cases to extract from the three largest files with their existing tests; and specifies the parity test table. Indexed from `docs/architecture/README.md`. Room v4, IDs and `BackupJson` v3 are named as frozen. |
| Files | `ui/history/HistoryViewModel.kt` (`shareIn`), `docs/architecture/completed-training-convergence.md` (new), `docs/architecture/README.md` |
| Commands run | Static gate as §3.5; JVM lane result recorded in §3.7. |
| Limitations | No measurement of History on a large log; the share is reasoned from the flow graph, not timed. |
| Remaining | The §5 measurement run; then §2 step 1 of the convergence record. |

### 3.7 Gate results on the Batch E/F tree, and the adversarial pass

`PT_STATIC_ONLY=1 PT_JARS=build/test-jars sh tools/preflight.sh` → `preflight: OK (static only)`:
every exit-code checker passed; `check-named-args` 0 mismatches over 363 main and 257
test files; `required_args_mixed` 180 (baseline 181), `required_args_lambda` 46,
`when_exhaustive` 47, `state_members` 2, every design-token family at baseline; 0 unwired
callbacks; 0 unused imports; syntax check clean. `PT_JARS=build/test-jars sh
tools/run-domain-tests.sh build/test-jars` → **170 classes, 1175 tests, 0 failures**.

Every finding was also put through an independent verify-then-refute pass against the
source (one agent confirming each finding's mechanics and reachability, a second told to
refute it). The refuters upheld every defect at the reviewed baseline `22161c5` line for
line and, for each finding already fixed on this branch, recorded the fix as landed and
listed residue. Residue they named that is still open after this batch:

- R07: Robolectric tests for a past-block set edit, an older weigh-in correction, and
  delete/undo through the revision are not written (the horizon-readout edit test is);
  `PlanViewModel.completedBlockSessions` is still keyed on the block alone.
- R05: closure is owner-held (keystore, four secrets, `DEBUG_CERT_SHA256`); which key
  signed the Temper Debug on the phone cannot be read from the repository.
- R11: no dispatcher-contract test (no seam around `DriveAuthClient`).
- R15/R16: nothing with an SDK, an emulator or a device has run.

## 4. Delivery state

Everything above is **local and committed** on branch `claude/read-zip-files-suok0y`
in the session container. Nothing has been pushed, merged, published, deployed or
released, and no production configuration, credential or user data was touched. The
branch is eight commits on the reviewed baseline:

| Commit | Batch | Subject |
|---|---|---|
| `d8c5296` | A | Make the no-SDK gate runnable again on the reviewed commit. |
| `99c5198` | B | Restore recovery proves what Room holds; every export is importable. |
| `367f4b3` | C | Give Temper Debug drops one signing identity. |
| `86fd33e` | D (R12) | A rest row that never reached disk no longer arms its wakeup. |
| `ad6108b` | E (R13) | Drive listing follows page tokens; a cut listing says so. |
| `e7e8073` | E (R14) | Crash diagnostics survive the process that produced them. |
| `5bf52f5` | D | History keys on finished work; activity saves outlive their cleanup. |
| `f03d0b8` | E/F | Screens are tested as screens; docs and tools say what runs today. |

A `git bundle` of the branch was handed over alongside this record so the work survives
the session container.

**Pushed with the owner's approval on 2026-09-07** (`97fc05d` as the tip at the time).
`ci.yml` run 840 (`34078528654`) started on the push and both jobs — "Tests, lint, debug
build" and "Instrumented smoke (non-blocking)" — ended as `failure` within 2–4 seconds
with no steps recorded and job logs returning HTTP 404. That is not a result of this
branch: every run on every branch since 2026-09-05 05:24 UTC (trunk, `cursor/*`,
`debug-live/*`, thirteen runs) ends the same way, while run 833 at 05:07 UTC the same
day executed for eleven minutes and reached Gradle. The runner stopped being assigned to
the account at that point, which the workflow comment and DEVELOPMENT.md attribute to an
account-level billing/limits condition only the owner can change. The Gradle, lint,
Robolectric and emulator evidence therefore still stands pending; it can be produced on
any machine with the Android SDK (`./gradlew testDebugUnitTest assembleDebug lintDebug`),
or by CI once the account runs jobs again. Nothing was merged, tagged or released.

The pass completed after §3.7 was first written (38 agents, every finding verified and
then refuted; nothing errored). Residue it named that was fixed in the follow-up commit:
`docs/DEVELOPMENT.md`'s Owner-loop bullet still said the phone merges a packet (now: the
JVM gate merges, the phone gates the release, consistent with the lanes table); the
documented Python floor was 3.10 but `tools/checker_baseline.py` needs `tomllib`, so it is
3.11; "17-checker" wording in the CI paragraph (twenty run today; `ci.yml`'s own comment
still says 17 and is left alone as workflow text); `P3.6-envelope.md` labelled historical
where it states 210,000 iterations. Residue it named that stays open:

- R16: no production-screen pass yet for Settings, onboarding, routine editor, custom week,
  summary, session detail's Options → Delete dialog, exercise detail or Library; font 1.6
  is uncovered (1.3 was chosen); `AccessibilityMatrix` still says `automatedEvidence =
  true` for every page, which overstates the isolated-control tests; the isolated Settings
  case mounts a secondary button where production renders the primary one.
- R18: `HistoryViewModel.pastBlockReviews`, `horizonProgress` and
  `PlanViewModel.completedBlockSessions` still read the strength store alone (matrix row
  "Horizon readout / past-block reviews"); that is §2 step 1 of the convergence record.
- R15: `ci.yml`'s reviewed run and the 3 Sep emulator failures (golden 945×1731 vs 320×616
  from an AVD created without `--device`; the "Top set 202.5 kg × 5" journey assertion)
  are unchanged by this branch and wait on an emulator profile pinned to 1080×1920 / 420 dpi.

## 5. Self-test pass after the push (2026-09-07)

With no Android SDK reachable (every host serving AGP, AndroidX and the SDK is denied by
the session's egress policy, and the proxy notes forbid routing around a denial), the
branch's Android-dependent sources were checked the only other way available: three
independent compile-level reviews, each reading every symbol the changed code uses
against its declaration in the repository and the pinned library versions, and tracing
each new test's path through the current main code. Scope: the Android-side main sources
of Batch D; the Robolectric tests of Batches B, D and R12; the backup, timer, diagnostics
and instrumented-test sources; and `app/build.gradle.kts`'s signing block.

Findings, all fixed and pushed:

| Commit | Finding |
|---|---|
| `c46dff7` | `LiveCardioViewModel` guarded the block-value override on the type key alone, so an indoor toggle made without a type change was restored and then overwritten after recreation. Each input now checks its own key; `recreationKeepsIndoorWhenOnlyIndoorChanged` pins it. The mixed-argument ratchet was lowered to 180. |
| `034fad1` | `ProductionScreensPassInstrumentedTest` imported `onAllNodes`, which is a member of the Compose test rule rather than a package-level function; the import would fail the androidTest compilation. |
| `9113bdf` | `HistoryViewModelTest` called `sidecarFromHealth(DataHealth.Unavailable(...))`, whose type parameter cannot be inferred from `DataHealth<Nothing>`; that would have failed the whole test source set. The new live-cardio and activity-detail read-fault waits are time-boxed so a never-matching predicate fails instead of hanging. |

Everything else the reviews checked (Room aliases and nullability, `combine` arities,
`SavedStateHandle` calls, Compose parameters, delegation of the `@Dao` interface,
JUnit 4.13.2 `assertThrows`, the signing block's behaviour when the properties file is
absent, cancellation preservation, every checker rule) came back clean, and each traced
test was judged to pass. Both gates are green on the final tree: static gate OK with every
ratchet at baseline; JVM lane 170 classes, 1,175 tests, 0 failures. The Gradle, lint,
Robolectric and emulator runs remain the owner's, on an SDK machine or once CI has a
runner again.

## 6. Merge and drop (2026-09-07)

On the owner's instruction ("Merge and push to Obtainium for testing") the branch is
squash-merged into `trunk` through a pull request, per the Owner-loop rule, and the
`debug-live-2026-09-07` tag is pushed so `debug-live.yml` builds and publishes the Temper
Debug pre-release Obtainium watches. `debugLiveCode` moves from 21 (shipped in the
2026-09-03 pre-release) to 22 so Obtainium offers the update. Whether the tag becomes an
APK depends on GitHub assigning a runner, which it has not done for any job since
2026-09-05; the result is recorded below the tag once it is known. Without the four
`DEBUG_KEYSTORE_*` secrets the drop is signed by a throwaway key and is marked
THROWAWAY SIGNER: it installs beside, not over, the Temper Debug already on the phone.

**Outcome.** PR #168 squash-merged into `trunk` as `28f485f`. The session's git credential is
scoped to its own branch (a tag push and the branch delete both returned HTTP 403), so the
drop was triggered through the workflow's branch spelling: `debug-live/2026-09-07` was
created at `28f485f` through the owner's GitHub connection. Run 16 of `debug-live.yml`
(`34080847708`) queued and then ended as `failure` after four seconds with no runner
assigned, exactly like CI run 848 on the merge commit and every run since 2026-09-05 05:24
UTC. No APK was built, no pre-release was minted, and Obtainium still offers
`debug-live-2026-09-03` (code 21). Until the account runs jobs again, the drop is manual:
build `PersonalTrainer-1.0.0-debug.apk` with `./gradlew assembleDebug` on an SDK machine
at `28f485f`, create a pre-release for tag `debug-live-2026-09-07` on that commit, attach
the APK, and pull Obtainium on Temper Debug. The merged branch `claude/read-zip-files-suok0y`
could not be deleted from here (403) and is the leftover head the Owner-loop rule names.
