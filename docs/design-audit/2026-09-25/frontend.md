# Frontend findings

Part of the [25 September whole-app audit](AUDIT.md) (packet X6). Every row was written by the pass named in its ID and re-checked against the code at `1ad3b11`; P1/P2 rows carry the verifier's verdict (see [verification.md](verification.md)). Files are cited in backticks, relative to `app/src/main/java/com/sinura/personaltrainer` unless a path says otherwise. Severity: P1 protect data or a first-run blocker · P2 next correctness or polish work · P3 worth doing, not urgent. C = confirmed from code, R = needs the phone.


## B9a — Home, Body, app shell, first launch


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| L-3 (B9a) | P2 | defect | C | — | `MainActivity.kt:74-93`, `ui/navigation/AppNav.kt:296-314`, `ui/saveposture/SavePostureHost.kt:25-30,43-53`, `ui/settings/SettingsViewModel.kt:198-210`, `data/repository/prefs/SettingsStore.kt:46-53,59-60`, `data/repository/prefs/SavePosturePrefsStore.kt:24-49`, `data/repository/PreferencesRepository.kt:85-87,430-443` | The onboarding gate reads `dataStore.data` raw and turns any `IOException` (DataStore's `CorruptionException` is one; no `corruptionHandler` is set) into `UNAVAILABLE`, showing "Settings unavailable… not a first install" with Retry — but `SavePostureHost` is composed outside that gate, reads the same store through `safePreferences`, which swallows the same exception into empty prefs (`chosen = false`), and so draws the first-launch chooser over the retry screen, blocking Retry from touch (`SavePostureChooser.kt:82`) and hiding it from TalkBack (`MainActivity.kt:72`); any choice then calls `store.data.edit` with no catch (`SavePosturePrefsStore.kt:30-37`) inside `viewModelScope`, which on the same unreadable file throws uncaught. | If the settings file on the phone is ever damaged, the app puts up a "try again" screen and immediately covers it with the "where should we save your training?" question; you cannot reach Try again, and answering the question crashes the app. | Robolectric: overwrite `user_settings.preferences_pb` with garbage in `FakeAppDependencies`, compose `PersonalTrainerNav` + `SavePostureHost`; assert `save-posture-chooser` is absent while `gate == UNAVAILABLE`. Today it appears. | Confirmed; folds into DB-2 |
| UI-1 (B9a) | P2 | defect | C | — | `ui/navigation/AppNav.kt:408-418,434-438`, `MainActivity.kt:41-51,109-117,126-130`, `ui/home/HomeViewModel.kt:257-278,409-462`, `ui/home/DailyAgendaCard.kt:91-101,184-188`, `data/repository/PlannerRepository.kt:491-499` | A reminder tap (Start or body) always runs `goToTab(Home)`, which pops whatever is pushed — the live workout floor, a routine editor, a cardio composer — and for a Start tap `consumeStartedDelivery` has already marked the delivery STARTED and cancelled the notification (which also cancels its scheduler) before `startOccurrence` can come back `Blocked` (another session live) or `Missing`; a body tap while a session is live is then consumed silently (`DailyAgendaCard.kt:93-96`), so the only visible effect is the ejection. | Tapping a workout reminder while you are already in a workout throws you out to Home, and the reminder is gone for good even if you then decide to keep the workout you were in. The workout itself is safe; the live bar takes you back. | Robolectric nav test: live session in `FakeAppDependencies`, current route `session/{id}`, then set `reviewOccurrenceId`; assert the route stays on the floor (today it becomes `home`). ViewModel test: `startOccurrence` → `Blocked` must not leave the delivery STARTED. | Confirmed |
| UI-12 (B9a) | P2 | defect | C | raised by B4b | `ui/navigation/LiveSessionBarViewModel.kt:206-236,249-279`, `PendingOccurrence.kt:66-72,83-87,115-118`, `data/repository/PlannerRepository.kt:354-365`, `data/repository/PreferencesRepository.kt:176`, `workout/CompleteTraining.kt:34-45`, `workout/FinishWorkout.kt:46-65`, `workout/DiscardWorkout.kt:29-37`, `PersonalTrainerApp.kt:26-29,173-186` | The bar's strength Finish and Discard degrade on the *read/write of the session itself* (both use cases wrap their work in `runCatchingCancellable` and return `Failed`, which the bar shows), but the step after a successful outcome — `PendingOccurrence.complete` on finish (:231) and `forgetIfSession` on discard (:269), each a Room `upsertOccurrence` plus a DataStore `edit` with no catch — runs outside any try in the strength branch (the ACTIVITY branch is wrapped, :210-214 and :256-265); a throw there leaves `viewModelScope` uncaught (no handler; the app's `CoroutineExceptionHandler` is on `applicationScope` only), reaches the default uncaught handler and kills the process, with the session already finished/discarded but the plan row not marked DONE and no summary navigation (`_finishedNavigation` is set only after `complete` returns, :232). `HomeViewModel.startOccurrence` (:435-439) has the same unguarded `bindForSession` after a successful start. | Finishing or discarding from the little bar at the bottom is safe for the workout itself — but if the phone's storage refuses the *next* small write (marking the plan row done, clearing the reminder link), the app crashes, and after it comes back the plan still shows that session as not done even though it is saved in History. | ViewModel test: `FakeAppDependencies` whose planner DAO throws on `upsertOccurrence`; call `finishFromBar()` on a session bound to an occurrence; assert `actionError` is set and no exception escapes (today it escapes). | Confirmed |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**L-3.** Chain, all `C`: `PreferencesRepository.kt:85-87` builds `preferencesDataStore(name = "user_settings")` with no corruption handler, so a corrupt file throws `CorruptionException` (an `IOException`) from `data` on every read. `onboardingCompleteHealth` (:430-441) uses `observeHealth` on the raw `dataStore.data`, so the gate becomes `UNAVAILABLE` (`OnboardingGateViewModel.kt:56-60`) and `PersonalTrainerNav` returns the retry `EmptyState` (`AppNav.kt:300-312`). `SettingsStore.safePreferences` (:46-53) catches the same `IOException` and emits `emptyPreferences()`; `savePostureState` reads through `store.pref` (`SavePosturePrefsStore.kt:24-28`), so it reports `chosen = false`. `ensureLegacyMigrated` (:39-49) returns early only when `chosen` is true; with all-default inputs `inferLegacySavePosture` returns null (a fresh pattern), so `_savePostureReady` flips true (`SettingsViewModel.kt:198-204`) and `needsChooser` (:488) is true. `MainActivity.kt:93` composes `SavePostureHost` beside, not inside, `PersonalTrainerNav` (:74-81), and :72 clears the semantics of everything beneath. Counter-evidence looked for: a `corruptionHandler` (none, grep over `app/src/main`); a try/catch in `chooseSavePosture` (none, :206-210); a test with a throwing store (`OnboardingGateViewModelTest:95-107` tests only the pure `gateFromHealth`). The `DataHealthCopy.SETTINGS_BODY` copy "This is not a first install" (`domain/DataHealth.kt:55-56`) says the app already decided this case must not look like a first install; the chooser on top says the opposite.

**UI-1.** `ReminderHandoff` (`AppNav.kt:235-242`) and its doc say reminder taps "always land on Home first" — deliberate for the tab case, never considered for a pushed screen. `goToTab` is `popUpTo(start){saveState}` (:411-413), so a pushed screen above Home is popped, and by Navigation's keying (UI-9) it is not restored. `MainActivity.kt:51` / `:116` call `consumeStartedDelivery` on every Start launch before Home has run `startOccurrence`; `markDeliveryStatus(STARTED)` cancels the scheduler for that delivery (`PlannerRepository.kt:498`), so a `Blocked` outcome (`HomeViewModel.kt:452-457`, then `ResumeOrDiscardDialog` → Resume) or `Missing` (:412-415) leaves a delivery that says STARTED for a session that never started and cannot be snoozed. Counter-evidence: `DailyAgendaCard.kt:184-188` does print "Finish or discard the live session first." on Home, so the ejection is explained once you look; and `HomeViewModelTest.reminderReviewOpensConfirmAndStartsNothing` / `reminderStartStartsTheOccurrence` (:262-281) pin the non-live path only. Data is never lost (the floor's session is persisted; the occurrence stays PLANNED), which is why this is P2, not P1.

**UI-12.** Asked by the coordinator on B4b's behalf: does a throwing read in the bar's strength branch crash or degrade? The read degrades. `FinishWorkout.kt:49-52` wraps `getSession` and :58-65 wraps the finish write in `runCatchingCancellable`, returning `FinishOutcome.Failed`, which `CompleteTraining.finishWorkout` (:43-44) maps to `Failed` and the bar renders (`LiveSessionBarViewModel.kt:242-245`); `DiscardWorkout.kt:31-37` likewise. What is not guarded is the follow-up write: `applyFinishOutcome` (:229-236) calls `PendingOccurrence.complete` before setting `_finishedNavigation`; `complete` (`PendingOccurrence.kt:66-72`) calls `plannerRepository.markOccurrenceDone` (`PlannerRepository.kt:354-365`, a `dao.upsertOccurrence` with no catch) and then `write(deps, null)` → `setPendingOccurrenceId` (`PreferencesRepository.kt:176`, a DataStore `edit`). `finishFromBar` (:206-225) wraps only the ACTIVITY branch (:210-214); the strength branch (:216-221) is bare. `discardFromBar` (:249-279) has the same asymmetry: the ACTIVITY branch's comment (:253-255) says exactly why it was wrapped — "an unhandled throw here took the whole process down" — and the strength branch's `forgetIfSession` (:269) is one step past `discardWorkout`'s guard. `viewModelScope` has no `CoroutineExceptionHandler` (`AppViewModel.kt:20-30`; the app's handler at `PersonalTrainerApp.kt:26-29` is on `applicationScope`), so the throw reaches `Thread.setDefaultUncaughtExceptionHandler` (:173-186), is recorded, and the process dies. Consequence: the session is already `Written`/`Discarded` (data safe), the plan row stays PLANNED so Home lists it under Still open, and the summary never opens. Counter-evidence: `LiveSessionBarViewModelTest.finishFromBarPreservesNotesStopsRestAndEmitsOneShotNavigation` (:109-136) and `discardFromBarDeletesZeroSetSessionAndDraft` (:208) run over a healthy in-memory Room, so they cannot see it. P2 rather than P1 because nothing already saved is lost; P2 rather than P3 because it is a crash reachable from every screen and leaves the plan wrong.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-2 (B9a) | P3 | architecture debt | C | ADR-021 §3 (13 Sep), W2a | `ui/home/HomeViewModel.kt:225-238,558-614,635-647,671-676`, `ui/home/HomeScreen.kt:80,96-100`, `ui/navigation/AppNav.kt:514`, `app/src/test/.../ui/home/HomeViewModelTest.kt:508-591` | `addDaySession`, `HomeDayAdd`, `requestWeekSuggestion`, `requestAnswerReplay`, the `navigateToEditor` flow and Home's `onOpenRoutine` wiring have no caller in `app/src/main` (grep); four tests keep them compiling. ~90 lines W2a missed. | Home still carries the machinery for "add a session from Home", a feature removed on 13 September; only tests use it. | `grep -rn "addDaySession\ |
| UI-3 (B9a) | P3 | validation gap | C (measure) / R (result) | Shared contract "reflow 3 + 2" | `ui/navigation/AppNav.kt:808-829,907-913`, `app/src/androidTest/.../FrontendShellInstrumentedTest.kt:138-164,208-222` | The bar reflows 3 + 2 when a label plus 8 dp exceeds a fifth of the width (:821-824); by glyph estimate that is already at font ≈ 1.3 on 360 dp. In the 3-wide row a label gets 112 dp (:911) and "SETTINGS" at 2.0 measures ≈ 116 dp, so a mid-word break is plausible; the only pin (`lineCount == 1`, 3 + 2, 48 dp floors, at 360×640 font 2.0) runs on the hosted non-blocking lane. No JVM render covers the shell. | Whether the largest text size splits "SETTINGS" in two on a small phone is checked only by a test the merge gate does not run. | Add the bar to a JVM render (ADR-032) at 360×640 × font 1.0/1.6/2.0 asserting one line per label and the row count. |
| L-4 (B9a) | P3 | validation gap | C | — | `ui/permissions/LaunchPermissionsHost.kt:39-40,56-61`, `ui/settings/SettingsViewModel.kt:156-160,171-184,198-204`, `ui/navigation/AppNav.kt:353-364` | `launchPermissionsAsked` starts `false` with no loaded flag; the host's `LaunchedEffect(alreadyAsked)` sets `walking = true` and persists "asked" on the first `false` it sees. Today `postureChosen` (gated on `loaded`, which flips after two DataStore round-trips) lands later than the one read `launchAsked` needs, so the order holds — but nothing pins it, and a reorder re-runs the walk on every cold start for anyone who tapped "Not now". | The "ask once" promise of the permission dialogs rests on timing, not on a check. | ViewModel/compose test delaying the reminders pref flow behind the posture flow; assert `onAsked` is not called. Fix: gate `LaunchPermissionsHost` on a loaded flag like `savePostureUi.loaded`. |
| UI-4 (B9a) | P3 | defect | C | 22 Sep "process death" row | `ui/navigation/LiveSessionBarViewModel.kt:160-168,229-236` | A finish confirmed on the bar writes the session (`Written`) and completes the plan row, then holds the summary route only in `_finishedNavigation`; process death between the write and the `LaunchedEffect` (`AppNav.kt:381-388`) drops the summary. Same for `HomeViewModel.undoableSkip` (:94-95): the Skip stands, the undo offer vanishes. | If Android kills the app at the instant you press Finish on the bar, the workout is saved but you never see its summary; a "Skip" you meant to undo cannot be undone after a restart. | Read; the summary is reachable from History. |
| UI-5 (B9a) | P3 | validation gap | C | 22 Sep "10 of 21 ViewModels" | `ui/home/HomeScreen.kt:115,158,165,236`, `ui/home/DailyAgendaCard.kt:77`, `ui/home/ThisWeekCard.kt:67`, `ui/progress/ProgressScreen.kt:79-80`, `ui/progress/ProgressViewModel.kt:109-111`, `ui/navigation/AppNav.kt:352,441`, `ui/navigation/LiveSessionBar.kt:96-97`, `ui/workout/StartOptionsSheet.kt:95,98-100`, `ui/workout/StartOptionsViewModel.kt:195-199`, `ui/permissions/LaunchPermissionsHost.kt:39,71-73` | Inventory for the five no-`SavedStateHandle` screens: selected day, `lastToday`, start sheet, weigh-in dialog, pending confirm, fallback confirm, body view, selected muscle, Body/History/Plan start sheet, discard confirms, walk-showing and skipped-step flags are all `rememberSaveable`; the heat window is in DataStore; `menuOpen` (bar) is `remember` only; VM-only losses are the one-shot nav flows and undo offer (UI-4), plus `blockedByInProgress`, `actionError`, `reviewOccurrenceId`, `focusEpochDay`, which are harmless. `StartOptionsSheet` clears its nav flows on dispose so a stale value cannot fire on the next open. Nothing pins any of this. | Rotating or being killed in the background keeps where you were on Home and Body; the only things that vanish are a dialog you had open and a pending undo. | A Robolectric `StateRestorationTester` pass over `HomeScreen` and `ProgressScreen`. |
| L-5 (B9a) | P3 | defect | C | ADR-023 §3 | `ui/intro/ColdStartIntro.kt:70-88`, `domain/ColdStartIntroCopy.kt:10`, `MainActivity.kt:83-92` | Under reduced motion `LaunchedEffect(reduceMotion)` calls `finishOnce()` on the first frame, so the intro is composed for one frame (a flash), fires `Haptics.tickLight` with no user action, and skips the 2,000 ms dwell; ADR-023 says animation collapses and dwell stays, which means either show it at rest for the dwell or do not compose it. `rememberInfiniteTransition` is correctly avoided (:156-165). | With "remove animations" on, the opening screen flickers for a frame and the phone buzzes once, instead of either resting for two seconds or not appearing. | Robolectric with `reduceMotion = true`: assert `onFinished` is not called before the dwell, or that the root never composes. |
| UI-6 (B9a) | P3 | opportunity | C / R | Body contract "stale request never shows old totals" | `ui/progress/ProgressViewModel.kt:67-102,86`, `ui/progress/ProgressScreen.kt:92-96,306-315` | `window = insights.snapshot?.window ?: stored` keeps the chip paired with the snapshot, so old totals never sit under a new label (contract met) — but after a tap the selected chip stays on the old window until the recompute lands, with no busy state; how long is `R`. | Tapping "Month" on Body does nothing visible until the new numbers are ready, which can read as a missed tap. | ViewModel test: `setWindow(MONTH)`; assert `uiState.window == WEEK` until the insights flow emits. |
| UI-7 (B9a) | P3 | architecture debt | C | — | `ui/navigation/AppNav.kt:164,187-195,526-533`, `app/src/test/.../ShippingTabsTest.kt:42` | The Library composable is registered as the literal `"library?muscle={muscle}"` while `Route.Library.path` is `"library"`; navigation works because the query arg is optional, but `Tab.matchPattern`'s "plain equality" doc no longer describes this route, and `ShippingTabsTest:42` asserts `routeAllowsPermissionWalk(Route.Library.path)` against a string the graph never reports, so it passes vacuously. | The one screen whose address is typed by hand in two different spellings is also the one whose test checks the wrong spelling. | Add `Route.Library.pattern` and use it in both places; assert with the pattern. |
| UI-8 (B9a) | P3 | defect | C | Shared contract "state restoration" | `ui/navigation/AppNav.kt:408-418,494-516` | `goToTab` pops to the start destination (Home) with `saveState`; Navigation keys the saved stack by the first popped entry, which for Home is the pushed screen, so returning to `home` with `restoreState` finds nothing — Home's pushed screens are lost on a tab switch while Body/Plan/History's come back. | Open a routine from Home, tap Body, tap Home: you are on Home. Do the same from Plan and the routine comes back. | `TestNavHostController`: push `routine/x` over Home, navigate to `progress`, back to `home`; assert the top route. |
| UI-9 (B9a) | P3 | validation gap | R | 16 Sep acceptance "TalkBack complete journeys" | `ui/navigation/AppNav.kt:802,813,885-889,905,909`, `ui/navigation/LiveSessionBar.kt:130,144-153,187-192`, `ui/components/WeekStrip.kt:118-126,141-161` | Tab labels are `uppercase()` in the text itself with no `contentDescription` (icon is `null`), the role is `Role.Tab` in a `selectableGroup` — what TalkBack says for "PLAN" (word or letters) and whether it announces "n of 5" needs a phone. The bar's clock changes every second inside the merged clickable node; the only live region is the error line (:130), so no flood by design, but a focused bar may re-read each tick. Week cells set a `contentDescription` on a node that also has child text — the double-announce shape W1a fixed on chips. | Whether the screen reader reads the tabs, the ticking clock and the week days cleanly is unverified. | Phone with TalkBack: focus each tab, the bar for 5 s, a week cell. |
| UI-10 (B9a) | P3 | defect | C | 22 Sep "two start sheets" | `ui/workout/StartOptionsViewModel.kt:209-218`, `ui/home/HomeStartSheet.kt:212-222`, `ui/activity/LiveCardioScreen.kt:179` | "Start cardio" on Body/History/Plan starts `CardioType.RUN` titled "Cardio" with no type pick; Home's sheet asks the type. The live screen lets you change it afterwards, so it is a wrong default, not a trap. | Starting cardio from the Body tab records a run until you change it; starting from Home asks first. | Read. |
| UI-11 (B9a) | P3 | defect | C | — | `ui/navigation/AppNav.kt:296-299`, `MainActivity.kt:63-65,83-92`, `PersonalTrainerApp.kt:35-41` | `UNKNOWN` returns nothing until DataStore's first emission, with no timeout; on a cold start the intro (2 s) hides it, but a process-death restore is a new process too (`coldStartIntroPending` is per process), so the branded intro replays after every background kill and is what hides the blank frame. | After Android quietly restarts the app, you see the opening screen again. | Read; `R` for how often a given phone kills the process. |
| UI-13 (B9a) | P3 | defect | C | raised by B9b (Plan has the same shape) | `ui/home/HomeViewModel.kt:140-144,146,169,391-407`, `ui/home/HomeScreen.kt:320-346`, `domain/TrainingInsights.kt:192-210`, `domain/MissedWorkPolicy.kt:12-25`, `ui/plan/MissedWorkCard.kt:30-36` | `applyMissedWork` returns silently when `uiState.value.weekPlan` is null (:393), before the `runCatching` that would set `actionError`; but `weekPlan` is null whenever the plan stage failed (`recoverStage` → null, `failures += InsightFailure.PLAN`), and in that state Home still derives `weekStart` from preferences (:140-144), computes `overdue` from occurrences (:146) and shows the card (:169), so all four buttons (Move remaining / Adapt / Keep dates / Skip missed) do nothing: no write, no banner, the card stays. `HomeUiState.weekStartEpochDay` (:183) already holds the derived start, so the early return is unnecessary. No data can be lost. | When the app could not work out this week's plan, Home still asks "you missed a session — what do you want to do?" and then ignores whatever you tap, without saying so. | ViewModel test: `FakeAppDependencies` with an overdue PLANNED occurrence and a plan stage that fails (or `includeWeekPlan` insights with `weekPlan = null`); call `applyMissedWork(KEEP_DATES)`; assert a decision row is written (today: nothing) or at least `actionError` is set. Nothing pins it: `applyMissedWork` is tested only at `PlannerRepositoryTest:188`. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- **B14 (docs truth):** `docs/PRIVACY.md:117` "Onboarding never asks for notification permission" — the first-open walk asks `POST_NOTIFICATIONS` right after the save-posture chooser (`ui/permissions/LaunchPermissionsHost.kt:90-96`, order notifications → exact alarm → battery, `domain/LaunchPermissions.kt:49-59`). Literally true of the questionnaire route, misleading for the owner. `domain/LaunchPermissions.kt:4` says "for Temper Debug" but the host is composed unconditionally (`AppNav.kt:361`; no `BuildConfig` gate anywhere under `ui/permissions`, `ui/navigation`, `ui/saveposture`). `ui/components/WeekStrip.kt:50` "never scrolling" vs ADR-026 §6 (F4 will settle it). `AppNav.kt:187-195` `Tab.matchPattern` doc vs the literal at :526 (UI-7).
- **B10a / whoever owns DataStore:** `data/repository/PreferencesRepository.kt:85-87` builds `user_settings` with no `corruptionHandler`; the root of L-3. A `ReplaceFileCorruptionHandler` would make the gate's `UNAVAILABLE` branch unreachable and the app silently a "first install" — the copy at `domain/DataHealth.kt:55-56` says that is the wrong answer, so the decision needs an owner.
- **B12 (accessibility):** `ui/components/InstrumentSelection.kt:32,54` already gives Body's window and Front/Back pickers `selectableGroup` + `Role.RadioButton`; F8a "single-choice radio roles app-wide" may be partly done. WeekStrip double-announce shape (UI-9).
- **B13a (performance):** `AppNav.kt:811-817` measures all five labels on every recomposition of the bar; `LiveSessionBarViewModel.kt:74-85` runs a 1 Hz ticker on every non-hidden route while a session is live (bar host only, by design at :366-367).
- **B13b / TS- owner:** `HomeStartSheetTest`, `DayBlockLayoutTest`, `BodyViewportTest:31-64` are source-string assertions (`readOwned(...).contains(...)`), the pattern T1 retired on the workout floor; nothing renders Home's agenda, the week strip, the bar or the nav bar on the JVM gate (`FirstLaunchRenderTest` covers the chooser and the masthead only).
- **B7 (updater):** `DebugUpdateBanner` sits in Home's list between the missed-work card and the agenda (`ui/home/HomeScreen.kt:347-355`); `rememberDebugUpdatePort` reaches the container by cast (known debt).
- **B9c (cardio):** UI-10 default; and whether the activity composer keeps a draft when UI-1 pops it.
- **B11 (workout floor):** a stale rest-notification tap lands on `session/{id}` and `WorkoutSessionReader.kt:41` resolves `MISSING` — the navigation side is fine; what the floor shows is B11's.
- **B7 (reminders):** the STARTED-before-outcome ordering in UI-1 is a MainActivity choice; `ReminderNotifications.kt:133-140` documents it.
- **B4b:** see UI-12 — the bar's strength read/write degrades, the plan-row and DataStore writes after it do not. **B9b:** see UI-13 — Home's missed-work card has the silent early return B9b described on Plan; the fix on Home is to use `HomeUiState.weekStartEpochDay`.


## B9b — Plan, Plan day, routines


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| UI-1 (B9b) | P2 | defect | C | — | `UI/plan/PlanScreen.kt:302-307,379-387,405-414`, `UI/plan/PlanDayScreen.kt:72,81-83,144,182-192`, `UI/components/WeekStrip.kt:118-121` | Plan's filled "Add session" opens `PlanDayScreen(add=true)` for whatever strip day is selected, but the day page refuses every add when `epochDay < today` (picker suppressed, no Add button, caption "Past days are a record"), and the strip only shows the current week; so weekdays already gone this week cannot be planned at all, and the Volt leads to a page that offers nothing. | On a Wednesday, tap Monday and press the big Add session button: you land on a page that says the day is a record and lets you add nothing. There is no way to set up next Monday until Monday comes. | Robolectric: render `PlanScreen` with today = Wednesday, select Monday, tap `PlanTags.ADD_SESSION`, assert the day page shows no `PlanDayTags.ADD` and no picker; or read the two files. | Confirmed |
| UI-2 (B9b) | P2 | target gap | C (feel R) | F8a (partial) | `UI/routines/CustomWeekScreen.kt:287-348` (`:303-315`, `:318-322`, `:324-331`) | The week builder's day selector is a plain `clickable` `Column` with no `Role`, no `selected` semantics and a filled-day check icon with `contentDescription = null`; its only text is the two-letter label, so TalkBack cannot say which day is chosen or which days already hold lifts, while the app's own `WeekStrip` already does this right (`selectableGroup`, `Role.Tab`, spoken cell). | A screen-reader user building their week hears "Mo, Tu, We" and nothing else — not which day is open, not which days are done. | Robolectric semantics test on `CustomWeekScreen`: assert a day node has `Selected` and a `Role`; today it has neither. | Confirmed — duplicate of AX-2, folded |
| UI-3 (B9b) | P2 | defect | C | — | `ui/navigation/AppNav.kt:554-570`, `domain/CustomWeekLaunch.kt:1-13`, `UI/routines/CustomWeekViewModel.kt:84-86,103-104,196-208,367-379`, `data/repository/OnboardingApplier.kt:236-247`, `UI/routines/SavedStateCustomWeekDraft.kt:52-70,72-101` | The guided answers and weight unit reach the week builder only through in-memory `container.pendingCustomWeek`; after process death the draft days come back from `SavedStateHandle` but `launch` is null, so Confirm applies the week with `answers = null` (goal, emphasis, equipment, training age, places, focus and the typed bodyweight are skipped) and never sets the unit; the same draft also drops `targetSeconds`/`targetSecondsMax`, so a hold lift's time resets. | If the phone kills the app while you are building your first week, the screen comes back and Confirm still works — but the answers you just gave (goal, kit, bodyweight, kg/lb) are quietly thrown away, and any plank time you typed is back to the default. | Extend `CustomWeekViewModelTest.processDeathRestoresDaysAndSelectedDay` (`:341`): seed with answers + unit, recreate on a fresh VM with `pendingCustomWeek = null`, confirm, assert goal/unit stored and a hold lift's seconds kept; today both fail. | Confirmed (survivor; B10c UI-3 folds in) |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**UI-1.** `addSessionVolt` (`PlanScreen.kt:318-319`) depends only on proposals and the missed-work prompt, never on `selectedEpochDay < today`. `WeekCell` is `selectable(role = Role.Tab, …)` with no `enabled` gate (`WeekStrip.kt:118-121`), and `LaunchedEffect(weekStart, today)` (`PlanScreen.kt:302-307`) only clamps a selection that is outside the week. On the day page `picking` is initialised `if (startInAdd && !isPast) KIND else NONE` (`PlanDayScreen.kt:81-83`), the picker is gated `!isPast` (`:144`) and the Add button too (`:182`). Counter-evidence looked for: the rules are weekly (`PlannerRepository.addTimedRule` writes a weekday rule, `:202-227`; ADR-020 §2 "Plan add is recurring"), so adding on a past *date* of this week would be a valid way to plan next week — the refusal is the screen's, not the model's. The empty-routines path is inconsistent with it: "Create a routine" calls `viewModel.buildDay(selectedEpochDay)` (`PlanScreen.kt:444`) with no past check and pins that weekday. No test pins either behaviour (grep for `PlanDayCopy.PAST`/`startInAdd` under `app/src/test` and `androidTest`: none).

**UI-2.** Compare `UI/components/WeekStrip.kt:64-80,118-121`, which has `selectableGroup()`, `Role.Tab`, `selected` and a `spoken` string per cell. `AuxiliaryPackList.kt:126-144` also uses `selectableGroup` + `selected` on its rows. The builder's strip predates both.

**UI-3.** `CustomWeekLaunch.kt:6-8` says it is held in memory "rather than stuffed into a route" for size; nothing re-arms it on restore (the only writer is `AppNav.kt:554`). `applyCustom` (`OnboardingApplier.kt:236-247`) stores goal/emphasis/equipment/age/places/focus/bodyweight only inside `answers?.sanitized()?.let`. `pendingWeightUnit?.let { setWeightUnit }` (`CustomWeekViewModel.kt:376-379`). `SavedStateCustomWeekDraft.bundleOf` (`:52-70`) writes sets/reps/rest/weight and no seconds; `liftFrom` (`:72-101`) leaves `targetSeconds` at its null default although `CustomWeekPolicy.updateTargets` (`domain/CustomWeekPolicy.kt:138-143`) does stage them. The existing process-death test (`CustomWeekViewModelTest.kt:341-365`) uses a non-hold lift and no launch seed, so it passes.

**Seam 1 premise (stated for the lead, not a finding).** The brief assumed process death mid-edit loses the editor's lift list. It does not: every picker tap writes straight to Room (`RoutineEditorViewModel.kt:1080-1089`), and typed targets live in `rememberSaveable` wells (`SessionLiftStrip.kt:507-515`) that re-stage and re-commit on restore through `LaunchedEffect(rowKey, differsFromStored)` (`:535-539`); `RoutineEditorViewModelTest.kt:932` (name, no second routine) and `:1338` (restored target still written by Save) pin it. What *is* lost after process death: open picker and its query, an open swap sheet, the Save-error caption, the Back prompt, and the paste results (`PasteUi` at `:1449-1454` is not persisted) — the "unmatched lines" list after a paste import vanishes, though the matched lifts are already on the routine (UI-7).

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-4 (B9b) | P3 | architecture debt | C | 22 Sep Plan row d (extends) | `UI/plan/PlanViewModel.kt:108,165-175,201,248-251,253-271,295-319,360-459,461-463,575-580,608-632,658`, `UI/plan/PlanScreen.kt:312-314,318-319,385,415-429,577-587,694-804`, `ui/home/HomeViewModel.kt:635-637,645-647`, `AppDependencies.kt:79` | The whole proposal flow is unreachable: `HomeViewModel.requestWeekSuggestion`/`requestAnswerReplay` have no caller in `main`, so `pendingWeekSuggestion`/`pendingAnswerReplay` are never true, `suggestFills`/`replayStoredAnswers`/`acceptFills`/`dismissFills` never run, `proposals` is always empty, the quiet-Add-session branch and `WeekStrip(proposals=)` never fire, and `PlanRecoveryCommands`/`PlanLighterChip` are never composed; eleven more `PlanViewModel` functions (`agendaFor`, `pinFocus`, `unpin`, `swapRoutine`, `addMorningCardio`, `removeTimedRule`, `setTrainingDays`, `setWeekStart`, `setLighterWeek`, `setSessionHour`, `onErrorShown`) and `PlanDayViewModel.agendaFor` (`:82-85`) are called only by tests. ADR-017 §4 (docs/architecture/ADR-017-home-week-board.md:79-81) still says Lighter is a Plan chip and Suggest/Replay/Use this week "remain recovery"; in code Lighter is set from Body (`ui/progress/ProgressViewModel.kt:118-131`). | About 400 lines of the Plan screen and its brain are wiring for buttons that no screen shows, and the design record still describes those buttons as live. | `grep -rn "requestWeekSuggestion\ |
| UI-5 (B9b) | P3 | defect | C | — | `UI/plan/PlanViewModel.kt:183-192,217-227,582-596`, `UI/plan/PlanScreen.kt:336-338,351-377`, `domain/TrainingInsights.kt:192-210`, `domain/StageRecovery.kt:32-42` | `missedWorkPrompt` is computed from planner rows with a week-start fallback, but `applyMissedWork` returns silently when `uiState.week` (the insights `weekPlan`) is null — which is exactly the state where the "Couldn't read this week's plan" banner shows — so Keep/Move/Adapt/Skip all do nothing, no error, card stays. (Home has the same shape, `HomeViewModel.kt:391-393`, see §4.) | If the app could not work out this week's plan, the "Missed this week" card still appears and every button on it is dead. | `PlanViewModelTest`: feed `TrainingInsights(failures = setOf(PLAN))` with an overdue occurrence, call `applyMissedWork(KEEP_DATES)`, assert a decision row exists — none will. |
| UI-6 (B9b) | P3 | architecture debt | C | — | `UI/routines/SessionLiftStrip.kt:516-528` (`:523`), `:697-781`, `UI/components/NumberEntryDialog.kt:45,116`, `domain/TargetStepper.kt:18-25`, `UI/routines/RoutineEditorViewModel.kt:62-73,416-441,465-487,544-555,1277-1282`, `UI/routines/CustomWeekViewModel.kt:94-100,319-337,357-362,403-434` | The card's only entry surfaces are steppers that clamp at ≥1 and `NumberEntryDialog`, whose Set is disabled until the text parses, and `persist` always hands `invalidReason = null`; so the "unreadable box" refusal family (`ERR_TARGET_RULE`, `invalidReason`, `forgetTargetRule`, `invalidTargets`, the `DisposableEffect` at `:389-391`) and `writeTargets`' Rejected branch cannot be entered from the shipped UI, yet ~170 ViewModel lines and 13 tests (`RoutineEditorViewModelTest.kt:1118-1308`, `CustomWeekViewModelTest.kt:367-615`) keep them alive. | A big chunk of the editor's "what if you typed 8.5 reps" safety net can no longer be reached, because the number pad already refuses 8.5 — it is weight the 1,490-line file carries for nothing. F7b's split can drop it. | Read `SessionLiftStrip.kt:516-528`; grep `invalidReason` in `main` — every producer passes `null`. |
| UI-7 (B9b) | P3 | validation gap | C | 22 Sep cross-tab | `UI/routines/RoutineEditorViewModel.kt:911-925,1449-1454,1485-1490`, `UI/routines/RoutineEditorScreen.kt:161-170,281-289` | Paste-import results (`unmatched`, `createdFromPaste`) are in-memory `PasteUi`; after process death the "couldn't match these lines" block and the "also created" caption vanish while the routine keeps the matched lifts, so the user loses the list of what did not import. | Paste a workout, some lines don't match, the app is killed in the background: the list of the lines that failed is gone when you come back. | ViewModel test over `FakeAppDependencies`: `importPaste` with an unknown lift, recreate the VM on the same `SavedStateHandle`, assert `unmatched` is empty (documents the gap). |
| UI-8 (B9b) | P3 | target gap | C | — | `UI/plan/PlanDayViewModel.kt:36-43,57-76`, `UI/plan/PlanDayScreen.kt:103-114` | `PlanDayUiState` has loading, empty and write-error states but no read-error state: the `combine` of four Room flows has no `.catch` (contrast `CustomWeekViewModel.kt:157-163`), so a read fault has nowhere to land except an uncaught exception, and `error` only ever carries an action failure. | The day page can say "Could not add that session" but has no way to say "Could not read this day". | Read `PlanDayViewModel.kt:57-76`; a `FakeAppDependencies` whose occurrence flow throws. |
| UI-9 (B9b) | P3 | validation gap | C | CURRENT_STRUCTURE:208; D17 | `app/src/test/java/com/sinura/personaltrainer/ui/plan/PlanDayConfirmTest.kt:11-24`, `UI/plan/PlanDayViewModel.kt`, `UI/plan/PlanDayScreen.kt`, `app/src/debug/.../ui/plan/PlanPreview.kt:20-65` | PlanDay is the least-tested screen: no ViewModel test, no render test, no preview (`PlanPreview` only draws `PlanHeader` and the dead `PlanRecoveryCommands`), and the one test that names it reads the `.kt` source as text and asserts substrings (`readOwned(...).contains("ConfirmActionDialog(")`) — the pattern T1 retired for the workout floor — plus two substrings of `RestTimerScreen.kt` that are not its subject. | The page where you build a day has no test that runs it and no picture of it; its only "test" checks that certain words appear in the code file. | Add `PlanDayViewModelTest` over `FakeAppDependencies` (pin/add/delete/reorder on a weekday) and a Robolectric render of `PlanDayScreen` in the ADR-032 matrix; retire the source-string test. |
| UI-10 (B9b) | P3 | opportunity | C (feel R) | — | `UI/routines/SessionLiftStrip.kt:251-260` | The lift card's header is `clickable(onClick)` with no `Role`, and its expanded/collapsed state is conveyed through `selected` rather than an expand/collapse state description; a merged `contentDescription` is present. | A screen reader says the lift's name and numbers but not that tapping opens its settings, nor whether it is open. | Semantics assertion in a Robolectric test; feel needs a phone. |
| UI-11 (B9b) | P3 | opportunity | C | F8b (picker side) | `UI/components/ExercisePickerSheet.kt:130,308-324`, `domain/ExercisePickerContract.kt:51-52` | In the picker's empty state the branch "Nothing in the library matches. Create it as a custom lift above." (`:316-317`) is unreachable (it needs `!canCreate` with a non-empty needle and no muscle chip, which implies an exact-name hit and a non-empty list), the no-query empty case reads "Search the library" even when the library is genuinely empty, and a no-hit query under a muscle chip offers no way to clear the chip. | Two of the picker's "nothing found" messages can never show or show the wrong thing, and when a muscle filter hides everything there is no "clear filter". | Read `:308-324` against `canCreate` at `:130`. |
| UI-12 (B9b) | P3 | opportunity | C | — | `UI/routines/SessionLiftStrip.kt:530-539,550-559` | Every stepper tap calls `persist` (stage + commit) and, because the tap also flips `differsFromStored` to true, `LaunchedEffect(rowKey, differsFromStored)` fires a second `persist` for the same values; the second commit is a no-op once the first lands but is a second Room round trip per tap. | Each − / + on a lift's numbers asks the database twice. | Count `updateExercise` calls per tap in a Robolectric test of `CompactTargetFields`. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- `ui/home/HomeViewModel.kt:391-393` — Home's `applyMissedWork` also returns silently when `weekPlan` is null (same shape as UI-5). → B9a (Home).
- `ui/home/HomeViewModel.kt:635-647` — `requestWeekSuggestion`/`requestAnswerReplay` and `AppDependencies.kt:79-80` flags have no caller in `main`; Home's "empty hero" that the comments describe is gone. → B9a; dead-code owner.
- `ui/plan/PlanViewModel.kt:138-239` and `ui/plan/PlanDayViewModel.kt:57-76` — Room-backed `combine(...).stateIn(viewModelScope)` chains with no `.catch`; a thrown read propagates out of `stateIn` uncaught. Cross-cutting pattern (CustomWeekViewModel catches, `:157-186`). → the architecture/coroutines pass (B13 or AR owner).
- `ui/components/ExercisePickerSheet.kt:167-189` — muscle chips are `InstrumentChip` (D08 role question). → B12.
- `ui/progress/ProgressViewModel.kt:118` — comment "The same write as Plan's lighter chip" is stale; there is no Plan chip. → B14 (DC) / Body pass.
- Docs contradicting code, for B14: `docs/architecture/ADR-017-home-week-board.md:79-81` (Lighter chip on Plan; Suggest/Replay/Use this week "remain recovery") vs no reachable UI (UI-4); `docs/architecture/ADR-021-home-start-and-day-add.md:90-92` ("Plan routines are collapsed", "Long-press delete is unchanged") vs the 22 Sep decision and F7a row (`docs/FRONTEND_REDESIGN.md:93`) that reverse both; `ui/plan/PreferenceBlock.kt:27` comment says Lighter "is a Plan chip (ADR-017)".
- `app/src/test/.../ui/plan/PlanDayConfirmTest.kt:20-23` asserts substrings of `ui/workout/RestTimerScreen.kt` — a source-string assertion on the workout floor that T1's bans did not catch because it lives under `ui/plan`. → B13b / tests pass.
- `domain/AccessibilityMatrix` page "plan-day" (`AccessibilityMatrixTest.kt:52`) is the only thing that pins PlanDay's Volt name — test scaffolding in `main` (CURRENT_STRUCTURE known debt).
- `ui/plan/PlanScreen.kt:555-558` — Plan's header hosts `StartSheetOpener` (allowed by ADR-021 §2, contradicts ADR-015 §1's "Plan does not offer Start Cardio" as written). → B14 (DC, minor).


## B10a — History, Session detail, Library, Exercise detail, Summary


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| UI-1 (B10a) | P2 | defect | C | — | `UI/exercise/ExerciseDetailScreen.kt:127-134, 290-303, 539-575, 641-647, 653-664` | For a lift measured in reps the "Weekly reps" card feeds a reps series into `TrendCard`, which unconditionally prints `unit.suffix` as the headline unit, the delta unit and the TalkBack description, so it reads "45 kg", "+5 kg" and announces "latest 45 kg". | A pull-up's weekly rep count is shown and spoken as kilograms, on the one chart that tracks that lift's progress. | Robolectric render (harness: `app/src/test/.../ui/exercise/ExerciseDetailThisWorkoutRenderTest.kt:92-120`) with a `LoadType.BODYWEIGHT` lift and finished sets in two weeks; assert no node under the weekly card contains "kg" and the card's `contentDescription` ends in "reps". | Confirmed |
| UI-2 (B10a) | P2 | defect | C | 22 Sep History (contract "stale requests never display old totals under a new period") | `UI/history/HistoryViewModel.kt:188-229, 231-268`; `UI/history/HistoryScreen.kt:452-457, 459-485`; `domain/BlockReview.kt:45-51` | `uiState` is a `combine` of `horizon` and `horizonProgress`; switching the period re-emits at once with the new title and totals but the *previous* period's `HorizonProgress` (PRs count, "Moved most"), because the new progress arrives only after `completedTrainingRepository.all()` (a full-log read) and `BlockReviewBuilder.overRange` finish, and `HorizonProgress` carries no range to check against; on first load the same slot prints "0 PRs" for a null progress. | Tap Year and, for as long as the whole log takes to re-read, the card says "This year" over last month's PR count and best mover, which is exactly the mix-up the redesign contract forbids. | ViewModel test over `FakeAppDependencies(workoutDaoDecorator = …)` whose `sessionsBetween` awaits a gate; call `setHorizon(YEAR)` and assert the first `uiState` with `horizon == YEAR` has `horizonProgress == null` (or a progress stamped with the year's range). Today it carries the month's. | Confirmed |
| UI-17 (B10a) | P2 | defect | C (blank frame); whether production crashes or blanks is R | 22 Sep F6b; convergence §4 "Errors" | `UI/history/HistoryViewModel.kt:112-119, 123, 142-148, 218`; `data/repository/CompletedTrainingRepository.kt:219-231`; `data/repository/ActivityRepository.kt:116-124`; `data/repository/WorkoutRepository.kt:192-193, 208-211` | History's `revision` token and both `all()` full-log reads bypass `observeHealth`, so a throwing activity-summary (or finished-work) read propagates through `combine`/`flatMapLatest` into `shareIn(viewModelScope)` instead of reaching the `unavailable` retry page; the parity "Errors" case fails only the guarded record-sets read (`CompletedTrainingParityTest.kt:338-352`), so no test sees it. | If the phone cannot read past activities, History shows nothing (or the app stops) instead of "Retry". | `FakeAppDependencies(activityDaoDecorator = { throwing observeCompletedSummaries })`, collect `HistoryViewModel.uiState`; expect `unavailable = true`; today the collector throws. Frame `history/error-360x640-font1.0.png`. | Confirmed by the lead: the render harness run itself (the four `history_<size>` methods failed on the escaped exception and the 12 History error frames never left loading); see verification.md |
| UI-18 (B10a) | P2 | defect | C (frame) | 22 Sep cross-tab hand-built headers | `UI/history/HistoryScreen.kt:122-138` | The hand-built header gives the title `weight(1f)` beside the "Log or start cardio" opener with no `maxLines` or `softWrap = false`, so at 360 dp / font 2.0 the word breaks as "Hist / ory". | At large text the History tab's own name is split across two lines. | Frame `history/populated-360x640-font2.0.png`; `ProductionScreensPassInstrumentedTest.kt:142-169` renders this size but asserts only that chips and readout are displayed. | Confirmed by the lead from the frame; carried as P3 in the record (the tab's own name splits only at 360 dp / font 2.0, nothing is lost) |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**UI-1.** `weeklySeries` is `history.weeklyTonnage.map { it.bodyweightReps.toDouble() }` when `repsAreTheMeasure` (:128-134; `LoadClass.repsAreTheMeasure` is `this != LOADED`, `domain/LoadClass.kt:38`). The call passes `unit = unit` (:300) and `TrendCard` does:

```kotlin
headlineUnit = unit.suffix,                                   // :566
deltaLabel = seriesDelta(values, unit, grouped),              // :567 → "…$magnitude ${unit.suffix}" :646
contentDescription = trendDescription(title, values, pointNoun, unit, grouped), // :570 → "latest $latest ${unit.suffix}" :663
```

No branch on the measure exists inside `TrendCard`. Counter-evidence looked for: the all-time tile handles reps correctly (:249-253), which shows the intent; no test mentions "Weekly reps" or a bodyweight lift on this screen (`grep` over `app/src/test/.../ui/exercise` and `ui/history`: none). The hero above the chart therefore contradicts `SetCopy`'s own rule (`domain/SetCopy.kt:3-8`).

**UI-2.** `horizonProgress` (:188-229) is `combine(catalog, horizon, weightUnit) → key → distinctUntilChanged → flatMapLatest { flow { all(); emit(overRange(...)) } }`. The inner flow never emits a leading null (only when `key == null`, :214-217). `uiState` (:231-263) is `combine(catalog, visibleMonth, horizon, horizonProgress)`; `combine` re-runs on any source change with the others' last values, so the emission triggered by `horizon` carries the old progress. The screen prints `(progress?.recordsBroken ?: 0)` (:453) and `moved.exerciseName` (:472). `completed-training-convergence.md` §5 names `sessionsBetween(0, end)` as "the expected outlier", so the window grows with the log. Counter-evidence: `CompletedTrainingParityTest.kt:211-217` and `HistoryViewModelTest.kt:171-173` wait with `first { … > 0 }`, so they pass through the transient without pinning it; `ProductionScreensPassInstrumentedTest.kt:172-190` asserts only that the readout is displayed. The `HorizonTotals` half is computed synchronously in the same lambda (:254-259), so totals are never stale — the contract is broken only for PRs and mover, which sit in the same card under the same title.

**UI-17.** Which History reads are guarded and which are not, traced from `HistoryViewModel.catalog` (:142-180): guarded — `observeSessionSummariesHealth` (:145), `observeCompletedSummariesHealth` (:146), both `observeRecordSetsHealth` (:150-151); unguarded — `revision` (:112, :118, :147) and `all()` (:123, :218). `observeRevision` (`CompletedTrainingRepository.kt:219-231`) was written to key recomputes on activity completions, but it takes `activities.observeCompletedSummaries()` — the raw `mapLatest` over the DAO (`ActivityRepository.kt:120-124`) — rather than the `observeHealth` twin one function below (:126-127). `observeHealth` (`FlowGuards.kt:21-37`) is the only catch in the pipeline; nothing downstream has `.catch`. Counter-evidence looked for: the sidecar test `aSidecarReadFailureMarksThePageStaleInsteadOfVanishing` (`HistoryViewModelTest.kt:219`) and the parity case (`CompletedTrainingParityTest.kt:338`, `FailingObserveActivityRecordSetsDao`) both fail `observeCompletedStrengthSetRecords`, which is guarded; neither fails `observeCompletedSummaries` or `getAllGraphs`. My original §1 row read only the guarded branch; the coordinator's harness fails the unguarded one and never reaches a ready frame.

**UI-18.** `Text("History", modifier = Modifier.weight(1f), style = InstrumentType.display)` (:128-133) has no `maxLines`, and the `StartSheetOpener` (:134-137) takes its intrinsic width first; at font 2.0 the remaining width is narrower than the word, so Compose breaks it mid-word. The other tabs' headers are outside my scope (B-owners of Home/Body/Plan); Library uses `ScreenHeader` and renders whole at font 2.0 (`library/populated-360x640-font2.0.png` not reviewed; C from `ExerciseLibraryScreen.kt:96-101`, R for the frame).

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-3 (B10a) | P3 | defect | C | 22 Sep cross-tab | `UI/library/ExerciseLibraryViewModel.kt:84-97, 192-198`; `UI/library/ExerciseLibraryScreen.kt:86-89, 330-339` | The editor draft lives only in the ViewModel, so process death while creating or editing a custom lift discards the typed name, muscle, notes and load; query, both filters and expanded families reset; the Body muscle re-seeds even if it had been cleared. | Half-way through naming a new lift, a phone call or low memory throws the form away; coming back to Library also forgets what you were searching for. | ViewModel test: build with a `SavedStateHandle` after adding one; today the constructor has none, so the probe is "read :84-97". |
| UI-4 (B10a) | P3 | defect | C | — | `UI/history/SessionDetailScreen.kt:296-319, 321-349, 268-276`; `UI/history/SessionDetailViewModel.kt:181-213, 287-293` | `onSave` sets `editingSetId = null` before calling `updateSet`/`addSet`, so a repository refusal (`SetLogRules.isUserMessage`) lands as a page banner after the sheet has closed and the sheet's `rememberSaveable` values are gone. | If a corrected set is refused, the edit sheet has already shut and the numbers you typed are lost; the reason appears at the bottom of the page instead of on the field. | Robolectric: open Edit on a set, enter a value the rules refuse, tap Save; assert the sheet is still shown with the value. `SessionDetailViewModelTest.kt:159` pins only that the message surfaces. |
| UI-5 (B10a) | P3 | defect | C | — | `UI/summary/WorkoutSummaryViewModel.kt:50-57, 73-81, 110-147`; `UI/navigation/AppNav.kt:651-659, 665-674`; `UI/history/SessionDetailScreen.kt:122-124` | Summary reads once; "Open session" pushes Session Detail on top (`launchSingleTop`) and its Back/`deleted` pops back to Summary, so after editing a set the hero, tiles and "N personal records" are stale, and after deleting the session the summary still celebrates it and "Open session" leads to "Session not found". | Fix a set or delete the workout from the receipt's own "Open session" button, come back, and the receipt still shows the old numbers or a workout that no longer exists. | `WorkoutSummaryViewModelTest.kt:243-258` pins the one-shot read as deliberate for the edit case; the delete case is new: journey test Summary → Open session → Delete → assert Summary re-reads or leaves. |
| UI-6 (B10a) | P3 | defect | C | — | `UI/library/ExerciseLibraryScreen.kt:341-350` (:344); `UI/library/ExerciseLibraryViewModel.kt:309-360`; `data/repository/ExerciseRepository.kt:210-225`; `data/local/entity/ActivityEntities.kt:51-74` | The confirm copy promises "Lifts used in routines or history can't be deleted", but `usageFor` counts routines, `set_logs` and `session_exercises` only; `activity_blocks.exerciseId` is a soft key with no `exercises` FK, so a custom lift used only in a backdated strength activity deletes silently (history keeps its snapshot; Exercise Detail then reads "Exercise missing"). `confirmDelete` (:336-360) also has no `try/catch`, unlike `requestDelete` (:315-333). | The app says a lift with history is protected, yet one logged only through the "log a past workout" path can be deleted; and if the delete itself throws, nothing catches it. | ViewModel test: create a custom lift, confirm a backdated activity with a strength block on it, `requestDelete` → assert `blockedDelete`; today it offers `pendingDelete`. |
| UI-7 (B10a) | P3 | validation gap | C | — | `data/repository/ExerciseRepository.kt:66-70, 128-131`; `data/repository/FlowGuards.kt:39-43`; `UI/library/ExerciseLibraryScreen.kt:125-127`; `UI/exercise/ExerciseDetailViewModel.kt:193-241` | Library and Exercise Detail read through `observeHealth(...).presentValues()`, which drops `Unavailable`, so a first read that throws leaves `isLoading = true` and the spinner for ever, and a later throw silently keeps the old list; neither screen has a failed/retry state (Session Detail and Summary do). | If the phone cannot read the catalogue, Library and a lift's page just spin with no message and no retry. | Robolectric with `FakeAppDependencies(exerciseDaoDecorator = { throwing })`: mount `ExerciseLibraryScreen`; assert an error state; today `ScreenLoading` persists. |
| UI-8 (B10a) | P3 | defect (a11y) | C | — | `UI/components/GymSurfaces.kt:466-471, 530-541` | `SessionLogRow` is two clickable targets for one action: the identity column carries the merged description, the metrics row (`clickable` at :533, no `semantics`) is a second focusable read as "3 sets, 1240 kg, 45 min" with no title; a long list costs TalkBack two swipes per session. | Each history row is two separate taps for a screen reader, and the second one does not say which workout it is. | Robolectric: `onAllNodes(hasClickAction())` under one row = 2 (3 with the menu); expect 1 plus the menu. |
| UI-9 (B10a) | P3 | defect (a11y) | C | — | `UI/history/TrainingCalendarCard.kt:223-226, 239-242` | A trained day is `clickable` with no `role`, and "today" is a hairline border only — the label says "5, trained, 1200 kg" with no "today" and no "button". | The calendar's tappable days do not tell a screen reader they can be tapped, nor which one is today. | Robolectric: assert `Role.Button` and "today" in the cell's semantics. `TrainingCalendarTouchTest.kt` covers size only. |
| UI-10 (B10a) | P3 | defect | C | adjacent to the refuted 22 Sep item (distinct claim) | `domain/SessionSummary.kt:11-25, 119-130` (:127); `UI/history/HistoryScreen.kt:543`; `domain/SetCopy.kt:174-184`; `UI/history/SessionDetailScreen.kt:232-236` | `SessionSummary` carries no bodyweight-rep total and `toHistoryEntry` hard-codes `bodyweightReps = 0`, so a session of only bodyweight lifts shows "— kg" in the History row and the day sheet while its Session Detail (`session.work()`) and Summary count the same session in reps. | A pull-up-only workout reads as "nothing lifted" in the list but "36 reps" when you open it. Not "zero" — a dash — which is why the earlier claim was refuted; the contradiction between screens is the new point. | Unit test: seed a `LoadType.BODYWEIGHT` session; assert `HistoryUiState.monthGroups[0].entries[0].work.bodyweightReps > 0`. |
| UI-11 (B10a) | P3 | defect (polish) | C | Foundation item 4 (rounding closed) | `UI/summary/WorkoutSummaryScreen.kt:349`; `UI/exercise/ExerciseDetailScreen.kt:257, 629-630`; `domain/WeightFormat.kt:116-117`; `util/QuantityFormat.kt:16-33` | The same whole-number tonnage prints grouped on the Summary hero and the Exercise Detail tile ("1,240") and ungrouped everywhere `SetCopy.workColumn` is used ("1240"). | The receipt says 1,240 kg at the top and 1240 kg in the rows below it. | `SessionRowSpokenTest.kt:58` expects "8000 kg"; a render of Summary shows "1,240". |
| UI-12 (B10a) | P3 | target gap | C | ADR-011 d.2 | `UI/units/DateCopy.kt:37-42`; `UI/history/HistoryScreen.kt:259-260, 541`; `domain/HistoryEntries.kt:203-212`; `UI/history/SessionDetailScreen.kt:233`; `UI/summary/WorkoutSummaryScreen.kt:283-285`; `UI/exercise/ExerciseDetailScreen.kt:619-624, 671-672` | Month headers and calendar cells use the captured `localEpochDay`, but every printed date on rows, receipts and chart axes formats the instant in `ZoneId.systemDefault()`, so after a zone change a session sits under "October" with a row date of "30 Sep". | Travel across time zones and a workout's printed date can disagree with the month it is filed under. | Unit test of `DateCopy.dateTime` with a captured zone argument; today callers pass none. |
| UI-13 (B10a) | P3 | defect (copy) | C | — | `UI/exercise/ExerciseDetailScreen.kt:263-289` (:277-282); `domain/PersonalRecords.kt:85-90` | `estimatedOneRepMaxKg` is null for `weightKg <= 0`, so a bodyweight lift's "Estimated 1RM" card is a permanent ghost whose hint tells the user to "log a loaded set" of that lift. | A push-up's page keeps a blank chart with advice you cannot follow. | Render a `BODYWEIGHT` lift; assert the e1RM card is absent. |
| UI-14 (B10a) | P3 | defect | C | see B5 (upload) | `UI/summary/WorkoutSummaryScreen.kt:144-157, 252-260`; `UI/summary/WorkoutSummaryViewModel.kt:209-231` | The auto-backup caption is an item of the full-summary `LazyColumn` only; a warm-up-only session (`!summary.hasWork`) or a failed summary runs the upload but never shows RUNNING/DONE/FAILED/NEEDS_SIGN_IN. | After a workout with only warm-ups the Drive copy still happens, but the screen never says whether it worked. | Robolectric with a warm-up-only fixture; assert the caption exists. |
| UI-15 (B10a) | P3 | defect | C | — | `UI/history/HistoryViewModel.kt:176, 196-202, 254-259`; `UI/history/HistoryScreen.kt:106, 192`; `UI/units/TodayTicker.kt:47-61` | `cat.today` is evaluated when the catalog recomputes, while the composition's `LocalTodayEpochDay` ticks at midnight and on resume, so a History screen left open overnight moves the calendar's today ring but keeps yesterday's Day/Week window until a write or re-subscription. | Leave History open past midnight and "Today" still totals yesterday while the calendar has moved on. | ViewModel test with `FakeClock` advanced past midnight; assert `horizonTotals.startEpochDay` moves without a write. |
| UI-16 (B10a) | P3 | architecture debt | C | — | `UI/history/HistoryScreen.kt:92-370`; `UI/library/ExerciseLibraryScreen.kt:76-384` | One 278-line and one 309-line composable hold the whole screen, the sheets and the dialogs; neither appears in CURRENT_STRUCTURE's "Known debt". | Two screens are single very long functions, which makes the F6/F8 packets riskier to land. | read |
| UI-19 (B10a) | P3 | defect | C (frame) | — | `UI/history/HistoryScreen.kt:430-458`; `UI/components/GymSurfaces.kt:248-279` | Four `weight(1f)` `MetricCluster`s share the readout row and their kicker labels are `maxLines = 1`/ellipsis, so at 360 dp / font 2.0 "DAYS" and "SETS" print as "DA…" and "SE…". | At large text the totals card's labels are cut to two letters. | Frame `history/populated-360x640-font2.0.png`. |
| UI-20 (B10a) | P3 | opportunity | C (frame) | — | `UI/library/ExerciseLibraryViewModel.kt:91, 141-146`; `UI/library/ExerciseLibraryScreen.kt:254-291` | Every family starts collapsed (`expandedFamilies` is empty), so an unfiltered library of four custom lifts shows one "Your lifts · 4 variants" row and not a single lift until it is tapped. | Open Library and you see no lifts, only one folder to open. | Frame `library/populated-360x640-font1.0.png`. |
| UI-21 (B10a) | P3 | defect | C (code + frame) | ADR-007 | `domain/SessionSummary.kt:43-58` (:49); `domain/ActivityInsight.kt:95-98`; `UI/history/HistoryScreen.kt:258-277` | An activity summary's `durationMinutes` is `cardioMinutes()`, which sums cardio blocks only and ignores `performedStart`/`performedEnd`, so a backdated strength session's History row and the horizon "min" total always read 0. Fix is domain (B8). | A past lifting session you log by hand shows "0 min" in History however long it took. | Frame `history/populated-600x960-font1.0.png` ("Make-up legs … 0 MIN"); unit test: backdated strength activity spanning 45 min → `toSummary().durationMinutes`. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- **B5** — `maybeAutoBackup` fires from `LaunchedEffect(Unit)` before the read settles (`UI/summary/WorkoutSummaryScreen.kt:109-110`), so it runs for a failed or missing summary too; Done while uploading clears the ViewModel and cancels `createBackup` mid-flight (`WorkoutSummaryViewModel.kt:219-221`) with no UI or Settings note that the copy was abandoned; the next finish re-snapshots, so nothing is lost — see B5 for the half-written-file question.
- **B7 (DB/repositories)** — `ExerciseRepository.usageFor` (`data/repository/ExerciseRepository.kt:210-214`) ignores `activity_blocks.exerciseId`; `deleteCustom` (:216-225) is check-then-delete outside `writeExercise`'s transaction. `CompletedTrainingRepository.all()` (:195-201) is the full-graph read per horizon switch (convergence §5, R17 still unmeasured).
- **B8 (domain)** — `ActivitySession.toHistoryEntry` (`domain/HistoryEntries.kt:184-201`) has no callers (W2a's dead-code pass missed it); `kotlin.math.round` in `volumeDisplayWhole` (`domain/WeightFormat.kt:102`) rounds half to even, which is now the single rule everywhere.
- **B12 (a11y)** — `HorizonPicker` merges the card under `contentDescription = "$title, N sessions"` (`UI/history/HistoryScreen.kt:409, 415`), so days, sets, min, PRs and "Moved most" are never spoken (pinned that way by `HistoryPassInstrumentedTest.kt:64-65`). `ExerciseThumb` is `clearAndSetSemantics {}` by design (`UI/components/ExerciseThumb.kt:178`).
- **B13 (tests)** — `HistoryStrongerTest`, `SessionDetailSheetTest`, `HistoryListThumbsTest` (`app/src/test/.../ui/history/`) are source-text assertions (`readText().contains`), the kind T1 retired from `ui/workout`; the only pin of foundation item 5 (`IdentityBeforeMetricsInstrumentedTest`) is in the non-blocking hosted lane.
- **B14 (docs)** — `domain/HistoryCopy.kt:11-12` states the D07 behaviour as intended, against ADR-026 decision 5 (expected until F6a). `completed-training-convergence.md` §1 "failed on the two detail screens" is true but Exercise Detail is not one of them (UI-7).
- **B3/B6 (floor)** — `SetTable.kt:112` rows: the 44 dp rule (:137, :174) is not the touch height; the text column's `space3` padding plus 20 sp + 4 dp + 16 sp (:148-168) makes every row ≥ 64 dp at font 1.0. Not a finding.


## B10b — Settings


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| UI-1 (B10b) | P2 | defect | C | — | `data/repository/BackupService.kt:77-80`, `data/repository/prefs/BackupPrefsStore.kt:185-190,116-123`, `UI/settings/BackupCoordinator.kt:344-350`, `UI/settings/BackupRestoreSection.kt:200-257`, `docs/PRIVACY.md:102-104` | Drive sign-out calls only `clearDriveSession()` (removes the account email and folder id), never `disarmAutoBackup()`, so `AUTO_BACKUP_ENABLED` and the Keystore-sealed passphrase `AUTO_BACKUP_SECRET` survive sign-out while PRIVACY.md promises "signing out of Drive deletes that sealed copy"; the toggle disappears from the screen (it is drawn only when `accountEmail != null`) so the armed state is invisible, and the next finished workout still tries an unattended upload (`UI/summary/WorkoutSummaryViewModel.kt:181-224`). | Signing out of Google Drive is supposed to forget your backup password and switch automatic backup off; it does neither, it just hides the switch, so the password stays on the phone and backups resume by themselves the moment you sign in again. | JUnit (`SettingsViewModelTest` harness): arm auto-backup as `aConfirmedPassphraseArmsAutomaticBackupAndSurvivesAsCiphertext` does, call `viewModel.backup.signOut(activity)`, assert `preferencesRepository.autoBackupSettings().sealedPassphrase == null && !enabled` — fails on trunk. Also read `BackupService.kt:77-80`. | Confirmed; folds into BK-1 |
| UI-2 (B10b) | P2 | defect | C (what is missing) / R (what TalkBack says) | 16 Sep D16; F11 acceptance `docs/FRONTEND_REDESIGN.md:232` | `UI/settings/SettingsSubpage.kt:27-36`, `UI/components/ScreenHeader.kt:84-91`, `UI/settings/SettingsChrome.kt:23-34`, `UI/settings/SettingsScreen.kt:176-407` | Opening any of the 13 sub-pages swaps content inside one NavHost destination (`when (page)`) and neither `SettingsSubpage`'s `ScreenHeader` title nor the root `SettingsHeader` carries `heading()` or `paneTitle`, and the package has no `semantics` call at all (the only ones in scope: `ReminderTimeWheel.kt:71`); the app already knows the technique (`UI/saveposture/SavePostureChooser.kt:83`, `UI/components/RestTimerUi.kt:828`), and only the uppercase section kickers are headings (`GymSurfaces.kt:82-86`). | When someone using the screen reader taps "Backup", the phone never says a new page opened or what it is called; they have to hunt for the Back button to work out where they are. | Robolectric compose test: `SettingsSubpage(title = "Backup") {}` then `onNodeWithText("Backup").assert(SemanticsMatcher.keyIsDefined(SemanticsProperties.Heading))` fails, and no node has `PaneTitle`. Device: TalkBack, tap a row, note what is spoken (R). | Confirmed |
| UI-3 (B10b) | P2 | defect | C | 22 Sep "two ways to regenerate a week" (F10b) — this consequence was not recorded | `UI/settings/SettingsScreen.kt:543-547`, `UI/settings/SettingsViewModel.kt:330-369`, `data/repository/OnboardingApplier.kt:34-42,62-119`, `data/repository/PreferencesRepository.kt:561-572`, `domain/PlanSetupCopy.kt:11-13` | Settings → Week generator → "Generate a week" runs `OnboardingApplier.apply` on the stored answers with no caption and no confirmation: it creates a fresh set of routines beside the existing ones (:34-38, :111) and calls `beginBlock`, which **deletes the current 12-week block if it is not finished** (`PreferencesRepository.kt:569`), while the other door to the same code (Your plan → "Add a new block") says "This adds a new block. It does not delete history" and previews first. | One tap on "Generate a week" quietly throws away the training block you are in the middle of and adds a second copy of every routine; the button next door that does the same job warns you and shows a preview first. | JUnit: seed a block started 3 weeks ago (`preferencesRepository.beginBlock`), seed the catalog, call `viewModel.generateWeek()`, assert the block row is gone (`trainingBlockDao.getCurrent()` start date changed) and `routineRepository` count doubled — passes today, which is the defect. | Confirmed |
| UI-4 (B10b) | P2 | defect / target gap | C | ADR-028 §4 | `UI/settings/SettingsScreen.kt:314-316`, `UI/settings/SettingsViewModel.kt:206-210`, `data/repository/prefs/SavePosturePrefsStore.kt:30-37`, `UI/settings/SavePostureSection.kt:41-48`, `domain/SavePostureCopy.kt:35-37`, `data/sync/SyncCoordinator.kt:22-34` | "Save on this phone" writes only `save_posture`/`save_posture_chosen` (and bumps the sync stamp); it does not sign out, clear the outbox or stop queuing, and nothing in `data/sync` reads the posture (only the synced row fields `SyncRemoteRows.kt:637-638`), so the root can show "How you save · This phone · optional Drive backup" and "Account · Signed in · sync paused" at once, and the LOCAL caption "Your workouts stay on this phone" is untrue while edits keep landing in the upload queue (PRIVACY.md:41-43). | Choosing "Save on this phone" does not sign you out of Temper Account, so Settings tells you your data stays on the phone while the app is still lining changes up to upload when sync resumes. ADR-028 never said which of the two should win, so this needs the owner's call as much as a fix. | JUnit: sign in through `FakeAppDependencies.accountAuth`, `chooseSavePosture(LOCAL)`, assert `accountAuth.session.first() == null` (or that a later save enqueues nothing) — fails on trunk. | Confirmed |
| UI-5 (B10b) | P2 | defect | C (no accessibility model) / R (whether scroll gestures work) | F11 acceptance `docs/FRONTEND_REDESIGN.md:232` | `UI/reminders/ReminderTimeWheel.kt:66-92`, `UI/components/SnapWheel.kt:42-91`, `UI/reminders/ReminderPrefsSection.kt:212-238` | The reminder time is set only through three `VerticalPager`s of plain `Text` with no role, `stateDescription`, `setProgress` or custom action (`SnapWheel.kt:67-89`); the wheel's `contentDescription = spoken` sits on a non-merging Row so the value is read once and then each page as loose digits, and there is no typed alternative, so a screen-reader user has no reliable way to change the time. | A blind user can hear the reminder time but has no dependable way to set it — the wheel is flick-only. | Robolectric: `onNodeWithTag(REMINDER_TIME_HOUR)` has no `OnClick`, `SetProgress` or `StateDescription` (assert `SemanticsMatcher.keyNotDefined`). Device with TalkBack for the pager scroll gesture (R). | Confirmed — duplicate of AX-15, folded |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**UI-1.** Counter-evidence looked for: any other disarm on sign-out. `disarmAutoBackup` has two callers (`BackupCoordinator.kt:381` toggle off; `WorkoutSummaryViewModel.kt:197` when the Keystore blob will not open); `clearDriveSession` has one (`BackupService.kt:79`). No test under `app/src/test` mentions sign-out together with the sealed passphrase (grep `signOut` × `autoBackup|sealedPassphrase`: none). After sign-out the unattended upload declines consent (`WorkoutSummaryViewModel.kt:204-207,224`) and writes `AUTO_BACKUP_NEEDS_SIGN_IN`, which is what the "Paused — sign in to Drive again" subtitle (`BackupRestoreSection.kt:209-210`) is for — so the code treats sign-out as a pause, and PRIVACY.md:104 says delete. One of the two has to move; B5 owns the service, B14 the doc.

**UI-2.** What is announced is R; what is not present is C. The `Back` icon has `contentDescription = "Back"` (`ScreenHeader.kt:41,72`), so the likely experience is focus landing on "Back, button" with no title. Three heading styles as a visual matter are in §1; this row is the semantic gap only.

**UI-3.** `OnboardingApplier.kt:34-38` says a *finished* program is never deleted; `beginBlock` (`PreferencesRepository.kt:554-560`) says an abandoned block "is not" worth keeping and `:569` deletes it. `publishPinnedWeek` → `ensureWeek` (`PlannerRepository.kt:277-290`) only fills missing occurrences; that half is safe. The two `generateWeek` tests (`SettingsViewModelTest.kt:586,615`) pin inputs, not this.

**UI-4.** Adversarial: ADR-028 §4 only says "switch posture and open Account or Backup"; §2 says the chooser "does not repeat until the owner changes posture". Nothing binds posture to the session, so this may be classed as a target gap rather than a defect. Switching to ACCOUNT does the sensible thing (writes posture, opens the Account page: `SettingsScreen.kt:310-313`).

**UI-5.** `ReminderCopy.timeLabel` is spoken on the Row (`ReminderTimeWheel.kt:66-71`) so the *current* value is available. The gap is input. `SettingsHomeLayoutTest.kt:105-120` pins that the wheel exists (source text), not that it is operable.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-6 (B10b) | P3 | defect | C | — | `UI/settings/BackupDialogs.kt:181-189`, `UI/settings/BackupCoordinator.kt:407-414`, `UI/settings/BackupRestoreSection.kt:133` | `ProtectBackupDialog` discards `onConfirm`'s Boolean; when `submitAutoBackupPassphrase` refuses because the Keystore would not seal, the dialog stays open with no message and the refusal is drawn as `state.error` under the dialog's scrim. | If the phone cannot store the backup password, the dialog just sits there and the reason is hidden behind it. | Robolectric: `ProtectBackupDialog(arming = true, onConfirm = { _, _ -> false })`, tap "Turn on automatic backup", assert no error text inside the dialog. |
| UI-7 (B10b) | P3 | defect | C | `docs/FRONTEND_REDESIGN.md:199` | `UI/settings/BackupRestoreSection.kt:80-86,129-132`, `UI/components/GymStatus.kt:154-193`, `UI/theme/Motion.kt:169`, `UI/settings/BackupCoordinator.kt:827-835` | Every backup outcome (restore reconciliation counts, "Protected backup saved…", "Found N backups") is a `GymStatusBanner` that fades after 6 s, is remembered as dismissed so it never returns, and has no `liveRegion` (only `GymUndoHost` `:216-218` and the PR banner add one), so TalkBack is not told the result. The persistent facts (Last backup / Last restore stamps `:92-109`, safety copies `:276-298`) remain; the count sentence does not. | The one sentence that says what a restore actually did disappears after six seconds and a blind user is never told at all. | Robolectric: render `BackupRestoreSection` with `status = "Restored …"`, assert no node with `LiveRegion`; advance 6 s, assert the text is gone. |
| UI-8 (B10b) | P3 | defect (privacy consistency) | C | — | `UI/settings/AccountSection.kt:329-330`, `UI/settings/BackupDialogs.kt:87-92` | The account password field is `rememberSaveable`, so the typed password is serialised into the Activity's saved-state Bundle; the backup dialogs deliberately use `remember` for exactly this reason and say so. | The app is careful never to write the backup password into the phone's "remember this screen" file, but it does write the account password there. | Read the two sites; Robolectric `StateRestorationTester` around `AccountSection` shows the password survives recreation. |
| UI-9 (B10b) | P3 | validation gap | R | 22 Sep "no loading state" | `UI/settings/SettingsViewModel.kt:55-72,150-154`, `UI/settings/AccountCoordinator.kt:61-68`, `UI/settings/SettingsViewModel.kt:163-168`, `UI/navigation/AppNav.kt:289-299` | Initial values are lb / 12-hour / signed-out / LOCAL, so the root subtitles can read "lb · Regular", "Optional cloud sign-in", "This phone · optional Drive backup" for a kg, signed-in, Account user until DataStore and the auth session emit; AppNav subscribes `uiState` before the onboarding gate opens (`:289`) so the window is small, but nothing marks "loading". | For a moment the Settings list may show the wrong choices before it catches up; how visible that is needs a phone. | Device: cold start straight into Settings after process death; watch the Display/Account rows. |
| UI-10 (B10b) | P3 | defect | C | — | `UI/reminders/ReminderPrefsSection.kt:66-72` | `editing` (which weekday the wheel edits) is `remember`, not `rememberSaveable`; a rotation falls back to the first alarm day, so the wheel silently edits a different day than the one tapped. | Turn the phone while setting Wednesday's reminder and the wheel is suddenly changing Monday's. | Robolectric `StateRestorationTester` around `ReminderPrefsSection` with two alarms; select the second, restore, assert the selected row. |
| UI-11 (B10b) | P3 | defect | C | — | `UI/settings/AccountSection.kt:85-90` | `signedOutStep` is `remember`, so rotation or process death on the sign-in form bounces the user back to the "Open your account" entry page (fields may or may not come back — R). | Turn the phone while typing your email and you are sent back to the first Account page. | Robolectric `StateRestorationTester`: open Credentials, restore, assert `SettingsTags.ACCOUNT_CREDENTIALS` still exists — fails. |
| UI-12 (B10b) | P3 | defect (copy) | C | ADR-012 d18 | `UI/settings/RestTimerPrefsSection.kt:42-44`, `domain/RestTimer.kt:172-183` | The Rest timer caption "Default rest is used after a working set if the lift has none and you haven't picked a preset" omits the first term of the order (coach's suggestion → routine's rest → last preset → default) that ADR-012 d18 fixed on 23 Sep. | The Rest settings page describes the old rule; the coach's suggested rest now comes first. | Read both; B6 owns the rule, the caption is Settings copy. |
| UI-13 (B10b) | P3 | opportunity | C | `docs/FRONTEND_REDESIGN.md:197` "discoverable version/build/update" | `UI/settings/SettingsScreen.kt:86-89,215-219`, `UI/update/DebugUpdateBanner.kt:82-91`, `UI/settings/AboutSections.kt:144-154` | Opening Settings triggers a silent update check and the Update row exists only while an offer exists, so there is no "up to date / last checked" state and no manual check; the Version row does show the live drop (`app/build.gradle.kts:197-198` sets `versionCode = debugLiveCode`). | You can see which build you have, but not whether the app looked for a newer one or when. | Read; render `SettingsHome(updateSummary = null)` and note no update row. |
| UI-14 (B10b) | P3 | validation gap | R | 22 Sep cross-tab process-death row | `UI/settings/BackupCoordinator.kt:120,136,850-870`, `UI/settings/SettingsScreen.kt:98-102`, `UI/settings/SettingsViewModel.kt:82-86,186-190` | With no `SavedStateHandle`, process death loses: the restore preview (`pendingPlan`, dialog vanishes; re-pick), the pending ciphertext (unlock dialog), the generate notice, the pending sub-page request, and the Drive consent waiter — the consent Activity's result then reaches a fresh coordinator whose `resolutionWaiter` is null (`:851`) and the sign-in ends silently with no error and no busy state. Export protection is handled (`:720-726`) and restore is journaled (`:574-583`). | If Android kills the app while Google's permission screen is up, granting permission does nothing and nothing says so; you have to tap Sign in again. | Device: "Don't keep activities" on, tap Sign in with Google, grant, observe. |
| UI-15 (B10b) | P3 | opportunity | C | — | `UI/settings/BackupDialogs.kt:138,157,235`, `UI/settings/AccountSection.kt:365` | Four password fields, none with a show/hide toggle; the backup one is min 8 with a confirm field (`data/backup/BackupEnvelope.kt:42,77-81`), which is the right gate but a typo in both fields still seals a password nobody can read back without the lock-screen reveal. | You cannot peek at the password you are typing. | Read. |
| UI-16 (B10b) | P3 | defect (copy) | C | — | `UI/settings/AccountSection.kt:372,394,406`, `data/auth/SupabaseAccountAuth.kt:115-147`, `domain/AccountAuthCopy.kt:54-63` | Client validation is `isNotBlank()` only; four server cases are mapped and every other Supabase message is shown verbatim (`AccountAuthError.Message`), e.g. the server's own wording for a too-short sign-up password. | Some error messages come straight from the server in its words, not the app's. | JUnit on `toAccountAuthError` with a sign-up weak-password exception → `Message(raw)`. |
| UI-17 (B10b) | P3 | defect | C | — | `UI/settings/BackupRestoreSection.kt:379-383`, `UI/settings/BackupCoordinator.kt:312-317` | "Yesterday"/relative stamps and the 14-day staleness are computed from the clock inside `remember(atMillis, clock)` and inside `combine`, so they only refresh on a new emission or re-subscription (`WhileSubscribed(5_000)` makes that roughly each Settings visit). | The "last backup: Yesterday" wording can go stale if the page is left open across midnight. | Read; device across midnight (R). |
| UI-18 (B10b) | P3 | defect (copy) | C | ADR-031 §3 | `domain/AccountAuthCopy.kt:8-10`, `UI/settings/AccountSection.kt:253-262,136-137` | The Account entry page's headline blurb promises "Keep … in the cloud, and bring them back on another phone" unconditionally; the paused notice is drawn further down the same page. | The big promise at the top of the Account page is not true while sync is paused; the small print lower down says so. | Read. |
| UI-19 (B10b) | P3 | opportunity (copy) | C | ADR-005 | `UI/settings/BackupCoordinator.kt:340`, `data/backup/BackupJson.kt:18`, `data/repository/BackupService.kt:613` | "Backups stay in your PersonalTrainer Backups Drive folder" is accurate (the folder is named that) but carries the retired product name; renaming would orphan existing folders, so this is a naming decision, not a copy fix. | The Drive folder is still called by the app's old name. | Read. |
| UI-20 (B10b) | P3 | defect | C | — | `UI/update/DebugUpdateBanner.kt:54-64,94-111`, `UI/components/GymStatus.kt:320-338`, `update/DebugUpdateMonitor.kt:111-118` | The update banner is a `GymNoticeBanner` with no live region and its download bar is a bare `Box` with no `progressBarRangeInfo`, so TalkBack hears neither progress nor state changes; and because the monitor publishes `Idle` the moment the APK is handed to Android (`:118`), the "Android will ask you to install" state is visible only during that hand-off — there is no banner state for a system install sheet that was cancelled or stalled (B7 owns the cause). | The update banner cannot tell a blind user it is downloading, and if Android's install screen is dismissed the banner just goes back to "Update" as if nothing happened. | Robolectric: `DebugUpdateBanner(ui = Downloading 40%)`, assert no `ProgressBarRangeInfo`; read the monitor for the missing state. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- **B5 (backup service):** `BackupService.signOut` (`data/repository/BackupService.kt:77-80`) does not disarm auto-backup — see UI-1; the fix sits in the service.
- **B3 (Plan):** `PreferencesRepository.beginBlock` (`:561-572`) deletes an unfinished current block; every re-run of guided setup and Settings' Generate a week goes through it (`OnboardingApplier.kt:102-108`). See UI-3.
- **B6 (rest timer):** changing "Default rest" in Settings writes prefs only (`SettingsViewModel.kt:465-469`); whether a running rest is affected is B6's; presets are 30/60/90/120/180 s + Custom (`domain/RestTimer.kt:86`; `RestPresetChips` `UI/components/RestTimerUi.kt:775-801`), custom validated by `RestTimer.parseCustom` (`:155-170`, 15 s–30 min, "90" or "1:30").
- **B7 (updater):** `DebugUpdateMonitor.kt:111-118` publishes `Idle` right after `installer.install(dest)` returns; no stalled/cancelled-sheet state exists for the banner to show (UI-20).
- **B8 (reminders/WorkManager):** `setReminderQuietHours` does not call `rebuildWorkoutAlarms()` (`SettingsViewModel.kt:268-274`) while opt-out and day alarms do (`:261-292`); that is fine only because quiet hours are applied at fire time (`data/repository/PlannerRepository.kt:431-459`, `domain/ReminderPolicy.kt:28-46`). Worth one test.
- **B11 (accessibility/design system):** `ScreenHeader`'s title has no heading semantics app-wide (`UI/components/ScreenHeader.kt:84-91`), `GymStatusBanner`/`GymErrorBanner`/`GymNoticeBanner` have no `liveRegion` (`GymStatus.kt:121-193,320-338`; only the undo host `:216-218` and the PR banner `:284-286` do), `SettingsStrip` uses `Role.Button` for exclusive choices (`SettingsChoice.kt:86-92`) — D08/F8a.
- **B12/B13 (sync/auth):** raw Supabase messages pass through `AccountAuthError.Message` (`SupabaseAccountAuth.kt:146`) — UI-16 is the visible half. `abandonOutboxOnSignOut` clears the outbox and `lastError` but not pull cursors (`SyncCoordinator.kt:41-50`) — S-9, S1.
- **B14 (docs truth):** `docs/PRIVACY.md:104` "signing out of Drive deletes that sealed copy" — contradicted by code (UI-1). `docs/PRIVACY.md:113` "Quiet hours are 22:00–07:00" — that is the default (`domain/SchedulePlannerModels.kt:117-118`) but the owner can set 20–23 / 5–8 in Settings (`domain/ReminderCopy.kt:26-27`; `ReminderPrefsSection.kt:169-203`; ADR-017 d6). `data/repository/prefs/SettingsStore.kt:22` and `BackupPrefsStore.kt:9` say "forty-six keys"; the file declares 55 (`SettingsStore.kt:76-139`) and `CURRENT_STRUCTURE.md` says 56. `LegalCopy.PRIVACY_POLICY_URL` (`domain/LegalCopy.kt:5-6`) points at `sinura7/PersonalTrainer/blob/trunk/docs/PRIVACY.md`, which matches the remote and the sitting branch; whether the repository is public enough for that link to open for a stranger is R (B13b can check).
- **B4 (first launch):** `SavePostureHost.kt:43-53` writes the posture and requests the Account/Backup sub-page through the shared Activity-scoped `SettingsViewModel` (`AppNav.kt:286,542-547` passes the same instance), so the hand-off is one object; nothing to add from my side.


## B10c — Onboarding and activity screens


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| UI-1 (B10c) | P2 | defect | C (heights) / R (pixels) | — | `ui/onboarding/OnboardingScreen.kt:98-103,291-335,341-373,384-409,420-445` | Four setup steps (Place, Days per week, Which days, Bodyweight) are plain `Column`s inside a non-scrolling root `Column`; the file has no `verticalScroll` (only `LazyColumn` at `:203,:229,:456`), so at 360×640 dp the Place step's `Continue` sits below the fold even at font 1.0 (≈740 dp of content, see notes), and at font 1.6/2.0 or landscape so do the other three. | On a short phone, with large text, or held sideways, the "Where will you train?" question's Continue button is pushed off the bottom and the page will not scroll, so guided setup cannot be finished. | Robolectric render of `OnboardingScreen` at `w360dp-h640dp` (and `fontScale=2f`) after `setFocus(STRENGTH)`, `next()`×3; assert `onNodeWithText("Continue").assertIsDisplayed()` — expected to fail. | Confirmed; folds into BK-1 |
| UI-2 (B10c) | P2 | defect | C | — | `ui/activity/LiveCardioScreen.kt:80-83,112-118`, `ui/activity/LiveCardioViewModel.kt:74-81,270-273,329-337`, `ui/activity/SavedStateCardioDraft.kt`, `ui/navigation/AppNav.kt:419-424,695`, `ui/navigation/LiveSessionBarViewModel.kt:182-204`, `domain/CardioCopy.kt:21-22` | "Leave running" pops the LiveCardio back-stack entry; the type / indoor / distance the owner set live only in that entry's `SavedStateHandle` (never in Room), so Resume from the bar opens a fresh entry that re-reads the Room row, and the bar's "Finish session" reads Room only — the inputs are lost either way, while the caption promises "This clock keeps running if you leave". | If you switch the run to a walk or type a distance and then tap "Leave running", those changes are silently thrown away: coming back shows a fresh screen and finishing from the bottom bar saves without them. Only the clock survives. | `LiveCardioViewModelTest`: start a live run, VM on handle A, `setDistanceKm("2.5")`, `clearAndJoinForTest()`; new VM on a **new** `SavedStateHandle(mapOf("sessionId" to id))` (what a re-navigation gives) → `distanceKm == ""`. Bar: `LiveSessionBarViewModel.finishFromBar()` after the same steps → `distanceMeters == null`. | Confirmed |
| UI-3 (B10c) | P2 | defect | C | 22 Sep Cross-tab (process death) | `ui/navigation/AppNav.kt:553-570`, `AppContainer.kt:292`, `domain/CustomWeekLaunch.kt:6-8`, `ui/routines/CustomWeekScreen.kt:84-86`, `ui/routines/CustomWeekViewModel.kt:196-208,367-379`, `ui/routines/SavedStateCustomWeekDraft.kt:104-106`, `data/repository/OnboardingApplier.kt:236-247` | "I'll build my own" hands the questionnaire to CustomWeek through an in-memory `pendingCustomWeek`; after process death the restored CustomWeek entry re-seeds from `null`, so Confirm applies with `answers = null` and writes no goal, emphasis, age, places, focus or bodyweight and skips the pending weight unit — the custom-week draft saves only days and lifts. | If you choose "I'll build my own" and Android quietly closes the app while you are building the week, everything you answered before (goal, where you train, your weight, kg or lb) is dropped without a word; the week itself still saves. | `CustomWeekViewModelTest`: `seedFromGuided(days, answers(goal=STRENGTH, bodyweight=80), KG)`, add a lift, `clearAndJoinForTest()`; new VM on the same handle, `seedFromGuided(emptySet(), null, null)` (what the screen does after death), `confirm()` → assert `trainingGoal`, a weigh-in and `weightUnit == KG` landed; fails today. | Confirmed |
| UI-4 (B10c) | P2 | target gap | C | F9; FRONTEND_REDESIGN "readable … date/zone" | `ui/activity/ActivityDetailScreen.kt:138-145,294-359`, `domain/ActivityDetailCopy.kt` (no date function), `ui/units/DateCopy.kt:12-13` | The activity detail / receipt renders title, modality caption, hero number, tiles, notes and blocks but never the captured local date, time or zone, although ADR-011 §2 makes the captured date the day's identity and `DateCopy` (:13) says it serves "session detail". | Opening a saved run, or a workout you logged after the fact, never says which day it happened on. | Read `ActivityDetailScreen.kt` end to end; `ActivityDetailViewModelTest` can assert a `dateLabel` in state once one exists. | Confirmed |
| UI-5 (B10c) | P2 | defect | C | — | `ui/onboarding/OnboardingScreen.kt:117-125,463-475,535-543`, `ui/onboarding/OnboardingViewModel.kt:66-69`, `domain/RoutineGenerator.kt:432-442`, `domain/OnboardingPreviewCopy.kt:11-13,31-33`, `data/repository/OnboardingApplier.kt:80-92,160-168`, `data/repository/PreferencesRepository.kt:225` | The first question ("What do you want to record?") changes nothing after accept: `BOTH` is generated and applied exactly as `STRENGTH` (no cardio day is pinned; the stored focus has no reader outside re-seeding and sync), and `CARDIO` still asks days, which days and place, previews a week of seven "Rest" rows under the line "…not generated.. 12 weeks of Full Body." (double period, split named for a plan with no routines), then pins nothing. | Answering "Both" promises "lifting days and cardio, kept as their own work" but the saved plan has no cardio days; answering "Cardio" asks four questions for nothing and shows a week of rest days with a garbled sentence. | `OnboardingFocusTest`: `setFocus(BOTH)`, seed catalog, walk to PREVIEW → `preview.days.none { cardio }`; `OnboardingApplierTest`: apply with `focus=CARDIO, daysPerWeek=3` → `scheduleRepository.slots().isEmpty()`. Read `OnboardingScreen.kt:463-473` for the string. | Confirmed |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**UI-1 (arithmetic, 360 dp wide, font 1.0).** Root: `OnboardingHeader` ≈ 56 dp + `space4` 16. `PlaceStep` (`:291-335`, `spacedBy(sectionGap = 28)`): `QuestionTitle` = title 20 + 8 + `STEP_BLURB` (≈100 chars at 14 sp on 328 dp → 3 lines × 20) = 88; four cards each `cardPadding` 16 + 20 + 4 + 20 + 16 = 76 (`Metrics.kt:35`, `Type:94,110`), with 3 gaps = 388; `mixCaption` for gym-only is `STEP_BLURB` again ≈ 48; `PrimaryGymButton` = `Metrics.control` 56 (`GymButtons.kt:61`, `Metrics.kt:89`). Sum ≈ 56+16+88+28+388+28+48+28+56 = **736 dp** against ≈ 570–616 dp usable on a 640 dp screen. Counter-evidence looked for: a scroll modifier on the root (`:98-103`, none), on `PlaceStep` (`:291`, none), `imePadding`/`weight` tricks (none), and a render test of `OnboardingScreen` (none — `app/src/androidTest/.../ProductionScreensPassInstrumentedTest.kt:281-356` mounts composer, live cardio and detail only; `ui/onboarding` tests are ViewModel tests plus `BodyweightWheelRenderTest`, which renders the wheel alone). `BodyweightStep` at font 2.0 is the same shape: a 76 sp hero numeral (`Type:76`), a 144 dp wheel (`BodyweightWheel.kt:155`), two 56 dp buttons and a four-line blurb. Not P1 because guided setup is not on the forced first-run path (F8c) and Back still works.

**UI-2.** `LeaveCardioDialog.onLeaveRunning` and the dock's `Leave running` both call `onExit` (`LiveCardioScreen.kt:80-83,115`), which is `navController.popBackStack()` (`AppNav.kt:695`). `SavedStateHandle` belongs to the popped `NavBackStackEntry`, so `inputs.read()` (`LiveCardioViewModel.kt:79-81`) is null on the next entry and `loadAndTick` takes type/indoor from the Room block (`:270-273`); the Room block is what `StartLiveCardio` wrote at start (`workout/StartLiveCardio.kt:102-115`: the scheduled type, `indoor = false`, no distance), and the ViewModel deliberately never writes the draft to Room (`:74-77`). `finishActivityFromBar` (`LiveSessionBarViewModel.kt:182-204`) copies only `elapsedSeconds` onto those blocks. Counter-evidence: `recreationKeepsTheChangedInputs` (`LiveCardioViewModelTest:162`) reuses the **same** handle, which models process death, not a pop; no test covers leave-and-return. The bar's one-tap Finish with no confirm (`LiveSessionBar.kt:224-231` vs Discard's confirm `:246-258`) is B9a's — see §4.

**UI-3.** `CustomWeekLaunch.kt:7-8` explains the in-memory hand-off as protection against an Activity recreation dropping a lambda; it does not survive the process. `CustomWeekScreen.kt:84-86` is `LaunchedEffect(preferredDays, answers, pendingWeightUnit) { seedFromGuided(...) }`, which runs again on the restored entry with `launch == null`, overwriting nothing useful because the ViewModel was also recreated with `guidedAnswers = null` (`CustomWeekViewModel.kt:196-202` are plain fields, not saved state; the draft store keys are only `customWeek.selectedDay/userPicked/days`, `SavedStateCustomWeekDraft.kt:104-106`). The Onboarding entry underneath still holds the encoded answers (`OnboardingViewModel.kt:403-406`) but nothing reads them across the fork. `OnboardingApplierTest:315 aCustomWeekFromGuidedKeepsTheQuestionnaire` pins the happy path only. Fix shape (for B9b): persist the launch in CustomWeek's own `SavedStateHandle` via `OnboardingAnswers.encodeDraft`, which already exists for exactly this (`OnboardingAnswers.kt:341-345`).

**UI-4.** `ActivityReceiptHero` (`:294-359`) and `ActivityMetricTiles` (`:361-427`) are the whole top of the page; `ActivityDetailHeader` (`:233-253`) passes `title` only, no `subtitle`. `ActivitySession.performedStart` carries `localEpochDay`, zone and offset (ADR-011 §1), so the data is there. The celebration variant has no header at all (`:137-145`), so the Done receipt also lacks the day.

**UI-5.** Readers of the stored focus: `grep -rn TrainingFocus` outside `domain/` and `ui/onboarding/` finds only `data/repository/prefs/CoachingPrefsStore.kt` (store), `PreferencesRepository.kt:225` (the re-seed of setup), `OnboardingApplier.kt:92,243` (writes) and `data/sync/*` — no `ui/`, `workout/` or coach reader. `questionsFor(BOTH)` is the full list (`OnboardingViewModel.kt:66-69`) and `RoutineGenerator.generate` branches only on `CARDIO` (`:432`). The preview string: `headline` returns `"Cardio logging is ready. A lift week is not generated."` (`OnboardingPreviewCopy.kt:11-13`) and the screen appends `". "` then `"${TrainingBlock.DEFAULT_WEEKS} weeks of ${it.splitStyle.displayName}."` unconditionally (`OnboardingScreen.kt:464-467`); `OnboardingPreviewCopyTest:20` pins the headline alone, not the concatenation. `cardioFocusPreviewDoesNotInventLifts` (`OnboardingFocusTest:278`) pins "no routines", which is the promise; what it does not pin is that nothing cardio is scheduled either.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-6 (B10c) | P3 | validation gap (accessibility) | C | — | `ui/onboarding/OnboardingScreen.kt:296-327` | The place cards are `Modifier.clickable` with no `toggleable`/role/state; "on" is only a visible "Selected" caption (`:324-326`), "off" has no cue, unlike the Experience step's `selectable(role = RadioButton)` (`:252-259`). | With the screen reader, tapping "A full gym" gives no "checked / not checked"; you only hear "Selected" when it is on and nothing when it is off. | Robolectric semantics: node with text "A full gym" lacks `SemanticsProperties.ToggleableState` and `Selected`. |
| UI-7 (B10c) | P3 | validation gap (accessibility) | C / R (announcement) | — | `ui/onboarding/BodyweightWheel.kt:105-112,167-185`, `ui/components/SnapWheel.kt:254-276` | The hero numeral's `semantics { contentDescription = "Type bodyweight" }` replaces its spoken text, the wheel's pages are plain `Text` with no selected/value semantics and the pager exposes only scroll, so the chosen weight is spoken nowhere on the step; the typed dialog (`:131-145`, `TalkBackPolicy`) is the only path that reads it back. | A screen-reader user can flick the wheel but is never told which number it landed on; they have to open the "Type bodyweight" box to hear it. | Semantics: `onNodeWithText("80")` has `ContentDescription = ["Type bodyweight"]` and no `StateDescription`; TalkBack reading order on the phone (R). |
| UI-8 (B10c) | P3 | target gap | C | 22 Sep Cardio row; ADR-011 §3 | `ui/activity/ActivityComposerViewModel.kt:171-180,368-375`, `ui/activity/ActivityComposerScreen.kt:154-177`, `domain/ComposerCopy.kt:27` | Backdating sets only the day: time is fixed at noon (honestly captioned), the zone is the device zone at save (`now.zoneId`) and is neither shown nor settable, and `Earlier` has no lower bound (`setEpochDay` clamps the upper bound only), so a 1970 activity is accepted; ADR-011 §3 asks for date, time and zone. | You can only pick the day, never the time or the time zone, and you can tap "Earlier" back to any year. | `ActivityComposerViewModelTest`: `setEpochDay(-10_000)` then `save()` → a row with `localEpochDay = -10_000`. |
| UI-9 (B10c) | P3 | opportunity | C (absence) / R (phone) | — | `docs/DATA_SAFETY.md:30-31`, `app/src/main/AndroidManifest.xml:56`, `ui/navigation/LiveSessionBarViewModel.kt:116-117,138`, `timer/CardioTimerPersistence.kt` | A running cardio has no foreground service or notification (the FGS is rest-only), and the cardio bar hard-codes `stale = false` while a workout bar warns "Left open Nh"; recovery after process death is by timestamps in `cardio_timer_state` (pinned by `timer/CardioElapsedTest`), so the clock stays honest but nothing ever tells the owner a session was left running. | If you forget a run is going, nothing on the phone reminds you; days later the app still counts it as running and the bar does not warn you the way it does for a forgotten workout. | Phone: start cardio, swipe the app away, reopen next day — bar shows the session with a huge elapsed and no warning (R). Code: `LiveSessionBarViewModel.kt:138`. |
| UI-10 (B10c) | P3 | validation gap | C (absence) / R | — | `ui/activity/LiveCardioScreen.kt:107`, `ui/activity/ActivityComposerScreen.kt:90-94,108`, `MainActivity.kt:56`, `AndroidManifest.xml:36` | The live cardio `Scaffold` has no `imePadding`, unlike the composer's (which carries a comment and an instrumented proof, `ProductionScreensPassInstrumentedTest.kt:281-303`); with edge-to-edge and `adjustResize` the keyboard can cover the distance box and the Finish dock. | Typing a distance on the live screen may hide the box and the Finish button behind the keyboard. | Extend `liveCardioAt360Font2KeepsTheDockAndReadout` with `performClick()` + `performTextInput` on the distance field, then `assertIsDisplayed()` on `CardioTags.FINISH`. |
| UI-11 (B10c) | P3 | defect (polish) | C | — | `ui/activity/LiveCardioViewModel.kt:222-245`, `ui/activity/LiveCardioScreen.kt:122-135` | After the owner confirms Discard, the screen stays and shows the "No live cardio — That session is gone. Start a new one from Home." empty state with a Back button instead of leaving. | After you delete a run the app tells you "that session is gone" as if something went wrong, and you still have to tap Back. | `LiveCardioViewModelTest:148` already reaches `missing = true`; the screen mapping is at `:122-135`. |
| UI-12 (B10c) | P3 | defect by design (owner decision) | C | CURRENT_STRUCTURE "six soft foreign keys" | `data/repository/ActivityRepository.kt:248-262`, `ui/activity/ActivityDetailViewModel.kt:112-129` | Deleting a completed activity leaves its Plan day `DONE` with `completedActivityId` pointing at a row that no longer exists (documented as intentional, "as deleting a finished strength session does"). | If you delete the only record of a planned day, the plan still shows that day as done, and it cannot be redone. | `ActivityDetailViewModelTest`: confirm with `occurrenceId`, `deleteSession()`, read the occurrence → still `DONE`. Owner: is that the wanted behaviour? |
| UI-13 (B10c) | P3 | validation gap (accessibility) | C | — | `ui/activity/ActivityComposerScreen.kt:180-191,206-222,560-571` | Each line's `Remove` is a `TextButton` whose only name is "Remove"; the line title is a sibling `InstrumentRow`, not merged, so several lines announce identically. | With the screen reader, three "Remove" buttons in a row all sound the same; you cannot tell which set each one removes. | Semantics: `onAllNodesWithTag(ComposerTags.REMOVE_STRENGTH)` nodes have text "Remove" and no contentDescription naming the lift. |
| DC-1 (B10c) | P3 | docs truth | C | — | `docs/artifacts/onboarding-programs-review.md:1-11`, `domain/PlanReviewRenderer.kt:9,40-52,67`, `domain/OnboardingAnswers.kt:223-243`, `domain/RoutineGenerator.kt:449`, `app/src/test/.../domain/RoutineGeneratorTest.kt:615-631` | The review claims "Every program `RoutineGenerator` can produce" (and the renderer comment says "420 reachable combinations"), but the renderer iterates single places × age × days × goal plus two emphasis samples, while the screen lets places combine (`withToggledPlace`: Gym + Hyper Pro, Home + Bodyweight, Hyper Pro + Home + Bodyweight …) and those unions change the allowed catalog; the golden test pins doc = renderer, not renderer = generator space. | The "every programme" review leaves out the mixed-equipment weeks people can actually pick, so a reviewer reading it has not seen them. | Render `RoutineGenerator.generate(OnboardingAnswers(places = setOf(FULL_GYM, HYPER_PRO)))` and search the doc for its Hyper Pro lifts — absent. For B14. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- **B9a (live bar):** "Finish session" for cardio is a one-tap menu item with no confirm (`ui/navigation/LiveSessionBar.kt:224-231`) while Discard confirms (`:246-258`); together with UI-2 it saves a run without the distance the owner typed on the live screen. Also the cardio bar never goes stale (`LiveSessionBarViewModel.kt:138`, UI-9).
- **B9a (start sheets):** `ResumeOrDiscardDialog` is hard-wired to WORKOUT copy (`ui/components/ResumeOrDiscardDialog.kt:385,399`) and is shown only for a strength session in progress (`ui/home/HomeScreen.kt:120-126`, `HomeViewModel.kt:452-457`); a live **cardio** blocking a start surfaces as the bare string "One live activity at a time." (`HomeViewModel.kt:370-371`, `ui/workout/StartOptionsViewModel.kt:224-225`), not the ADR-018 §6 fork. Mostly unreachable because starts hide while anything is live (`HomeViewModel.kt:79,133`; `HomeViewModelTest:610`). The invariant itself is enforced in the store on both lanes (`ActivityRepository.kt:172-176,198-200`, `WorkoutRepository.kt:119-126,430,444`; `activity/OneLiveCrossLaneTest`).
- **B9a/B10a:** the Body/History/Plan sheet's cardio path starts a `RUN` titled "Cardio" with no type choice (`StartOptionsViewModel.kt:209-218`), unlike Home's picture cards (`HomeViewModel.kt:315-322`); ADR-021 §2's cardio path exists only on Home. Also `StartOccurrenceOutcome.OpenComposer` from the sheet (`:424-425`) binds the occurrence but the composer's date defaults to today (`ActivityComposerViewModel.kt:113-116`) — Home relocates a leftover first (ADR-019); check the sheet does too.
- **B9b (CustomWeek):** receiving side of UI-3; fix belongs there (persist the launch in CustomWeek's `SavedStateHandle`).
- **B10b / B11 (Settings, components):** `SnapWheelColumn` (`ui/components/SnapWheel.kt:229-278`), used by the reminder wheel, has the same no-value semantics as UI-7. `ChoiceStep` rows are `InstrumentRow(selected)` + `RadioButton(onClick = null)` (`OnboardingScreen.kt:212-219`); whether `InstrumentRow` exposes a radio role is F8a's.
- **B4b (OnboardingApplier):** preferences are written before the routine transaction (`OnboardingApplier.kt:72-108`), so a failed apply leaves changed preferences with the routines dropped (documented at `:72-75`); and `applyPlan` treats a failed `publishPinnedWeek` as log-only (`OnboardingViewModel.kt:381-386`).
- **Tests pass (TS-):** no test mounts `OnboardingScreen` at any size or font — the ADR-032 matrix has never been run on setup, which is how UI-1 survived; `ProductionScreensPass` mounts composer (360/2.0), live cardio (360/2.0) and detail (600/1.0) only (`:281-356`).
- **B14 (docs):** DC-1; `OnboardingScreen.kt:79` stale entry comment (F8c); `OnboardingViewModel.kt:100-102,115-117` says setup "never writes anything until Use this plan" while `retryCatalog` seeds the catalog (`:302-304`) and Back with an existing program writes `onboardingComplete` (`:260-269`) — both defensible, neither stated.


## B11 — Live workout floor


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| UI-1 (B11) | P2 | defect | C | — | `ui/workout/RestNotificationGate.kt:41-62,74,81`, `ui/workout/RestTimerScreen.kt:84`, `Screen:221-222`, `ui/navigation/AppNav.kt:617-645` | `decided` and `showWhy` are `rememberSaveable` inside the composable, so the "Rest alerts" explanation dialog opens on every new instance of the floor and of the rest page (each `navigate` is a new back-stack entry) while `POST_NOTIFICATIONS` is not granted on Android 13+, and after two system denials its Continue launches nothing the phone will show. | On a newer Android phone, once you have said "Not now" to rest alerts, the app asks the same question again every time you open the rest screen and every time you come back to a workout, and eventually its Continue button does nothing. | Robolectric (project default SDK 35, `robolectric.properties`) with the permission ungranted: compose `RestTimerScreen(...)` in two separate `showFloor` compositions and assert a node with `RestNotificationCopy.TITLE` both times. Nothing pins the gate today: every floor render passes `restNotificationsEnabledOverride = true` (`FloorTestKit.kt:365`), and `RestPageSkipTest.kt:132` composes the real page with the dialog up and never asserts on it. | Confirmed — duplicate of RT-2 |
| UI-2 (B11) | P2 | defect | C | — | `Screen:409-413`, `VM:202,210-211,1520-1523,2133-2136`, `ui/components/EndWorkoutDialog.kt:24-72` | The header's Finish is enabled while a saved set is being edited (`canFinish` ignores `editingSetId`), `EndWorkoutDialog` says nothing about the open edit, and `finishWorkout` passes `canChangeEntry()` and writes `finishedAt`, so the correction typed into the entry is dropped without a word; only the dock's commit is guarded (it reads Save changes). | If you are fixing a set you already logged and tap Finish at the top of the screen, the workout ends and your fix is thrown away silently. | `showWorkoutScreen` with one logged set: `vm.editSet(id)`, change the weight, tap `WorkoutTestTags.FINISH` then `EndWorkoutTags.SAVE`; assert the session is not finished, or that the dialog names the open edit. `DockCommitRenderTest.anOpenEditSavesBeforeTheWorkoutCanFinish` (:170-176) covers the dock only. | Confirmed |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**UI-1.** `rememberRestNotificationsEnabled()` (`RestNotificationGate.kt:41`) holds `decided` in `rememberSaveable` (`:48`), keyed to the composable instance; `LaunchedEffect(Unit)` (`:55-62`) sets `showWhy` whenever SDK ≥ 33 and the permission is not granted. `AppNav.kt:624-628` navigates to `Route.RestTimer` with `launchSingleTop` (a new entry each open, since the page is never already on top when tapped from the floor), and Back from the floor pops its entry (`AppNav.kt:622`), so the next entry from Home is new. Counter-evidence looked for: no preference key records the decision (`LaunchPermissionsHost.kt:71` keeps its `skippedNotifications` in `rememberSaveable` too); the copy's own KDoc says the persistent surface is the one-line recovery row, not this sentence (`domain/RestNotificationCopy.kt:19-22`), which the dock and the rest page do show (`WorkoutDock.kt:253-257`, `RestTimerScreen.kt:242-246`). Below API 33 the dialog never opens.

**UI-2.** `canFinish` (`VM:210-211`) = found ∧ sets non-empty ∧ `!entryLocked`; `entryLocked` (`:202`) is `save.pending || logging || mutating || loadState != FOUND` — editing is none of these once `editSet` has returned. `finishWorkout` (`:2133-2136`) checks only `canChangeEntry()` (`:1520-1523`), which also ignores editing. The dialog's body is `EndWorkoutCopy.body(loggedSets)` only. Nothing is lost from the database (the set keeps its stored values); what is lost is the typed correction and the intent. Counter-evidence: the dock derives `SAVE_CHANGES` before `FINISH` (`ui/workout/WorkoutPrimaryAction.kt:153,158`) — so the contract "Editing … take[s] precedence over ordinary completion" holds for the dock and not for the header. No test drives header Finish while editing (`finishIsDisabledWithZeroSetsAndWhileLogging`, `finishWaitsWhileASetIsStillBeingSaved` cover zero sets and saving).

**W2b-4 (from §1; pending at `1ad3b11`, closed by #413 while this audit ran).** The Log's coach call (`VM:561-575`) passes the live `draft`, `wantAnotherSet = core.wantAnother` (`:569`), `historySets = core.lastPerformance?.sets` (`:572`) and `editingSetId`. The rest page's call (`RestVM:139-155`, and the seed at `:75-90`) passes a draft rebuilt from `workoutDraftCache.get(sessionId)` (`:138`), `editingSetId = null` (`:149`), and takes the defaults `wantAnotherSet = false` and `historySets = emptyList()` (`ui/workout/SetMicroRecUi.kt:87-90`). So: (a) after **Add another set** the Log's `allowExtra` is true and its line names the extra set, while the page's inputs say the lift is done (`LIFT_DONE`) and its line is the done call or absent; (b) on a **lift's first set** the Log seeds the call from last session's working sets (`WorkoutMicroRecTest.theLastSessionsEffortSeedsTheFirstSet`: reason `FIRST_SET`, RPE 9), the page has no history and cannot — `RestTimerViewModelTest.kt:391-393` says so in its comment and tests only the hint-driven weight. Two smaller differences ride along: while a set is being edited the Log shows no call and the page does; and `WorkoutDraftCache.get` falls back to another lift's draft while the selected lift has none (`workout/WorkoutDraftCache.kt:52-56`), a millisecond window during prefill. Fix shape for the packet: give `RestVM` the same three inputs (`lastPerformance`, `wantAnotherSet`, `editingSetId`) — the first needs `WorkoutRepository.lastPerformance` read the way `VM:766` reads it.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| UI-3 (B11) | P3 | architecture debt | C | 22 Sep W2b/W2d | `VM:1548-1557,715,371-377,648-676` | `primaryAction` is `stateIn(viewModelScope, Eagerly)` over `uiState`, so `uiState`'s `WhileSubscribed(5_000)` never idles: the 18-`combine` chain, including `exerciseRepository.observeAll()`, `search("")`, `observeLastLogged()` and `trainingInsights.observeShared()`, runs for the ViewModel's whole life (the rest page included); the KDoc at `:371-377` describing the projection as stopping five seconds after the screen stops collecting is now false. | The workout screen keeps three catalogue queries and the insights feed awake even while you are on the rest screen; harmless today, but it is the opposite of what the code's own comment promises. | ViewModel test: collect nothing from `uiState`, advance 6 s, insert a catalogue row, assert `uiState.value.searchResults` changed. Or read. |
| UI-4 (B11) | P3 | defect | C | — | `Screen:250-258,312-318`, `ui/navigation/AppNav.kt:624-628`, `ui/workout/RestTimerCard.kt:97-105` | `logReceipt` and `personalRecord` are cleared only at the end of a dwell inside `LaunchedEffect`; opening the rest page or Details (a new destination disposes the floor) or an Activity recreation within the dwell cancels it, so on return the receipt is announced again and the chip re-marked "saved", and a record replays its haptic and gold banner. The rest card's `flashedTimerId` is a plain `remember` (`:100`), so the gold "Rest complete" flash also replays on every recomposition of the floor while `completedTimerId` is set. | Log a set, tap the rest clock, come back: the phone tells you the same set was saved again (and, if it was a record, celebrates it again). | `FloorFeedbackRenderTest`-style: log, dispose the floor (compose the rest page), recompose within `Motion.STATUS_DWELL_MS`, assert `felt.announcements` holds one line. `aSavedSetIsAnnouncedOnceFromTheScreen` (`:110-121`) keeps one composition. |
| UI-5 (B11) | P3 | defect (AX) | C | — | `ui/workout/WorkoutHeader.kt:128-138`, `VM:202,210-211` | The header's Finish sets `stateDescription = "Log a set to save."` whenever `!canFinish`, and `canFinish` is also false during a save or any mutation with sets already logged, so TalkBack gives the wrong reason for the disabled button. | While a set is saving, a screen-reader user is told to "log a set" when they already have. | Render the real screen with three sets and a slow save (`WorkoutSaveRecoveryTest`'s fixture); assert the `FINISH` node's state description while `logging` is true. |
| UI-6 (B11) | P3 | defect (polish) | C | — | `Screen:845-849`, `ui/workout/LiftSwitcherSheet.kt:172-176`, `domain/LiftChipCopy.kt:22-42` | The switcher's row for the current lift names `lift.restSeconds` first and only falls back to the dock's planned length when the routine has none, so after a dock pick (ADR-012 §18) the row still says the routine's rest. | Pick 2:00 on the dock, open the lift list: it still says the old 1:30. | `WorkoutDockTimerRenderTest`-style: pick 120 s in the sheet, open `LIFT_SWITCHER`, assert `liftRest(current)` reads `Rest: 2:00`. `FloorRestAndCoachWiringRenderTest.aRunningRestIsTheCurrentLiftsAloneInTheSwitcher` covers only the running case. |
| UI-7 (B11) | P3 | defect (polish) | C | — | `Screen:261-266` | `LaunchedEffect(state.loadState, state.selectedExerciseId)` runs on every new composition and `scrollToItem(0)`, overriding the position `rememberLazyListState` just restored, so a rotation or Activity recreation resets the floor's scroll to the top. | Turn the phone while scrolled down to your sets and the screen jumps back to the top. | Robolectric `ActivityScenario.recreate()` with the list scrolled to the chips; assert `listState.firstVisibleItemIndex` after recreation. |
| UI-8 (B11) | P3 | validation gap (AX) | R | — | `ui/workout/WorkoutDock.kt:288`, `ui/workout/WorkoutPrimaryAction.kt:21-33`, `VM:1583` | The commit is wrapped in `key(action.identity)` and the identity carries `draft` and `activation`, so the button node is recreated on every stepper tick and after every accepted tap; whether TalkBack/switch-access focus then leaves the commit after each log needs a phone. | After each "Log set" the button is technically a new button, so a screen reader may lose its place on it. | Phone with TalkBack: focus Log set, double-tap, observe where focus lands. The mechanism is pinned on purpose by `DockCommitRenderTest.aPressBegunOnOneActionNeverLandsOnTheNext`. |
| UI-9 (B11) | P3 | architecture debt | C | — | `ui/workout/WorkoutSessionSummary.kt:28,32` | The session summary reads `System.currentTimeMillis()` directly instead of the app's `TimePort`, so its elapsed line cannot be driven in tests and bypasses `docs/architecture/time-seams.md`. | The "elapsed" number in the summary pop-up is read from the phone's raw clock rather than the app's shared clock. | Read; `FloorPacketHFinalPassTest.telemetryIsMinuteGrain` pins the grain, not the source. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- B6: the floor's unnamed stops besides the dock's Skip — `startHoldSet` (`VM:1000`), `startSetStopwatch` (`:1077`), delete of the latest set (`:1926`) and lift completion (`:1830`) all call `restTimer.stop()`; each is guarded by `cancelPendingRest()`/`canChangeEntry()` so none can race the next set's auto-rest, but they belong in the same owner decision as the dock's Skip (ADR-012 Consequences, "the dock's Skip still ends whatever runs").
- B6 (secondary line): the floor's `restTimerState` is `WhileSubscribed(5_000)` (`VM:535`); after a long rest page the dock's first frame on return shows the retained stale value until `RestCommands.restState`'s DataStore input (`RestCommands.kt:75`) re-emits — milliseconds, not a finding.
- B4b: `DiscardWorkout` stops the rest before the delete (`workout/DiscardWorkout.kt:29-33`) and `FinishWorkout` before the write (`workout/FinishWorkout.kt:62`); neither clears `SavedStateFloorTimer`/`SavedStateFloorUndo`, which die with the nav entry — consistent with `VM:2150-2153`.
- B9a: the in-workout use of `StartOptionsSheet` is the live-bar path only (`AppNav.kt:746-773`, `StartOptionsSheet.kt:146-172` "Go to session"/Discard); Discard there routes through the same `container.discardWorkout` (`StartOptionsViewModel.kt:311-323`). Nothing floor-specific found.
- B12: `ui/components/RestTimerUi.kt` — only `RestDurationSheet` takes `rememberFullSheetState` (`:396`); `LiftSwitcherSheet.kt:71` and `WorkoutSavedSets.kt:78` still slide under reduced motion (known, ADR-023 Consequences).
- B13/TS: `RestPageSkipTest.kt:132` composes the real `RestTimerScreen` under SDK 35 with `POST_NOTIFICATIONS` ungranted, so the "Rest alerts" dialog is on screen during those four tests; they pass because Compose finds nodes across windows. Worth an explicit override so the tests say what they render.
- B14 (docs truth): (1) `docs/FRONTEND_REDESIGN.md:123` "112 dp image-led exercise identity" — ADR-027's amendment item 1 and `ui/theme/Metrics.kt:104` say 88 dp. (2) `docs/FRONTEND_REDESIGN.md:81` still reads "W2b-3 in review"; #412 merged it and HANDOFF-NEXT lists it as done. (3) `VM:371-377` KDoc on the `WhileSubscribed` projection is false since `primaryAction` went Eager (UI-3). (4) The X6 brief's "components used only by the floor" is wrong for `SetEntryPanel.kt` — it is History's (`ui/history/SetEditSheet.kt:95`).
- C-3 (coach): nothing new beyond C-2's status in §1. Apply changes the draft only (`VM:2100-2111`), the compact strip stays as "Applied" (`NextSetRecommendation.kt:90-122`, ADR-030 §3), Why opens the trace (`:229-250`).


## B12 — Design system, theme, accessibility


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| AX-15 (B12) | P2 | defect | C (semantics) / R (unusable) | ADR-026 "Accessibility" (roles, accessible alternatives); UX25-AC01 | `UI/components/SnapWheel.kt:42-91`; `UI/reminders/ReminderTimeWheel.kt:66-92`; `UI/reminders/ReminderPrefsSection.kt:229` | The reminder time is three `VerticalPager`s of plain `Text` with no role, no `stateDescription`, no `customActions` and no `selected`/value semantics (`SnapWheel.kt:67-89`); the wrapping Row only sets `contentDescription = spoken` (`ReminderTimeWheel.kt:71`) on a non-merging node, so TalkBack hears the current time once and then three scrollable lists of unlabelled numbers with nothing to press. There is no typed alternative, although `BodyweightWheel.kt:110, 132` already pairs its wheel with a `Role.Button` "Type" path into `NumberEntryDialog` | A screen-reader user cannot reliably set a reminder time: the hour, minute and AM/PM wheels do not say what they are, do not say their value, and cannot be typed into — the bodyweight wheel already solved this for onboarding | Robolectric: render `ReminderTimeWheel`; `onNodeWithTag(REMINDER_TIME_HOUR)` has no `Role`, no `StateDescription`, no `CustomActions`; `onAllNodesWithText("Type").assertCountEquals(0)`. Fix shape: `BodyweightWheel`'s pattern — a typed `NumberEntryDialog` (or the platform time picker) plus `stateDescription`/`customActions` (Next/Previous) per column | Confirmed (the C part; whether TalkBack's two-finger scroll can set it stays R) |
| AX-14 (B12) | P2 | defect | C | ADR-026 "Accessibility" (no clipped essential values) | `UI/history/HistoryScreen.kt:407-458` | History's readout card sets `semantics(mergeDescendants = true) { contentDescription = "$title, N sessions" }` (`:409, :415`); on a merging node a `contentDescription` replaces the merged child text, so the four `MetricCluster`s (days, sets, min, PRs, `:434-457`) and the "Moved most" row (`:459-470`) are never read | The History totals a sighted user sees at a glance — days trained, sets, minutes, records, the lift you moved most — are silently dropped for a screen-reader user, who hears only "This month, 12 sessions" | Robolectric merged tree: `onNodeWithTag(HistoryTags.READOUT)` `contentDescription` lacks "days"/"sets"/"min"/"PRs". Fix shape: build `spoken` from all six values as `StatTile` does (`GymSurfaces.kt:294-306`), or drop the description and let the merged text speak | Confirmed |
| AX-1 (B12) | P2 | defect | C | ADR-026 "Accessibility" (roles; no repeated descriptions) | `UI/components/GymSurfaces.kt:456-473, 530-540`; callers `UI/history/HistoryScreen.kt:258, 535` | `SessionLogRow` is two separate clickables: the identity column (`:469-472`, `mergeDescendants`, `contentDescription = spoken` which already includes sets, work and minutes) and the metrics row (`:531-534`, `clickable(onClick)` with no role and no description, so it reads its own "3 sets, 1,240 kg, 42 min"); neither carries `Role.Button`, and the identity column has no height floor (title 20 + gap 4 + caption 16 = 40 dp at font 1.0 when a session has no stills) | A screen-reader user on History hears every workout twice, the second time as a bare list of numbers, and is never told either is a button; the top half of the row is also thinner than the 48 dp target the app promises | Robolectric: render `SessionLogRow(...)` and assert `onAllNodes(hasClickAction()).assertCountEquals(1)`, that node has `Role.Button`, and `assertHeightIsAtLeast(48.dp)`. `HistoryListThumbsTest` (the only test that mounts it) asserts none of this | Confirmed |
| AX-2 (B12) | P2 | defect | C | ADR-023 §2 (non-colour channel); 22 Sep week-strip row | `UI/routines/CustomWeekScreen.kt:295-345, 356` | The custom-week day strip's seven cells are `clickable { onSelect(day) }` with no role, no `selected` semantics and no spoken form; the selected state is `VoltDim` + Volt border + Volt text only (`:307-311, 321`), the FILLED mark is `Icons.Outlined.Check` with `contentDescription = null` (`:324-331`) and PREFERRED is a hairline dot (`:332-338`); each cell is `weight(1f)` in a 7-across row with 4 dp gaps (`:295-305`), so ≈ 43 dp wide at 360 dp | When building a custom week with a screen reader, the day buttons say only "Mo", "Tu"… — not which day is being edited, which days already have lifts, or which are the preferred training days; sighted users get all three by colour and a tick. The cells are also narrower than the 48 dp promise, like Home's week strip | Robolectric: render `WeekDayStrip` with `filled = {MON}`, `selected = TUE`; `onNodeWithText("Mo")` has no `SemanticsProperties.Selected`, no `Role`, and its merged text is "Mo" alone. The kicker at `:176` names the selected day and count, which softens the selection gap but not the filled/preferred one | Confirmed |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**AX-1.** Counter-evidence looked for: a single merged node — there is none, because the two `clickable`s sit on sibling layouts (`:467-473` and `:531-540`) under a non-clickable Column (`:450-455`); a role — `Role.Button` is used by `InstrumentRow` (`:383`) but `SessionLogRow` never went through it; a height floor — the outer Column's `heightIn(min = Metrics.rowMin)` (`:453`) covers the whole row, not the identity target. Fix shape: one `clickable(role = Role.Button)` on the Column at `:450` with `clearAndSetSemantics { contentDescription = spoken }`, the menu kept outside it as now; then `:531-534` loses its click. Home's recent list does not use this row (callers are History only).

**AX-15.** Counter-evidence looked for: a typed path anywhere in `ui/reminders` (grep `NumberEntryDialog|typing|customActions|stateDescription`: none); semantics inside `SnapWheelColumn` (none: `:52-89` is layout and `Text` only); a `selectableGroup`/`Role` on the pager (none). TalkBack can scroll a `VerticalPager` with two-finger swipes and the `settledPage` collector (`ReminderTimeWheel.kt:47-64`) would commit, so "cannot set" is R; "cannot tell which column is which, what it holds, or type it" is C. `TalkBackPolicyTest.kt:66-68` pins the bodyweight wheel's typed path, not this one.

**AX-14.** Counter-evidence: `StatTile` (`GymSurfaces.kt:294-311`) uses the same merge-plus-description shape but builds `spoken` from label, value, unit and caption, so it is not affected; the History card copied the shape and not the sentence. No test under `app/src/test` references `HistoryTags.READOUT` at all.

**AX-2.** Counter-evidence: `WeekStrip.kt` (Plan/Home) does this right — `selectable(role = Role.Tab)` in a `selectableGroup` with `WeekBoard.spoken` naming fill and selection (`:118-126`, `domain/WeekBoard.kt:88-111`). The custom-week strip is a second, older copy. Fix shape: reuse `WeekStrip`'s pattern (`selectable(selected, role = Role.Tab)` + a spoken form from `CustomWeekPolicy.dayMark`), and the F4 scroll-when-narrow rule.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| AX-3 (B12) | P3 | defect | C (tree) / R (TalkBack) | ADR-026 "Accessibility" (roles/groups) | `UI/components/ScreenHeader.kt:78-91`; `UI/components/GymSurfaces.kt:77-93, 307-314`; `UI/history/HistoryScreen.kt:411-417`; `UI/plan/PlanScreen.kt:640-651, 675` | Page titles are never headings unless `kickerTitle` (only 3 callers: `CustomWeekScreen.kt:122`, `RoutineEditorScreen.kt:391`, `OnboardingScreen.kt:188`), while `Kicker` defaults to `heading()` (`:82-86`, ~81 callers) and is composed inside nodes that merge descendants — `StatTile` (`:308-314`, 16 call sites on Summary, ExerciseDetail, ActivityDetail), History's readout card (`:415-417`), Plan's `RoutineRow` (`combinedClickable` `:644` + `Kicker(updatedLabel)` `:675`). Compose's default merge policy (`parentValue ?: childValue`) lifts `Heading` into the merged node, so in the merged tree every stat tile and routine row is a heading and the page title is not. Settings instance (from B10b): `SettingsHeader` is a plain `Text` (`UI/settings/SettingsChrome.kt:23-34`) and sub-pages are an in-place `page` state, not routes (`UI/settings/SettingsScreen.kt:175-179`), with no `heading()` or `paneTitle`, so opening Display/Reminders/Backup is never announced; `paneTitle` exists on only two surfaces app-wide (`RestTimerUi.kt:828`, `SavePostureChooser.kt:83`) | Jumping "by headings" with a screen reader skips the screen's name and lands on number tiles and routine rows instead; opening a Settings sub-page says nothing | Robolectric merged tree: `onNodeWithTag(HistoryTags.READOUT).assert(keyIsDefined(SemanticsProperties.Heading))` currently passes (that is the defect). `ExerciseDetailThisWorkoutRenderTest.kt:337, 413` checks headings on the *unmerged* tree only. Whether TalkBack's heading list uses the merged config is the R part: phone check, "navigate by headings" on Summary |
| AX-4 (B12) | P3 | defect | C | ADR-026 "Accessibility" | `UI/components/LiftCard.kt:77`; `ExercisePickerSheet.kt:603`; `GymSurfaces.kt:469, 533`; `UI/history/TrainingCalendarCard.kt:241`; `UI/library/ExerciseLibraryScreen.kt:535`; `UI/routines/SessionLiftStrip.kt:254`; `UI/onboarding/OnboardingScreen.kt:309`; `UI/plan/PlanScreen.kt:644`; `UI/intro/ColdStartIntro.kt:94`; `UI/components/SetEntryPanel.kt:263` | Eleven live `clickable`/`combinedClickable` sites carry no `role` (and nine of them no `onClickLabel`), so TalkBack ends each with "double-tap to activate" and no "button": History lift card (`LiftCard:77`, live via `FilledLiftCard.kt:77`), "Create "x"" row, calendar trained day, "Edit lift"/"Delete lift" sheet rows, routine-editor lift card (has `selected` but no role), onboarding place toggles (multi-select with no toggle state — only a "Selected" caption when on, `:324-326`), routine rows (long-press label present, `:645`), the intro, the history set well. `SetTable.kt:112` is the twelfth but dead: no caller passes `onSelect` (only `FilledLiftCard.kt:130`, without it). `BodyMap.kt:144` is deliberate (`clearAndSetSemantics`, rows are the alternative, `:241-275`) | Many tappable things do not tell a screen reader they are buttons, and the onboarding "where will you train" choices do not say on/off | Robolectric per screen: `onAllNodes(hasClickAction() and not(SemanticsMatcher.keyIsDefined(SemanticsProperties.Role))).assertCountEquals(0)`. Fix shape: `role = Role.Button` (or `toggleable(role = Role.Checkbox)` for the place step, `selectable(role = Role.RadioButton)` for the lift strip) |
| AX-5 (B12) | P3 | defect | C | ADR-026 (measured contrast) | `UI/components/ExercisePickerSheet.kt:383-388, 401-404`; `UI/theme/Color.kt:76, 89` | A selected picker row draws its "Selected" caption in `TextTertiary` on `VoltDim` over the sheet's `Surface3`; the composite measures 3.15:1 (WCAG AA needs 4.5:1 for 12 sp caption). Every other load-bearing pair measured clears AA: `TextTertiary` on Pit/S1/S2/S3 = 5.71/5.39/5.07/4.66; `TextSecondary`/S3 6.60; Volt on `VoltDim`-over-S2 10.27; `TextDisabled`/S2 3.23 (exempt by policy, `Contrast.kt:11-12`). `TextTertiary` on `SurfacePressed` = 4.16 would fail but no site was found | The small "Selected" word on a highlighted lift in the picker is too faint to be sure of; nothing else measured is | Pure JUnit over `ContrastPolicy.ratio` with the composite of `VoltDim` on `Surface3` (alpha 0x24) as background. `ContrastPolicyTest` checks only opaque surfaces |
| AX-6 (B12) | P3 | target gap | C | ADR-026 "Shared contracts" (gallery covers state and role variants); D15 | `app/src/debug/java/com/sinura/personaltrainer/ui/preview/ComponentStateGallery.kt:91-231`; `TabularNumeralTest.kt:52-68` | The gallery shows the primary button's five states, choice/toggle chips, one preset, one suggestion, `InstrumentRow`+switch, `Numeral`, `NumberEntryDialog`, `FieldComplaint`, `ScreenSkeleton`, `RestDurationSheet` and `GymDialog`. Not covered: `InstrumentChip` `recommended` and disabled (the D02 fix itself), `QuietButton` (accent/plain/disabled), `StepperButton` (hold, emphasis, inset plates), `WeekStrip` fills, `SetTable` (selected/editing/warm-up), `LiftCard` selected, `ExerciseRow` selected/cart, `TrendChart`, the four banners incl. `GymUndoHost` and `PersonalRecordBanner`, `RestSweepRing` running/urgent/finished, `RestSegments`, `InstrumentNavBar` 3+2 reflow, `LiveSessionBar` stale/error, `ScreenHeader` variants, `StatTile`, the three real workout dialogs (`EndWorkoutDialog`, `LeaveCardioDialog`, `ResumeOrDiscardDialog`), `ExercisePickerSheet`, `AddToRoutineSheet`, and a reduced-motion variant (only `FoundationStateGallery.kt:121-127` has one). The pinning test names 9 atoms | The reference page of building blocks shows fewer than half of them and few of their states, so a change can break a state nobody can see side by side | Extend `TabularNumeralTest.componentGalleryNamesThePublicAtoms` to the list above; add `@TemperReducedMotionPreview` to `ComponentGalleryPreview` |
| AX-7 (B12) | P3 | architecture debt | C | 22 Sep "every ceiling at 100 %" | `tools/checker-baselines.toml:35-61`; `tools/check-design-tokens.py:80-150, 178-195` | Every advisory family is exactly at its ceiling (numbers in §1), so the next packet that adds one `.dp` literal, one `tween(` or one stock icon fails the gate with no note of intent; and the checker has three blind spots: (a) `stock_icons` regex `Icons\.(Filled | Default | Rounded |
| AX-8 (B12) | P3 | defect | C | ADR-026 "Saved haptics follow writes"; ADR-012 (late Skip is a no-op, W2b-1) | `UI/components/RestTimerUi.kt:616-633, 743-750`; `UI/theme/Haptics.kt:50-53` | `RestControl` and `RestSegments` fire `Haptics.commit(view)` (documented as "a set landed, a workout finished") *before* `onClick()` when `confirm = true` (Skip), so the commit buzz plays on the press, not after the stop lands, and also on a late Skip that W2b-1/1b made a deliberate no-op | Pressing Skip buzzes "done" even when the app decides the rest already finished and does nothing | Read the two sites; JUnit with the `FeltView` fake (`test/.../workout/FeltView.kt`) pressing Skip on a finished rest and asserting no `CONFIRM`. `HapticsPaletteTest.aRefusedEntryIsTheDoubleBeat` pins the log-set case only |
| AX-9 (B12) | P3 | validation gap | C | ADR-026 "accessibility-adjusted undo timeouts"; UX26-AC02 | `UI/components/GymStatus.kt:153-169`; callers `UI/exercise/ExerciseDetailScreen.kt:150`, `UI/library/ExerciseLibraryScreen.kt:193`, `UI/settings/BackupRestoreSection.kt:131`; `UI/navigation/LiveSessionBar.kt:98-103`; `UI/workout/ActiveWorkoutScreen.kt:244-249`; `UI/workout/ActiveWorkoutViewModel.kt:2039-2043` | The floor's undo (`UndoDwell` + `systemUndoTimeout`, `UndoFeedback.kt:27-41`), the floor receipt and the live bar's error extend their 6 s under TalkBack; the three `GymStatusBanner` sites outside the floor and `SessionDetailScreen.kt:278` / `HomeScreen.kt:313` `GymUndoHost`s keep the 6 s base (default `dwellMs`, `:161, :212`) | Undo on the workout floor waits longer for screen-reader users; undo on History's session page and Home does not | Grep the callers; JUnit: render `GymUndoHost` with `LocalAccessibilityManager` faked to 10 s and assert it is still visible at 7 s |
| AX-10 (B12) | P3 | validation gap | C (no live region) / R | AccessibilityMatrix "timer ticks remain silent"; ACC-02 | `UI/navigation/LiveSessionBar.kt:144-153, 187-207`; `UI/workout/RestTimerCard.kt:145-153, 203-209`; `UI/components/RestTimerUi.kt:546-550, 601-606` | No running clock is a live region (C). But the elapsed label sits inside the merged resume button, and the rest card's and sweep ring's `contentDescription` are rebuilt every second with the time in them; a node that has accessibility focus and whose description changes is re-read by TalkBack on content-change events | If a screen-reader user rests their focus on the live bar or the rest card, it may re-read itself every second | Phone: TalkBack, focus the live bar for 10 s. If it chatters, move the elapsed text outside the merged node or set `clearAndSetSemantics` to the static label |
| AX-11 (B12) | P3 | validation gap | C | AccessibilityMatrix claims (seam 9) | `domain/AccessibilityMatrix.kt:80-87`; `UI/settings/SettingsHome.kt:54-154`; `test/.../domain/AccessibilityMatrixTest.kt:44-47` | Home's note holds (`WeekStrip.kt:117, 140` filled-not-faint; `DayBlock.kt:70-71, 102` one button with controls inside; no "Get started"/Add row in `ui/home`), and the floor's note holds where checked (`WeightRepsEditor.kt:162-163, 407-410`; `ExerciseHeader.kt:226` traversal order; `WorkoutHeader.kt:194` decorative bar; `RestTimerCard.kt:206-208` finish announced once; VM `:2040` timeout). Settings' note names six destinations; the index has twelve rows (Update, Display, Reminders, Generator, Rest, Bodyweight, Save posture, Account, Backup, Plan, Diagnostics, About) and the test asserts the stale sentence verbatim | The written promise of what the Settings screen reads out is a year-old list; the test locks the old words in | Update the note and the assertion together (B14 for the record; the test is TS-) |
| AX-12 (B12) | P3 | target gap (accepted) | C | ADR-023 §3 amended, "Consequences"; ADR-005 §5 | `UI/theme/Motion.kt:87-115`; the 13 `ModalBottomSheet(` sites (`RestTimerUi.kt:398` uses `rememberFullSheetState`; `AddToRoutineSheet.kt:53`, `ExercisePickerSheet.kt:132`, `history/SetEditSheet.kt:78`, `history/HistoryScreen.kt:509`, `library/ExerciseLibraryScreen.kt:506`, `library/ExerciseEditorSheet.kt:59`, `progress/ProgressScreen.kt:332`, `routines/RoutineEditorScreen.kt:566`, `home/HomeStartSheet.kt:74`, `workout/StartOptionsSheet.kt:125`, `workout/WorkoutSavedSets.kt:76`, `workout/LiftSwitcherSheet.kt:69` do not) | Twelve sheets still slide up under reduced motion. ADR-023's Consequences say exactly this ("The app's other twelve bottom sheets still slide in under reduced motion (24 September 2026). Each can take `rememberFullSheetState` in a later packet; none is changed without a rendered test"), so it is an accepted gap, not a violation — but no packet in `FRONTEND_REDESIGN.md` carries it. Every raw `tween(`/`infiniteRepeatable(` outside the theme is guarded: `RestTimerUi.kt:531-545`, `TrendChart.kt:252-263`, `GymStatus.kt:254-271`, `LiveSessionBar.kt:279-282` via `barMs = 0` (`AppNav.kt:323-326`), `ColdStartIntro.kt:150-165`. Dwell (`STATUS_DWELL_MS`) is correctly not collapsed | The phone's "reduce motion" setting is honoured everywhere except that twelve pop-up sheets still slide; the decision to leave them is written down but not scheduled | Add a row to the packet table (B14); per sheet, the W2a-style rendered test |
| AX-13 (B12) | P3 | opportunity | C | 16 Sep "Items to reproduce" #6 (measure composited states); ADR-005 §5 hairline depth | `UI/theme/Color.kt:44-52`; `UI/theme/Contrast.kt:17-21, 46-47`; `ForeignControlsTest.kt:12-25` | Unselected control boundaries are `Hairline` (white 8 %): composited on `Surface2` that edge measures 1.24:1, and a `Surface2` chip/button on `Pit` 1.35:1; WCAG 1.4.11 asks 3:1 for boundaries that identify a control. Only `OutlineSolid` (3.75:1 on S2) is held at 3:1 by test. This is the Instrument identity by decision ("depth is light, not shadow"), and every control also has a text or glyph, but the decision is not written as a contrast exemption anywhere | Buttons and chips are told apart from their background by a very faint line; that is on purpose, but nobody wrote down that it is | Pure test over `ContrastPolicy.ratio(Surface2, Pit)`; a sentence in ADR-005/023 naming the exemption |
| AX-16 (B12) | P3 here; B6 owns the render severity | defect | C (colours) / R (light shade) | ADR-005 §2 (colour through semantic roles) | `app/src/main/res/values/colors.xml:7-8`; `app/src/main/res/values-night/colors.xml`; `app/src/main/res/layout/notification_rest_running.xml:17, 26` and `_big.xml`; `timer/RestTimerNotifications.kt:141-166`; `UI/theme/Color.kt:72-73` | `rest_notification_text #F2F5F7` and `rest_notification_kicker #9BA7AE` are `TextPrimary` and `TextSecondary` hand-copied into XML (they are tokens by value, not by reference — RemoteViews cannot read `Color.kt`, so drift is unguarded); they are `xml_hex` entries 7 and 8 of the 12. The layouts set `textColor` explicitly and no background, and `DecoratedCustomViewStyle` (`:166`) draws them inside the system's template, whose background follows the phone's shade theme, not the app's dark-only theme. `values-night/colors.xml` mirrors only `window_background`, so there is no light/night pair for the one place in the app that is drawn on a surface the app does not own | The rest clock in the notification shade is painted near-white because the app is dark; on a phone whose shade is light, that is white-on-white — and the two colours are copies of the app's text tokens that nothing keeps in sync | R: phone in light mode, running rest, pull the shade. Fix shape: a `values`/`values-night` pair for the notification text (dark-on-light by day, the tokens by night), or `?android:attr/textColorPrimary` in the layout; and a JUnit that `colorResource(R.color.rest_notification_text) == TextPrimary` if the copy stays |
| AX-17 (B12) | P3 | defect | C | ADR-026 "Shared contracts" (required text reflows, never clips); F11 matrix | `UI/components/GymSurfaces.kt:403-411`; `UI/components/LiftCard.kt:105-113`; `UI/progress/BodyMap.kt:255-262`; `UI/onboarding/OnboardingScreen.kt:264-270` | `InstrumentRow` subtitles and `LiftCard` muscle lines are `maxLines = 1` + ellipsis, so committed-setting summaries ("4 days · Auto · Ge…", "This phone · optio…"), the Body row's recency ("Trained tod…" — already at font 1.0 on 360 dp), Backup's "Replaces ever…", session-detail's muscle ("Q…") and onboarding blurbs are cut instead of wrapping (frames in §7) | At large text, and once at normal text, the second line of many rows is chopped off with "…" rather than wrapping | Rendered assert at font 2.0 that no `Text` in these rows reports `TextOverflow.Ellipsis` truncation; fix shape: `maxLines = 2` on subtitles, as titles already have |
| AX-18 (B12) | P3 | defect | C | same; D06 (legend) | `UI/components/GymSurfaces.kt:77-93` (`Kicker` `maxLines = 1`, ellipsis) | Metric labels and section kickers clip at font 2.0: "DA…"/"SE…" under History's totals, "HEAV…"/"TOP …"/"EST …" on Records, "SAFETY COPIES ON THI…" on Backup, and the Body legend "NO WORK LOW PRODU…" (D06's "legend doesn't wrap", still open in F5) | The little capital labels under numbers get cut to two letters at the largest text size, so "days" and "sets" become "DA…" and "SE…" | Rendered assert at 360×640 font 2.0 on `HistoryTags.READOUT` that the four labels are whole; fix shape: let `Kicker` wrap to two lines where it labels a value, or shorten labels at scale |
| AX-19 (B12) | P3 | defect | C | ADR-026 "no clipped essential values"; W1d (same class, fixed on the floor) | `UI/components/GymSurfaces.kt:247-281` (`MetricCluster`: value ellipsis, unit `maxLines = 1`, no fit); `UI/exercise/ExerciseDetailScreen.kt` Records panel | In a three-column weighted row at 360×640 font 2.0 the Records panel's first cluster draws "135 l": the unit "lb" is cut in half (frame `exerciseDetail/populated-360x640-font2.0`), while the third column's "180 lb" survives; W1d's `fittedNumeral` fix applies to the floor's hero values only | A record's unit is chopped to "l" at the largest text size — the exact bug that was fixed for the workout screen in drop 104, still present on the lift's own page | Rendered assert that every `MetricCluster` unit text is fully laid out (`TextLayoutResult.hasVisualOverflow == false`) at font 2.0; fix shape: apply `LogLoopScale.fittedNumeral`/stat-value stepping to `MetricCluster` |


### Noticed, outside this pass's scope

Noticed, outside my scope

- `UI/components/ThumbCache.kt:38-41, 77`: LRU of 8 MB ≈ 32 thumbs at 256 px ARGB, while the Library lists 143 lifts; a full scroll decodes each still at least once and revisits re-decode after 32 others. `UI/components/PoseArtwork.kt:60-66, 73-92`: `TemperStillCache.bind` decodes the two 1024² unlit stills (≈ 8 MB) synchronously on whatever thread calls it at application start. Same image is never decoded per row (`peek` first, `ThumbCacheTest`). → B13a (performance).
- `app/src/main/res/font/` holds five subset TTFs and no licence text; `tools/build-fonts.py:22` fetches from google/fonts `ofl/`. SIL OFL requires the licence to travel with the font. Usage is fine (all styles set `tnum`, `Type.kt:36, 61`; `TabularNumeralTest`). → B14 (licence/supply chain).
- ADR-022 says "129 keys, 129 WebPs" (`docs/architecture/ADR-022-keyed-catalog-stills.md:21, 67`); today `KeyedArtwork.kt:19-163` has 143 entries, `drawable-nodpi` has 143 `ex_*` WebPs, `tools/check-still-pack.py:21` pins 143, `CATALOG_VERSION = 10` (`domain/DefaultExercises.kt:62`). `ExerciseThumbLogicTest.kt:163` pins keys == catalog. → B14 (docs truth).
- `app/src/main/res/layout/notification_rest_running{,_big}.xml:25` use `fontFamily="monospace"` (the retired system face; `xml_monospace` 2/2) and `timer/RestLockActivity.kt:244` uses `Icons.Outlined.Close`. The notification colours themselves are AX-16 (token statement here; rendering severity is B6's). → B6 (rest timer / notifications).
- `UI/library/ExerciseLibraryScreen.kt:105-120`: the one stock `FloatingActionButton` (ceiling `stock_component 1`) with `Icons.Outlined.Add`; F8b/F8c. → B-Library pass.
- `UI/history/SetEditSheet.kt:108-118`: History's set editor RPE chips are bare `InstrumentChip` (checkbox) while the floor's are radio — the 22 Sep History row "different effort control"; F6b. → B-History pass.
- `test/.../domain/AccessibilityMatrixTest.kt:44-60` asserts three `talkBackNotes` sentences verbatim (see AX-11); `TalkBackPolicyTest`, `MotionPolicyTest`, `ForeignControlsTest`, `GymChromeTest`, `ComponentInventoryTest`, `HapticsPaletteTest` are source-text scans (`File(...).readText().contains`), the pattern T1 retired for `ui/workout`. `ui/components` has 1,588 test lines for 9,021 production; `WeekStrip`, `InstrumentChoiceChip`, `LabelledTrend`/`TrendBars`, `InstrumentNavBar`, `LiveSessionBar`, `SnapWheelColumn`, `ExercisePickerSheet`, `EndWorkoutDialog`, `StatTile`, `ComponentStateGallery` are mounted by zero test files. → B-tests (TS-).
- `UI/components/ScreenHeader.kt:42` defaults the back arrow to `Icons.AutoMirrored.Outlined.ArrowBack` on 10 screens although `TemperIcons.Back` (`TemperIcons.kt:107-108`) was drawn for it (AX-7c). → design packet, with the ceiling note.
