# Architecture, tests, build and docs findings

Part of the [25 September whole-app audit](AUDIT.md) (packet X6). Every row was written by the pass named in its ID and re-checked against the code at `1ad3b11`; P1/P2 rows carry the verifier's verdict (see [verification.md](verification.md)). Files are cited in backticks, relative to `app/src/main/java/com/sinura/personaltrainer` unless a path says otherwise. Severity: P1 protect data or a first-run blocker · P2 next correctness or polish work · P3 worth doing, not urgent. C = confirmed from code, R = needs the phone.


## B13a — Architecture, DI, coroutines, performance


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| AR-1 (B13a) | P2 | defect | C | — | `insights/TrainingInsightsSource.kt:112,123-129,163-190`, `data/repository/WorkoutRepository.kt:148-156,171-177,185-190`, `data/repository/ActivityRepository.kt:120-124`, `data/repository/FlowGuards.kt:21-36` | Four of the insights pipeline's inputs (`observeSessionSummaries`, `observeFinishedLastLogged`, `observeFinishedSince`, `observeCompletedSummaries`) are the only repository flows not wrapped in `observeHealth`, and the pipeline is `shareIn`'d on `CoroutineScope(SupervisorJob() + computeDispatcher)` with no `CoroutineExceptionHandler`, so a Room read fault in any of them (or in the `mapLatest` DAO reads they make) kills the sharing coroutine as an uncaught exception and the process; Home subscribes on the first frame (`ui/home/HomeViewModel.kt:97-98`), so it recurs on every launch while the fault lasts. | If the phone's database has a hiccup while the Home tab is up, the app closes instead of showing the "could not read" message the rest of the app uses, and because Home is the first screen it would keep closing on every open until the hiccup clears. | JUnit: build `TrainingInsightsSource` over in-memory `TemperDatabase`, capture `Thread.setDefaultUncaughtExceptionHandler`, collect `observeShared()` once, then `execSQL("DROP TABLE set_logs")` on the same connection and log a write to invalidate; assert the handler received an `SQLiteException` (today) and, after wrapping, that the flow degrades instead. | Confirmed (mechanism); folds into B4b DB-1 |
| AR-2 (B13a) | P2 | defect | C | — | `PersonalTrainerApp.kt:96-102,159-170`, `logging/AppLog.kt:31-32,52-57`, `data/local/TemperPreMigrationCopy.kt:86,105,118`, `data/local/PreMigrationSnapshot.kt:65,118` | `installDiagnosticCapture()` (which sets `AppLog.onError`, the only route into `DiagnosticRing`) runs at `:102`, after `PreMigrationSnapshot.ensure(this)` at `:101`; `AppLog.onError` is null until then, so every `AppLog.e`/`AppLog.w` the pre-migration copy raises ("copy failed; migrating without it", "too little free space", "could not read the database version") reaches logcat only and never the diagnostic ring or bundle. | The one failure the rollback-copy exists to warn about (the copy could not be taken before a database upgrade) is the one failure that is never written into the diagnostics the owner can send; it is only in logcat, which needs a computer. | Extend `PreMigrationStartupOrderTest` (`app/src/test/.../data/local/PreMigrationStartupOrderTest.kt:20-29`, already source-order based) to assert `installDiagnosticCapture()` precedes `PreMigrationSnapshot.ensure(`; plus a unit test: `AppLog.onError = null; AppLog.e(tag, msg, RuntimeException())` leaves `DiagnosticRing.shared` empty. `installDiagnosticCapture` needs only the `Context` (`:160`), so the swap is one line. | Partly |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**AR-1.** What I looked for that would make it false: (a) a `.catch` anywhere in
`TrainingInsightsSource.kt` — none (grep); the only guard is `cachedHints`
(`:259-264`), which covers `loadHints`, and that is exactly what
`TrainingInsightsSourceTest.hintFailureIsolatesToProgression` pins. (b)
`observeHealth` on the four inputs — `WorkoutRepository.kt:158` and
`ActivityRepository.kt:126-127` wrap them *separately* for History
(`observeSessionSummariesHealth`, `observeCompletedSummariesHealth`); the
un-wrapped originals are what `TrainingInsightsSource.kt:159,167,173,187` take.
Every other input is wrapped (`RoutineRepository.kt:26-28`,
`ExerciseRepository.kt:66-70`, `ScheduleRepository.kt:27-29`,
`ActivityRepository.kt:146-149`, DataStore `prefs/SettingsStore.kt:47`). (c) A
collector-side `.catch`: `ActiveWorkoutViewModel.kt:600-603` has one, but a
`shareIn` upstream failure is raised inside the sharing coroutine, not in the
subscriber, so that `catch` cannot see it; kotlinx routes a failed child of a
`SupervisorJob` with no handler to the thread's uncaught handler, which on
Android ends the process. Contrast `PersonalTrainerApp.kt:26-29`, whose
`applicationScope` carries a handler for precisely this reason, and
`ActivityRepository.kt:92-104`, which wraps its own `shareIn` source. The same
scope shape without a handler also sits at `ActivityRepository.kt:81` and
`timer/RestTimerController.kt:54`; those upstreams are guarded, so only the
insights scope has an unguarded source.

**AR-2.** `AppLog.redactMessages = true` at `:96` is itself redundant
(`AppLog.kt:43` defaults to `true`), so the block before the copy does nothing;
the comment at `:92-95` explains redaction, not why capture waits.
`installDiagnosticCapture` reads the last-crash file (`:165-169`) and sets the
handler; it does not touch the container, so it can run first.

**Input list for `uiState` (18 `combine` calls, `ActiveWorkoutViewModel.kt:605-717`):**
`session, selectedExerciseId, draft, hint | lastPerformance | restTotal,
searchQuery, showPicker | notes, error.messages, finished, editingSetId |
sessionReader.observations | searchQuery.flatMapLatest(search), observeLastLogged
| swapTargetItemId, exerciseRepository.observeAll | suggestedLift | mutating |
saveOperation | logging | liftReadiness | suggestionUnavailable | draftDirty`.
Each emission runs 14 `state.copy(...)` of the 23-field state before `stateIn`
dedupes once at the end. `microRec` (`:544-580`): `session, selectedExerciseId,
draft, hint, wantAnotherSet | lastPerformance | editingSetId, lighterWeek,
weightUnit, coachPreferences`, `SharingStarted.Eagerly`.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| AR-3 (B13a) | P3 | opportunity | C (structure) / R (cost) | C-2 | `ActiveWorkoutViewModel.kt:657-671,672-689`, `data/local/dao/WorkoutDao.kt:454-455`, `WorkoutRepository.kt:149,172,186`, `ui/navigation/LiveSessionBar.kt:274`, `LiveSessionBarViewModel.kt:88-146` | Per logged set, Room invalidation re-runs: `observeSession` (a 3-query relation load), `observeSessionActivity`, `observeLastLogged` (a `GROUP BY` over all `set_logs`) and `observeFinishedWorkGeneration` three times over (one subscription per repository flow, each with its own `distinctUntilChanged`, `WorkoutRepository.kt:149-150,172-173,186-187`); the floor's `uiState` then sorts the whole catalog (`ExerciseOrdering.pickerOrder`, 179 lifts) on every emission with no `showPicker` guard, and `LiveSessionBarHost` keeps `uiState` collected while the bar is hidden on the floor (only the 1 Hz ticker stops, `LiveSessionBarViewModel.kt:69-85,97-99`). The heavy insight reloads are gated (pinned by `TrainingInsightsSourceTest.loggingASetOnAnInProgressSessionDoesNotRecomputeInsights`). | Every time a set is saved, the app re-reads several things it does not show on that screen (the whole lift list is re-sorted for a picker that is closed; the hidden bottom bar keeps watching the database). Not visible yet; it is the kind of cost that grows with history. | W2c input: an emission-count test on `uiState` per stepper tap and per `logSet` (collect into a list under `runTest`), and a `RoomDatabase.invalidationTracker` observer counting query re-runs per `logSet`. |
| AR-4 (B13a) | P3 | opportunity | C | C-2 | `ActiveWorkoutViewModel.kt:576-580,1548-1557,379-381` | `microRec`, `primaryAction` and `session` are `SharingStarted.Eagerly` in `viewModelScope`, so the coach engine and `WorkoutPrimaryActions.derive` keep running on every Room re-emission while the floor is on the back stack or the app is in the background (the ViewModel outlives the screen by design; `uiState` itself is `WhileSubscribed(5_000)`). The hold/stopwatch loops tick at 250 ms (`:103,1056,1111`) but the StateFlow dedupes equal seconds, so emission is 1 Hz (C). | The workout page keeps doing its coach arithmetic even when you are on another tab or the phone is in your pocket. Cheap per run; it is wasted battery, not a bug. | Read; or a test that logs a set with no `uiState` collector and counts `compute` calls through an injected engine. |
| AR-5 (B13a) | P3 | opportunity | C (size) / R (memory) | — | `PersonalTrainerApp.kt:109-112`, `ui/components/PoseArtwork.kt:53-92,101-110` | Every cold start decodes two 1024×1024 ARGB stills (2 × 4 MB resident, plus a transient 4 MB `IntArray` per still for `punchPit`) on `applicationScope`, for the Body tab only, and holds them for the process life whether Body is opened or not; `ThumbCache` (`ui/components/ThumbCache.kt:22-77`) by contrast is bounded (8 MB LRU, sampled, off-main via `withContext(io)`, keyed by `(id, sample)`, `produceState` in `ExerciseThumb.kt:164-168`). 177 WebPs, 1,008 KB, `res/drawable-nodpi`. **B12 hand-off, settled from code:** every `BitmapFactory.decodeResource` in `ui/` runs off the main thread — `ThumbCache.kt:52-63` under `withContext(io)`, `PoseArtwork.kt:73-92` only from `bind`, whose sole caller is `PersonalTrainerApp.kt:112` on the IO `applicationScope` (`:26-28`); the "8 MB" is that `bind` (2 × 1024×1024 ARGB, header-verified), not a main-thread decode. The only composition-time decodes are two `painterResource` calls in `ui/components/ExerciseThumbGallery.kt:223,232` (the 768×768 heat stills), and that composable has no caller in main or debug (AR-6). Thumb maths: all 143 `ex_*.webp` are 256×256 (header-verified, 467 KB on disk) = 256 KB ARGB each, 35.75 MB if all resident; the 8 MB LRU holds 32 (the comment at `ThumbCache.kt:76` says "~32"; no "~98" comment exists in `ThumbCache.kt`, `ExerciseThumb.kt` or `KeyedArtwork.kt`), so a scroll through the full Library re-decodes 3–7 KB WebPs on IO as it goes (R: cost). At 40 dp rows the 1:1 decode is 1.6–2.1× the display edge; `inSampleSize = 2` would fall below xxxhdpi, so 1:1 is the right power of two. | The app loads the big body pictures on every launch, even if you never open the Body tab, and keeps them in memory all day. | Move `TemperStillCache.bind` behind the first Body composition (it already tolerates an empty cache, `PersonalTrainerApp.kt:110-111`); measure with `Debug.getNativeHeapAllocatedSize` before/after on the phone. |
| AR-6 (B13a) | P3 | architecture debt | C | 22 Sep W2a | `ui/plan/PlanViewModel.kt:295,407,461,616,627`, `ui/home/HomeViewModel.kt:558,635,645`, `ui/components/GymSurfaces.kt:90,532`, `ui/components/InstrumentSelection.kt:59,106`, `ui/plan/PlanScreen.kt:540,647`, `ui/plan/PreferenceBlock.kt:23`, `ui/components/ExerciseThumbGallery.kt:223,232`, `domain/coach/CoachEngine.kt:128`, `data/repository/DbMaintenance.kt:170` | Zero production callers (direct grep, comments excluded): ViewModel functions `pinFocus`, `dismissFills`, `addMorningCardio`, `setLighterWeek`, `setSessionHour`, `addDaySession`, `requestWeekSuggestion`, `requestAnswerReplay` (three have no test either); composables `SectionHeader` (pinned in place by `ComponentInventoryTest.kt:19`, which asserts the signature exists), `Numeral`, `InstrumentToggleChip`, `InstrumentSuggestion`, `ExerciseThumbGallery` (`ui/components/ExerciseThumbGallery.kt`; no caller in main or debug, 0 test file(s) reference it; it holds the app's only composition-time `painterResource` still decodes, `:223,232`), plus the three F7a items; `CoachEngine.toCoachSuggestion` and `DbMaintenance.parseCollisions` have no caller anywhere. Test-only helpers shipped in main beyond the doc's five: `emptyInkPoints`, `poseCoversTheCatalog`, `poseAndKitPoints` (ui), `prSummary`, `previousSameRoutine`, `signedWorkDelta`, `homeWork`, `toSessionStub`, `toKgLabel`, `toKgNumber`, `toWeekday` (domain/util). `PreferenceBlock.kt:42` is also the only `items(` in main without a `key` (34 of 35 keyed; 8 `contentType`s). | About twenty functions and seven screen pieces are still shipped that nothing in the app uses; one test even insists a dead piece stays. | A `ComponentInventoryTest`-style pin listing these as absent after removal; the scan is `scratchpad/deadscan.py` (word-boundary, comment-stripped). |
| AR-7 (B13a) | P3 | architecture debt | C | — | 66 sites (grep `catch (\w+: (kotlinx.coroutines.)?CancellationException)` in main), `util/CoroutineErrors.kt:14-30`; 23 `on*Handled()`/`consume*` acks across 12 ViewModels (e.g. `HomeViewModel.kt:208-249`, `StartOptionsViewModel.kt:170-195`, `LiveSessionBarViewModel.kt:165-168`) | Two idioms for one rule: 124 `runCatchingCancellable`/`recoverWith` uses coexist with 66 hand-written rethrow clauses (`check-cancellation.py` holds the *unguarded* count at 0 but does not converge the idiom); and each ViewModel hand-rolls the same one-shot navigation ack (`MutableStateFlow<String?>` + `onXHandled()`). | The same safety rule is written out by hand in 66 places when a helper exists, and the "tell the screen to navigate, then forget it" step is copied into a dozen files. Maintenance cost, no user effect. | A checker ceiling on the manual clause count, ratcheting down. |
| AR-8 (B13a) | P3 | validation gap | C | — | `ui/home/HomeViewModel.kt:93`, `ui/plan/PlanViewModel.kt:107`, `ui/plan/PlanDayViewModel.kt:53`, `ui/library/ExerciseLibraryViewModel.kt:97`, `ui/navigation/LiveSessionBarViewModel.kt:175`, `ui/activity/LiveCardioViewModel.kt:88`; `util/ErrorSlot.kt:17-45` | Six ViewModels still hold the raw `MutableStateFlow<String?>` error whose success-clears-everything race `ErrorSlot` was written to end (12 ViewModels adopted it); `HomeViewModel` has 14 `viewModelScope.launch` sites writing `actionError`. | The "a second problem's message gets wiped by the first action finishing" bug that was fixed on the workout page can still happen on Home, Plan, Library and the live bar. | For each: two overlapping actions, first slow-success, second instant-refuse; assert the refusal survives (the `ErrorSlotTest` shape). |
| AR-9 (B13a) | P3 | validation gap | C (when) / R (how long) | X2b | `PersonalTrainerApp.kt:97-101`, `data/local/TemperPreMigrationCopy.kt:64-123,130-131,151-158`, `data/local/PreMigrationSnapshot.kt:52-58` | The whole-database copy with per-file `fsync` runs synchronously on the main thread in `Application.onCreate`, but **only on the first launch after a schema bump** (marker `KEY_CHECKED_VERSION` at `:71`, written at `:122` after a copy or when nothing needs copying `:79,91,99`), never on ordinary launches; it needs `usable ≥ 3 × (db + wal + shm + journal) + 32 MB` or it skips. The next such launch is S2b's v8. Size of `temper.db` on the owner's phone is unknown (R); a few MB copies in well under a second, tens of MB in seconds. Two exceptions leave the check un-marked so it re-runs every launch: an unreadable version (`:83-88`) and too little space (`:104-107`) — each is one read-only SQLite open per launch, cheap. Comment contradiction: `TemperPreMigrationCopy.kt:38` "never blocks launch" vs `PreMigrationSnapshot.kt:53` "Synchronous by design". | The safety copy of the database is taken while the app is still opening, so the first launch after a database upgrade will pause for as long as the copy takes; that happens once per upgrade, not every day, and for a normal-sized history it is a blink — but nobody has measured it. | Add an `AppLog.d` with elapsed ms around `TemperPreMigrationCopy.ensure` before the v8 drop; JVM estimate: Robolectric with a synthetic 50 MB `temper.db` timing `ensure` (an estimate only). |
| AR-10 (B13a) | P3 | defect | C | — | `PersonalTrainerApp.kt:115-133`, `PendingOccurrence.kt:55-57,102-104,115-118` | `PendingOccurrence.restore` runs third in the startup chain, behind `recoverInterruptedRestore` and `importEncodedHistoryIfNeeded`, and assigns `pendingOccurrenceId.value` unconditionally from a DataStore read; a `write()` that lands between that read and the assignment is overwritten in memory. A null is self-healing (`stored()` falls back to prefs, `:102-104`), a stale non-null is not: `complete()` for a composer arm (`:66-72`, no session id to compare) would mark an already-done occurrence again. | In the first second or two after launch, finishing or starting a planned session can be forgotten in memory because the start-up "remember what I was doing" step runs late and writes over it; rare, and mostly self-correcting. | `PendingOccurrenceTest`: interleave `write(null)` between `restore`'s read and assignment (inject the DataStore flow) and assert `value` is null after both; `restoreSurvivesADroppedInMemoryBinding` (`:169`) covers the opposite direction only. |
| AR-11 (B13a) | P3 | validation gap | C (scope) / R (cost) | — | `ui/workout/ActiveWorkoutScreen.kt:191-201,360-365,433-439,677,838`, `ui/workout/WorkoutDock.kt:117-198`, `ui/workout/RestTimerCard.kt:83-175,291-335`, `ui/navigation/LiveSessionBar.kt:262-296`, `ui/navigation/AppNav.kt:347,366-373`, `timer/RestTimerController.kt:91-120` | Per-tick recomposition is contained by construction: the four ticking `State`s are held as `State` objects and read only inside the `bottomBar` lambda (`:436-439`), one `item(key = "entry")` (`:677`) and a `derivedStateOf` (`:360-365`), so the 795-line `ActiveWorkoutContent` and the `LazyColumn` do not re-run each second; `WorkoutDock` and `RestTimerCard` do (their `remainingSeconds` params change), the ring animates per frame in the draw phase only (`animateFloatAsState` read inside `Canvas`, `:298-334`), control widths are remembered per control set (`:167-175`); the nav root reads only `hasLiveSession` and the whole `LiveSessionBar` recomposes at 1 Hz inside its host; `remainingSeconds` is one shared boundary-aligned poll for every collector. No test counts recompositions, so FRONTEND_REDESIGN's "no timer-driven full-screen recomposition" target is held by reading, not by a gate. | The clock only redraws the small bottom strip each second, not the whole workout page — that is right, but it is true because someone was careful, and nothing would catch it if a later change broke it. | Robolectric: a `SideEffect { count++ }` planted in `ActiveWorkoutContent` and in `RestTimerCard`, advance `ShadowLooper` 10 s with a rest running, assert content count stays 1 while the card's rises. |
| AR-12 (B13a) | P3 | opportunity | C (frequency) / R (cost) | B6 hand-off | `timer/RestTimerService.kt:186-208,252-257`, `timer/RestTimerNotifications.kt:48-90`, `timer/RestTimerController.kt:300-310`, `MainActivity.kt:102-103` | `ensureChannels` (three `deleteNotificationChannel` + `createNotificationChannel` Binder calls) runs on every SYNC command — each rest start, each ±15, each rehydrate — at `:198`, and again on the first foreground claim `:255`, on the main thread, although `PersonalTrainerApp.kt:107` already ran it once per process; it is **not** on the tick path (`handleTick`, `:227-250`). `refreshAlarmCapability` on every `onResume` costs one `canScheduleExactAlarms` query and, only while a rest runs, one alarm re-arm (`:302-304`). Per user action and per foreground, never per second. | Each tap on the rest timer's −15/+15 makes the app re-tell Android about its notification channels, work that was already done at launch; a few milliseconds per tap, not per second. | A once-per-process `AtomicBoolean` guard in `ensureChannels` after the `onCreate` call; measure with `Trace.beginSection` around `:198` on the phone. |
| AR-13 (B13a) | P3 | validation gap | C | — | `tools/check-unbounded-waits.py:1-40`, `ActiveWorkoutViewModel.kt:748-750,1514`, `RestTimerViewModel.kt:63`, `PlanViewModel.kt:362,420` | The unbounded-wait checker scans `*ViewModelTest.kt` only; five `first { }` waits in main sit inside `viewModelScope` with no ceiling and no failure branch — bounded by the ViewModel's life, so a predicate that never holds (e.g. `observations` never leaving `LOADING`) makes the action silently never finish rather than fail. | Five places in the app wait for "the session has loaded" with no time limit; if it never loads, the action just never happens and nothing says so. | Read; or a test that feeds `observations` a permanent `LOADING` and asserts `prefill` reports rather than hangs. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- **B5 (backup):** `recoverInterruptedRestore()` is launched twice on every cold start — `PersonalTrainerApp.kt:117` and `ui/settings/BackupCoordinator.kt:574-583` (`SettingsViewModel` lives at the nav root, `AppNav.kt:286`). Serialised by `dbMaintenance.withMaintenanceLock` (`data/repository/BackupService.kt:338-339`), so harmless duplicate IO; see B5.
- **B7 (planner):** `ensureCurrentWeek()` runs from `onCreate`'s launch (`PersonalTrainerApp.kt:133`) and from the first `onResume` (`MainActivity.kt:105`) concurrently; both go through `database.withTransaction` (`PlannerRepository.kt:283`), Room's single transaction executor serialises them and the second sees `existing`. Benign; see B7.
- **B6 (timer):** the only direct `Dispatchers.Main` is the service's own scope (`timer/RestTimerService.kt:48`); `RestTimerAlarmReceiver.kt:76` launches on a fresh handler-less scope but the body is fully guarded (`:77-95`) and `goAsync`'d. `rehydrate()` runs synchronously in `onCreate` (`PersonalTrainerApp.kt:114`): a SharedPreferences read and, when a rest is live, `persistThenArm` → `startForegroundService` on the main thread (`RestTimerController.kt:240-270,326-356`).
- **B9a (first frame):** the `OnboardingGate.UNKNOWN` blank frame's cause is DataStore's asynchronous first read (`ui/onboarding/OnboardingGateViewModel.kt:42-49` over `PreferencesRepository.kt:430`); `AppNav.kt:296-299` composes nothing until it answers. It is masked by the cold-start intro (`MainActivity.kt:61-92`, `ColdStartIntroCopy.AUTO_ADVANCE_MS = 2_000`), and `stateIn` keeps the resolved value across rotation, so the blank is only ever the first composition of a process (C). What the first frame otherwise waits on: `PreMigrationSnapshot.ensure` (AR-9, once per bump), `AppContainer` construction (`AppContainer.kt:72-360`: no database open — Room builds lazily, `TemperDatabase.kt:84-101`; `AccountAuthFactory.createRuntime()` and `KeystoreBackupPassphraseSealer()` not read for cost), two channel IPCs, the last-crash file read (`:165-169`), and `rehydrate()`.
- **B12 (hand-off answered):** no main-thread bitmap decode is reachable in production; the ~8 MB decode is `TemperStillCache.bind` on IO (AR-5), the 143-thumb cache arithmetic is in AR-5, and the unreachable `painterResource` site is in AR-6.
- **B12 (privacy):** `AppLog.e` hands `onError` the *unredacted* throwable (`AppLog.kt:52-57`); `DiagnosticRedaction.fromThrowable` (`PersonalTrainerApp.kt:193-198`) is the only redaction point before the 64-event ring (`diagnostics/DiagnosticRing.kt:29-30`).
- **B10a/B11 (workout ViewModel):** `suggestedLift`'s `.catch` (`ActiveWorkoutViewModel.kt:600-603`) cannot observe an upstream failure of the shared insights flow (AR-1); it guards only `getById`.
- **B11 (tests):** `ComponentInventoryTest.kt:19` asserts `fun SectionHeader(` exists; nothing renders it (AR-6).
- **B3/B13b (sync):** `SyncWorker.kt:14` cast — S4, unchanged.
- **B4a (migrations):** `TemperPreMigrationCopy.ensure` leaves the check un-marked on an unreadable version (`:83-88`), so it retries every launch with one read-only SQLite open until the file reads; correctness is B4a's.
- **B14 (docs):** (1) `TemperPreMigrationCopy.kt:38` "never blocks launch" vs `PreMigrationSnapshot.kt:53` "Synchronous by design"; ADR-010 §12 (`docs/architecture/ADR-010-schema-reset-migrations.md:88-100`) does not say the copy is synchronous on the main thread — it should. (2) CURRENT_STRUCTURE says "24 checkers" (Verification section); `tools/check-*.py` numbers 26 files — verify against what `preflight.sh` runs. (3) CURRENT_STRUCTURE's counts are within three lines of today's (main 94,071 vs 94,074; `ui` 46,099; `domain` 23,280; `data` 16,947; tests 460 files / 78,919 lines / 3,149 `@Test`) — no correction needed. (4) The 22 Sep record's "13-stage combine" is now 18 calls (§1).


## B13b — Test posture


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| TS-1 (B13b) | P2 | validation gap | C | X5 (#410) | `tools/verify.sh:21-28`, `tools/preflight.sh:246-252`, `.github/workflows/ci.yml:64-98`, `app/build.gradle.kts:317-338,395-400`, `tools/coverage-floors.txt:146-152` | The coverage ratchet (`jacocoTestReport` + `check-coverage.py`) is invoked only by `tools/verify.sh`, which first runs the full `tools/preflight.sh`, which takes the broken plain-JVM lane (TS-2) and exits 1 before the coverage step; CI runs preflight with `PT_STATIC_ONLY=1` and never runs `jacocoTestReport`, and no Gradle task depends on it; so the seven floors X5 re-based on 24 September are enforced by no automated lane. | The "coverage floors" are a safety line that says "the tests must keep checking at least this much of the app" — but the one script that checks the line is broken, and the cloud build never checks it, so a drop below the line would go unnoticed. | Run `sh tools/verify.sh` on `trunk`: it stops at `== JVM tests` with "tests did not compile" (baseline §7). Then grep `ci.yml` and `build.gradle.kts` for `jacocoTestReport`/`check-coverage`: only the report task exists, nothing depends on it. | Confirmed |
| TS-2 (B13b) | P2 | defect (tooling) | C | X5 commit message | `tools/run-domain-tests.sh:146-167,186-191,221-222`; `test/…/domain/CompletedTrainingParityTest.kt:3-7,34-36,54-55`; `test/…/domain/DraftStoreTest.kt:3-14,26-27`; `test/…/domain/UndoQueueTest.kt:3,32,40`; `test/…/data/backup/ProtectBackupTest.kt` (main `data/backup/ProtectBackup.kt`, `OpenBackup.kt` absent from the lane list) | The plain-JVM lane compiles `test/…/domain` and `test/…/data/backup` as whole directories against a hand-picked main list, and four test files in those directories no longer link: three Robolectric/UI tests filed under `domain` and one backup test whose two pure-Kotlin main files are not in the backup lane's list; DEVELOPMENT.md:21 still calls this lane an "every commit" gate and `:292` quotes its old count. | The "quick test run without Android" that a laptop with no Android tools is supposed to use has been unable to start for about two weeks, because four test files were put in the wrong folder or their code was never added to its list. | `sh tools/preflight.sh` (full): fails at `tools/run-domain-tests.sh` with exactly these four files (lead's log, §7). Fix per file in the notes. | Confirmed; survivor TS-2 |
| TS-3 (B13b) | P2 | validation gap | C | DEVELOPMENT.md:400-401 half-names it; X5 made it measurable | `ui/*` screens (list in notes), `ui/navigation/AppNav.kt:279`, `ui/permissions/LaunchPermissionsHost.kt:35`, `ui/intro/ColdStartIntro.kt:59`; JaCoCo XML (§7) | Of the 25 screen-level composables in `main`, 20 are never composed by any test under `app/src/test` (0 call sites; only their names appear in source-string tests), so on the JVM merge gate the Home, Body, Plan, Plan-day, History, Session-detail, Library, Routine-editor, Custom-week, Summary, Settings, Onboarding, intro, permissions, navigation, cardio and composer screens are exercised only through their ViewModels; JaCoCo reads `ui.home` 16.0 %, `ui.plan` 13.6 %, `ui.settings` 17.0 %, `ui.history` 25.5 %, `ui.navigation` 9.0 %, `ui.progress` 9.1 %, `ui.summary` 11.7 %, `ui.activity` 24.6 %, `ui.library` 20.9 %, `ui.intro`, `ui.permissions`, `ui.reminders` 0 %. The hosted emulator lane composes some of them (`HomePassInstrumentedTest` 24 tests, `PlanPassInstrumentedTest`, `HistoryPassInstrumentedTest`, `BodyPassInstrumentedTest`, `LibraryPassInstrumentedTest`, `ProductionScreensPassInstrumentedTest` 12) but it blocks nothing (ADR-024) and nobody can run it locally. | The automatic checks that decide whether a change may ship draw only the workout floor and a handful of pieces; every other tab is checked by reading its code, not by showing it on a screen — so a tab that stopped drawing would pass the gate. | For any screen in the list: `grep -rnE "[^\"A-Za-z.]HomeScreen\(" app/src/test` → 0 hits. Read the XML: `<package name="com/sinura/personaltrainer/ui/home">` INSTRUCTION covered 16.0 %. | Confirmed |
| TS-4 (B13b) | P2 | architecture debt (test) | C | 22 Sep T1 (the same finding, outside `ui/workout`) | 23 files, e.g. `ui/settings/SettingsHomeLayoutTest.kt:10-140`, `ui/theme/HapticsPaletteTest.kt:14-133`, `ui/components/ComponentInventoryTest.kt:13-111`, `domain/TalkBackPolicyTest.kt:35-95`, `ui/components/TemperIconsGlyphTest.kt:18-43`, `ui/theme/MotionPolicyTest.kt:19-76`, `ui/home/HomeStartSheetTest.kt:14-56` | About 400 `assertTrue(source.contains("…"))` pins on production `.kt` text survive outside `ui/workout` (top twelve files alone: 66, 47, 36, 28, 25, 23, 21, 16, 15, 13, 13, 11), most of them positive pins on how code is spelled (`viewModel::generateWeek`, `_deleteFeedback.tryEmit(DeleteFeedback.DELETED)`, `val PrGold = Color(0xFFFFC53D)`, `if (BuildConfig.DEBUG)`), one on a docs sentence (`MotionPolicyTest.kt:73-75` reads `docs/architecture/ADR-023…md`); T1 removed this class from the floor but F4–F10 will hit the same wall on every other tab. | Many tests check that a sentence of code is still written a certain way, not that the screen still behaves; renaming a variable breaks them, while a real bug that keeps the words passes. The workout screen was cleaned of this in T1; the other tabs were not. | `grep -rln 'readText()' app/src/test | Confirmed |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**TS-1.** Counter-evidence looked for: `:app:staticChecks` (`build.gradle.kts:395-400`) runs `tools/preflight.sh` with `PT_STATIC_ONLY=1`, which `exit 0`s at `preflight.sh:246-249` before the JVM lane and never mentions coverage; `tasks.named("check") { dependsOn(staticChecks) }` adds nothing else; `ci.yml:76-98` runs `testDebugUnitTest`, `lintDebug`, `assembleDebug`, `assembleDebugAndroidTest`; the X5 commit message itself says "CI does not run it". The floors file's own rule ("Raise a floor when a packet adds tests", `coverage-floors.txt:129`) is already unmet: timer reads 81.7 % against a floor of 73 after W2b-2/W2b-3 added `timer/RestLockSkipTest.kt` (163 lines), `ui/workout/RestPageSkipTest.kt` (145) and 40 lines to `RestTimerControllerTest.kt` (`git diff --stat 28de703..HEAD`), which is why the lead's reading is 7 points above X5's 74.7 %. `isIncludeNoLocationClasses` is correct as configured: Robolectric's sandbox loader gives app classes no code source but does not rewrite their bytes (only framework classes are instrumented), so JaCoCo's class ids still match the on-disk classes that `jacocoTestReport` reads from `tmp/kotlin-classes/debug` (`build.gradle.kts:325-331`); X5's "same 3,346 classes … no class or method lost coverage" is the empirical check. `check-coverage.py:89-103` keys on the exact JaCoCo package name, and JaCoCo packages are flat, so `domain.coach` (892 instr, 85.4 %) and `data.repository.prefs` (3,907, 64.1 %) are *not* under the `domain` / `data.repository` floors; `ui.workout` has no subpackage, so its floor is whole.

**TS-2.** Per file, the fix that fits: `CompletedTrainingParityTest.kt` — (a) move; it is a Robolectric test of three ViewModels (`ui.activity.ActivityDetailViewModel`, `ui.history.HistoryViewModel`, `ui.history.SessionDetailViewModel`) with `FakeAppDependencies`, and belongs under `test/…/ui/history`. `DraftStoreTest.kt` — (a) move to `test/…/ui/activity`; it imports the composer's `SavedStateComposerDraft`/`SavedStateCardioDraft` and Robolectric. `UndoQueueTest.kt` — it tests `domain.UndoDwell` and uses `ui.theme.Motion.STATUS_DWELL_MS` only as a base value (`:32,40`); either (a) move to `ui/components`, or keep it in `domain` with a local `6_000L` (the value `MotionPolicyTest.kt:58` already pins). `ProtectBackupTest.kt` — (b) add `ProtectBackup.kt` (24 lines) and `OpenBackup.kt` (12) to the backup lane's list at `run-domain-tests.sh:187-191`; both have no imports at all. Since when: this clone is shallow (50 commits; oldest `0c759e7`, the Phase 11 sync commit) and all four files already exist at that commit, so `git log -S` cannot date it here; X5's commit message (`28de703`) says "pre-existing since 11–15 September". `PT_ALLOW_NO_COMPILER` cannot hide it: it only governs the syntax check (`preflight.sh:222-238`); the JVM lane is chosen at `:250-252` whenever a jar directory exists, and `preflight.sh:138-141` builds that directory from any Gradle cache, so every machine that has ever built the app takes the broken lane. Only a machine with no jars at all falls back to `./gradlew testDebugUnitTest` (`:253-255`), which is why the Gradle gate and the hosted job (`PT_STATIC_ONLY=1`, `ci.yml:65`) never see the break. `DriveRestClientTest.kt:3-4` imports Gson, which is fine because the whole backup lane is skipped without the Gson jar (`:196`).

**TS-3.** Untested-surface table (production → tests that reference it → verdict). "Composed" means a real call site under `app/src/test`; "named" means only inside a string in a source-scan test.

| Production surface | Tests referencing it | Verdict |
|---|---|---|
| `HomeScreen`, `ProgressScreen`, `PlanScreen`, `PlanDayScreen`, `HistoryScreen`, `SessionDetailScreen`, `ExerciseLibraryScreen`, `RoutineEditorScreen`, `CustomWeekScreen`, `WorkoutSummaryScreen`, `SettingsScreen`, `OnboardingScreen`, `ActivityComposerScreen`, `LiveCardioScreen`, `ActivityDetailScreen` | named only (`GymChromeTest`, `MotionPolicyTest`, `ForeignControlsTest`, `TalkBackPolicyTest`, `SettingsHomeLayoutTest`, `DateCopyTest`, …) | never composed on the JVM; hosted `*PassInstrumentedTest` only |
| `ColdStartIntro`, `LaunchPermissionsHost`, `PersonalTrainerNav`, `LiveSessionBarHost`, `SavePostureHost` | none | never composed anywhere on the JVM; `ui.intro`/`ui.permissions` 0 %, `LiveSessionBarKt` 0 % (857 instr) |
| `ActiveWorkoutScreen`, `RestTimerScreen`, `ExerciseDetailScreen`, `RestLockScreen`, `StartOptionsSheet`, `SavePostureChooser`, `HomeMasthead`, `AccountSection` | composed (`WorkoutFloorRenderTest`, `FloorScreenWiringRenderTest`, `RestPageSkipTest`, `ExerciseDetailThisWorkoutRenderTest`, `RestPagesRenderTest`, `StartOptionsRoutineCardRenderTest`, `FirstLaunchRenderTest`, `HomeMastheadTest`, `AccountSectionTest`) | covered |
| `PlanDayViewModel` (`ui/plan/PlanDayViewModel.kt`) | none | 0 % — the only ViewModel of 21 with no test file |
| `OnboardingGateViewModel`, `LiveSessionBarViewModel`, `ProgressViewModel`, `CustomWeekViewModel`, `ExerciseLibraryViewModel`, `WorkoutSummaryViewModel` | one `*ViewModelTest` each | covered, thinly |
| `ui/settings/BackupCoordinator.kt` (985 lines) | none by name; reached through `SettingsViewModelTest` | no direct test; F10c owns the split |
| `TrendChart.kt` (`LabelledTrend`, `LineTrend`, `TrendBars`), `ExerciseThumbGallery.kt`, `FigureArt.kt` (517 lines, no composable — `BodyPlate` data), `KeyedArtwork.kt`, `PoseArtwork.kt`, `LiftPose.kt` (719), `LoadTypeChipRow`, `ResumeOrDiscardDialog`, `LeaveCardioDialog` | none | no unit reference; `ExerciseThumbGalleryKt`, `LoadTypeChipsKt`, `ResumeOrDiscardDialogKt`, `LeaveCardioDialogKt` 0 % |
| `data/sync/SupabaseRestClient.kt` (83 lines, 646 instr), `SyncWorker.kt`, `WorkManagerSyncScheduler.kt`, `SupabaseSyncRemote.kt` | none | 0 %; the network edge of sync is untested (S-9's family) |
| `data/sync/SyncAuthoring.kt` (185) | `SyncPauseTest.kt:162` constructs it | exercised only for the paused path |
| `data/auth/SupabaseAccountAuth.kt` incl. `deleteAccount` | none (`AccountCoordinatorTest.kt:133-150` tests the *fake*) | 0 % (268 instr) |
| `data/security/BackupPassphraseSealer.kt` (140 lines) | only the in-memory fake (`FakeAppDependencies.kt:430-434`) | 1.1 % — the Keystore sealer never runs in a test |
| `reminder/`: `WorkoutAlarmScheduler.kt` (99), `WorkoutAlarmReceiver.kt`, `ReminderRebuildReceiver.kt`, `ReminderWorker.kt` | none (the 15 reminder tests cover `ReminderNotifications`, `ReminderActionApply/Receiver`, `ReminderRebuild`, `ReminderWork`, `WorkManagerReminderScheduler`) | `reminder` 43.2 %; `ReminderWorker` 0 % |
| `update/`: `DataStoreDebugUpdateCache.kt`, `DebugUpdateModels.kt` | none (25 tests cover the other seven files) | `update` 56.9 %; Temper Debug only |
| `diagnostics/DiagnosticMetadata.kt` | none | `diagnostics` 79.7 %; the one Android file of the package |

**TS-4.** Classification of the 42 test files that read `.kt` source or another file as text. (a) legitimate structural ratchets, keep: `GymChromeTest` (17 sites share `ScreenHeader`/`PinnedDock`), `ComponentInventoryTest.kt:13-38,41-81` (inventory and retired files), `ForeignControlsTest.kt:37-91` (no Material `Switch`/`DropdownMenu`/`SnackbarHost` — a ban), `MotionPolicyTest.kt:32-44` (no `Modifier.animateItem` — a ban), `FoundationFreezeTest`, `TemperDatabaseTest.kt:47-52`, `UpgradeInPlaceTest.kt:93-104`, `FoundationResetTest.kt:124-129` (no `fallbackToDestructiveMigration`; the debug id suffix), `PreMigrationStartupOrderTest` (copy-before-Room order; its comment calls itself a tripwire), `SyncPauseTest.kt:186-195` (the pause switch is wired into the container), `SyncReleaseKeepRuleTest` (R8 keep rule), `DebugUpdatePolicyTest.kt:33-42` (manifest permissions), `EmptyArtTest.kt:66-80` (every `EmptyState(` caller passes a scene), `GetStartedCopyTest`/`SettingsHomeLayoutTest.kt:122-128` (a retired file stays gone), `NumericEntryTest.kt:148-153` (tags exist), `TemperSchemaAssetsTest`, `PreMigrationSnapshotTest`/`TemperPreMigrationCopyTest` (they read copied database files, not source), `WorkoutPaste*Test`/`DefaultExercisesTest`/`RoutineGeneratorTest` (fixtures and `docs/artifacts`, not source). (b) behaviour that should be a rendered or semantic test: `TalkBackPolicyTest.kt:35-95` (roles, headings, live regions — all readable from the semantics tree), `HapticsPaletteTest.kt:55-133` (which haptic fires on commit/reject/delete), `SettingsHomeLayoutTest.kt:10-119` (which rows and icons Settings shows, which pane holds Generate), `HomeStartSheetTest` (the start sheet's options), `HistoryStrongerTest`, `HistoryListThumbsTest`, `SessionDetailSheetTest`, `DayBlockLayoutTest`, `AddToRoutineSheetTest`, `PlanDayConfirmTest` (destructive confirm; "Start rest" wording), `BodyViewportTest.kt:31-63` (legend above the figure, chips under it — measurable bounds), `RoutineEditorPasteLayoutTest`, `DateCopyTest.kt:31-59`, `TabularNumeralTest.kt:52-68`, `TargetStepperTest.kt:42-48`. (c) brittle — pins a spelling a rename breaks with no behaviour change: `ComponentInventoryTest.kt:84-110` (`val numeralHero`, `val space7` …), `TemperIconsGlyphTest.kt:19-43` (path constant names), `MotionPolicyTest.kt:48-61,65-75` (`const val SLOW` absent, hex literals, a sentence of ADR-023), `HapticsPaletteTest.kt:126-133` (exact `tryEmit` call text), `SettingsHomeLayoutTest.kt:56,99-101` (`viewModel::generateWeek`), `DebugUpdatePolicyTest.kt:14-28` (`if (BuildConfig.DEBUG)`), `ForeignControlsTest.kt:89-90` (`tonalElevation = 0.dp`), `HistoryStrongerTest.kt:11-15` (`indexOf` ordering of two calls). Counter-evidence: some of these do have a rendered twin already (`ReducedMotionRenderTest` covers what `MotionPolicyTest.kt:20-31` pins; `RestTimerCardRenderTest` covers the rest kicker's live region), so the move is partly done; none of the (b) rows for Home/History/Settings/Plan/Body has one.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| TS-5 (B13b) | P3 | target gap | C | ADR-032 §Consequences (W3 pending) | `ui/workout/WorkoutFloorRenderTest.kt:92-216,337-345` | Twelve of the floor's fourteen render tests end in `assertExists()` on three to five tags, which passes for a node scrolled off-screen, so "a control that falls out of reach fails the gate" (ADR-032 §1) is not yet true for the floor; only `:218-243` measure a touch target, and the reachability pattern exists elsewhere (`FirstLaunchRenderTest.kt:70` `performScrollTo().assertIsDisplayed()`, `LargeTextEntryRenderTest.kt:193-230` ink boxes, `FirstWorkingSetRenderTest.kt:164-179` positions). | The pictures of the workout screen are taken, but most of those tests only check that the parts exist somewhere, not that they are on screen where a thumb can reach them; that stricter check is promised for packet W3. | Read the file; W3 is the fix. |
| TS-6 (B13b) | P3 | validation gap | C | ADR-010 §7 | `data/local/TemperMigration1To2Test.kt:36-70`, `TemperMigration2To3Test.kt:33-67`; `androidTest/…/TemperMigrationDeviceTest.kt:27-61` | The 1→2 and 2→3 JVM tests seed rows and then only count them after migrating (plus an index name), never reading a value back; 3→4 reads back only the new default columns; 4→5, 5→6 and 6→7 (with the 4→7 chain, `TemperMigrationFixtures.kt:43-57`) read values back and are the data-preserving shape ADR-010 §7 intends; the device chain 1→7 is count-only plus `PRAGMA foreign_key_check`. | Two of the six "does upgrading keep my data" checks only count rows; they would not notice a weight or a name coming through wrong. | Add a `SELECT weightKg … FROM set_logs` readback to 1→2 and 2→3 as 6→7 does. |
| TS-7 (B13b) | P3 | validation gap (flaky-prone, not observed) | C | — | `FakeAppDependencies.kt:109-111`; `insights/TrainingInsightsSourceTest.kt:195-210`; `timer/RestTimerControllerTest.kt:478-496,1040-1052`; `timer/RestTimerServiceTest.kt:486-495,670-680`; `timer/RestTimerRecoveryTest.kt:88-98` | 93 test files use `runBlocking` against 8 `runTest`; `FakeAppDependencies` defaults `ioDispatcher`/`computeDispatcher` to `Dispatchers.IO`/`Default` when no scheduler is passed; five timer tests poll with `Thread.sleep` on wall-clock deadlines (3 s, `PREFS_WAIT_NANOS`, `COMPLETION_WAIT_NANOS`) and `TrainingInsightsSourceTest` races a `delay(200)` against a 10 ms share grace on the shared `Default` pool. All are bounded and none has failed in the record (baseline 0 failures; 30/30 hosted runs green), so this is a hazard, not a flake. | Some tests wait on real clocks and real background threads instead of a pretend clock; they have not misfired, but a slow machine could make one fail for no reason. | `grep -rl runBlocking app/src/test |
| TS-8 (B13b) | P3 | validation gap | C | — | `ui/settings/SettingsViewModelTest.kt:362-370`; `timer/RestSoundTest.kt:92-118`; `reminder/ReminderReceiversAndWorkerTest.kt:128-134` | Of 52 `@Test` methods with no assertion keyword in their body, 47 delegate to helpers that assert (`showsTheSession`, `render`, `activeScenario`, `checkRemovedPendingOwner`, or a `withTimeout { flow.first { } }` that is itself the check); five are honest "does not throw" smokes; one, `actionReceiverWithoutTheAppClassIsANoOp`, claims a no-op and checks nothing. | Almost every test checks something; five only check "it did not crash", and one says "nothing happened" without looking. | The file:line list above; the scanner is `scratchpad/noassert.py`. |
| TS-9 (B13b) | P3 | validation gap | C | X5 | `tools/coverage-floors.txt:146-152`, `tools/check-coverage.py:106-121` | The floors cover 7 of 46 JaCoCo packages; unfloored and low: `data.sync` 48.0 % (10,334 instr; S1 will resume it), `reminder` 43.2 %, `data.auth` 12.0 %, `data.security` 1.1 %, `data.repository.prefs` 64.1 %, `update` 56.9 %, `domain.coach` 85.4 %; and the timer floor (73) sits 8.7 points under its reading. | The safety line exists for only a third of the app's parts, and the sync and reminder parts — the ones with the most moving pieces — have none. | Read the XML (§7). Recommend floors for `data.sync` and `data.repository.prefs` before S1, and timer → 80. |
| TS-10 (B13b) | P3 | validation gap | C | — | `tools/check-unbounded-waits.py:16-33`; `ui/workout/FloorVmContractTest.kt:121-125` | The unbounded-wait checker only reads `*ViewModelTest.kt`; ViewModel waits in render, kit and scenario files (`FloorTestKit`, `ManualRestStartTest`, `WorkoutSaveRecoveryTest`, `FloorVmContractTest`) are bounded today by `awaitFirst` (`TestWaits.kt:44-56`, 30 s) and `withTimeout` by convention, not by the checker; the one bare `.first {` I found (`FloorVmContractTest.kt:123`) is inside a `withTimeout`. | The tool that stops a test from hanging forever only looks at files with one particular name; the other files behave, but nothing forces them to. | `grep -rnE "\b(vm |
| TS-11 (B13b) | P3 | architecture debt (test) | C | — | `ui/theme/MotionPolicyTest.kt:65-75`, `:86-89` | A unit test reads `docs/architecture/ADR-023-palette-and-reduced-motion.md` and fails if the words "non-colour" or "Do not shift Warn toward orange" are edited, so a docs pass can break `testDebugUnitTest`. | One test fails if someone rewords a design note; the note is not code. | Read the lines. |


### Noticed, outside this pass's scope

Noticed, outside my scope

- `data/security/BackupPassphraseSealer.kt` at 1.1 % coverage: the Android Keystore sealer that keeps the Drive backup passphrase is swapped for `InMemoryBackupPassphraseSealer` in every JVM test (`FakeAppDependencies.kt:430-434`); a seal/open regression would reach the phone unseen. → backup pass (B5/B10).
- `data/sync/SupabaseRestClient.kt` (646 instr, 0 %), `SyncWorker` 0 %, `SupabaseSyncRemote` 0 %, `SyncRemoteRows` rows for account/display/reminder prefs and schedule occurrences 0 %: the network edge and the WorkManager entry of sync have no test; S-9's family. → sync pass, before S1.
- `data/auth/SupabaseAccountAuth.kt` 0 % including `deleteAccount` (S-7): the only `deleteAccount` test is on the fake (`AccountCoordinatorTest.kt:133-150`). → sync/privacy pass.
- `reminder/WorkoutAlarmScheduler.kt` (99 lines), `WorkoutAlarmReceiver.kt`, `ReminderRebuildReceiver.kt`, `ReminderWorker.kt` (0 %): the alarm side of reminders has no test at all. → reminders pass (RM).
- `ui/permissions/LaunchPermissionsHost.kt` (1,132 instr, 0 %) and `ui/intro/ColdStartIntro.kt` (0 %): the first-launch walk L-2 fixed in Q1 is proven by `SavePostureChooserTest`/`FirstLaunchRenderTest` for the chooser only; the permission host itself is never composed. → first-launch pass.
- Docs contradicting the code, for B14: `docs/DEVELOPMENT.md:21` (plain-JVM lane "every commit" gate — broken, TS-2), `:292-293` (lane "runs 170 classes and 1,175 tests"), `:166` ("a few seconds" — 8 m 39 s in the baseline), `:12` ("current as of the 2026-09-06 handoff"), `:311-312` ("twenty checkers … its comment still says 17" vs `ci.yml:61` "20 checkers, 4 fixture proofs" vs `DEVELOPMENT.md:20` "24 checkers, 9 fixture proofs"), `:396-401` ("Compose screens themselves are the remaining automation gap" — true for 20 screens, false for the floor's 44 render tests), `:488-489` ("`TemperDatabase` … version 4, frozen" — v7), `:602-603` ("X3 removed … the legacy 360 × 800 dp `GoldenCapture` mount" — `GoldenCapture.mountViewport(width = 360.dp, height = 800.dp)` is still called at `androidTest/…/FrontendControlsInstrumentedTest.kt:102,155,170` and `FoundationStateGalleryInstrumentedTest.kt:22`; `GoldenCapture.kt:35-41` documents itself as the kept mount), `ci.yml:149` ("the eighty instrumented tests" — 112 `@Test`), `docs/architecture/CURRENT_STRUCTURE.md:16` "0.15 test lines per production line" (0.18).
- `tools/hang-watchdog.sh:82` watches only the first `GradleWorkerMain` (`head -1`); correct while `maxParallelForks` is the default 1 (no setting in `build.gradle.kts`), wrong the day someone parallelises the test task. → build pass (BR).


## B14 — Build, CI, supply chain, tools, docs truth, privacy claims


### P1 and P2

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| BR-1 (B14) | P2 | defect | C | FND-029 | `app/proguard-rules.pro:11-12,28`, `gradle/libs.versions.toml:24,65`, `app/build.gradle.kts:148-158`, `.github/workflows/release.yml:131-132`, `app/build/outputs/mapping/release/missing_rules.txt:3` | `./gradlew assembleRelease` fails on `trunk` at `:app:minifyReleaseWithR8` ("Missing class org.slf4j.impl.StaticLoggerBinder … 3 other contexts") because Ktor 2.3.12 (pulled by `supabase-gotrue-kt` since commit `0c759e7`, 21 Sep) brings `slf4j-api` 1.7.36 and `proguard-rules.pro` has no `-dontwarn` for it; no release APK or `mapping.txt` is produced, so a `v*` tag fails at `release.yml:132`. | The signed gym-floor version of the app cannot be built at all right now: a five-day-old change added a library whose shrinker warning nobody has ever silenced, because nobody has built the release flavour since. Nothing already on a phone is affected; every drop the phone gets is the unshrunk debug build. One line fixes the build. | `./gradlew assembleRelease` → BUILD FAILED (lead's run, §7); `grep -c slf4j app/proguard-rules.pro` → 0; `cat app/build/outputs/mapping/release/missing_rules.txt` → `-dontwarn org.slf4j.impl.StaticLoggerBinder` | Confirmed |
| BR-2 (B14) | P2 | defect | C | — | `.github/workflows/release.yml:59-60` vs `debug-live.yml:58-66`, `ci.yml:46-51` | `release.yml`'s `android-actions/setup-android` step has no `packages: ''`, so it requests the retired `tools` package that `debug-live.yml:58-60` records as having aborted live 59; a `v*` tag would fail at "Set up Android SDK", before tests, lint or BR-1 are reached. | The release robot has a second, independent break in its very first step — the same one that stopped a debug drop weeks ago and was fixed in the other two robots but not this one. | Read the three `with:` blocks; or the owner runs `Release` via `workflow_dispatch` (artifact only, no publish) and reads the SDK step (R for the run itself) | Confirmed (by reading; the run itself is R) |
| DC-1 (B14) | P2 | defect | C | 22 Sep System 1 | `docs/DEVELOPMENT.md:488-493` vs `app/src/main/java/com/sinura/personaltrainer/data/local/FoundationGeneration.kt:17`, `docs/HANDOFF-NEXT.md:16-21` | The runbook's "Things that will bite you" says `TemperDatabase` is "version 4, frozen"; the code is v7 with migrations 1→7, and the JVM migration gate (ADR-032 §4, `TemperSchemaAssetsTest`) is not mentioned there at all — the one section a developer reads before a schema change is the one X1 left stale. | The "how to change the database safely" instructions still describe the database as it was two weeks ago and leave out the test that now guards it; someone following them could skip the step that catches a data-losing mistake. | `grep -n 'const val VERSION' …/FoundationGeneration.kt` → 7; `sed -n 488,489p docs/DEVELOPMENT.md` → "version 4, frozen" | Confirmed |
| BR-3 (B14) | P2 | validation gap | C | — | `tools/preflight.sh:250-252`, `tools/verify.sh:21-22`, `tools/run-domain-tests.sh:14-16`, `app/src/test/…/domain/CompletedTrainingParityTest.kt:4-8`, `domain/DraftStoreTest.kt:4-8`, `domain/UndoQueueTest.kt:3`, `data/backup/ProtectBackupTest.kt`, `docs/DEVELOPMENT.md:21,170-186` | The plain-JVM lane (`tools/run-domain-tests.sh`, which full `preflight.sh` runs whenever `build/test-jars` exists) cannot compile on `trunk`: four Robolectric/ViewModel tests sit in `test/domain` and `test/data/backup`, directories the lane compiles wholesale, and import `androidx.*`, `FakeAppDependencies`, `ui.theme.Motion` and `ProtectBackup` (`data/repository`). So `tools/verify.sh` (documented as "the single local verification command") fails at its first step, and DEVELOPMENT.md still calls the lane an every-commit gate. | One of the two documented self-check commands has been silently broken by tests filed in the wrong folder; the real gate (Gradle) is unaffected, but the docs promise a check that cannot pass. | `sh tools/preflight.sh` (full) → `FAILED: tests did not compile … preflight: FAIL — JVM tests` (lead's probe 7, `baseline/07-preflight-full.log`); `grep -n '^import androidx' app/src/test/java/com/sinura/personaltrainer/domain/CompletedTrainingParityTest.kt` | Confirmed; the same lane failure V4 verified as TS-2 (B13b), which is the survivor |


<details><summary>Notes and counter-evidence the pass recorded</summary>

**BR-1.** Provenance (C): `~/.gradle/caches/modules-2/files-2.1/io.ktor/ktor-client-core-jvm/2.3.12/…/ktor-client-core-jvm-2.3.12.module` declares `org.slf4j:slf4j-api` 1.7.36 and `kotlinx-coroutines-slf4j` for both `jvmApiElements` and `jvmRuntimeElements`; `ktor-client-android-jvm`, `-http-jvm`, `-io-jvm`, `-utils-jvm`, `-events-jvm` repeat it. The ledger carries `org.slf4j:slf4j-api` 1.7.36 (`gradle/verification-metadata.xml`). `slf4j-api` 1.7's `LoggerFactory.bind()` references `org.slf4j.impl.StaticLoggerBinder`, which no binding on the classpath provides; R8 treats a missing referenced class as fatal. The catalog gained `ktor-client-android` and `supabase-bom` in `0c759e7` (21 Sep, "Phase 11"); `proguard-rules.pro` was edited once since (`ba71dfd`, S0a, the `data.sync` keep) without a release build. Last recorded success: `docs/foundation-program/evidence/P12-field-operations.md:29-30`, pre-Phase 11. Counter-evidence looked for: (a) an slf4j rule — none in the 50-line file (`-dontwarn` only at :11-12,28); (b) a test pinning the release build — `SyncReleaseKeepRuleTest.kt:21` asserts the keep-rule text only, `check-play-rehearsal.py:46-49` greps `isMinifyEnabled = true`; (c) any lane running `assembleRelease` — only `release.yml:132` on `v*` tags, and origin has 0 `v*` tags (106 `debug-live-*`); (d) whether debug is hit — the debug block (`app/build.gradle.kts:135-147`) sets no `isMinifyEnabled`, so drops 99–104 built and shipped unshrunk, unaffected. After the rule lands, R8 may still break `gotrue-kt`/Ktor at runtime in a release build; that is moot while release builds ship no Supabase URL (`release.yml` never writes `supabase.properties`; `PRIVACY.md:46-47`), but B2's static R8 read should say so. Disposition (ADR-013): one quiet packet — add `-dontwarn org.slf4j.impl.StaticLoggerBinder` from `missing_rules.txt`, add `packages: ''` to `release.yml` (BR-2), and put an unsigned `assembleRelease` where it runs on every packet: the honest place under ADR-002 §5 is the local gate (`docs/HANDOFF-NEXT.md:121-125`), and ADR-024 also lets the deterministic hosted job carry it beside `assembleDebug` (`ci.yml:87-88`).

**BR-2.** The sibling workflows' own comments are the evidence (`debug-live.yml:58-62`: "Google no longer ships the obsolete `tools` package … which aborted live 59"; `ci.yml:49-51`). `release.yml` was written before that fix and has never run on a tag. Same packet as BR-1.

**DC-1.** Same section also still says "commit `<n>.json`" and the `2.json` note (`:495-501`) but not the byte-identical `app/src/debug/assets/` copy that `TemperSchemaAssetsTest` demands (`ADR-032 §4`, `HANDOFF-NEXT.md:16-21`). Disposition: the next docs pass (X7).

**BR-3.** `preflight.sh:138-141` bootstraps `build/test-jars` from the Gradle cache whenever it can, so on any machine that has run Gradle once the full preflight takes the domain-lane branch (`:250-252`) and fails; only a machine with no jars falls through to `./gradlew testDebugUnitTest` (`:253-255`) and passes. `verify.sh:38` also still says "re-run this script for FND-004" (closed 24 Aug). Disposition: tooling packet (X7): move the four files beside the tests they resemble (`test/ui/…`, `test/data/repository/…`) or make `run-domain-tests.sh` name test files individually as it already does for main sources (`:16-17`); then `tools/verify.sh` is honest again.

</details>


### P3

| ID | Sev | Type | C/R | Prior | Files | Claim | In plain terms | Probe or repro |
|---|---|---|---|---|---|---|---|---|
| BR-4 (B14) | P3 | architecture debt | C (PR state R) | FND-027 | `.github/dependabot.yml:9-13,27-76`, `docs/architecture/core-toolchain.md:18-20` | The ignore list omits `androidx.lifecycle:*` (2.11 refused: needs AGP ≥ 9.2), `androidx.activity` (1.13 deferred), `androidx.work`, `androidx.test*`, `io.github.jan-tennert.supabase:bom` and `io.ktor:*` (no signed matrix at all); with the `androidx` group at :9-11 every monthly group PR carries a refused bump and is closed unmerged (#114 pattern), so the patch/security updates riding in the same PR are frozen too. | The monthly "update libraries" robot always proposes at least one upgrade the project has decided not to take, so its whole bundle gets thrown away — including the small safety fixes it also carried. | Read the two files; B13b can confirm whether a group PR is open now (R) |
| BR-5 (B14) | P3 | architecture debt | C | — | `gradle/verification-metadata.xml` (785 components, 1,423 sha256, 0 pgp), `docs/architecture/lint-policy.md:43`, `tools/check-supply-chain.py:20-39` | The ledger carries checksums for generations the build does not use — supabase 3.2.4, ktor 3.1.3 and 3.3.0, kotlinx-serialization 1.9.0, `kotlin-serialization-compiler-plugin-embeddable` 2.0.21 and the serialization Gradle plugin dropped on 30 Aug (`core-toolchain.md:37-39`); the policy says the file "checksums the resolved graph" and the checker only tests presence, never surplus, so a catalog bump to one of those versions resolves without touching the ledger. | The "approved ingredients" list still approves ingredients the recipe stopped using, so swapping one back in would pass the check that is supposed to notice. | `grep -c 'version="3.2.4"' gradle/verification-metadata.xml` (≥5); lead: `./gradlew --write-verification-metadata sha256 --dry-run` and diff |
| BR-6 (B14) | P3 | opportunity | C | — | `tools/cloud-setup-android.sh:33-38`, `.claude/hooks/session-start.sh:16,46,104-105`, `gradle/wrapper/gradle-wrapper.properties:4-6`, `docs/DEVELOPMENT.md:581` | Both cloud SDK bootstraps download `commandlinetools-linux-*_latest.zip` on TLS trust alone (the Windows script pins the archive's SHA-256), and they name different fallback builds (11076708 vs 13114758) although the hook says "keep the two in step"; the wrapper comment's "the one unpinned byte-stream" is true only of the Gradle graph. | The toolkit the cloud sessions install is fetched without the fingerprint check everything else here gets. | Read the four line ranges |
| BR-7 (B14) | P3 | defect | C | — | `.github/workflows/release.yml:213-216,223-225,178` vs `debug-live.yml:230-238` | `gh release upload … \ | \ | echo` swallows an upload failure and goes green — the exact shape `debug-live.yml` removed after the 10 Sep drop that published nothing; `:178` interpolates `${{ steps.apk.outputs.path }}` inside `run:` while `:204` passes it through env. |
| BR-8 (B14) | P3 | validation gap | C | 22 Sep System 3 | `app/build.gradle.kts:396,405-409`, `docs/HANDOFF-NEXT.md:121-125`, `.github/workflows/ci.yml:84-85` | `lintDebug` is in the written gate and in the hosted job, but nothing makes `testDebugUnitTest assembleDebug` depend on it; in the container it runs by memory, and the hosted job that would catch a miss is not a required check (ADR-024, owner). | The "lint" step of the merge checklist is still something a person has to remember to type. | `./gradlew testDebugUnitTest assembleDebug --dry-run \ |
| BR-9 (B14) | P3 | defect | C | — | `docs/RECOVERY.md:9-13,50-51,56-80,98`, `.github/workflows/debug-live.yml:78-105`, `SETUP.md:327-365` | The recovery runbook covers only the release keystore and says debug builds "need nothing from you", yet every install on the phone today is Temper Debug signed by the stable debug distribution keystore restored from write-only secrets (106 `debug-live-*` tags, 0 `v*` tags on origin); losing it means uninstall/reinstall from a backup plus a new Drive OAuth SHA-1, and `:98` "every release is tagged vX.Y.Z" has no instance yet. | The "what if I lose the signing key" page forgets the one key actually in use. | Read the ranges; `git ls-remote --tags origin \ |
| DC-2 (B14) | P3 | defect | C | 22 Sep System 1 | `SETUP.md:70-74` vs `domain/AccountSyncGate.kt:23`, `docs/PRIVACY.md:39-44,58-60` | SETUP §4 says "When signed in and online, Temper copies finished workouts and plan rows to your Supabase project in the background" — sync is paused (`SYNC_PAUSED = true`) and live strength workouts were never replicated (S-6). | The setup guide still describes cloud copying that is switched off, and includes workouts that were never copied. | `grep -n SYNC_PAUSED …/AccountSyncGate.kt` |
| DC-3 (B14) | P3 | defect | C | 22 Sep System 1 | `.github/workflows/ci.yml:61-62,149`, `docs/architecture/CURRENT_STRUCTURE.md:160`, `docs/DEVELOPMENT.md:12,166,183-184,311-312`, `app/build.gradle.kts:87-88,97` | Counts and status comments disagree with the measured run: ci.yml "20 checkers, 4 fixture proofs" and "eighty instrumented tests"; CURRENT_STRUCTURE "8 fixture proofs"; DEVELOPMENT "twenty checkers today; its comment still says 17", "plain JVM tests … a few seconds", lint "existing ones live in the baseline", table "current as of 2026-09-06"; build script "P4.6 still owns cleaning the baseline to zero". Measured: 24 `check-*` and 9 `test_*` steps (`baseline/01-preflight-static.log`), 112 instrumented `@Test` (`CURRENT_STRUCTURE.md:23`), 3,149 JVM tests in 8m39s, baseline empty, P4.6 done (`FOUNDATION_PROGRAM.md:575`). | Four files quote four different sizes for the same checklist, and two comments say a cleanup is still owed that was finished weeks ago. | `grep -c '^== check-' baseline/01-preflight-static.log` → 24; `grep -c '^== test_' …` → 9 |
| DC-4 (B14) | P3 | defect | C | — | `SETUP.md:7-396` headings vs `SETUP.md:158,173,217-219,359`; `docs/RECOVERY.md:76,88,90`; `docs/DRIVE_SIGNIN_CHECK.md:4,39,185`; `docs/DEVELOPMENT.md:50,66`; `README.md:30`; `debug-live.yml:17,89,158,216`; `release.yml:150,210` | Inserting "§4 Temper Account" shifted SETUP's numbering; 17 cross-references in 7 files now point one section off ("§4" for Drive OAuth = §5, "§6" for the debug signer/drop = §7, "§5"/"§5.3" for GitHub Release = §6). | Every "see section N" pointer into the setup guide lands on the wrong section. | `grep -n '^## ' SETUP.md` against the cited references |
| DC-5 (B14) | P3 | defect | C | FND-042 | `.github/ISSUE_TEMPLATE/config.yml:4`, `.github/pull_request_template.md:7-9`, `tools/check-doc-authority.py:20-28`, `docs/HANDOFF-NEXT.md:119-135` | The issue template links `blob/main/docs/ROADMAP.md` (no `main` on origin; ROADMAP is superseded, `ROADMAP.md:3-5`), and the PR template's checklist (`testDebugUnitTest`, "ran on a device") is not the gate (static preflight, `testDebugUnitTest assembleDebug`, `lintDebug`, failing-test-first, two reviews, JVM renders); `.github/` is outside the doc-authority checker's file list. | The bug-report page links to a dead address, and the pull-request checklist asks for the wrong proof. | Open the URL; read `HANDOFF-NEXT.md:119-135` |
| DC-6 (B14) | P3 | defect | C | FND-041 | `README.md:43` vs `docs/foundation-audit/README.md:3-9` | README labels the foundation audit "canonical current-state audit" while the audit's own banner says it is "a dated audit, not a description of the app today". | The front page calls a museum piece the current map. | Read both |
| DC-7 (B14) | P3 | defect | C | — | `docs/DEVELOPMENT.md:373-380,461-467` vs `:422-427`; `docs/DEVELOPMENT.md:552` vs `ADR-002:31` vs `git ls-remote` | DEVELOPMENT contradicts itself on Windows (the JVM lane "requires macOS or Linux" / "does not work here" vs "passed 2,536 tests on Windows; do not assume Linux is required"), and three branch-naming rules coexist (`codex/<slug>`, `cursor/<slug>-b87f`, actual `claude/*`). | One page says the Windows machine can and cannot run the same tests. | Read the ranges |
| DC-8 (B14) | P3 | defect | C | — | `docs/PRIVACY.md:68-69`, `docs/architecture/sync-personal-build.md:57`, `domain/SetLogRules.kt:17`, `domain/LibraryGrouping.kt:8,21`, `domain/ExerciseOrdering.kt:8` vs `domain/DefaultExercises.kt:62` (`CATALOG_VERSION = 10`, 143 `id = "ex-` rows), `docs/artifacts/catalog-v10-review.md:396` | The published privacy text ("~98 default lifts"), the sync record ("98 built-ins stay device-local") and three domain KDocs still say 98; the catalog is 143 lifts across 47 families (143 `ex_*` stills per `check-still-pack`). README does not carry the number; ROADMAP/DESIGN_AUDIT/HIERARCHY_PLAN say 98 under historical banners, which is fine. (B12 hand-off.) | The privacy page and two code comments under-count the built-in exercise list by 45. | `grep -c 'id = "ex-' …/DefaultExercises.kt` → 143; `grep -rn '98 lifts\ |
| PV-1 (B14) | P3 | defect | C | — | `docs/PRIVACY.md:117`, `SETUP.md:402` vs `ui/permissions/LaunchPermissionsHost.kt:30-33,90-96`, `domain/LaunchPermissions.kt:42-58`, `ui/navigation/AppNav.kt:352-360`, `ui/workout/RestNotificationGate.kt:29,77` | "Onboarding never asks for notification permission" is not what a first-run user meets: after the save-posture choice, on the first non-Settings tab, the first-open walk shows its own dialog and then launches `POST_NOTIFICATIONS` (API 33+), exact-alarm and battery steps; SETUP's "the first time you open an active workout" describes only the in-workout re-ask. B9a owns the walk; this row is the doc claim. | The privacy page says first-run never asks about notifications; the app asks on first run, politely, with a "Not now". | `LaunchPermissionsTest.kt` pins `walkMayShow`/`nextStep`; read the doc lines |
| PV-2 (B14) | P3 | validation gap | C | FND-048 | `docs/COMMERCIAL_BOUNDARY.md:20-21`, `ADR-004:32-33` vs `tools/check-commercial-boundary.py:20-48`, `data/backup/DriveHttp.kt:5-6`, `NetworkChecker.kt:4-5`, `DriveAuthClient.kt:5` | The doc says the checker fails on "billing, ads, analytics, or account SDKs" and the ADR says it forbids "billing, account, and network dependencies" from the core; the script bans billing/ads/analytics vendors only (no supabase, ktor, gms.auth or `java.net` pattern), and `data/backup`, a listed core package, legitimately imports `java.net`, `android.net` and gms auth — so the ADR's literal rule is unenforceable as written and the doc overstates the guard. The core is not gated anywhere (0 findings; `domain`/`workout`/`activity`/`insights`/`data/repository` import none of them). | The promise "the free part can never depend on an account or the network" is kept in practice, but the tool that claims to enforce it checks something narrower. | Read `:29-48`; the import scan in §6 |
| PV-3 (B14) | P3 | defect | C | — | `docs/DATA_SAFETY.md:38` vs `SETUP.md:389`, `README.md:95-100`, `docs/architecture/drive-auth.md:9-11`, ledger `play-services-auth` 21.6.0 | "No Google Sign-In in v1" is true of Temper Account (email + password) but the APK ships `play-services-auth` and Settings offers "Sign in with Google" for Drive via `AuthorizationClient`; a Play reviewer reading the form against the APK sees a contradiction. | The store form says "no Google sign-in" while the app has a Google sign-in button (for backup). | Read the lines |
| BR-10 (B14) | P3 | validation gap | C (RFN status R) | — | `app/src/main/res/font/{inter_regular,inter_medium,inter_semibold,space_grotesk_medium,space_grotesk_bold}.ttf` name tables; `tools/build-fonts.py:22-26,83,90-98`; no `OFL*` file under the repo (`find . -iname '*OFL*'` → none) | The five bundled faces are subset instances of Google Fonts' OFL `inter/` and `spacegrotesk/` sources (`build-fonts.py:22-26`); no licence text ships beside them or under `docs/`. Each TTF does keep name ID 0 (copyright), 13 ("licensed under the SIL Open Font License, Version 1.1 … available … at https://openfontlicense.org" / `scripts.sil.org/OFL`) and 14 (URL) because the subsetter passes `--name-IDs=*` (`:96`) — the machine-readable form OFL §2 allows — but that is the notice with a link, not the licence itself, and `updateFontNames=False` (`:83`) leaves the Space Grotesk instances named "Space Grotesk Light" (cosmetic). Whether either family declares a Reserved Font Name, which would constrain a modified version's name, needs the upstream `OFL.txt` (R). (B12 hand-off.) | The two typefaces are free to bundle, but the licence they require to travel with them is only a one-line pointer inside the font files, not a readable text in the project. | Dump name IDs 0/13/14 of each TTF (a 20-line Python parser did it here without fontTools); `find . -iname '*OFL*'` |
| DC-9 (B14) | P3 | defect | C (diff per B8) | — | `docs/architecture/ADR-029-coach-engine.md:30-32`, `domain/coach/EvidenceCatalog.kt:6`, `docs/coach/evidence-seed.json`, `docs/ROADMAP.md:200` | ADR-029 §2 names `docs/coach/evidence-seed.json` as the "human mirror" of `EvidenceCatalog`, and the KDoc says "keep in sync", but no code, test or tool reads the file (`grep -rn evidence-seed` over `app/`, `tools/`, `*.kts` → only that KDoc); B8 found it differs from `CoachPolicyEvidence` on 10 of 17 reason codes. An accepted ADR (authority layer 2) promises a mirror that nothing holds true. (B8 hand-off.) | The "human-readable copy" of the coach's evidence list has drifted from the real one, because nothing checks it — a reader trusting it would be misled on most entries. | `grep -rn evidence-seed app tools` → one comment; B8's diff of the JSON against `CoachPolicyEvidence.kt`; a golden-file test in the style of `docs/artifacts/*` (`render-artifacts.sh`) would pin it |


### Noticed, outside this pass's scope

Noticed, outside my scope

- `docs/PRIVACY.md:26-27` "tokens in app-private storage" is true but under-stated: S-9 says plaintext SharedPreferences → B2 (packet S4 "encrypted tokens").
- `MainActivity.kt:62`, `RestTimerService.kt:75,99`, `AppViewModel.kt:10` also cast the Application (CURRENT_STRUCTURE known debt :112-117) → the architecture pass, with S4.
- `ui/workout/RestNotificationGate.kt:29,77` asks `POST_NOTIFICATIONS` a second time in-workout → B9a (behaviour), see PV-1.
- After BR-1's rule lands, whether R8 leaves `gotrue-kt`/Ktor usable in a release build with a Supabase URL is B2's static read; today release builds carry no URL (`release.yml` never writes `supabase.properties`).
- Coverage readings sit well above three floors (timer 81.7 vs 73, util 82.1 vs 76, ui.workout 88.2 vs 84) → B12 may ratchet.
- `tools/checker-baselines.toml` `stock_icons = 9`, `raw_dp = 14`, `motion = 11` are the remaining design-token debt → the design-system pass.
