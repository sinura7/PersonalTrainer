# Merged findings (auto-extracted)

## Report sizes
| Pass | lines | §2 rows | §3 rows |
|---|---|---|---|
| B1 | 111 | 4 | 6 |
| B2 | 159 | 4 | 7 |
| B3 | 427 | 4 | 5 |
| B5 | 112 | 4 | 7 |
| B6 | 109 | 3 | 9 |
| B7 | 243 | 3 | 9 |
| B8 | 277 | 2 | 8 |
| B11 | 121 | 2 | 7 |
| B12 | 105 | 4 | 12 |
| B14 | 111 | 4 | 18 |
| B4a | 181 | 0 | 14 |
| B4b | 124 | 2 | 14 |
| B9a | 132 | 3 | 13 |
| B9b | 109 | 3 | 9 |
| B10a | 116 | 2 | 14 |
| B10b | 121 | 5 | 15 |
| B10c | 106 | 5 | 9 |
| B13a | 152 | 2 | 11 |
| B13b | 110 | 4 | 7 |

Total §2 (P1/P2): 60 · §3 (P3): 194 · prior rows: 207

## §2 rows (P1/P2)

| Pass | ID | Sev | Type | C/R | Prior | Files | Claim | Plain | Probe |
|---|---|---|---|---|---|---|---|---|---|
| B1 | S-11 | B1 | P2 | defect | C (logic) / R (timing) | S-1 follow-up (a), ADR-031 §3 bullet 4 | `P/data/sync/SyncEngine.kt:178-181,230-232,247-249,264-266,345-347`; `SyncDao.kt:29-32`; `SyncOutboxWriter.kt:404` | A child row skipped because its parent is not on the phone yet still returns its `updatedAtMs`, which advances the table's watermark past it, so once the parent does arrive the child is below the cursor and is never pulled; parent-before-child arrival is not guaranteed because same-millisecond outbox rows are ordered by random UUID (`peekOutbox`) and the push can stop between them. | A workout can reach your second phone with its sets missing, for good, if the sets arrived at the server a moment before the workout did. | JUnit over `FakeSyncRemote`: seed a `RemoteActivityBlockRow` (updatedAt 300) with no session, `engine.run` (skipped, cursor 300); then seed its session and run again; assert `activityDao.getBlock(id) != null` — fails today. |
| B1 | S-12 | B1 | P2 | defect | C | S-1 follow-up (a), ADR-031 §3 bullet 4 | `P/data/sync/SyncEngine.kt:169-189,200-217`; `SyncJson.kt:6-10`; `SyncRemoteRows.kt:19-41,297-318` | `pullTable` has no per-row isolation and `syncGson` bypasses Kotlin constructors, so a server row with a missing or null column that the row class declares non-null (e.g. `notes`) parses fine, then throws `NullPointerException` in `toEntity()` at the constructor check; the throw aborts the whole pass, the table's cursor is not saved, and every later pass fails at the same row, stalling every table after it. | One odd row on the server can freeze downloads for every table behind it, on every attempt, and the Settings message just says sync could not finish. | JUnit: `remote.seed(ACTIVITY_SESSION, "{...}" without `notes`)` plus a `SCHEDULE_RULE`; assert `engine.run(...).isFailure`, the rule is not on the phone, `getCursor("activity_sessions") == null`, and a second run fails identically. |
| B1 | S-13 | B1 | P2 | defect | C (client) / R (server merge) | new; ADR-031 §4 | `P/data/sync/SyncJson.kt:6`; `SyncRemoteRows.kt:126-140`; `SupabaseRestClient.kt:22`; `P/data/repository/RoutineRepository.kt:159-167` | Uploads omit every null field (no `serializeNulls()`), and the server write is `resolution=merge-duplicates`, which updates only the columns present, so clearing a nullable field never reaches the server: a routine lift switched from a hold back to reps (`targetSeconds = null`) stays a hold on every other phone, a cleared target weight keeps its old value, a rule switched from a fixed zone to the device zone keeps `fixed_zone_id`. | Removing a value on one phone never removes it on the other; the two phones quietly disagree about the routine. | JUnit: `encodeSync(item.toRemote("u", 1L))` with `targetSeconds = null`; assert the JSON contains `"target_seconds"` — fails today. Server half needs a live check (S2a). |
| B1 | S-14 | B1 | P2 | defect | C | S-9 (twin, upload side) | `P/data/sync/SyncAuthoring.kt:54-69,98-102,126-139,153-157,174-184`; `SyncCoordinator.kt:41-50`; `SyncDao.kt:49-53` | Sign-out keeps local rows (ADR-004) and clears only the outbox; the next sign-in's `bootstrapLocalSnapshot` and every whole-table hook queue all rows on the phone under the new user id, and the pull starts from the previous account's cursors, so a second account on the same phone uploads the first account's lifts, weigh-ins, goals, prefs, schedule and routines into its own cloud and misses its own older rows. | If someone else signs in on your phone, your training data is copied into their account, and their own data never fully downloads. | JUnit with `FakeAccountAuth`: sign in as A, pull a custom lift from `FakeSyncRemote`, `abandonOutboxOnSignOut`, `setSession(B)`, `bootstrapLocalSnapshot()`; assert `peekOutbox` has A's lift with `user_id = B` (passes today = defect) and `getCursor` still holds A's watermark. |
| B2 | PV-3 | B2 | P2 | defect | C | S-3 | `P/data/auth/SupabaseAccountAuth.kt:32-45`, `P/ui/settings/AccountCoordinator.kt:47-68`, `P/ui/settings/AccountSection.kt:92-125`, `P/data/sync/SyncAuthoring.kt:37`, `PersonalTrainerApp.kt:148-150` | `session` maps `SessionStatus.NetworkError` to null, but the library enters that state only when a stored session's refresh fails for a network reason and keeps retrying every 10 s (bytecode `AuthImpl.tryImportingSession`, `AuthConfigDefaults.retryDelay = 10`), so an offline phone whose access token has expired (Supabase default 1 h) reads as signed out: Settings → Account shows "Open your account", `signedIn` is false, and edits stop queueing while the refresh token is intact. | After about an hour without signal, the app pretends you are signed out; the Account page invites you to sign in again and changes you make are not put in the upload queue, even though your login is still good and comes back by itself when the signal returns. | JUnit: extract the `when(status)` to a pure function and assert `NetworkError` yields a "signed in, offline" state, not null (on trunk it is null). Phone: sign in on Temper Debug, airplane mode for > 60 min, open Settings → Account. |
| B2 | S-11 | B2 | P2 | defect | C | S-5 | `P/data/sync/SupabaseRestClient.kt:14-30, 32-50, 53-67`, `P/data/sync/SupabaseSyncRemote.kt:11-32`, `P/data/sync/SyncWorker.kt:13-22`, `P/data/backup/DriveHttp.kt:44-45, 62-70, 90` | Every PostgREST call is a blocking `HttpURLConnection` with no timeouts, no `disconnect()`, and an unbounded `readText()` of the body, run inside `suspend` functions with no `withContext(Dispatchers.IO)`; the caller is `CoroutineWorker.doWork` on `Dispatchers.Default`, so a stalled socket holds a CPU-pool thread for the worker's whole 10-minute budget and cancellation cannot interrupt it. `DriveHttp` already does all three things right. S4 (packet 28, `docs/FRONTEND_REDESIGN.md:94`) is scheduled after S1 unpauses (packet 14, `:84`). | When sync resumes, one request that never gets an answer can jam the sync job and tie up one of the app's few background threads for ten minutes; the Drive code already has a stopwatch and a "hang up" for exactly this, the Account code does not. Fix belongs in S1, not S4. | JUnit: a local `ServerSocket` that accepts and never replies; `SupabaseRestClient("http://127.0.0.1:<port>", "k").getUpdatedSince(...)` must throw within a bounded time (on trunk it hangs; guard the test with a timeout rule). |
| B2 | PV-2 | B2 | P2 | defect | C (mechanism) / R (header-bearing trigger) | none | `P/data/auth/SupabaseAccountAuth.kt:102-112, 115-147`, `P/domain/AccountAuthCopy.kt:54-63`, `P/ui/settings/AccountSection.kt:127-134`, `P/logging/AppLog.kt:47-59, 61-67`, `P/data/sync/SupabaseRestClient.kt:28,47,65,80`, `P/data/sync/SyncEngine.kt:45,53` | `toAccountAuthError` falls through to `Message(message)` for every supabase-kt `RestException`, and `runAuthAction` logs the same exception; `AppLog` redacts only its free-text message and prints `Log.getStackTraceString(error)` verbatim (`:66`). A `RestException` built from a response (bytecode `RestException(String, HttpResponse, String)`: "…\n URL: … \n Headers: [request headers] …") therefore puts the request URL and the `apikey` and `Authorization` header values on the Settings screen and in logcat. The app's own `IllegalStateException("Supabase … failed (code): <body>")` puts the raw PostgREST error body in logcat the same way. | An error message from the server is shown to you and written to the phone's log exactly as the library built it; for one kind of server reply that text includes the login headers, which ADR-031 says must never enter a log. The diagnostics bundle is safe (it keeps only the error's class name). | JUnit (Robolectric, default sink): `AppLog.e("PT/T", "x", IllegalStateException("Headers: [Authorization=[Bearer ${DiagnosticCanaries.TOKEN}]]"))` → `ShadowLog.getLogs()` contains the canary; and `that exception.toAccountAuthError(true)` is `AccountAuthError.Message` containing the canary. R: which live GoTrue replies lack `error_code` (the headers path) — see notes. |
| B2 | S-12 | B2 | P2 | defect (gated) | C | S-7, S-8 | `P/data/sync/SupabaseRestClient.kt:53-67`, `P/data/auth/SupabaseAccountAuth.kt:79-100`, `P/domain/AccountSyncGate.kt:26` | `deleteAllRows` sends `DELETE /rest/v1/<table>?user_id=not.is.null` for all 17 tables — a filter that matches every user's rows — so correctness rests entirely on RLS, which S-8 says is absent from the repository for 9 tables; the only caller is the gated delete (`AccountCoordinator.kt:133`), and nothing else references it. | The "delete my account" code asks the server to delete every row that has an owner, not just yours; only the server's row-level security stands between that and other people's data. It cannot run today because deletion is switched off, but it is one flag away. | Read `SupabaseRestClient.kt:54`. JUnit: a local server recording the request line; expect `user_id=eq.<uid>` (on trunk: `not.is.null`). S2a's server-side delete function should replace it, and this method should go. |
| B3 | S-12 | B3 | P2 | defect | C (client half), R (server effect) | — | `data/sync/SyncJson.kt:6`, `Rows:35-36,103-104,134,136-137,164,590-591,630`, `Rest:22` | `syncGson` is `GsonBuilder().create()` with no `serializeNulls()`, so every null field is left out of the payload; a `merge-duplicates` upsert updates only the keys present, so a value cleared on the phone (a rule's `routine_id` or `fixed_zone_id`, a routine lift's `target_weight_kg`, display prefs' `bodyweight_check_in_weekday`, an occurrence's `completed_activity_id`, a session's `occurrence_id`) never becomes NULL on the server, and `deleted_at_ms` can never be cleared, so a row re-created under the same key (a weigh-in on a day that was deleted) stays a tombstone for every other phone. | When you clear a setting or unlink something on one phone, the cloud keeps the old value and your other phone never sees the change; a weigh-in re-entered on a deleted day vanishes on the other phone. | JUnit: `encodeSync(RemoteDisplayPrefsRow(userId = "u", weightUnit = "KG", clockFormat = "H24", bodyweightCheckInWeekday = null, updatedAtMs = 1))` — assert the JSON contains `"bodyweight_check_in_weekday"`; fails today. Server half (branch DB, owner-approved): upsert `{..., "target_weight_kg": 60}` then the same row without that key, `select target_weight_kg` — expect 60 still there. |
| B3 | S-11 | B3 | P2 | defect | C (DDL + client), R (error path on the live project) | S-8 | `packet-2:7`, `packet-3:7`, `Rest:22`, `data/sync/SyncCoordinator.kt:62-63`, `data/sync/SyncAuthoring.kt:174-184`, `Engine:109-111`, `data/repository/ExerciseRepository.kt:156`, `data/repository/GoalRepository.kt:44` | `custom_exercises.id` and `measurable_goals.id` are global primary keys, so when the same client id already exists under another `user_id` the upsert becomes `ON CONFLICT (id) DO UPDATE` on a row the UPDATE policy's `USING` refuses, which Postgres raises as an error (42501, PostgREST 403) rather than skipping; `bootstrapLocalSnapshot()` runs at every sign-in and re-queues every local lift and goal under the current account, so signing into a second account on a phone whose lifts were already uploaded, or restoring a Drive backup and signing into a different account, refuses that row on every pass and `throw pushError` stops the queue behind it. | If the same lift or goal ever ends up under two accounts (a second test account, or a restored backup), the cloud refuses the upload every time and nothing behind it in the queue gets sent. | Read `packet-2:7` (global `id`) against `packet-2:53` (composite key on the child table) and PostgreSQL CREATE POLICY: an `ON CONFLICT DO UPDATE` whose existing row fails `USING` errors. Live `R`: on a branch DB as user B, `insert into public.custom_exercises (...) values (...) on conflict (id) do update set name = excluded.name` against user A's row, expect `42501`. Client half: JUnit with `FakeSyncRemote.persistUpsertFailure = IllegalStateException("Supabase upsert failed (403)")` shows the outbox row stays and every pass fails. |
| B3 | S-13 | B3 | P2 | defect | C | S-2 | `Rest:33-34`, `Engine:173-188`, `packet-2`, `packet-3` (no trigger) | The server stores whatever `updated_at_ms` the client sends and the pull cursor is the largest value applied, with no clamp on the client and no trigger on the server, so one row carrying a future stamp (a phone with a wrong clock, or any bad value) advances every other phone's `sync_table_cursors` for that table past all later edits; the pass keeps reporting success while that table never pulls again, until a cursor reset. | If one phone's clock is wrong, it can quietly switch off downloads of that kind of data on every other phone, and nothing tells you. | JUnit over `FakeSyncRemote`: seed one rule with `updatedAtMs = Long.MAX_VALUE - 1`, run, then seed a normal rule at `now` and run again; assert the second rule is never applied. Server half is the S2a trigger (S-16). |
| B3 | S-14 | B3 | P2 | target gap (latent, gated) | C (client), R (which tables lack RLS) | S-8 | `Rest:53-67`, `data/auth/SupabaseAccountAuth.kt:79-100`, `domain/AccountSyncGate.kt:26` | `deleteAllRows` sends `DELETE /rest/v1/<table>?user_id=not.is.null`, an always-true filter, and relies on the DELETE policy to scope it; on any of the 9 tables that exists on the server without RLS, one signed-in user's account deletion would remove every user's rows in that table. `IN_APP_DELETE_AVAILABLE = false` holds it off today; S2b brings the button back. | The "delete my account" code asks the server to delete everything it can see, and only the server's per-user locks stop that being everyone's data; those locks are unconfirmed on nine of the seventeen tables. | Read-only: `select tablename, rowsecurity from pg_tables where schemaname = 'public'`; any `false` on a synced table is the exposure. Fix: the server-side delete function (S-17) and remove `deleteChildTablesFirst`. |
| B5 | BK-1 | B5 | P2 | defect | C | — | `P/data/repository/BackupService.kt:77-80`, `P/data/repository/prefs/BackupPrefsStore.kt:185-190`, `P/ui/settings/BackupCoordinator.kt:344-350`, `docs/PRIVACY.md` "Backup and Drive" ¶2, `docs/architecture/backup-threat-model.md` §4.3.1 | Drive sign-out calls `clearDriveSession()`, which removes only `drive_account_email` and `drive_folder_id`; `auto_backup_enabled` and the Keystore-sealed `auto_backup_secret` stay, although PRIVACY.md and the threat model both promise that signing out of Drive deletes the sealed copy and turns the toggle off. | Signing out of Google Drive is supposed to also forget your stored backup password and switch automatic backup off; it does neither, so the password stays on the phone and every finished workout shows a "sign in to Drive again" note instead. | JUnit on a `BackupPrefsStore` over a temp DataStore: `armAutoBackup("x")`, `clearDriveSession()`, assert `autoBackupSettings().sealedPassphrase == null` — fails on trunk. Or read `BackupPrefsStore.kt:185-190`. |
| B5 | BK-2 | B5 | P2 | target gap | C | S-9 / ADR-031 §3 | `P/data/repository/LocalBackupRepository.kt:352-580`, `P/AppContainer.kt:341-348`, `P/data/sync/SyncAuthoring.kt:54-59,126-133`, `P/data/sync/SyncEngine.kt:94-110`, `P/data/local/entity/ExerciseEntity.kt:35`, `P/data/repository/ActivityBackupIo.kt:31`, `P/data/sync/SyncOutboxWriter.kt:186-260`, `P/data/sync/SyncRemoteRows.kt:427-437,504-523` | Restore leaves the Temper Account lane inconsistent in four ways S1's scope line does not name: the wipe clears no sync table, so pre-restore outbox rows (payloads stored as JSON) would upsert the old rows to the server; `onPlanDataRestored` re-queues routines, templates, custom lifts, weigh-ins, goals and prefs but never activity sessions or schedule rules/occurrences, and writes no tombstone for anything wiped; restored custom lifts and templates get `updatedAtMs = 0` and are queued with that stamp, so a second phone's cursor never pulls them; and neither side excludes the other. | When you restore a backup, the app's cloud-sync bookkeeping is not reset to match it: old queued uploads survive, restored cardio sessions and your planned week are never queued, and restored custom lifts are stamped "changed at time zero" so your other phone would never receive them. Nothing happens today because sync is paused; it is what S1 must cover before it is unpaused. | Room JUnit (pattern: `RestoreRecoveryTest`): insert a custom lift with `updatedAtMs = 5` and one `sync_outbox` row, export, `replaceRoom(document)`, assert `exercises.updatedAtMs == 0` and the outbox row count unchanged. |
| B5 | BK-3 | B5 | P2 | defect | C | Foundation item 2 / T6, ADR-009 §11 | `P/data/backup/AuthoredInventory.kt:12-30,54-73,75-83`, `P/data/repository/LocalBackupRepository.kt:295-325`, `P/data/backup/BackupValidator.kt:400-404` | The authored inventory counts sessions, sets, routines, custom lifts, legacy schedule slots, weigh-ins, blocks and activities but not measurable goals, activity templates or v4 planner rules/occurrences, so a phone whose authored data is only those reads as empty and a catalog-only file wipes them without the refusal, a backup holding only those is refused as "no data", and the confirm dialog's "This phone: …" line omits them. | The safety check that stops an empty backup from wiping your data does not know about goals, saved cardio templates or your planned week, so a phone holding only those can be wiped by a catalog-only file without the warning, and the confirm box's counts leave them out. | JUnit with `FakeAppDependencies`: seed one `measurable_goals` row and one `schedule_rules` row and nothing else, call `prepareRestore(catalogOnlyJson(), …)`; expect `BackupException(EMPTY_INCOMING_REFUSED)`; on trunk it returns a plan. |
| B5 | BK-4 | B5 | P2 | defect | C | — | `P/ui/summary/WorkoutSummaryViewModel.kt:180-233`, `P/ui/summary/WorkoutSummaryScreen.kt:109-110`, `P/ui/navigation/AppNav.kt:662-668`, `P/data/repository/BackupService.kt:82-120` | Automatic backup after a workout runs on `viewModelScope` (181); Done or Back pops the summary to Home (AppNav 666-668), which clears the ViewModel and cancels the job at the next suspension in `createBackup` (authorize, snapshot, `setDriveFolderId`, `setLastBackup`, verify), the `CancellationException` branch (219-221) deliberately shows nothing, and `setAutoBackupLastSession` (216) is skipped, so the session is neither reported nor retried. | The after-workout Drive copy only runs while the summary screen is open; tapping Done a few seconds after finishing quietly cancels it, and nothing tells you or tries again later. | Robolectric JUnit (`FakeAppDependencies`, armed auto-backup, a `DriveHttp` fake that suspends): call `maybeAutoBackup`, then cancel the ViewModel before the fake completes; assert no `lastBackedUpSessionId` and no backup stamp. No test exists today (`T/ui/summary/WorkoutSummaryViewModelTest.kt` has no auto-backup test). |
| B6 | RT-1 | B6 | P2 | defect | C (colours, layout, style) / R (what the shade paints) | — | `app/src/main/res/layout/notification_rest_running.xml:17,26`, `notification_rest_running_big.xml:17,26`, `app/src/main/res/values/colors.xml:7-8`, `app/src/main/res/values-night/colors.xml` (no override), `P/timer/RestTimerNotifications.kt:163-166` | The running card's RemoteViews hard-code near-white text (`#F2F5F7`, kicker `#9BA7AE`) with no `values-night` twin and no background of their own, under `DecoratedCustomViewStyle`, whose background is the system's light or dark notification surface — on a phone in light mode the countdown is white on white. | On a phone set to the light system theme, the rest countdown in the shade and on the lock screen is unreadable; only the app's own screens are dark. | JUnit (Robolectric, `@Config(qualifiers="notnight")`): inflate `RemoteViews(pkg, R.layout.notification_rest_running).apply(context, FrameLayout(context))`, take `R.id.rest_chrono`'s `currentTextColor`, assert `ColorUtils.calculateContrast(it, Color.WHITE) >= 4.5` — fails today (≈1.07:1). Phone: Display → Light theme, start a rest, pull the shade, lock the phone. |
| B6 | RT-2 | B6 | P2 | defect | C | — | `P/ui/workout/RestNotificationGate.kt:29-39,47-48,55-65,67-85`, `P/ui/workout/ActiveWorkoutScreen.kt:221-222`, `P/ui/workout/RestTimerScreen.kt:84`, `P/ui/permissions/LaunchPermissionsHost.kt:34-40` | `rememberRestNotificationsEnabled` promises to ask "once" (:29) but its `decided` flag is only `rememberSaveable` (:48), so on API 33+ a lifter who tapped Not now (or was already asked by the first-open walk, which is the only persisted "asked" flag, `LAUNCH_PERMISSIONS_ASKED`) gets the modal "why" dialog again on every fresh `ActiveWorkoutScreen` and `RestTimerScreen` — each workout entry, each rest page, each process — for as long as the permission stays denied. | If you have said no to notifications, the app puts the same "please allow notifications" box in front of you every time you open a workout or the rest page, with the recovery row already there as the way back. | Robolectric compose test on SDK 33 with `shadowOf(app).denyPermissions(POST_NOTIFICATIONS)`: compose `RestTimerScreen`, click `RestNotificationCopy.NOT_NOW`, dispose, compose again → `onNodeWithText(RestNotificationCopy.SENTENCE)` is displayed a second time. Phone: deny notifications, open a workout, Not now, Back, open the workout again. |
| B6 | RT-3 | B6 | P2 | defect (contradicts ADR-012 §4, §13 and PRIVACY.md:117) | C | Foundation item 1 (the fix over-reached) | `P/domain/LaunchPermissions.kt:4-8,49-59`, `P/ui/permissions/LaunchPermissionsHost.kt:34-40,75-80,98-103,125-129`, `P/ui/navigation/AppNav.kt:353-365`, `docs/architecture/ADR-012-rest-and-reminders.md` decisions 4 and 13, `docs/PRIVACY.md:112-117`, `docs/architecture/ADR-028-...md:26-37` | The first-open walk asks for `SCHEDULE_EXACT_ALARM` special access (and POST_NOTIFICATIONS, and unrestricted battery) as soon as the save-posture chooser is answered and a non-Settings tab is shown, before any rest has been used; ADR-012 §4 says special access is requested "only after rest is used or configured, never during onboarding", §13 the same for reminders, and PRIVACY.md:117 tells the user "Onboarding never asks for notification permission"; ADR-028 §2 mentions "the permission walk" as part of first launch without amending ADR-012, and there is no debug-build gate on the walk. | A brand-new user is walked through three permission requests right after choosing how to save, before ever starting a rest — which the app's own rules and privacy page say it will not do. Either the rule or the walk has to change. | Read `LaunchPermissions.nextStep:49-59` (EXACT_ALARM whenever API ≥ 31 and not granted) and `AppNav.kt:353-365` (shown once `postureChosen`). Phone (API 31+, fresh install): choose "continue on this phone", land on Home → "Allow exact alarms" dialog appears with no rest run. |
| B7 | RM-1 | B7 | P2 | defect | C | — | `P/update/DebugApkInstaller.kt:55-61,68-94`, `P/MainActivity.kt:33-51,109-117`, `P/update/DebugUpdateMonitor.kt:111-118` | The `PackageInstaller` session's status `PendingIntent` targets `MainActivity` (`:82-87`), but `MainActivity` never reads `PackageInstaller.EXTRA_STATUS` or `EXTRA_INTENT`, so the `STATUS_PENDING_USER_ACTION` reply Android sends every non-privileged installer is dropped, the system install sheet is never launched, and the `ACTION_VIEW` fallback (`:96-108`) runs only when `commitSession` throws, which it does not. | Tapping "Update" on the Temper Debug banner downloads the file, says "Android will ask you to install this update", pops the app to the front, then the banner goes straight back to "Update" — Android never asks and the phone stays on the old build. Obtainium still works. | `grep -rn "EXTRA_STATUS\ |
| B7 | S-11 | B7 | P2 | defect | C | S-5, 22 Sep System row | `P/data/sync/SyncWorker.kt:13-22`, `P/data/auth/SupabaseAccountAuth.kt:32-45`, `P/data/sync/WorkManagerSyncScheduler.kt:16-27`, `P/PersonalTrainerApp.kt:22`, `app/src/main/AndroidManifest.xml:19-101` | `SyncWorker` returns `success()` when `session.first()` is null (`:16`), and `SupabaseAccountAuth` maps `SessionStatus.LoadingFromStorage` to null (`:41`), so a pass WorkManager starts in a fresh process — before the stored session is read — is dropped as if it had run; a real failure returns `retry()` with no attempt cap and default backoff (`:18-21`) under `APPEND_OR_REPLACE`, so every later `requestSync` is appended behind the failing head; and there is no `WorkerFactory`, so the worker casts the Application (`:14`). | Once sync is switched back on: an upload that WorkManager wakes the app to run can quietly do nothing, and one upload that keeps failing is retried forever with every later upload waiting behind it. Nothing happens today because sync is paused. | Dormant while `SYNC_PAUSED`. `work-testing` is not in `gradle/libs.versions.toml` (only `work = "2.10.1"` :22 and `work-runtime-ktx` :62), so `WorkManagerTestInitHelper` is R. JVM probe: extract the decision (`session` loading/absent/present, `configured`, `paused`, `runAttemptCount`) into a pure function and pin "loading → retry, not success" and "attempt ≥ cap → failure". |
| B7 | DB-1 | B7 (hand-off from B10a) | P2 | defect | C (UI effect R) | — | `P/data/repository/ExerciseRepository.kt:210-230`, `P/data/local/entity/ActivityEntities.kt:51,74`, `P/domain/ActivityInsight.kt:34,55`, `P/ui/library/ExerciseLibraryViewModel.kt:336-360`, `P/data/local/entity/RoutineExerciseEntity.kt:22`, `SessionExerciseEntity.kt:22`, `SetLogEntity.kt:21` | `usageFor` counts routine lifts, set logs and session exercises only, and `activity_blocks.exerciseId` has no foreign key, so `deleteCustom` lets a custom lift that only backdated activity blocks reference be deleted and leaves those blocks pointing at nothing; the same function is check-then-delete outside the `writeExercise` transaction with no catch, queues the sync tombstone (`:222`) before the row is deleted (`:223`), and `confirmDelete` has no `try` (its neighbours at `:263,:316,:403` do), so a RESTRICT violation from the three constrained tables would surface as an uncaught exception in `viewModelScope`. | Library will let you delete a custom lift that you have only logged inside activity sessions; those entries then point at a lift that no longer exists, and the Body insight that names it (`ActivityInsight`) has nothing to name (what it draws then is R). The crash path needs a set to be logged for that lift in the same instant as the delete, which one person cannot do, so it is theoretical. | JUnit on `ExerciseRepositoryTest`: seed a custom lift, an activity session with one block whose `exerciseId` is that lift, call `deleteCustom`, assert `InUse` — today it returns `Deleted` and the block row survives with a dangling id. Then: add `countBlocksForExercise` to `usageFor`, run the check and delete inside `writeExercise`, and queue the tombstone after the delete inside that transaction. |
| B8 | DM-1 | B8 | P2 | defect | C | — | `P/ui/workout/ActiveWorkoutViewModel.kt:806,825,1631,1880`, `P/data/repository/WorkoutRepository.kt:654,736,817`, `P/data/local/dao/WorkoutDao.kt:96–106,299–302`, `P/data/repository/WorkoutRepository.kt:1078–1093`, `P/domain/SetMicroRec.kt:204–209,237–298,313–326,336–357`, `P/domain/ProgressionCopy.kt:32`, `P/domain/SetCopy.kt:41–43`, `P/ui/workout/ActiveWorkoutScreen.kt:744` | A hold lift (plank, wall sit, dead hang, every stretch — the catalog's `isHold` rows) is logged with `reps = 0`; the progression query does not filter those rows, so its "top set" is 0 reps and the coach, which only knows reps for bodyweight lifts, computes "Keep 0 reps." for the strip and, after the first hold, a `BW_HOLD` rec whose call line is "Hold 0 reps" (numbers `0 reps`); before any set it offers "1 rep" with **Apply**. The screen shows the Next-set coach with no hold gate. | For planks and stretches the coach talks in reps instead of seconds, and after your first plank it literally tells you to hold zero reps. | Pure JUnit: `SetMicroRecCalculator.suggest(inputs(loadType = BODYWEIGHT, targetReps = 1, workingLogged = 1, working = listOf(set(0.0, 0, rpe = 8)), hint = hint(suggested = 0.0, lastReps = 0, targetReps = 1)))` → `SetMicroRecCopy.callLine(...)` is `"Hold 0 reps"`; and `ProgressionCopy.stripReason(ProgressionCalculator.hint(..., lastWorkingReps = 0, targetReps = 1, displayStep = null, loadType = BODYWEIGHT, unit = KG), KG)` is `"Keep 0 reps."`. |
| B8 | DM-2 | B8 | P2 | target gap | C | FND-039 | `P/data/local/entity/WorkoutSessionEntity.kt:21–28`, `P/domain/Models.kt:142–148`, `P/domain/SessionSummary.kt:26–37`, `P/domain/HistoryEntries.kt:32`, `P/domain/MuscleLoadCalculator.kt:68,216,253–262`, `P/domain/DeloadSignal.kt:59`, `P/domain/ExerciseHistory.kt:135`, `P/domain/WeekDerivation.kt:284–285`, `P/domain/TrainingCalendar.kt:60`, `P/domain/ActivityInsight.kt:27–73` | A strength session (`workout_sessions`, the thing the owner logs on the floor) stores three instants and no zone, offset or local day; every day/week attribution in `domain` re-derives the day from the zone passed at read (`time.civilDate(ms, zoneId)`, `zoneId = time.defaultZoneId()` by default). ADR-011 §1–2 and §6 require the captured local date and forbid re-deriving from the device zone. Even an `ActivitySession`, which does carry the four-tuple, is flattened by `toInsightSession()` to `date = instantMillis` and then re-derived, so heat, deload, weekly tonnage and week derivation all use the reading device's zone. Only the calendar's activity lane and `SessionSummary` for activities use the captured day. | If you train in one time zone and open the app after flying to another, the workout can file under a different day, and the body-heat map and "this week" shift with it — which the app's own time rule says must never happen. | JUnit: a `WorkoutSession` with `date` = 2026-09-26T00:30 Europe/London → `toSummary(JvmTime, "Europe/London").localEpochDay` (26th) ≠ `toSummary(JvmTime, "America/Chicago").localEpochDay` (25th); ADR-011's review question ("log in London, open in Chicago — does it move? No") is answered yes for strength sessions. |
| B11 | UI-1 | B11 | P2 | defect | C | — | `ui/workout/RestNotificationGate.kt:41-62,74,81`, `ui/workout/RestTimerScreen.kt:84`, `Screen:221-222`, `ui/navigation/AppNav.kt:617-645` | `decided` and `showWhy` are `rememberSaveable` inside the composable, so the "Rest alerts" explanation dialog opens on every new instance of the floor and of the rest page (each `navigate` is a new back-stack entry) while `POST_NOTIFICATIONS` is not granted on Android 13+, and after two system denials its Continue launches nothing the phone will show. | On a newer Android phone, once you have said "Not now" to rest alerts, the app asks the same question again every time you open the rest screen and every time you come back to a workout, and eventually its Continue button does nothing. | Robolectric (project default SDK 35, `robolectric.properties`) with the permission ungranted: compose `RestTimerScreen(...)` in two separate `showFloor` compositions and assert a node with `RestNotificationCopy.TITLE` both times. Nothing pins the gate today: every floor render passes `restNotificationsEnabledOverride = true` (`FloorTestKit.kt:365`), and `RestPageSkipTest.kt:132` composes the real page with the dialog up and never asserts on it. |
| B11 | UI-2 | B11 | P2 | defect | C | — | `Screen:409-413`, `VM:202,210-211,1520-1523,2133-2136`, `ui/components/EndWorkoutDialog.kt:24-72` | The header's Finish is enabled while a saved set is being edited (`canFinish` ignores `editingSetId`), `EndWorkoutDialog` says nothing about the open edit, and `finishWorkout` passes `canChangeEntry()` and writes `finishedAt`, so the correction typed into the entry is dropped without a word; only the dock's commit is guarded (it reads Save changes). | If you are fixing a set you already logged and tap Finish at the top of the screen, the workout ends and your fix is thrown away silently. | `showWorkoutScreen` with one logged set: `vm.editSet(id)`, change the weight, tap `WorkoutTestTags.FINISH` then `EndWorkoutTags.SAVE`; assert the session is not finished, or that the dialog names the open edit. `DockCommitRenderTest.anOpenEditSavesBeforeTheWorkoutCanFinish` (:170-176) covers the dock only. |
| B12 | AX-15 | B12 (from B10b) | P2 | defect | C (semantics) / R (unusable) | ADR-026 "Accessibility" (roles, accessible alternatives); UX25-AC01 | `UI/components/SnapWheel.kt:42-91`; `UI/reminders/ReminderTimeWheel.kt:66-92`; `UI/reminders/ReminderPrefsSection.kt:229` | The reminder time is three `VerticalPager`s of plain `Text` with no role, no `stateDescription`, no `customActions` and no `selected`/value semantics (`SnapWheel.kt:67-89`); the wrapping Row only sets `contentDescription = spoken` (`ReminderTimeWheel.kt:71`) on a non-merging node, so TalkBack hears the current time once and then three scrollable lists of unlabelled numbers with nothing to press. There is no typed alternative, although `BodyweightWheel.kt:110, 132` already pairs its wheel with a `Role.Button` "Type" path into `NumberEntryDialog` | A screen-reader user cannot reliably set a reminder time: the hour, minute and AM/PM wheels do not say what they are, do not say their value, and cannot be typed into — the bodyweight wheel already solved this for onboarding | Robolectric: render `ReminderTimeWheel`; `onNodeWithTag(REMINDER_TIME_HOUR)` has no `Role`, no `StateDescription`, no `CustomActions`; `onAllNodesWithText("Type").assertCountEquals(0)`. Fix shape: `BodyweightWheel`'s pattern — a typed `NumberEntryDialog` (or the platform time picker) plus `stateDescription`/`customActions` (Next/Previous) per column |
| B12 | AX-14 | B12 (from B10a) | P2 | defect | C | ADR-026 "Accessibility" (no clipped essential values) | `UI/history/HistoryScreen.kt:407-458` | History's readout card sets `semantics(mergeDescendants = true) { contentDescription = "$title, N sessions" }` (`:409, :415`); on a merging node a `contentDescription` replaces the merged child text, so the four `MetricCluster`s (days, sets, min, PRs, `:434-457`) and the "Moved most" row (`:459-470`) are never read | The History totals a sighted user sees at a glance — days trained, sets, minutes, records, the lift you moved most — are silently dropped for a screen-reader user, who hears only "This month, 12 sessions" | Robolectric merged tree: `onNodeWithTag(HistoryTags.READOUT)` `contentDescription` lacks "days"/"sets"/"min"/"PRs". Fix shape: build `spoken` from all six values as `StatTile` does (`GymSurfaces.kt:294-306`), or drop the description and let the merged text speak |
| B12 | AX-1 | B12 | P2 | defect | C | ADR-026 "Accessibility" (roles; no repeated descriptions) | `UI/components/GymSurfaces.kt:456-473, 530-540`; callers `UI/history/HistoryScreen.kt:258, 535` | `SessionLogRow` is two separate clickables: the identity column (`:469-472`, `mergeDescendants`, `contentDescription = spoken` which already includes sets, work and minutes) and the metrics row (`:531-534`, `clickable(onClick)` with no role and no description, so it reads its own "3 sets, 1,240 kg, 42 min"); neither carries `Role.Button`, and the identity column has no height floor (title 20 + gap 4 + caption 16 = 40 dp at font 1.0 when a session has no stills) | A screen-reader user on History hears every workout twice, the second time as a bare list of numbers, and is never told either is a button; the top half of the row is also thinner than the 48 dp target the app promises | Robolectric: render `SessionLogRow(...)` and assert `onAllNodes(hasClickAction()).assertCountEquals(1)`, that node has `Role.Button`, and `assertHeightIsAtLeast(48.dp)`. `HistoryListThumbsTest` (the only test that mounts it) asserts none of this |
| B12 | AX-2 | B12 | P2 | defect | C | ADR-023 §2 (non-colour channel); 22 Sep week-strip row | `UI/routines/CustomWeekScreen.kt:295-345, 356` | The custom-week day strip's seven cells are `clickable { onSelect(day) }` with no role, no `selected` semantics and no spoken form; the selected state is `VoltDim` + Volt border + Volt text only (`:307-311, 321`), the FILLED mark is `Icons.Outlined.Check` with `contentDescription = null` (`:324-331`) and PREFERRED is a hairline dot (`:332-338`); each cell is `weight(1f)` in a 7-across row with 4 dp gaps (`:295-305`), so ≈ 43 dp wide at 360 dp | When building a custom week with a screen reader, the day buttons say only "Mo", "Tu"… — not which day is being edited, which days already have lifts, or which are the preferred training days; sighted users get all three by colour and a tick. The cells are also narrower than the 48 dp promise, like Home's week strip | Robolectric: render `WeekDayStrip` with `filled = {MON}`, `selected = TUE`; `onNodeWithText("Mo")` has no `SemanticsProperties.Selected`, no `Role`, and its merged text is "Mo" alone. The kicker at `:176` names the selected day and count, which softens the selection gap but not the filled/preferred one |
| B14 | BR-1 | B14 | P2 | defect | C | FND-029 | `app/proguard-rules.pro:11-12,28`, `gradle/libs.versions.toml:24,65`, `app/build.gradle.kts:148-158`, `.github/workflows/release.yml:131-132`, `app/build/outputs/mapping/release/missing_rules.txt:3` | `./gradlew assembleRelease` fails on `trunk` at `:app:minifyReleaseWithR8` ("Missing class org.slf4j.impl.StaticLoggerBinder … 3 other contexts") because Ktor 2.3.12 (pulled by `supabase-gotrue-kt` since commit `0c759e7`, 21 Sep) brings `slf4j-api` 1.7.36 and `proguard-rules.pro` has no `-dontwarn` for it; no release APK or `mapping.txt` is produced, so a `v*` tag fails at `release.yml:132`. | The signed gym-floor version of the app cannot be built at all right now: a five-day-old change added a library whose shrinker warning nobody has ever silenced, because nobody has built the release flavour since. Nothing already on a phone is affected; every drop the phone gets is the unshrunk debug build. One line fixes the build. | `./gradlew assembleRelease` → BUILD FAILED (lead's run, §7); `grep -c slf4j app/proguard-rules.pro` → 0; `cat app/build/outputs/mapping/release/missing_rules.txt` → `-dontwarn org.slf4j.impl.StaticLoggerBinder` |
| B14 | BR-2 | B14 | P2 | defect | C | — | `.github/workflows/release.yml:59-60` vs `debug-live.yml:58-66`, `ci.yml:46-51` | `release.yml`'s `android-actions/setup-android` step has no `packages: ''`, so it requests the retired `tools` package that `debug-live.yml:58-60` records as having aborted live 59; a `v*` tag would fail at "Set up Android SDK", before tests, lint or BR-1 are reached. | The release robot has a second, independent break in its very first step — the same one that stopped a debug drop weeks ago and was fixed in the other two robots but not this one. | Read the three `with:` blocks; or the owner runs `Release` via `workflow_dispatch` (artifact only, no publish) and reads the SDK step (R for the run itself) |
| B14 | DC-1 | B14 | P2 | defect | C | 22 Sep System 1 | `docs/DEVELOPMENT.md:488-493` vs `app/src/main/java/com/sinura/personaltrainer/data/local/FoundationGeneration.kt:17`, `docs/HANDOFF-NEXT.md:16-21` | The runbook's "Things that will bite you" says `TemperDatabase` is "version 4, frozen"; the code is v7 with migrations 1→7, and the JVM migration gate (ADR-032 §4, `TemperSchemaAssetsTest`) is not mentioned there at all — the one section a developer reads before a schema change is the one X1 left stale. | The "how to change the database safely" instructions still describe the database as it was two weeks ago and leave out the test that now guards it; someone following them could skip the step that catches a data-losing mistake. | `grep -n 'const val VERSION' …/FoundationGeneration.kt` → 7; `sed -n 488,489p docs/DEVELOPMENT.md` → "version 4, frozen" |
| B14 | BR-3 | B14 | P2 | validation gap | C | — | `tools/preflight.sh:250-252`, `tools/verify.sh:21-22`, `tools/run-domain-tests.sh:14-16`, `app/src/test/…/domain/CompletedTrainingParityTest.kt:4-8`, `domain/DraftStoreTest.kt:4-8`, `domain/UndoQueueTest.kt:3`, `data/backup/ProtectBackupTest.kt`, `docs/DEVELOPMENT.md:21,170-186` | The plain-JVM lane (`tools/run-domain-tests.sh`, which full `preflight.sh` runs whenever `build/test-jars` exists) cannot compile on `trunk`: four Robolectric/ViewModel tests sit in `test/domain` and `test/data/backup`, directories the lane compiles wholesale, and import `androidx.*`, `FakeAppDependencies`, `ui.theme.Motion` and `ProtectBackup` (`data/repository`). So `tools/verify.sh` (documented as "the single local verification command") fails at its first step, and DEVELOPMENT.md still calls the lane an every-commit gate. | One of the two documented self-check commands has been silently broken by tests filed in the wrong folder; the real gate (Gradle) is unaffected, but the docs promise a check that cannot pass. | `sh tools/preflight.sh` (full) → `FAILED: tests did not compile … preflight: FAIL — JVM tests` (lead's probe 7, `baseline/07-preflight-full.log`); `grep -n '^import androidx' app/src/test/java/com/sinura/personaltrainer/domain/CompletedTrainingParityTest.kt` |
| B4b | DB-1 | B4b | P2 | defect | C | — | `insights/TrainingInsightsSource.kt:112,163-190`; `data/repository/WorkoutRepository.kt:148-156,171-177,185-190`; `data/repository/ActivityRepository.kt:120-124` | The shared insights pipeline is collected in a scope with no `CoroutineExceptionHandler`, and 4 of its 12 upstreams (`observeSessionSummaries`, `observeFinishedSince`, `observeFinishedLastLogged`, `activityRepository.observeCompletedSummaries`) skip `observeHealth`, so a thrown Room read (disk full on a GROUP BY spill, a row too big for the cursor window, a corrupt page) escapes `shareIn`'s launched collector as an uncaught exception and ends the process instead of degrading to `DataHealth` as the other 8 upstreams do. | If the phone's storage hiccups while Home is reading your history, the app closes itself instead of showing the last numbers it had. The app already has a rule for this ("show what we had, say it is stale"); four of the twelve feeds into Home skip it. | JUnit: in `FakeAppDependencies` pass a `workoutDaoDecorator` whose `sessionSummaries()` throws `SQLiteException` after one good value; collect `trainingInsights.observeShared()` in a test scope with a recording `CoroutineExceptionHandler`; expect no unhandled exception and the last value held. Today the handler records the crash. |
| B4b | DB-2 | B4b | P2 | defect | C (absence) / R (on-device reach) | — | `data/repository/PreferencesRepository.kt:85-87,430-446`; `data/repository/prefs/SettingsStore.kt:46-53` | The `user_settings` DataStore is built with no `corruptionHandler`; `CorruptionException` is an `IOException`, so reads degrade to defaults through `safePreferences`, but every `edit {}` rethrows it forever, and `onboardingCompleteHealth` reads `dataStore.data` directly so the gate reports "Settings unavailable… Retry" — a Retry that cannot succeed because the file is never replaced. | If the small settings file is ever damaged, every setting you change fails to save from then on, and the app's front door can show "Settings unavailable" with a Retry that never works. The only way out today is clearing the app's storage, which also deletes the training history. | Robolectric: write bytes that are not a valid proto to `datastore/user_settings.preferences_pb`, construct `PreferencesRepository`, call `setWeightUnit(KG)` → throws today; expect it to succeed after a `ReplaceFileCorruptionHandler { emptyPreferences() }`. |
| B9a | L-3 | B9a | P2 | defect | C | — | `MainActivity.kt:74-93`, `ui/navigation/AppNav.kt:296-314`, `ui/saveposture/SavePostureHost.kt:25-30,43-53`, `ui/settings/SettingsViewModel.kt:198-210`, `data/repository/prefs/SettingsStore.kt:46-53,59-60`, `data/repository/prefs/SavePosturePrefsStore.kt:24-49`, `data/repository/PreferencesRepository.kt:85-87,430-443` | The onboarding gate reads `dataStore.data` raw and turns any `IOException` (DataStore's `CorruptionException` is one; no `corruptionHandler` is set) into `UNAVAILABLE`, showing "Settings unavailable… not a first install" with Retry — but `SavePostureHost` is composed outside that gate, reads the same store through `safePreferences`, which swallows the same exception into empty prefs (`chosen = false`), and so draws the first-launch chooser over the retry screen, blocking Retry from touch (`SavePostureChooser.kt:82`) and hiding it from TalkBack (`MainActivity.kt:72`); any choice then calls `store.data.edit` with no catch (`SavePosturePrefsStore.kt:30-37`) inside `viewModelScope`, which on the same unreadable file throws uncaught. | If the settings file on the phone is ever damaged, the app puts up a "try again" screen and immediately covers it with the "where should we save your training?" question; you cannot reach Try again, and answering the question crashes the app. | Robolectric: overwrite `user_settings.preferences_pb` with garbage in `FakeAppDependencies`, compose `PersonalTrainerNav` + `SavePostureHost`; assert `save-posture-chooser` is absent while `gate == UNAVAILABLE`. Today it appears. |
| B9a | UI-1 | B9a | P2 | defect | C | — | `ui/navigation/AppNav.kt:408-418,434-438`, `MainActivity.kt:41-51,109-117,126-130`, `ui/home/HomeViewModel.kt:257-278,409-462`, `ui/home/DailyAgendaCard.kt:91-101,184-188`, `data/repository/PlannerRepository.kt:491-499` | A reminder tap (Start or body) always runs `goToTab(Home)`, which pops whatever is pushed — the live workout floor, a routine editor, a cardio composer — and for a Start tap `consumeStartedDelivery` has already marked the delivery STARTED and cancelled the notification (which also cancels its scheduler) before `startOccurrence` can come back `Blocked` (another session live) or `Missing`; a body tap while a session is live is then consumed silently (`DailyAgendaCard.kt:93-96`), so the only visible effect is the ejection. | Tapping a workout reminder while you are already in a workout throws you out to Home, and the reminder is gone for good even if you then decide to keep the workout you were in. The workout itself is safe; the live bar takes you back. | Robolectric nav test: live session in `FakeAppDependencies`, current route `session/{id}`, then set `reviewOccurrenceId`; assert the route stays on the floor (today it becomes `home`). ViewModel test: `startOccurrence` → `Blocked` must not leave the delivery STARTED. |
| B9a | UI-12 | B9a | P2 | defect | C | raised by B4b | `ui/navigation/LiveSessionBarViewModel.kt:206-236,249-279`, `PendingOccurrence.kt:66-72,83-87,115-118`, `data/repository/PlannerRepository.kt:354-365`, `data/repository/PreferencesRepository.kt:176`, `workout/CompleteTraining.kt:34-45`, `workout/FinishWorkout.kt:46-65`, `workout/DiscardWorkout.kt:29-37`, `PersonalTrainerApp.kt:26-29,173-186` | The bar's strength Finish and Discard degrade on the *read/write of the session itself* (both use cases wrap their work in `runCatchingCancellable` and return `Failed`, which the bar shows), but the step after a successful outcome — `PendingOccurrence.complete` on finish (:231) and `forgetIfSession` on discard (:269), each a Room `upsertOccurrence` plus a DataStore `edit` with no catch — runs outside any try in the strength branch (the ACTIVITY branch is wrapped, :210-214 and :256-265); a throw there leaves `viewModelScope` uncaught (no handler; the app's `CoroutineExceptionHandler` is on `applicationScope` only), reaches the default uncaught handler and kills the process, with the session already finished/discarded but the plan row not marked DONE and no summary navigation (`_finishedNavigation` is set only after `complete` returns, :232). `HomeViewModel.startOccurrence` (:435-439) has the same unguarded `bindForSession` after a successful start. | Finishing or discarding from the little bar at the bottom is safe for the workout itself — but if the phone's storage refuses the *next* small write (marking the plan row done, clearing the reminder link), the app crashes, and after it comes back the plan still shows that session as not done even though it is saved in History. | ViewModel test: `FakeAppDependencies` whose planner DAO throws on `upsertOccurrence`; call `finishFromBar()` on a session bound to an occurrence; assert `actionError` is set and no exception escapes (today it escapes). |
| B9b | UI-1 | B9b | P2 | defect | C | — | `UI/plan/PlanScreen.kt:302-307,379-387,405-414`, `UI/plan/PlanDayScreen.kt:72,81-83,144,182-192`, `UI/components/WeekStrip.kt:118-121` | Plan's filled "Add session" opens `PlanDayScreen(add=true)` for whatever strip day is selected, but the day page refuses every add when `epochDay < today` (picker suppressed, no Add button, caption "Past days are a record"), and the strip only shows the current week; so weekdays already gone this week cannot be planned at all, and the Volt leads to a page that offers nothing. | On a Wednesday, tap Monday and press the big Add session button: you land on a page that says the day is a record and lets you add nothing. There is no way to set up next Monday until Monday comes. | Robolectric: render `PlanScreen` with today = Wednesday, select Monday, tap `PlanTags.ADD_SESSION`, assert the day page shows no `PlanDayTags.ADD` and no picker; or read the two files. |
| B9b | UI-2 | B9b | P2 | target gap | C (feel R) | F8a (partial) | `UI/routines/CustomWeekScreen.kt:287-348` (`:303-315`, `:318-322`, `:324-331`) | The week builder's day selector is a plain `clickable` `Column` with no `Role`, no `selected` semantics and a filled-day check icon with `contentDescription = null`; its only text is the two-letter label, so TalkBack cannot say which day is chosen or which days already hold lifts, while the app's own `WeekStrip` already does this right (`selectableGroup`, `Role.Tab`, spoken cell). | A screen-reader user building their week hears "Mo, Tu, We" and nothing else — not which day is open, not which days are done. | Robolectric semantics test on `CustomWeekScreen`: assert a day node has `Selected` and a `Role`; today it has neither. |
| B9b | UI-3 | B9b | P2 | defect | C | — | `ui/navigation/AppNav.kt:554-570`, `domain/CustomWeekLaunch.kt:1-13`, `UI/routines/CustomWeekViewModel.kt:84-86,103-104,196-208,367-379`, `data/repository/OnboardingApplier.kt:236-247`, `UI/routines/SavedStateCustomWeekDraft.kt:52-70,72-101` | The guided answers and weight unit reach the week builder only through in-memory `container.pendingCustomWeek`; after process death the draft days come back from `SavedStateHandle` but `launch` is null, so Confirm applies the week with `answers = null` (goal, emphasis, equipment, training age, places, focus and the typed bodyweight are skipped) and never sets the unit; the same draft also drops `targetSeconds`/`targetSecondsMax`, so a hold lift's time resets. | If the phone kills the app while you are building your first week, the screen comes back and Confirm still works — but the answers you just gave (goal, kit, bodyweight, kg/lb) are quietly thrown away, and any plank time you typed is back to the default. | Extend `CustomWeekViewModelTest.processDeathRestoresDaysAndSelectedDay` (`:341`): seed with answers + unit, recreate on a fresh VM with `pendingCustomWeek = null`, confirm, assert goal/unit stored and a hold lift's seconds kept; today both fail. |
| B10a | UI-1 | B10a | P2 | defect | C | — | `UI/exercise/ExerciseDetailScreen.kt:127-134, 290-303, 539-575, 641-647, 653-664` | For a lift measured in reps the "Weekly reps" card feeds a reps series into `TrendCard`, which unconditionally prints `unit.suffix` as the headline unit, the delta unit and the TalkBack description, so it reads "45 kg", "+5 kg" and announces "latest 45 kg". | A pull-up's weekly rep count is shown and spoken as kilograms, on the one chart that tracks that lift's progress. | Robolectric render (harness: `app/src/test/.../ui/exercise/ExerciseDetailThisWorkoutRenderTest.kt:92-120`) with a `LoadType.BODYWEIGHT` lift and finished sets in two weeks; assert no node under the weekly card contains "kg" and the card's `contentDescription` ends in "reps". |
| B10a | UI-2 | B10a | P2 | defect | C | 22 Sep History (contract "stale requests never display old totals under a new period") | `UI/history/HistoryViewModel.kt:188-229, 231-268`; `UI/history/HistoryScreen.kt:452-457, 459-485`; `domain/BlockReview.kt:45-51` | `uiState` is a `combine` of `horizon` and `horizonProgress`; switching the period re-emits at once with the new title and totals but the *previous* period's `HorizonProgress` (PRs count, "Moved most"), because the new progress arrives only after `completedTrainingRepository.all()` (a full-log read) and `BlockReviewBuilder.overRange` finish, and `HorizonProgress` carries no range to check against; on first load the same slot prints "0 PRs" for a null progress. | Tap Year and, for as long as the whole log takes to re-read, the card says "This year" over last month's PR count and best mover, which is exactly the mix-up the redesign contract forbids. | ViewModel test over `FakeAppDependencies(workoutDaoDecorator = …)` whose `sessionsBetween` awaits a gate; call `setHorizon(YEAR)` and assert the first `uiState` with `horizon == YEAR` has `horizonProgress == null` (or a progress stamped with the year's range). Today it carries the month's. |
| B10b | UI-1 | B10b | P2 | defect | C | — | `data/repository/BackupService.kt:77-80`, `data/repository/prefs/BackupPrefsStore.kt:185-190,116-123`, `UI/settings/BackupCoordinator.kt:344-350`, `UI/settings/BackupRestoreSection.kt:200-257`, `docs/PRIVACY.md:102-104` | Drive sign-out calls only `clearDriveSession()` (removes the account email and folder id), never `disarmAutoBackup()`, so `AUTO_BACKUP_ENABLED` and the Keystore-sealed passphrase `AUTO_BACKUP_SECRET` survive sign-out while PRIVACY.md promises "signing out of Drive deletes that sealed copy"; the toggle disappears from the screen (it is drawn only when `accountEmail != null`) so the armed state is invisible, and the next finished workout still tries an unattended upload (`UI/summary/WorkoutSummaryViewModel.kt:181-224`). | Signing out of Google Drive is supposed to forget your backup password and switch automatic backup off; it does neither, it just hides the switch, so the password stays on the phone and backups resume by themselves the moment you sign in again. | JUnit (`SettingsViewModelTest` harness): arm auto-backup as `aConfirmedPassphraseArmsAutomaticBackupAndSurvivesAsCiphertext` does, call `viewModel.backup.signOut(activity)`, assert `preferencesRepository.autoBackupSettings().sealedPassphrase == null && !enabled` — fails on trunk. Also read `BackupService.kt:77-80`. |
| B10b | UI-2 | B10b | P2 | defect | C (what is missing) / R (what TalkBack says) | 16 Sep D16; F11 acceptance `docs/FRONTEND_REDESIGN.md:232` | `UI/settings/SettingsSubpage.kt:27-36`, `UI/components/ScreenHeader.kt:84-91`, `UI/settings/SettingsChrome.kt:23-34`, `UI/settings/SettingsScreen.kt:176-407` | Opening any of the 13 sub-pages swaps content inside one NavHost destination (`when (page)`) and neither `SettingsSubpage`'s `ScreenHeader` title nor the root `SettingsHeader` carries `heading()` or `paneTitle`, and the package has no `semantics` call at all (the only ones in scope: `ReminderTimeWheel.kt:71`); the app already knows the technique (`UI/saveposture/SavePostureChooser.kt:83`, `UI/components/RestTimerUi.kt:828`), and only the uppercase section kickers are headings (`GymSurfaces.kt:82-86`). | When someone using the screen reader taps "Backup", the phone never says a new page opened or what it is called; they have to hunt for the Back button to work out where they are. | Robolectric compose test: `SettingsSubpage(title = "Backup") {}` then `onNodeWithText("Backup").assert(SemanticsMatcher.keyIsDefined(SemanticsProperties.Heading))` fails, and no node has `PaneTitle`. Device: TalkBack, tap a row, note what is spoken (R). |
| B10b | UI-3 | B10b | P2 | defect | C | 22 Sep "two ways to regenerate a week" (F10b) — this consequence was not recorded | `UI/settings/SettingsScreen.kt:543-547`, `UI/settings/SettingsViewModel.kt:330-369`, `data/repository/OnboardingApplier.kt:34-42,62-119`, `data/repository/PreferencesRepository.kt:561-572`, `domain/PlanSetupCopy.kt:11-13` | Settings → Week generator → "Generate a week" runs `OnboardingApplier.apply` on the stored answers with no caption and no confirmation: it creates a fresh set of routines beside the existing ones (:34-38, :111) and calls `beginBlock`, which **deletes the current 12-week block if it is not finished** (`PreferencesRepository.kt:569`), while the other door to the same code (Your plan → "Add a new block") says "This adds a new block. It does not delete history" and previews first. | One tap on "Generate a week" quietly throws away the training block you are in the middle of and adds a second copy of every routine; the button next door that does the same job warns you and shows a preview first. | JUnit: seed a block started 3 weeks ago (`preferencesRepository.beginBlock`), seed the catalog, call `viewModel.generateWeek()`, assert the block row is gone (`trainingBlockDao.getCurrent()` start date changed) and `routineRepository` count doubled — passes today, which is the defect. |
| B10b | UI-4 | B10b | P2 | defect / target gap | C | ADR-028 §4 | `UI/settings/SettingsScreen.kt:314-316`, `UI/settings/SettingsViewModel.kt:206-210`, `data/repository/prefs/SavePosturePrefsStore.kt:30-37`, `UI/settings/SavePostureSection.kt:41-48`, `domain/SavePostureCopy.kt:35-37`, `data/sync/SyncCoordinator.kt:22-34` | "Save on this phone" writes only `save_posture`/`save_posture_chosen` (and bumps the sync stamp); it does not sign out, clear the outbox or stop queuing, and nothing in `data/sync` reads the posture (only the synced row fields `SyncRemoteRows.kt:637-638`), so the root can show "How you save · This phone · optional Drive backup" and "Account · Signed in · sync paused" at once, and the LOCAL caption "Your workouts stay on this phone" is untrue while edits keep landing in the upload queue (PRIVACY.md:41-43). | Choosing "Save on this phone" does not sign you out of Temper Account, so Settings tells you your data stays on the phone while the app is still lining changes up to upload when sync resumes. ADR-028 never said which of the two should win, so this needs the owner's call as much as a fix. | JUnit: sign in through `FakeAppDependencies.accountAuth`, `chooseSavePosture(LOCAL)`, assert `accountAuth.session.first() == null` (or that a later save enqueues nothing) — fails on trunk. |
| B10b | UI-5 | B10b | P2 | defect | C (no accessibility model) / R (whether scroll gestures work) | F11 acceptance `docs/FRONTEND_REDESIGN.md:232` | `UI/reminders/ReminderTimeWheel.kt:66-92`, `UI/components/SnapWheel.kt:42-91`, `UI/reminders/ReminderPrefsSection.kt:212-238` | The reminder time is set only through three `VerticalPager`s of plain `Text` with no role, `stateDescription`, `setProgress` or custom action (`SnapWheel.kt:67-89`); the wheel's `contentDescription = spoken` sits on a non-merging Row so the value is read once and then each page as loose digits, and there is no typed alternative, so a screen-reader user has no reliable way to change the time. | A blind user can hear the reminder time but has no dependable way to set it — the wheel is flick-only. | Robolectric: `onNodeWithTag(REMINDER_TIME_HOUR)` has no `OnClick`, `SetProgress` or `StateDescription` (assert `SemanticsMatcher.keyNotDefined`). Device with TalkBack for the pager scroll gesture (R). |
| B10c | UI-1 | B10c | P2 | defect | C (heights) / R (pixels) | — | `ui/onboarding/OnboardingScreen.kt:98-103,291-335,341-373,384-409,420-445` | Four setup steps (Place, Days per week, Which days, Bodyweight) are plain `Column`s inside a non-scrolling root `Column`; the file has no `verticalScroll` (only `LazyColumn` at `:203,:229,:456`), so at 360×640 dp the Place step's `Continue` sits below the fold even at font 1.0 (≈740 dp of content, see notes), and at font 1.6/2.0 or landscape so do the other three. | On a short phone, with large text, or held sideways, the "Where will you train?" question's Continue button is pushed off the bottom and the page will not scroll, so guided setup cannot be finished. | Robolectric render of `OnboardingScreen` at `w360dp-h640dp` (and `fontScale=2f`) after `setFocus(STRENGTH)`, `next()`×3; assert `onNodeWithText("Continue").assertIsDisplayed()` — expected to fail. |
| B10c | UI-2 | B10c | P2 | defect | C | — | `ui/activity/LiveCardioScreen.kt:80-83,112-118`, `ui/activity/LiveCardioViewModel.kt:74-81,270-273,329-337`, `ui/activity/SavedStateCardioDraft.kt`, `ui/navigation/AppNav.kt:419-424,695`, `ui/navigation/LiveSessionBarViewModel.kt:182-204`, `domain/CardioCopy.kt:21-22` | "Leave running" pops the LiveCardio back-stack entry; the type / indoor / distance the owner set live only in that entry's `SavedStateHandle` (never in Room), so Resume from the bar opens a fresh entry that re-reads the Room row, and the bar's "Finish session" reads Room only — the inputs are lost either way, while the caption promises "This clock keeps running if you leave". | If you switch the run to a walk or type a distance and then tap "Leave running", those changes are silently thrown away: coming back shows a fresh screen and finishing from the bottom bar saves without them. Only the clock survives. | `LiveCardioViewModelTest`: start a live run, VM on handle A, `setDistanceKm("2.5")`, `clearAndJoinForTest()`; new VM on a **new** `SavedStateHandle(mapOf("sessionId" to id))` (what a re-navigation gives) → `distanceKm == ""`. Bar: `LiveSessionBarViewModel.finishFromBar()` after the same steps → `distanceMeters == null`. |
| B10c | UI-3 | B10c | P2 | defect | C | 22 Sep Cross-tab (process death) | `ui/navigation/AppNav.kt:553-570`, `AppContainer.kt:292`, `domain/CustomWeekLaunch.kt:6-8`, `ui/routines/CustomWeekScreen.kt:84-86`, `ui/routines/CustomWeekViewModel.kt:196-208,367-379`, `ui/routines/SavedStateCustomWeekDraft.kt:104-106`, `data/repository/OnboardingApplier.kt:236-247` | "I'll build my own" hands the questionnaire to CustomWeek through an in-memory `pendingCustomWeek`; after process death the restored CustomWeek entry re-seeds from `null`, so Confirm applies with `answers = null` and writes no goal, emphasis, age, places, focus or bodyweight and skips the pending weight unit — the custom-week draft saves only days and lifts. | If you choose "I'll build my own" and Android quietly closes the app while you are building the week, everything you answered before (goal, where you train, your weight, kg or lb) is dropped without a word; the week itself still saves. | `CustomWeekViewModelTest`: `seedFromGuided(days, answers(goal=STRENGTH, bodyweight=80), KG)`, add a lift, `clearAndJoinForTest()`; new VM on the same handle, `seedFromGuided(emptySet(), null, null)` (what the screen does after death), `confirm()` → assert `trainingGoal`, a weigh-in and `weightUnit == KG` landed; fails today. |
| B10c | UI-4 | B10c | P2 | target gap | C | F9; FRONTEND_REDESIGN "readable … date/zone" | `ui/activity/ActivityDetailScreen.kt:138-145,294-359`, `domain/ActivityDetailCopy.kt` (no date function), `ui/units/DateCopy.kt:12-13` | The activity detail / receipt renders title, modality caption, hero number, tiles, notes and blocks but never the captured local date, time or zone, although ADR-011 §2 makes the captured date the day's identity and `DateCopy` (:13) says it serves "session detail". | Opening a saved run, or a workout you logged after the fact, never says which day it happened on. | Read `ActivityDetailScreen.kt` end to end; `ActivityDetailViewModelTest` can assert a `dateLabel` in state once one exists. |
| B10c | UI-5 | B10c | P2 | defect | C | — | `ui/onboarding/OnboardingScreen.kt:117-125,463-475,535-543`, `ui/onboarding/OnboardingViewModel.kt:66-69`, `domain/RoutineGenerator.kt:432-442`, `domain/OnboardingPreviewCopy.kt:11-13,31-33`, `data/repository/OnboardingApplier.kt:80-92,160-168`, `data/repository/PreferencesRepository.kt:225` | The first question ("What do you want to record?") changes nothing after accept: `BOTH` is generated and applied exactly as `STRENGTH` (no cardio day is pinned; the stored focus has no reader outside re-seeding and sync), and `CARDIO` still asks days, which days and place, previews a week of seven "Rest" rows under the line "…not generated.. 12 weeks of Full Body." (double period, split named for a plan with no routines), then pins nothing. | Answering "Both" promises "lifting days and cardio, kept as their own work" but the saved plan has no cardio days; answering "Cardio" asks four questions for nothing and shows a week of rest days with a garbled sentence. | `OnboardingFocusTest`: `setFocus(BOTH)`, seed catalog, walk to PREVIEW → `preview.days.none { cardio }`; `OnboardingApplierTest`: apply with `focus=CARDIO, daysPerWeek=3` → `scheduleRepository.slots().isEmpty()`. Read `OnboardingScreen.kt:463-473` for the string. |
| B13a | AR-1 | B13a | P2 | defect | C | — | `insights/TrainingInsightsSource.kt:112,123-129,163-190`, `data/repository/WorkoutRepository.kt:148-156,171-177,185-190`, `data/repository/ActivityRepository.kt:120-124`, `data/repository/FlowGuards.kt:21-36` | Four of the insights pipeline's inputs (`observeSessionSummaries`, `observeFinishedLastLogged`, `observeFinishedSince`, `observeCompletedSummaries`) are the only repository flows not wrapped in `observeHealth`, and the pipeline is `shareIn`'d on `CoroutineScope(SupervisorJob() + computeDispatcher)` with no `CoroutineExceptionHandler`, so a Room read fault in any of them (or in the `mapLatest` DAO reads they make) kills the sharing coroutine as an uncaught exception and the process; Home subscribes on the first frame (`ui/home/HomeViewModel.kt:97-98`), so it recurs on every launch while the fault lasts. | If the phone's database has a hiccup while the Home tab is up, the app closes instead of showing the "could not read" message the rest of the app uses, and because Home is the first screen it would keep closing on every open until the hiccup clears. | JUnit: build `TrainingInsightsSource` over in-memory `TemperDatabase`, capture `Thread.setDefaultUncaughtExceptionHandler`, collect `observeShared()` once, then `execSQL("DROP TABLE set_logs")` on the same connection and log a write to invalidate; assert the handler received an `SQLiteException` (today) and, after wrapping, that the flow degrades instead. |
| B13a | AR-2 | B13a | P2 | defect | C | — | `PersonalTrainerApp.kt:96-102,159-170`, `logging/AppLog.kt:31-32,52-57`, `data/local/TemperPreMigrationCopy.kt:86,105,118`, `data/local/PreMigrationSnapshot.kt:65,118` | `installDiagnosticCapture()` (which sets `AppLog.onError`, the only route into `DiagnosticRing`) runs at `:102`, after `PreMigrationSnapshot.ensure(this)` at `:101`; `AppLog.onError` is null until then, so every `AppLog.e`/`AppLog.w` the pre-migration copy raises ("copy failed; migrating without it", "too little free space", "could not read the database version") reaches logcat only and never the diagnostic ring or bundle. | The one failure the rollback-copy exists to warn about (the copy could not be taken before a database upgrade) is the one failure that is never written into the diagnostics the owner can send; it is only in logcat, which needs a computer. | Extend `PreMigrationStartupOrderTest` (`app/src/test/.../data/local/PreMigrationStartupOrderTest.kt:20-29`, already source-order based) to assert `installDiagnosticCapture()` precedes `PreMigrationSnapshot.ensure(`; plus a unit test: `AppLog.onError = null; AppLog.e(tag, msg, RuntimeException())` leaves `DiagnosticRing.shared` empty. `installDiagnosticCapture` needs only the `Context` (`:160`), so the swap is one line. |
| B13b | TS-1 | B13b | P2 | validation gap | C | X5 (#410) | `tools/verify.sh:21-28`, `tools/preflight.sh:246-252`, `.github/workflows/ci.yml:64-98`, `app/build.gradle.kts:317-338,395-400`, `tools/coverage-floors.txt:146-152` | The coverage ratchet (`jacocoTestReport` + `check-coverage.py`) is invoked only by `tools/verify.sh`, which first runs the full `tools/preflight.sh`, which takes the broken plain-JVM lane (TS-2) and exits 1 before the coverage step; CI runs preflight with `PT_STATIC_ONLY=1` and never runs `jacocoTestReport`, and no Gradle task depends on it; so the seven floors X5 re-based on 24 September are enforced by no automated lane. | The "coverage floors" are a safety line that says "the tests must keep checking at least this much of the app" — but the one script that checks the line is broken, and the cloud build never checks it, so a drop below the line would go unnoticed. | Run `sh tools/verify.sh` on `trunk`: it stops at `== JVM tests` with "tests did not compile" (baseline §7). Then grep `ci.yml` and `build.gradle.kts` for `jacocoTestReport`/`check-coverage`: only the report task exists, nothing depends on it. |
| B13b | TS-2 | B13b | P2 | defect (tooling) | C | X5 commit message | `tools/run-domain-tests.sh:146-167,186-191,221-222`; `test/…/domain/CompletedTrainingParityTest.kt:3-7,34-36,54-55`; `test/…/domain/DraftStoreTest.kt:3-14,26-27`; `test/…/domain/UndoQueueTest.kt:3,32,40`; `test/…/data/backup/ProtectBackupTest.kt` (main `data/backup/ProtectBackup.kt`, `OpenBackup.kt` absent from the lane list) | The plain-JVM lane compiles `test/…/domain` and `test/…/data/backup` as whole directories against a hand-picked main list, and four test files in those directories no longer link: three Robolectric/UI tests filed under `domain` and one backup test whose two pure-Kotlin main files are not in the backup lane's list; DEVELOPMENT.md:21 still calls this lane an "every commit" gate and `:292` quotes its old count. | The "quick test run without Android" that a laptop with no Android tools is supposed to use has been unable to start for about two weeks, because four test files were put in the wrong folder or their code was never added to its list. | `sh tools/preflight.sh` (full): fails at `tools/run-domain-tests.sh` with exactly these four files (lead's log, §7). Fix per file in the notes. |
| B13b | TS-3 | B13b | P2 | validation gap | C | DEVELOPMENT.md:400-401 half-names it; X5 made it measurable | `ui/*` screens (list in notes), `ui/navigation/AppNav.kt:279`, `ui/permissions/LaunchPermissionsHost.kt:35`, `ui/intro/ColdStartIntro.kt:59`; JaCoCo XML (§7) | Of the 25 screen-level composables in `main`, 20 are never composed by any test under `app/src/test` (0 call sites; only their names appear in source-string tests), so on the JVM merge gate the Home, Body, Plan, Plan-day, History, Session-detail, Library, Routine-editor, Custom-week, Summary, Settings, Onboarding, intro, permissions, navigation, cardio and composer screens are exercised only through their ViewModels; JaCoCo reads `ui.home` 16.0 %, `ui.plan` 13.6 %, `ui.settings` 17.0 %, `ui.history` 25.5 %, `ui.navigation` 9.0 %, `ui.progress` 9.1 %, `ui.summary` 11.7 %, `ui.activity` 24.6 %, `ui.library` 20.9 %, `ui.intro`, `ui.permissions`, `ui.reminders` 0 %. The hosted emulator lane composes some of them (`HomePassInstrumentedTest` 24 tests, `PlanPassInstrumentedTest`, `HistoryPassInstrumentedTest`, `BodyPassInstrumentedTest`, `LibraryPassInstrumentedTest`, `ProductionScreensPassInstrumentedTest` 12) but it blocks nothing (ADR-024) and nobody can run it locally. | The automatic checks that decide whether a change may ship draw only the workout floor and a handful of pieces; every other tab is checked by reading its code, not by showing it on a screen — so a tab that stopped drawing would pass the gate. | For any screen in the list: `grep -rnE "[^\"A-Za-z.]HomeScreen\(" app/src/test` → 0 hits. Read the XML: `<package name="com/sinura/personaltrainer/ui/home">` INSTRUCTION covered 16.0 %. |
| B13b | TS-4 | B13b | P2 | architecture debt (test) | C | 22 Sep T1 (the same finding, outside `ui/workout`) | 23 files, e.g. `ui/settings/SettingsHomeLayoutTest.kt:10-140`, `ui/theme/HapticsPaletteTest.kt:14-133`, `ui/components/ComponentInventoryTest.kt:13-111`, `domain/TalkBackPolicyTest.kt:35-95`, `ui/components/TemperIconsGlyphTest.kt:18-43`, `ui/theme/MotionPolicyTest.kt:19-76`, `ui/home/HomeStartSheetTest.kt:14-56` | About 400 `assertTrue(source.contains("…"))` pins on production `.kt` text survive outside `ui/workout` (top twelve files alone: 66, 47, 36, 28, 25, 23, 21, 16, 15, 13, 13, 11), most of them positive pins on how code is spelled (`viewModel::generateWeek`, `_deleteFeedback.tryEmit(DeleteFeedback.DELETED)`, `val PrGold = Color(0xFFFFC53D)`, `if (BuildConfig.DEBUG)`), one on a docs sentence (`MotionPolicyTest.kt:73-75` reads `docs/architecture/ADR-023…md`); T1 removed this class from the floor but F4–F10 will hit the same wall on every other tab. | Many tests check that a sentence of code is still written a certain way, not that the screen still behaves; renaming a variable breaks them, while a real bug that keeps the words passes. The workout screen was cleaned of this in T1; the other tabs were not. | `grep -rln 'readText()' app/src/test |

## §3 rows (P3)

| Pass | ID | Sev | Type | C/R | Prior | Files | Claim |
|---|---|---|---|---|---|---|---|
| B1 | S-15 | B1 | P3 | validation gap | C (guard absent) / R (trigger) | new; ADR-031 §4 | `P/data/sync/SyncEngine.kt:202-216`; `SyncRemoteRows.kt:317`; `P/data/local/entity/ActivityEntities.kt:12,39` | `applyActivitySession` checks only the remote status, so a remote COMPLETED row with a higher revision, or any tombstone, overwrites or deletes a session that is ACTIVE on this phone and clears `liveToken`; a realistic route needs the same id live here and finished elsewhere (a Drive restore of a mid-workout backup onto a second phone). |
| B1 | TS-1 | B1 | P3 | validation gap | C | S-11 | `T/data/sync/SyncOutboxWriterTest.kt:73-96`; `P/data/local/dao/SyncDao.kt:29-32` | `enqueueAllRoutinesOrdersParentBeforeChild` asserts `createdAtMs <=` with a fixed clock (`nowMillis = { 1_000L }`), so it passes on equal stamps and does not pin the order `peekOutbox` will actually use (ties fall to random UUID). |
| B1 | AR-1 | B1 | P3 | opportunity | C | — | `P/data/sync/SyncAuthoring.kt:54-69,98-102,126-139,153-157`; `SyncOutboxWriter.kt:138-161,186-221` | Every schedule, routine, template, custom-lift or goal edit re-queues every row of that kind (and stamps every lift of a routine with the parent's time), so one edit becomes N uploads and N re-pulls on the other phone. |
| B1 | S-16 | B1 | P3 | defect | C | — | `P/data/sync/SyncAccountPrefs.kt:115-162` | Pulled preference strings are written to DataStore raw (`prefs[TRAINING_GOAL] = remote.trainingGoal`) without `fromStorage` validation, so an enum name from a newer app version lands unchecked; readers fall back, but the stamp is still advanced so the row is never re-pulled. |
| B1 | S-17 | B1 | P3 | defect | C | S-9 | `P/data/sync/SyncCoordinator.kt:41-50`; `P/domain/SyncCopy.kt:59-61` | `abandonOutboxOnSignOut` deliberately keeps `lastSuccessAtMs`, so a new account (or the same one after a server wipe) reads "Last synced successfully." before its first pass. |
| B1 | S-18 | B1 | P3 | architecture debt | C | S-3 | `P/data/repository/PlannerRepository.kt:238-240,248-254,274`; `RoutineRepository.kt:71-74` | In the rule-retire and routine-delete paths the sync hook runs inside `withTransaction`, so `requestSync()` (a WorkManager enqueue) and `auth.session.first()` run on the Room transaction thread; harmless while paused, a transaction-length hazard once `requestSync` reaches WorkManager. |
| B2 | PV-1 | B2 | P3 | docs truth / validation gap | C | S-9 | `docs/architecture/backup-threat-model.md:59-75, 88`, `app/src/main/res/xml/backup_rules.xml:31-32`, `data_extraction_rules.xml:29-30, 52-53`, `tools/check-backup-policy.py:20-30`, `docs/PRIVACY.md:26` | The Supabase session store (`shared_prefs/<applicationId>_preferences.xml`, holding access token, refresh token and email) is absent from threat-model §3, from the named excludes and from the drift checker; it is covered only by the domain-wide `sharedpref` exclude. PRIVACY.md's "tokens in app-private storage" is true but does not say plaintext. |
| B2 | PV-4 | B2 | P3 | validation gap | C | none | `app/src/main/AndroidManifest.xml:19-30`, `P/data/auth/AccountAuthFactory.kt:9-17`, `P/data/sync/SupabaseRestClient.kt:15,35,55,71`, `app/build.gradle.kts:73-74` | No `usesCleartextTraffic="false"` and no network security config; `AccountAuthFactory` accepts any scheme and supabase-kt honours `http://` (bytecode `SupabaseClientBuilder` sets `useHTTPS=false`), so on API 26–27 devices — whose platform default permits cleartext when the attribute is absent — an `http://` value in `supabase.properties` would ship a plain-HTTP account lane. Every shipped URL literal is `https://` (grep of `P`). |
| B2 | PV-5 | B2 | P3 | defect | C | none | `P/logging/AppLog.kt:43-59, 61-67`, `app/src/test/java/…/logging/AppLogRedactionTest.kt:33-42` | `redactMessages` redacts the free-text argument only; the sink appends `Log.getStackTraceString(error)`, so any throwable message (file paths in `IOException`s, server bodies, library text) reaches logcat unredacted in both build types, and the only test covers the message argument. |
| B2 | PV-6 | B2 | P3 | defect | C | none | `P/data/auth/SupabaseAccountAuth.kt:66-69, 119-121`, `P/domain/AccountAuthCopy.kt:58-59` | Sign-out offline fails and keeps the session: the library catches only `RestException` around `POST /auth/v1/logout` (bytecode `AuthImpl.signOut` exception table), a connection failure propagates, the app shows "Connect to the internet to use Temper Account." and the stored session stays. |
| B2 | PV-7 | B2 | P3 | docs truth | C | none | merged manifest (build output), `app/src/main/AndroidManifest.xml:31-51`, `app/src/debug/AndroidManifest.xml:5-16` | Manifest verdict: every declared permission has a code path (INTERNET/ACCESS_NETWORK_STATE: `DriveHttp`, `SupabaseRestClient`, `NetworkChecker.kt:8-14`, `WorkManagerSyncScheduler.kt:19`; POST_NOTIFICATIONS: `LaunchPermissionsHost.kt:92`, `ReminderNotifications.kt:44-50`; VIBRATE: `RestTimerAlerts.kt:194-199`; FOREGROUND_SERVICE(+SPECIAL_USE): `RestTimerService` with the subtype property `:58-60`; WAKE_LOCK: `RestTimerAlarmReceiver.kt:32`; SCHEDULE_EXACT_ALARM: `ExactAlarmCapability.kt:41-58` with `canScheduleExactAlarms` before every exact call and `ExactAlarmStateReceiver`; USE_FULL_SCREEN_INTENT: `RestTimerNotifications.kt:109-111` gated by `canUseFullScreenIntent` `:224-234` for API 34; RECEIVE_BOOT_COMPLETED: `ReminderRebuildReceiver`; REQUEST_INSTALL_PACKAGES debug-only with `PackageInstaller` + `USER_ACTION_REQUIRED` `DebugApkInstaller.kt:72-74`). The merged debug manifest exports five components, not one: `MainActivity`, WorkManager `SystemJobService` (BIND_JOB_SERVICE) and `DiagnosticsReceiver` (DUMP), GMS `RevocationBoundService` (GMS signature permission), Compose `PreviewActivity` (debug only); `androidx.startup.InitializationProvider` is added non-exported by gotrue-kt and multiplatform-settings. `RestLockActivity` (`singleInstance`, `taskAffinity=""`, `showWhenLocked`, `exported=false`) shows only the clock, ±15 and Skip (`RestLockActivity.kt:222-226`), so nothing about training reaches the lock screen; Back leaves its own task. Debug `FileProvider` serves only `cache/debug-update/` (`debug_update_paths.xml:3-5`). `tools:targetApi="33"` only silences lint for the API 31/33 attributes. |
| B2 | BR-1 | B2 | P3 | validation gap | C / R | none | `app/proguard-rules.pro:14-22, 31, 36`, `app/build.gradle.kts:149-154, 273-277` | R8 static reading (C): the app keeps `data.sync.**`, `data.backup.**` (Gson by reflection; no other Gson-reflected class exists — `DbMaintenance.kt:205` is hand-rolled `org.json`), Room, `diagnostics.**`, and its own `@Serializable` classes (it has none). Library consumer rules present in the cache: ktor-utils `META-INF/proguard/ktor.pro` (keeps `HttpClientEngineContainer` implementations for `ServiceLoader`), kotlinx-serialization-core 1.7.3 (`Companion`/`serializer()` keeps + r8 rule, covering gotrue-kt's `UserSession`, `UserInfo`, `GoTrueErrorResponse`), androidx.startup 1.1.1 (`Initializer` keeps for `SupabaseInitializer` and `SettingsInitializer`). supabase-kt, gotrue-kt, multiplatform-settings and Kermit ship none and use no reflection on their own types. slf4j-api **1.7.36** is on the classpath via ktor; its `LoggerFactory` references `org.slf4j.impl.StaticLoggerBinder`, a class not present — the usual ktor-on-Android R8 "missing class" failure unless `-dontwarn`. (R) Whether `assembleRelease` passes and a release sign-in works is B14's. |
| B2 | PV-8 | B2 | P3 | opportunity | C | none | `P/data/auth/AccountAuthFactory.kt:7` | `AccountAuthFactory.create()` has no caller (only `createRuntime()` is used, `P/AppContainer.kt:105`); if it were called it would build a second `SupabaseClient` with a second auto-refresh loop over the same stored session. |
| B3 | S-15 | B3 | P3 | opportunity | C | S-2 | `Rest:34`, `Engine:176-182` | Fix shape for the page-boundary tie (S-2): order by `updated_at_ms.asc,id.asc` and filter `or=(updated_at_ms.gt.X,and(updated_at_ms.eq.X,id.gt.Y))`, cursor `(updated_at_ms, id)`; tables without `id` use their key column (`epoch_day`; `exercise_id,muscle_key`), and the four singleton prefs tables hold one row per user so a tie is impossible. Superseded by S-16's `server_seq`, which makes ties impossible everywhere. |
| B3 | S-16 | B3 | P3 | opportunity | C | S-2 | `packet-2`, `packet-3`, ADR-031 §4 | S2a DDL shape (proposal, not a decision): a shared sequence `sync_change_seq`; on every table `server_seq BIGINT` and `server_updated_at_ms BIGINT` set by a `BEFORE INSERT OR UPDATE` trigger that also sets `user_id := auth.uid()`; `PRIMARY KEY (user_id, id)`; index `(user_id, server_seq)`; pull `server_seq=gt.X&order=server_seq.asc&limit=500`; writes through `rpc/sync_upsert_<table>(rows jsonb)` (SECURITY INVOKER, `search_path = ''`) doing `ON CONFLICT (user_id, id) DO UPDATE ... WHERE` the incoming version is newer (revision first for sessions, then `updated_at_ms`; a tombstone wins a tie), returning `(id, accepted, server_seq)`. Client needs: cursor on `server_seq`, `accepted` handling (drop the outbox row, let the pull bring the winner), and `serializeNulls()` (S-12). Caveat: sequence order is not commit order; PostgREST requests are single statements so the window is milliseconds, and a re-read of the last page on the next pass (applies are idempotent) closes it. |
| B3 | S-17 | B3 | P3 | opportunity | C (8 tables), R (9) | S-7 | `SupabaseAccountAuth.kt:79-100`, `packet-2:8,48,81`, `packet-3:8,48,84,117,149` | Client-side per-table deletion is redundant on the 8 defined tables (`user_id ... REFERENCES auth.users (id) ON DELETE CASCADE`), and the child-first order is moot because the committed DDL declares no table-to-table FKs; the one missing piece is a way to delete the `auth.users` row as the user. Shape: `create function public.delete_my_account() returns void language sql security definer set search_path = '' as $$ delete from auth.users where id = auth.uid(); $$; revoke all on function public.delete_my_account() from public, anon; grant execute on function public.delete_my_account() to authenticated;` called as `POST /rest/v1/rpc/delete_my_account`; the cascade removes every synced row; the client then `clearSession()`. Alternative: an Edge Function with the service role calling `auth.admin.deleteUser` (heavier, keeps SQL out of the auth schema). |
| B3 | S-18 | B3 | P3 | validation gap | C | S-8 | `Rows:39,111,122,149,167,521`, `packet-2:19`, `data/sync/SyncJson.kt:8` | Column parity is strict one way only: Gson writes every non-null field and PostgREST rejects an unknown key (PGRST204, HTTP 400, which poisons the outbox row like S-11), so the 9 undefined tables must carry every Kotlin column, including `revision` on sessions, rules, occurrences, routines and templates; the reverse is lax (Gson ignores unknown server columns). `custom_exercises.revision` is always sent as 0 (`Rows:521`) and ADR-031 §4 compares custom lifts by change time, so that column is dead. |
| B3 | S-19 | B3 | P3 | opportunity | C | — | `packet-2:20,52,87`, `packet-3:21,57,90,122,153`, `Engine:173` | Tombstones are never purged, and a phone whose cursor is 0 (fresh sign-in, restore reset) replays every tombstone ever written. Fine for one owner; if S2a adds a purge, a phone whose cursor is older than the purge horizon must reconcile in full, so the horizon has to be recorded server-side and the client must reset its cursor when older. |
| B5 | BK-5 | B5 | P3 | defect | C | — | `P/data/backup/DriveHttp.kt:42-56,57-91` | The request body is written inside `apply {}` before the `try`, so an `IOException` while connecting or writing a POST (upload, create folder) escapes as a raw `IOException` instead of `BackupException("Connect to the internet…")` and skips `disconnect()`; Settings then shows the Java message (`BackupCoordinator.kt:895-899` falls back to `thrown.message`). |
| B5 | BK-6 | B5 | P3 | validation gap | R | T10 | `P/data/backup/BackupScaleBudget.kt:54-57`, `P/data/backup/DriveRestClient.kt:60-94` | The export budget admits a 12 MiB protected envelope and the scale fixture (500 sessions, 15,000 sets) is only asserted under 8 MiB, but every Drive upload is `uploadType=multipart`, which Google documents for files of 5 MB or less; a large history may be refused by Drive while file export still works. |
| B5 | BK-7 | B5 | P3 | defect | C | — | `P/PersonalTrainerApp.kt:115-122`, `P/ui/settings/BackupCoordinator.kt:559-571,573-583`, `P/data/repository/BackupService.kt:356-397` | After a crash mid-restore, launch recovery that ends `Finished` or `NothingChanged` is logged only; no note is shown and `setLastRestore` is written only by `confirmRestore` after a returning commit, so Settings' "last restore" stays at the previous value while the data is the new backup. Only `Unresolved` and `SettingsLost` set a durable note (393, 473). |
| B5 | BK-8 | B5 | P3 | validation gap | C | — | `P/data/backup/BackupValidator.kt:78-407`, `P/data/repository/LocalBackupRepository.kt:394-449,503`, `P/data/repository/ActivityBackupIo.kt:28-35` | The validator guards ids, names and enum fields but not the other non-null strings Gson can leave null (`BackupRoutine.notes`, `BackupSession.notes`, `BackupActivity.title/notes`, `BackupScheduleRule.modality`, `BackupScheduleOccurrence.status/zoneId`, `BackupReminderDelivery.status`, `BackupMissedWorkDecision.choice`) and never looks at `activityTemplates` at all; such a file passes preview, is confirmed, has its safety copy written, and only then fails inside the transaction (entity constructor NPE or constraint), rolling back with "Restore failed. Nothing was changed." |
| B5 | BK-9 | B5 | P3 | validation gap | C | — | `P/data/local/dao/ActivityDao.kt:196-197,215-216`, `P/data/backup/BackupValidator.kt:304-371` | `insertBlock` and `upsertTemplate` are `REPLACE`; block ids are checked unique across activities only, not across templates, so a crafted file whose template block shares an id with an activity block silently replaces the activity's block (and whatever hangs off it) during restore, and a document with two templates on one id keeps the last. |
| B5 | BK-10 | B5 | P3 | opportunity | R | — | `P/data/backup/BackupEnvelope.kt:41,162-167` | A file may demand up to 5,000,000 PBKDF2 iterations (8.3× the default) before anything is authenticated; each wrong-password attempt on such a file costs that many on the IO thread while Settings shows "Checking backup…". The ceiling is pinned by `BackupEnvelopeTest.aForgedIterationCountAboveTheCeilingIsRefusedBeforeDeriving`; the phone cost of even the 600,000 default is unmeasured. |
| B5 | BK-11 | B5 | P3 | defect | C | — | `P/data/repository/LocalBackupRepository.kt:357,666-671`, `P/data/repository/BackupService.kt:265-266` | `onBeforeRestore()` stops the rest timer and clears the draft before the transaction, so a rolled-back replace leaves them stopped; and `writeVerifiedSafetySnapshot` computes `expected` in a second transaction after `current`, so a DataStore write landing between them (a weigh-in) aborts the restore with "did not match" — safe direction, spurious message. |
| B6 | RT-4 | B6 | P3 | defect | C | HANDOFF-NEXT:131-134 (a) | `P/timer/RestTimerController.kt:296-298,300-310,338-341,402-411`, `RestTimerAlarmReceiver.kt:61-74`, `RestTimerStatePersistence.kt:181-194` | An alarm can be armed for a rest whose row is not yet on disk: `refreshAlarmCapability` (every `MainActivity.onResume`, :103) and `rescheduleCurrent` arm from the live store while the persist job is still queued, and the job's own arm at :341 re-reads `store.current()` instead of arming the `snap` it wrote, so between a newer call's publish (`start` :128) and its `takeNumber()` (:130) an older job can arm the new rest over the old rest's row. If the process dies in that gap the receiver reads a row for a different rest → STALE → silence; at the next start that stale row rehydrates as `Expired` and, the new process having "held nothing", posts "Rest done" for a rest that was already replaced. |
| B6 | RT-5 | B6 | P3 | defect | C | HANDOFF-NEXT:131-134 (b) | `P/timer/RestTimerController.kt:296-298` vs `:303-309`, `RestTimerCompletion.kt:43-47`, `RestTimerAlarmReceiver.kt:70-74` | `rescheduleCurrent()` arms regardless of `_persistenceHealthy`, recreating the "alarm without a row" state `refreshAlarmCapability` guards against; after a process kill that alarm finds no row and is read as already completed (:70-74), so the rest ends in silence while the UI's "Rest may not survive leaving the app" line was the only warning. |
| B6 | RT-6 | B6 | P3 | validation gap | R | — | `P/timer/RestTimerController.kt:270,417-430`, `RestTimerService.kt:114-125,252-257,287-304`, `ExactAlarmStateReceiver.kt:30-31`, `PersonalTrainerApp.kt:114` | Two background starts of the foreground service are unproven on API 31+: a sticky restart (`onStartCommand(null)`) and the exact-alarm grant broadcast both run `rehydrate()` → `Running` → `persistThenArm(syncService=true)` → `dispatch(ACTION_SYNC)` → `startForegroundService` from a background process; both exceptions are caught (:422-428, :300-303) and the alarm still fires, but whether the shade card comes back before the next in-app start is a phone fact. The alarm receiver never starts the service (`completeIfCurrent(fromService=true)` → no STOP, :226-231), so the reliable path is exempt from this. The API 26–30 five-second `startForeground` window is met: `ensureForegroundClaimed()` is the first thing both paths do (:118, :130). |
| B6 | RT-7 | B6 | P3 | architecture debt | C | — | `P/timer/CardioTimerPersistence.kt:42-54,68-69`, `P/workout/StartLiveCardio.kt:54-68`, `P/ui/activity/LiveCardioViewModel.kt:297-306,320-323`, `P/workout/CompleteTraining.kt:64`, `P/ui/navigation/LiveSessionBarViewModel.kt:258`, `P/ui/workout/StartOptionsViewModel.kt:331` | The cardio row's `commit()` runs on the caller's thread — `viewModelScope` (Main) in every caller — whereas the rest timer moved its commits onto IO on purpose (`RestTimerStore.kt:12-14`); a false commit is logged and the clock falls back to the session's wall-clock start (`CardioElapsed.seconds:95-123`), and process death and reboot are both recovered (`LiveCardioViewModel.kt:275-288`, `CardioElapsedTest.kt:68-107`). Correctness holds; the write is on the wrong thread. |
| B6 | RT-8 | B6 | P3 | opportunity | C | — | `P/timer/RestTimerAlerts.kt:21-24,143-160,162-169` | The cue plays on `USAGE_ALARM` with no `AudioManager.requestAudioFocus` (transient, may-duck), so music from another app is neither paused nor ducked under the cue; silent ringer does not mute it (by design, `RestSound.kt:8-11`), DND "total silence" does (platform). |
| B6 | RT-9 | B6 | P3 | validation gap | R | — | `notification_rest_running.xml:14-28`, `notification_rest_running_big.xml:14-28` | No fixed `dp` heights (all `wrap_content`), text in `sp` (12/20 and 12/36), so at font scale 2.0 the compact view grows past the collapsed template's fixed content height and the system clips it; RemoteViews cannot be rendered in the JVM lane, so this is a phone check. |
| B6 | RT-10 | B6 | P3 | validation gap | C (window) / R (likelihood) | — | `P/timer/RestTimerAlarmReceiver.kt:76-97,119`, `RestTimerCompletion.kt:58-72` | `withTimeoutOrNull(10 s)` wraps `completeOnce`, whose one suspension point (`restTimerPreferences.first()`, :64-68) sits *after* the claim and the store clear (:58-61) and *before* the cue and the "Rest done" card (:70-72); a DataStore stall of ten seconds on a cold process would end the rest silently with the claim spent. The wake lock (:30-36, 10 s) and `pendingResult.finish()` are released on every path (:94-96). |
| B6 | RT-11 | B6 | P3 | opportunity | C | — | `P/timer/RestTimerService.kt:91-94,342-356` | On a *skip* (not a finish) the snapshot collector still calls `freezeShadeAtZero(idle)` before `stopNow()`, so the last frame some phones keep after cancel reads "0:00" for a rest that was skipped with time left. |
| B6 | RT-12 | B6 | P3 | opportunity | C | — | `P/ui/workout/WorkoutDock.kt:136-142`, `P/domain/RestHonestyCopy.kt:7,53-55` | The dock passes `exactBestEffort = false`, so "May be late when the phone sleeps" appears only on the rest page (and the Settings row after the first rest); a lifter who rests from the dock alone never sees the app "say so" as PRIVACY.md:115 promises — a documented choice (`RestHonestyCopy.kt:7`), not a defect, but worth an owner yes/no. |
| B7 | RM-2 | B7 | P3 | defect | C (loss trigger R) | — | `P/reminder/WorkoutAlarmScheduler.kt:27-51`, `P/ui/settings/SettingsViewModel.kt:261-292,371-374`, `P/timer/ExactAlarmStateReceiver.kt:20-33`, `P/MainActivity.kt:99-107`, `P/PersonalTrainerApp.kt:90-157` | `WorkoutAlarmScheduler.rebuild` is called only from Settings edits, from its own receiver after a fire, and from the boot/zone/TIME_SET receiver; `ExactAlarmStateReceiver` re-arms only the rest timer and `onResume` only refreshes rest capability, so after anything that clears the app's alarms (a force-stop by the user or an OEM battery manager, or the system's force-stop on `SCHEDULE_EXACT_ALARM` revoke, which the receiver's own comment at `:12-13` describes) the seven weekday alarms stay dead until Settings is touched or the phone reboots. |
| B7 | RM-3 | B7 | P3 | defect | C | — | `P/reminder/WorkoutAlarmReceiver.kt:28-44`, `P/domain/ReminderPolicy.kt:42-44`, `P/data/repository/PlannerRepository.kt:458-466`, `P/ui/reminders/ReminderPrefsSection.kt:156-205`, `P/domain/ReminderCopy.kt:16`, `docs/PRIVACY.md:112-113` | Quiet hours are consulted only on the WorkManager path (`DEFER_QUIET`); the per-day alarm receiver posts "Time to train" without checking `ReminderPolicy.inQuietHours`, while Settings draws "Quiet hours" directly under the day alarms with the caption "Quiet hours can still defer a reminder that lands overnight", and PRIVACY states one rule for all workout reminders. |
| B7 | RM-4 | B7 | P3 | validation gap | C (user effect R) | — | `P/reminder/ReminderRebuildReceiver.kt:27-31`, `P/data/repository/PlannerRepository.kt:614-628,283-305`, `P/reminder/WorkoutAlarmScheduler.kt:31-49` | On `TIMEZONE_CHANGED` the receiver re-enqueues each PENDING plan-row reminder at its stored `scheduledAtMs` instant, and no code re-resolves a `FOLLOW_DEVICE` occurrence's `captured` instant for the new zone (`ensureWeek` only adds missing ids), whereas the day alarms are re-resolved from `defaultZoneId()`; ADR-012 decision 12 asks for a safe rebuild after a zone change without saying which clock a plan row follows. |
| B7 | RM-5 | B7 | P3 | defect | C | — | `P/reminder/ReminderNotifications.kt:29,82-102,173-184`, `P/MainActivity.kt:126-130`, `P/data/repository/PlannerRepository.kt:491-499` | The day-alarm card and the plan-reminder card for the same occurrence build their Start action with the same request code `("start" + occurrenceId).hashCode()` and the same intent shape under `FLAG_UPDATE_CURRENT`, so whichever posts last rewrites `EXTRA_DELIVERY_ID` for both (`alarm-<occ>` vs `rem-<occ>`); `markReminderStarted("alarm-<occ>")` then finds no row and the real delivery is never marked STARTED; and the alarm card is posted under `0x71A1` while Start's launch cancels `occurrenceId.hashCode()`, so the alarm card's own Start leaves it in the shade with a live button. |
| B7 | RM-6 | B7 | P3 | opportunity | C | — | `P/update/DebugApkInstaller.kt:63-66`, `P/update/DebugApkDownload.kt:12`, `app/src/debug/res/xml/debug_update_paths.xml:3-5` | Each offered version is staged as `cacheDir/debug-update/PersonalTrainer-<code>-debug.apk` and nothing deletes it after install or when the next offer arrives (grep of `P` for `debug-update` hits only these two files), at up to `MAX_BYTES` = 96 MB each. |
| B7 | RM-7 | B7 | P3 | validation gap | C | — | `app/src/test/java/com/sinura/personaltrainer/FakeAppDependencies.kt:163`, `app/src/test/.../data/repository/PlannerRepositoryTest.kt:361-369`, `P/reminder/WorkoutAlarmScheduler.kt`, `P/reminder/WorkoutAlarmReceiver.kt` | `FakeAppDependencies` wires `NoOpReminderScheduler`, the only recording scheduler is the ad-hoc throwing one in one test, no test asserts which unique work names are cancelled when a snoozed occurrence is settled, and no test at all mentions `WorkoutAlarmScheduler` or `WorkoutAlarmReceiver` (grep of `app/src/test` for either name returns nothing); the day-alarm path is proven only by `WorkoutAlarmsTest`'s date arithmetic. |
| B7 | RM-8 | B7 | P3 | opportunity | C | — | `P/data/repository/PlannerRepository.kt:439-448`, `P/reminder/ReminderNotifications.kt:53-55,69-73` | `processDueDelivery` writes `DELIVERED` before `onDeliver`, and `show` returns silently when `POST_NOTIFICATIONS` is denied or `notify` throws, so the authoritative delivery record (ADR-012 decision 10) says delivered when nothing was shown; only the Settings banner (`ReminderPrefsSection.kt:85-92`) hints at the cause. |
| B7 | RM-9 | B7 | P3 | opportunity | C | — | `.github/workflows/debug-live.yml:213-216,160-180`, `P/update/GitHubDebugReleases.kt:87-110` | A drop built without the stable signer is still published as a pre-release with a `-debug.apk` asset ("THROWAWAY SIGNER" in the notes), and the checker accepts any such pre-release, so the banner would offer an APK Android must refuse over the installed signer; the `DEBUG_CERT_SHA256` gate stops it only when that repository variable is set. |
| B7 | RM-10 | B7 (hand-off from B6) | P3 | defect (contradicts an accepted decision) | C (budget collision R) | — | `P/reminder/WorkoutAlarmScheduler.kt:19-22,59-71`, `P/reminder/ReminderNotifications.kt:16-19,82-102`, `docs/architecture/ADR-012-rest-and-reminders.md:63-64,118,200-201`, `docs/PRIVACY.md:112-115`, `P/timer/ExactAlarmCapability.kt:47-70` | ADR-012 decision 9 reads "Reminders are **best-effort WorkManager**, not exact alarms. They are a different capability from rest." and its consequences add "A reminder cannot be implemented by `setAlarmClock`" (`:118`) and "Do reminders have to be exact to the minute? No. Rest does. Reminders do not." (`:200-201`); the per-weekday Settings alarms are `AlarmManager` `RTC_WAKEUP` alarms armed with `setExactAndAllowWhileIdle` whenever `canScheduleExactAlarms()` is true (`:59-65`), falling back to `setAndAllowWhileIdle` (`:66-71`), never `setAlarmClock` and never `USE_EXACT_ALARM`; no ADR amends decision 9 for them (grep of `docs/architecture` for day alarms hits only ADR-028:65's sync table; the file arrived in `0c759e7` with no decision record), and `ReminderNotifications`' own KDoc still says "never an exact-alarm path (ADR-012)" while `showWorkoutAlarm` is posted only by that path. PRIVACY.md:112-115 promises exact alarms for rest completion alone. Budget: both branches are allow-while-idle alarms, the same class the rest completion uses (`ExactAlarmCapability.kt:52-56,65-69`), which Android throttles per app to roughly one delivery per nine minutes in Doze; a weekday alarm fires once a day at a chosen time and a phone mid-workout is rarely dozing, so a real collision with a rest alarm is possible but unproven (R). |
| B8 | C-3 | B8 | P3 | validation gap | C | ADR-029 §2 | `docs/coach/evidence-seed.json`, `P/domain/coach/EvidenceCatalog.kt:6,21`, `P/domain/coach/CoachPolicyEvidence.kt:9–47`, `T/domain/coach/EvidenceCatalogTest.kt:14,40` | The JSON "human mirror" is not loaded at build time, not read by any test, and already disagrees with the Kotlin policy map on 10 of 17 reason codes (e.g. JSON says `hackett-2017` cites `IN_TANK`, Kotlin does not; Kotlin cites `helms-2016` for `BW_HOLD`, `LIGHTER_HOLD`, `SKIP_RPE_HOLD`, `BW_DROP_REP`, JSON lists none). The app runs without the file; `EvidenceCatalogTest` checks only the Kotlin catalog (DOI format, known ids, no duplicates). Both are `version 1`. |
| B8 | DM-3 | B8 | P3 | defect | C | Foundation item 4 | `P/domain/WeightFormat.kt:64–69,99–103,134–136`, `P/ui/exercise/ExerciseDetailScreen.kt:132,633–635` | Volume is rounded twice — to the half-pound (`roundToHalf`) and then to the whole (`round`, half-to-even) — so a quarter of pound totals land one pound off the correctly rounded true value: 75.7 kg × 3 = 227.1 kg = 500.669 lb prints **500** (true 501); 75.8 kg × 3 = 227.4 kg = 501.331 lb prints **502** (true 501). Kilograms have the same fault in a narrower band (500.52 kg → 500; 501.46 kg → 502). All surfaces agree with each other (the 22 Sep-era inconsistency stays closed); they agree on a number that can be wrong by one. |
| B8 | DM-4 | B8 | P3 | defect | C | — | `P/domain/SessionSummary.kt:85–92` | `signedWorkDelta` suppresses only `abs(delta) < 0.05` kg, but prints the magnitude through `formatVolumeNumber`, which rounds anything under 0.5 kg (0.75 lb) to 0 — so a 0.05–0.5 kg difference prints "+0 kg"/"+0 lb", the exact string the function's own comment says it must not invent. |
| B8 | DM-5 | B8 | P3 | opportunity | C | — | `P/domain/LibraryGrouping.kt:36–75,100–102`, `P/domain/DefaultExercises.kt:72–86` | `FAMILY_LABELS` names 39 of the 47 movement families; the batch-5/7 families `couch-stretch` (2 rows), `elephant-walk` (2), `pigeon` (3), `glute-ham-raise`, `joint-circles`, `ql-raise`, `reverse-hyper`, `reverse-nordic` (1 each) fall back to their first member's full name as the family header. |
| B8 | DM-6 | B8 | P3 | architecture debt | C | time-seams.md §5 | `P/domain/RestTimer.kt:129`, `P/domain/LiveSessionRules.kt:47`, `P/domain/WorkoutAlarms.kt:25`, `P/domain/ClockCopy.kt:42,49`, `tools/check-domain-seams.py:24–33` | `String.format` in `domain` uses `Locale.getDefault()` implicitly; in a locale with native digits the rest clock, the live-session clock, the reminder time and — worse — the *stored* alarm token (`"MONDAY=7:%02d"`) change form. `check-domain-seams.py` bans the `Locale` import but cannot see this. `WorkoutAlarms.decode` uses `toIntOrNull()`, which accepts Unicode digits, so the round trip probably survives; the on-disk token is still locale-shaped. |
| B8 | DM-7 | B8 | P3 | target gap | C | ADR-008 §2 | `P/domain/RuleTrace.kt:88–99,112–120`, `P/domain/RuleTraceCopy.kt:117–122` | The trace's evidence window is a placeholder in three builders: `forHint` stamps today..today although the evidence is the last finished session; `forStall` stamps `1L..0L`, so `evidenceLine` returns null; `forGeneration` stamps `0..0`. ADR-008 §2 lists "evidence window" as a required field of every trace. |
| B8 | DM-8 | B8 | P3 | validation gap | C | — | `P/domain/VolumeRamp.kt:18,31–38,64–72`, `P/domain/RecommendationEngine.kt:103–106,372–396`, `P/domain/MuscleLoadCalculator.kt:75–91` | The LOAD card counts a muscle's sets by primary muscle only over a trailing 7 civil days, while the map bands count junction-weighted, RPE-weighted sets — so "Chest ran 8 sets last week" can disagree with the map's number for chest — and it says "last week" although the engine's stated rule is that every time phrase reads "the last 14 days". |
| B8 | DM-9 | B8 | P3 | opportunity | C | — | `P/domain/TrainingCalendar.kt:145–147,161,181`, `P/domain/SessionSummary.kt:11–26` | `buildSummaries` (the History calendar's cheap path) writes `bodyweightReps = 0` because `SessionSummary` carries no rep total, so a calisthenics day's "work" reads empty there while the graph path (`build`, :51–113) carries reps; `HistoryScreen.kt:543` does the same. Documented as a limitation in `SessionSummary.homeWork`, not as debt. |
| B11 | UI-3 | B11 | P3 | architecture debt | C | 22 Sep W2b/W2d | `VM:1548-1557,715,371-377,648-676` | `primaryAction` is `stateIn(viewModelScope, Eagerly)` over `uiState`, so `uiState`'s `WhileSubscribed(5_000)` never idles: the 18-`combine` chain, including `exerciseRepository.observeAll()`, `search("")`, `observeLastLogged()` and `trainingInsights.observeShared()`, runs for the ViewModel's whole life (the rest page included); the KDoc at `:371-377` describing the projection as stopping five seconds after the screen stops collecting is now false. |
| B11 | UI-4 | B11 | P3 | defect | C | — | `Screen:250-258,312-318`, `ui/navigation/AppNav.kt:624-628`, `ui/workout/RestTimerCard.kt:97-105` | `logReceipt` and `personalRecord` are cleared only at the end of a dwell inside `LaunchedEffect`; opening the rest page or Details (a new destination disposes the floor) or an Activity recreation within the dwell cancels it, so on return the receipt is announced again and the chip re-marked "saved", and a record replays its haptic and gold banner. The rest card's `flashedTimerId` is a plain `remember` (`:100`), so the gold "Rest complete" flash also replays on every recomposition of the floor while `completedTimerId` is set. |
| B11 | UI-5 | B11 | P3 | defect (AX) | C | — | `ui/workout/WorkoutHeader.kt:128-138`, `VM:202,210-211` | The header's Finish sets `stateDescription = "Log a set to save."` whenever `!canFinish`, and `canFinish` is also false during a save or any mutation with sets already logged, so TalkBack gives the wrong reason for the disabled button. |
| B11 | UI-6 | B11 | P3 | defect (polish) | C | — | `Screen:845-849`, `ui/workout/LiftSwitcherSheet.kt:172-176`, `domain/LiftChipCopy.kt:22-42` | The switcher's row for the current lift names `lift.restSeconds` first and only falls back to the dock's planned length when the routine has none, so after a dock pick (ADR-012 §18) the row still says the routine's rest. |
| B11 | UI-7 | B11 | P3 | defect (polish) | C | — | `Screen:261-266` | `LaunchedEffect(state.loadState, state.selectedExerciseId)` runs on every new composition and `scrollToItem(0)`, overriding the position `rememberLazyListState` just restored, so a rotation or Activity recreation resets the floor's scroll to the top. |
| B11 | UI-8 | B11 | P3 | validation gap (AX) | R | — | `ui/workout/WorkoutDock.kt:288`, `ui/workout/WorkoutPrimaryAction.kt:21-33`, `VM:1583` | The commit is wrapped in `key(action.identity)` and the identity carries `draft` and `activation`, so the button node is recreated on every stepper tick and after every accepted tap; whether TalkBack/switch-access focus then leaves the commit after each log needs a phone. |
| B11 | UI-9 | B11 | P3 | architecture debt | C | — | `ui/workout/WorkoutSessionSummary.kt:28,32` | The session summary reads `System.currentTimeMillis()` directly instead of the app's `TimePort`, so its elapsed line cannot be driven in tests and bypasses `docs/architecture/time-seams.md`. |
| B12 | AX-3 | B12 | P3 | defect | C (tree) / R (TalkBack) | ADR-026 "Accessibility" (roles/groups) | `UI/components/ScreenHeader.kt:78-91`; `UI/components/GymSurfaces.kt:77-93, 307-314`; `UI/history/HistoryScreen.kt:411-417`; `UI/plan/PlanScreen.kt:640-651, 675` | Page titles are never headings unless `kickerTitle` (only 3 callers: `CustomWeekScreen.kt:122`, `RoutineEditorScreen.kt:391`, `OnboardingScreen.kt:188`), while `Kicker` defaults to `heading()` (`:82-86`, ~81 callers) and is composed inside nodes that merge descendants — `StatTile` (`:308-314`, 16 call sites on Summary, ExerciseDetail, ActivityDetail), History's readout card (`:415-417`), Plan's `RoutineRow` (`combinedClickable` `:644` + `Kicker(updatedLabel)` `:675`). Compose's default merge policy (`parentValue ?: childValue`) lifts `Heading` into the merged node, so in the merged tree every stat tile and routine row is a heading and the page title is not. Settings instance (from B10b): `SettingsHeader` is a plain `Text` (`UI/settings/SettingsChrome.kt:23-34`) and sub-pages are an in-place `page` state, not routes (`UI/settings/SettingsScreen.kt:175-179`), with no `heading()` or `paneTitle`, so opening Display/Reminders/Backup is never announced; `paneTitle` exists on only two surfaces app-wide (`RestTimerUi.kt:828`, `SavePostureChooser.kt:83`) |
| B12 | AX-4 | B12 | P3 | defect | C | ADR-026 "Accessibility" | `UI/components/LiftCard.kt:77`; `ExercisePickerSheet.kt:603`; `GymSurfaces.kt:469, 533`; `UI/history/TrainingCalendarCard.kt:241`; `UI/library/ExerciseLibraryScreen.kt:535`; `UI/routines/SessionLiftStrip.kt:254`; `UI/onboarding/OnboardingScreen.kt:309`; `UI/plan/PlanScreen.kt:644`; `UI/intro/ColdStartIntro.kt:94`; `UI/components/SetEntryPanel.kt:263` | Eleven live `clickable`/`combinedClickable` sites carry no `role` (and nine of them no `onClickLabel`), so TalkBack ends each with "double-tap to activate" and no "button": History lift card (`LiftCard:77`, live via `FilledLiftCard.kt:77`), "Create "x"" row, calendar trained day, "Edit lift"/"Delete lift" sheet rows, routine-editor lift card (has `selected` but no role), onboarding place toggles (multi-select with no toggle state — only a "Selected" caption when on, `:324-326`), routine rows (long-press label present, `:645`), the intro, the history set well. `SetTable.kt:112` is the twelfth but dead: no caller passes `onSelect` (only `FilledLiftCard.kt:130`, without it). `BodyMap.kt:144` is deliberate (`clearAndSetSemantics`, rows are the alternative, `:241-275`) |
| B12 | AX-5 | B12 | P3 | defect | C | ADR-026 (measured contrast) | `UI/components/ExercisePickerSheet.kt:383-388, 401-404`; `UI/theme/Color.kt:76, 89` | A selected picker row draws its "Selected" caption in `TextTertiary` on `VoltDim` over the sheet's `Surface3`; the composite measures 3.15:1 (WCAG AA needs 4.5:1 for 12 sp caption). Every other load-bearing pair measured clears AA: `TextTertiary` on Pit/S1/S2/S3 = 5.71/5.39/5.07/4.66; `TextSecondary`/S3 6.60; Volt on `VoltDim`-over-S2 10.27; `TextDisabled`/S2 3.23 (exempt by policy, `Contrast.kt:11-12`). `TextTertiary` on `SurfacePressed` = 4.16 would fail but no site was found |
| B12 | AX-6 | B12 | P3 | target gap | C | ADR-026 "Shared contracts" (gallery covers state and role variants); D15 | `app/src/debug/java/com/sinura/personaltrainer/ui/preview/ComponentStateGallery.kt:91-231`; `TabularNumeralTest.kt:52-68` | The gallery shows the primary button's five states, choice/toggle chips, one preset, one suggestion, `InstrumentRow`+switch, `Numeral`, `NumberEntryDialog`, `FieldComplaint`, `ScreenSkeleton`, `RestDurationSheet` and `GymDialog`. Not covered: `InstrumentChip` `recommended` and disabled (the D02 fix itself), `QuietButton` (accent/plain/disabled), `StepperButton` (hold, emphasis, inset plates), `WeekStrip` fills, `SetTable` (selected/editing/warm-up), `LiftCard` selected, `ExerciseRow` selected/cart, `TrendChart`, the four banners incl. `GymUndoHost` and `PersonalRecordBanner`, `RestSweepRing` running/urgent/finished, `RestSegments`, `InstrumentNavBar` 3+2 reflow, `LiveSessionBar` stale/error, `ScreenHeader` variants, `StatTile`, the three real workout dialogs (`EndWorkoutDialog`, `LeaveCardioDialog`, `ResumeOrDiscardDialog`), `ExercisePickerSheet`, `AddToRoutineSheet`, and a reduced-motion variant (only `FoundationStateGallery.kt:121-127` has one). The pinning test names 9 atoms |
| B12 | AX-7 | B12 | P3 | architecture debt | C | 22 Sep "every ceiling at 100 %" | `tools/checker-baselines.toml:35-61`; `tools/check-design-tokens.py:80-150, 178-195` | Every advisory family is exactly at its ceiling (numbers in §1), so the next packet that adds one `.dp` literal, one `tween(` or one stock icon fails the gate with no note of intent; and the checker has three blind spots: (a) `stock_icons` regex `Icons\.(Filled |
| B12 | AX-8 | B12 | P3 | defect | C | ADR-026 "Saved haptics follow writes"; ADR-012 (late Skip is a no-op, W2b-1) | `UI/components/RestTimerUi.kt:616-633, 743-750`; `UI/theme/Haptics.kt:50-53` | `RestControl` and `RestSegments` fire `Haptics.commit(view)` (documented as "a set landed, a workout finished") *before* `onClick()` when `confirm = true` (Skip), so the commit buzz plays on the press, not after the stop lands, and also on a late Skip that W2b-1/1b made a deliberate no-op |
| B12 | AX-9 | B12 | P3 | validation gap | C | ADR-026 "accessibility-adjusted undo timeouts"; UX26-AC02 | `UI/components/GymStatus.kt:153-169`; callers `UI/exercise/ExerciseDetailScreen.kt:150`, `UI/library/ExerciseLibraryScreen.kt:193`, `UI/settings/BackupRestoreSection.kt:131`; `UI/navigation/LiveSessionBar.kt:98-103`; `UI/workout/ActiveWorkoutScreen.kt:244-249`; `UI/workout/ActiveWorkoutViewModel.kt:2039-2043` | The floor's undo (`UndoDwell` + `systemUndoTimeout`, `UndoFeedback.kt:27-41`), the floor receipt and the live bar's error extend their 6 s under TalkBack; the three `GymStatusBanner` sites outside the floor and `SessionDetailScreen.kt:278` / `HomeScreen.kt:313` `GymUndoHost`s keep the 6 s base (default `dwellMs`, `:161, :212`) |
| B12 | AX-10 | B12 | P3 | validation gap | C (no live region) / R | AccessibilityMatrix "timer ticks remain silent"; ACC-02 | `UI/navigation/LiveSessionBar.kt:144-153, 187-207`; `UI/workout/RestTimerCard.kt:145-153, 203-209`; `UI/components/RestTimerUi.kt:546-550, 601-606` | No running clock is a live region (C). But the elapsed label sits inside the merged resume button, and the rest card's and sweep ring's `contentDescription` are rebuilt every second with the time in them; a node that has accessibility focus and whose description changes is re-read by TalkBack on content-change events |
| B12 | AX-11 | B12 | P3 | validation gap | C | AccessibilityMatrix claims (seam 9) | `domain/AccessibilityMatrix.kt:80-87`; `UI/settings/SettingsHome.kt:54-154`; `test/.../domain/AccessibilityMatrixTest.kt:44-47` | Home's note holds (`WeekStrip.kt:117, 140` filled-not-faint; `DayBlock.kt:70-71, 102` one button with controls inside; no "Get started"/Add row in `ui/home`), and the floor's note holds where checked (`WeightRepsEditor.kt:162-163, 407-410`; `ExerciseHeader.kt:226` traversal order; `WorkoutHeader.kt:194` decorative bar; `RestTimerCard.kt:206-208` finish announced once; VM `:2040` timeout). Settings' note names six destinations; the index has twelve rows (Update, Display, Reminders, Generator, Rest, Bodyweight, Save posture, Account, Backup, Plan, Diagnostics, About) and the test asserts the stale sentence verbatim |
| B12 | AX-12 | B12 | P3 | target gap (accepted) | C | ADR-023 §3 amended, "Consequences"; ADR-005 §5 | `UI/theme/Motion.kt:87-115`; the 13 `ModalBottomSheet(` sites (`RestTimerUi.kt:398` uses `rememberFullSheetState`; `AddToRoutineSheet.kt:53`, `ExercisePickerSheet.kt:132`, `history/SetEditSheet.kt:78`, `history/HistoryScreen.kt:509`, `library/ExerciseLibraryScreen.kt:506`, `library/ExerciseEditorSheet.kt:59`, `progress/ProgressScreen.kt:332`, `routines/RoutineEditorScreen.kt:566`, `home/HomeStartSheet.kt:74`, `workout/StartOptionsSheet.kt:125`, `workout/WorkoutSavedSets.kt:76`, `workout/LiftSwitcherSheet.kt:69` do not) | Twelve sheets still slide up under reduced motion. ADR-023's Consequences say exactly this ("The app's other twelve bottom sheets still slide in under reduced motion (24 September 2026). Each can take `rememberFullSheetState` in a later packet; none is changed without a rendered test"), so it is an accepted gap, not a violation — but no packet in `FRONTEND_REDESIGN.md` carries it. Every raw `tween(`/`infiniteRepeatable(` outside the theme is guarded: `RestTimerUi.kt:531-545`, `TrendChart.kt:252-263`, `GymStatus.kt:254-271`, `LiveSessionBar.kt:279-282` via `barMs = 0` (`AppNav.kt:323-326`), `ColdStartIntro.kt:150-165`. Dwell (`STATUS_DWELL_MS`) is correctly not collapsed |
| B12 | AX-13 | B12 | P3 | opportunity | C | 16 Sep "Items to reproduce" #6 (measure composited states); ADR-005 §5 hairline depth | `UI/theme/Color.kt:44-52`; `UI/theme/Contrast.kt:17-21, 46-47`; `ForeignControlsTest.kt:12-25` | Unselected control boundaries are `Hairline` (white 8 %): composited on `Surface2` that edge measures 1.24:1, and a `Surface2` chip/button on `Pit` 1.35:1; WCAG 1.4.11 asks 3:1 for boundaries that identify a control. Only `OutlineSolid` (3.75:1 on S2) is held at 3:1 by test. This is the Instrument identity by decision ("depth is light, not shadow"), and every control also has a text or glyph, but the decision is not written as a contrast exemption anywhere |
| B12 | AX-16 | B12 (from B6) | P3 here; B6 owns the render severity | defect | C (colours) / R (light shade) | ADR-005 §2 (colour through semantic roles) | `app/src/main/res/values/colors.xml:7-8`; `app/src/main/res/values-night/colors.xml`; `app/src/main/res/layout/notification_rest_running.xml:17, 26` and `_big.xml`; `timer/RestTimerNotifications.kt:141-166`; `UI/theme/Color.kt:72-73` | `rest_notification_text #F2F5F7` and `rest_notification_kicker #9BA7AE` are `TextPrimary` and `TextSecondary` hand-copied into XML (they are tokens by value, not by reference — RemoteViews cannot read `Color.kt`, so drift is unguarded); they are `xml_hex` entries 7 and 8 of the 12. The layouts set `textColor` explicitly and no background, and `DecoratedCustomViewStyle` (`:166`) draws them inside the system's template, whose background follows the phone's shade theme, not the app's dark-only theme. `values-night/colors.xml` mirrors only `window_background`, so there is no light/night pair for the one place in the app that is drawn on a surface the app does not own |
| B14 | BR-4 | B14 | P3 | architecture debt | C (PR state R) | FND-027 | `.github/dependabot.yml:9-13,27-76`, `docs/architecture/core-toolchain.md:18-20` | The ignore list omits `androidx.lifecycle:*` (2.11 refused: needs AGP ≥ 9.2), `androidx.activity` (1.13 deferred), `androidx.work`, `androidx.test*`, `io.github.jan-tennert.supabase:bom` and `io.ktor:*` (no signed matrix at all); with the `androidx` group at :9-11 every monthly group PR carries a refused bump and is closed unmerged (#114 pattern), so the patch/security updates riding in the same PR are frozen too. |
| B14 | BR-5 | B14 | P3 | architecture debt | C | — | `gradle/verification-metadata.xml` (785 components, 1,423 sha256, 0 pgp), `docs/architecture/lint-policy.md:43`, `tools/check-supply-chain.py:20-39` | The ledger carries checksums for generations the build does not use — supabase 3.2.4, ktor 3.1.3 and 3.3.0, kotlinx-serialization 1.9.0, `kotlin-serialization-compiler-plugin-embeddable` 2.0.21 and the serialization Gradle plugin dropped on 30 Aug (`core-toolchain.md:37-39`); the policy says the file "checksums the resolved graph" and the checker only tests presence, never surplus, so a catalog bump to one of those versions resolves without touching the ledger. |
| B14 | BR-6 | B14 | P3 | opportunity | C | — | `tools/cloud-setup-android.sh:33-38`, `.claude/hooks/session-start.sh:16,46,104-105`, `gradle/wrapper/gradle-wrapper.properties:4-6`, `docs/DEVELOPMENT.md:581` | Both cloud SDK bootstraps download `commandlinetools-linux-*_latest.zip` on TLS trust alone (the Windows script pins the archive's SHA-256), and they name different fallback builds (11076708 vs 13114758) although the hook says "keep the two in step"; the wrapper comment's "the one unpinned byte-stream" is true only of the Gradle graph. |
| B14 | BR-7 | B14 | P3 | defect | C | — | `.github/workflows/release.yml:213-216,223-225,178` vs `debug-live.yml:230-238` | `gh release upload … \ |
| B14 | BR-8 | B14 | P3 | validation gap | C | 22 Sep System 3 | `app/build.gradle.kts:396,405-409`, `docs/HANDOFF-NEXT.md:121-125`, `.github/workflows/ci.yml:84-85` | `lintDebug` is in the written gate and in the hosted job, but nothing makes `testDebugUnitTest assembleDebug` depend on it; in the container it runs by memory, and the hosted job that would catch a miss is not a required check (ADR-024, owner). |
| B14 | BR-9 | B14 | P3 | defect | C | — | `docs/RECOVERY.md:9-13,50-51,56-80,98`, `.github/workflows/debug-live.yml:78-105`, `SETUP.md:327-365` | The recovery runbook covers only the release keystore and says debug builds "need nothing from you", yet every install on the phone today is Temper Debug signed by the stable debug distribution keystore restored from write-only secrets (106 `debug-live-*` tags, 0 `v*` tags on origin); losing it means uninstall/reinstall from a backup plus a new Drive OAuth SHA-1, and `:98` "every release is tagged vX.Y.Z" has no instance yet. |
| B14 | DC-2 | B14 | P3 | defect | C | 22 Sep System 1 | `SETUP.md:70-74` vs `domain/AccountSyncGate.kt:23`, `docs/PRIVACY.md:39-44,58-60` | SETUP §4 says "When signed in and online, Temper copies finished workouts and plan rows to your Supabase project in the background" — sync is paused (`SYNC_PAUSED = true`) and live strength workouts were never replicated (S-6). |
| B14 | DC-3 | B14 | P3 | defect | C | 22 Sep System 1 | `.github/workflows/ci.yml:61-62,149`, `docs/architecture/CURRENT_STRUCTURE.md:160`, `docs/DEVELOPMENT.md:12,166,183-184,311-312`, `app/build.gradle.kts:87-88,97` | Counts and status comments disagree with the measured run: ci.yml "20 checkers, 4 fixture proofs" and "eighty instrumented tests"; CURRENT_STRUCTURE "8 fixture proofs"; DEVELOPMENT "twenty checkers today; its comment still says 17", "plain JVM tests … a few seconds", lint "existing ones live in the baseline", table "current as of 2026-09-06"; build script "P4.6 still owns cleaning the baseline to zero". Measured: 24 `check-*` and 9 `test_*` steps (`baseline/01-preflight-static.log`), 112 instrumented `@Test` (`CURRENT_STRUCTURE.md:23`), 3,149 JVM tests in 8m39s, baseline empty, P4.6 done (`FOUNDATION_PROGRAM.md:575`). |
| B14 | DC-4 | B14 | P3 | defect | C | — | `SETUP.md:7-396` headings vs `SETUP.md:158,173,217-219,359`; `docs/RECOVERY.md:76,88,90`; `docs/DRIVE_SIGNIN_CHECK.md:4,39,185`; `docs/DEVELOPMENT.md:50,66`; `README.md:30`; `debug-live.yml:17,89,158,216`; `release.yml:150,210` | Inserting "§4 Temper Account" shifted SETUP's numbering; 17 cross-references in 7 files now point one section off ("§4" for Drive OAuth = §5, "§6" for the debug signer/drop = §7, "§5"/"§5.3" for GitHub Release = §6). |
| B14 | DC-5 | B14 | P3 | defect | C | FND-042 | `.github/ISSUE_TEMPLATE/config.yml:4`, `.github/pull_request_template.md:7-9`, `tools/check-doc-authority.py:20-28`, `docs/HANDOFF-NEXT.md:119-135` | The issue template links `blob/main/docs/ROADMAP.md` (no `main` on origin; ROADMAP is superseded, `ROADMAP.md:3-5`), and the PR template's checklist (`testDebugUnitTest`, "ran on a device") is not the gate (static preflight, `testDebugUnitTest assembleDebug`, `lintDebug`, failing-test-first, two reviews, JVM renders); `.github/` is outside the doc-authority checker's file list. |
| B14 | DC-6 | B14 | P3 | defect | C | FND-041 | `README.md:43` vs `docs/foundation-audit/README.md:3-9` | README labels the foundation audit "canonical current-state audit" while the audit's own banner says it is "a dated audit, not a description of the app today". |
| B14 | DC-7 | B14 | P3 | defect | C | — | `docs/DEVELOPMENT.md:373-380,461-467` vs `:422-427`; `docs/DEVELOPMENT.md:552` vs `ADR-002:31` vs `git ls-remote` | DEVELOPMENT contradicts itself on Windows (the JVM lane "requires macOS or Linux" / "does not work here" vs "passed 2,536 tests on Windows; do not assume Linux is required"), and three branch-naming rules coexist (`codex/<slug>`, `cursor/<slug>-b87f`, actual `claude/*`). |
| B14 | DC-8 | B14 | P3 | defect | C | — | `docs/PRIVACY.md:68-69`, `docs/architecture/sync-personal-build.md:57`, `domain/SetLogRules.kt:17`, `domain/LibraryGrouping.kt:8,21`, `domain/ExerciseOrdering.kt:8` vs `domain/DefaultExercises.kt:62` (`CATALOG_VERSION = 10`, 143 `id = "ex-` rows), `docs/artifacts/catalog-v10-review.md:396` | The published privacy text ("~98 default lifts"), the sync record ("98 built-ins stay device-local") and three domain KDocs still say 98; the catalog is 143 lifts across 47 families (143 `ex_*` stills per `check-still-pack`). README does not carry the number; ROADMAP/DESIGN_AUDIT/HIERARCHY_PLAN say 98 under historical banners, which is fine. (B12 hand-off.) |
| B14 | PV-1 | B14 | P3 | defect | C | — | `docs/PRIVACY.md:117`, `SETUP.md:402` vs `ui/permissions/LaunchPermissionsHost.kt:30-33,90-96`, `domain/LaunchPermissions.kt:42-58`, `ui/navigation/AppNav.kt:352-360`, `ui/workout/RestNotificationGate.kt:29,77` | "Onboarding never asks for notification permission" is not what a first-run user meets: after the save-posture choice, on the first non-Settings tab, the first-open walk shows its own dialog and then launches `POST_NOTIFICATIONS` (API 33+), exact-alarm and battery steps; SETUP's "the first time you open an active workout" describes only the in-workout re-ask. B9a owns the walk; this row is the doc claim. |
| B14 | PV-2 | B14 | P3 | validation gap | C | FND-048 | `docs/COMMERCIAL_BOUNDARY.md:20-21`, `ADR-004:32-33` vs `tools/check-commercial-boundary.py:20-48`, `data/backup/DriveHttp.kt:5-6`, `NetworkChecker.kt:4-5`, `DriveAuthClient.kt:5` | The doc says the checker fails on "billing, ads, analytics, or account SDKs" and the ADR says it forbids "billing, account, and network dependencies" from the core; the script bans billing/ads/analytics vendors only (no supabase, ktor, gms.auth or `java.net` pattern), and `data/backup`, a listed core package, legitimately imports `java.net`, `android.net` and gms auth — so the ADR's literal rule is unenforceable as written and the doc overstates the guard. The core is not gated anywhere (0 findings; `domain`/`workout`/`activity`/`insights`/`data/repository` import none of them). |
| B14 | PV-3 | B14 | P3 | defect | C | — | `docs/DATA_SAFETY.md:38` vs `SETUP.md:389`, `README.md:95-100`, `docs/architecture/drive-auth.md:9-11`, ledger `play-services-auth` 21.6.0 | "No Google Sign-In in v1" is true of Temper Account (email + password) but the APK ships `play-services-auth` and Settings offers "Sign in with Google" for Drive via `AuthorizationClient`; a Play reviewer reading the form against the APK sees a contradiction. |
| B14 | BR-10 | B14 | P3 | validation gap | C (RFN status R) | — | `app/src/main/res/font/{inter_regular,inter_medium,inter_semibold,space_grotesk_medium,space_grotesk_bold}.ttf` name tables; `tools/build-fonts.py:22-26,83,90-98`; no `OFL*` file under the repo (`find . -iname '*OFL*'` → none) | The five bundled faces are subset instances of Google Fonts' OFL `inter/` and `spacegrotesk/` sources (`build-fonts.py:22-26`); no licence text ships beside them or under `docs/`. Each TTF does keep name ID 0 (copyright), 13 ("licensed under the SIL Open Font License, Version 1.1 … available … at https://openfontlicense.org" / `scripts.sil.org/OFL`) and 14 (URL) because the subsetter passes `--name-IDs=*` (`:96`) — the machine-readable form OFL §2 allows — but that is the notice with a link, not the licence itself, and `updateFontNames=False` (`:83`) leaves the Space Grotesk instances named "Space Grotesk Light" (cosmetic). Whether either family declares a Reserved Font Name, which would constrain a modified version's name, needs the upstream `OFL.txt` (R). (B12 hand-off.) |
| B14 | DC-9 | B14 | P3 | defect | C (diff per B8) | — | `docs/architecture/ADR-029-coach-engine.md:30-32`, `domain/coach/EvidenceCatalog.kt:6`, `docs/coach/evidence-seed.json`, `docs/ROADMAP.md:200` | ADR-029 §2 names `docs/coach/evidence-seed.json` as the "human mirror" of `EvidenceCatalog`, and the KDoc says "keep in sync", but no code, test or tool reads the file (`grep -rn evidence-seed` over `app/`, `tools/`, `*.kts` → only that KDoc); B8 found it differs from `CoachPolicyEvidence` on 10 of 17 reason codes. An accepted ADR (authority layer 2) promises a mirror that nothing holds true. (B8 hand-off.) |
| B4a | DB-1 | B4a | P3 | architecture debt | C | S-1 | `P/data/local/dao/RoutineDao.kt:29-30`, `WorkoutDao.kt:160-161`, `ExerciseDao.kt:91-92`, `ActivityDao.kt:196-197,215-216`, `P/data/repository/RoutineRepository.kt:48`, `WorkoutRepository.kt:431`, `ActivityBackupIo.kt:43,54`, `LocalBackupRepository.kt:376,395` | Five `@Insert(onConflict = REPLACE)` writes target parents whose children cascade (`routines` → `routine_exercises`, `schedule_slots`, `workout_sessions.routineId` SET NULL; `workout_sessions` → `session_exercises`, `set_logs`; `activity_templates`/`activity_blocks` → blocks, sets, intervals; `exercises` → `exercise_muscles` and three RESTRICT children); each is safe today only because its caller mints a fresh id or has just emptied the table, and nothing pins that. |
| B4a | DB-2 | B4a | P3 | architecture debt | C | — | `P/data/local/dao/RoutineDao.kt:46-47`, `P/data/repository/RoutineRepository.kt:138,156` | `swapExercise` and `updateExercise` use `upsertRoutineExercise` (REPLACE) as an update of an existing row: SQLite deletes and re-inserts it; harmless only because nothing references `routine_exercises`. |
| B4a | DB-3 | B4a | P3 | architecture debt | C | S-1 | `P/data/repository/ActivityRepository.kt:342-350`, `P/ui/activity/LiveCardioViewModel.kt:177-198` | `completeLive` finishes a live activity by `deleteSession` (cascading its blocks, sets and intervals) then re-inserting the session from memory with `blocks ?: session.blocks`; the ViewModel passes `listOf(block)` built from the first cardio block only, so any other block a live activity ever carries would be dropped on finish. Safe today because live activities are cardio-only (`:319-321`). |
| B4a | DB-4 | B4a | P3 | validation gap | C | — | `P/data/backup/BackupValidator.kt:304-332`, `P/data/repository/ActivityBackupIo.kt:28-66`, `P/data/local/dao/ActivityDao.kt:196-200` | The validator checks ids for activities and their blocks (`:310,330`) but nothing at all for `activityTemplates` (grep "template" in the file: no hits), while restore inserts templates with `upsertTemplate` (REPLACE) and their blocks with `insertBlock` (REPLACE); a duplicated template id or a template block id equal to a session block id would silently lose blocks instead of rolling back like every other bad row. An ABORT `insertBlocks` exists unused (`:199-200`). |
| B4a | DB-5 | B4a | P3 | defect (latent) | C | — | `P/data/repository/ExerciseRepository.kt:210-225`, `P/data/local/dao/ExerciseDao.kt:88-89`, `P/ui/library/ExerciseLibraryViewModel.kt:339-358` | `deleteCustom` runs the usage check (`usageFor`, three counts) and the `DELETE` outside any transaction (`writeExercise` is used by create/update at `:150,183` but not here), so a set logged between the check and the delete makes the RESTRICT foreign key throw `SQLiteConstraintException` out of the repository; `confirmDelete` has no catch around it. The check also ignores the two soft references (`measurable_goals.exerciseId`, `activity_blocks.exerciseId`), both harmless by design (snapshot columns, `ActivityDao.kt:76-104`, `GoalEntity.kt:16`). |
| B4a | DB-6 | B4a | P3 | validation gap | C | — | `P/data/local/TemperMigrations.kt:167-172,233-235`, `P/data/local/entity/HistoryEntities.kt:7-14`, `app/schemas/com.sinura.personaltrainer.data.local.TemperDatabase/4.json` (bodyweight fields carry no `defaultValue`) | Two ways a migrated phone differs from a fresh install: v3→4 adds `zoneId … DEFAULT 'UTC'` and `offsetSeconds … DEFAULT 0` that the entity never declares (Room tolerates a DB-side default the entity lacks, so `TemperMigration3To4Test` passes), and v5→6 seeds a `sync_metadata` row that fresh installs never get (readers are null-safe: `P/data/sync/SyncEngine.kt:56`, `SyncCoordinator.kt:25,43`). Later migration tests build from `N.json`, so they never run against the migrated DDL. |
| B4a | DB-7 | B4a | P3 | validation gap | C | 22 Sep System | `T/data/local/TemperSchemaAssetsTest.kt:33-40`, `T/data/local/TemperMigration6To7Test.kt:66-81`, `app/src/androidTest/…/TemperMigrationDeviceTest.kt:34-44` | The guard checks that `TemperMigration{N-1}To{N}Test.kt` exists by name only, not that it calls `runMigrationsAndValidate` for that version; and the only full v1→v7 chain runs in the non-blocking device lane (JVM chains v4→v7). |
| B4a | DB-8 | B4a | P3 | opportunity | C | — | `P/data/local/entity/SetLogEntity.kt:24`, `WorkoutSessionEntity.kt:18`, `P/data/local/dao/WorkoutDao.kt:23,43,119,123,270-275,337-345` | `set_logs` (the fastest-growing table) is indexed on `sessionId` and `exerciseId` only; `observeFinishedWorkingSets` orders by `completedAt` and `recordPriorsBefore` ranges on `(completedAt, setNumber)` after the `exerciseId` filter — a composite `(exerciseId, completedAt)` serves both. `workout_sessions.date` has no index but every history read orders or ranges on it (`:23,43,119,123`). |
| B4a | DB-9 | B4a | P3 | opportunity | C | — | `P/data/local/entity/ActivityEntities.kt:8-14,66`, `P/data/local/dao/ActivityDao.kt:41,70,98,128,150` | `activity_sessions.status` is unindexed yet every completed-history query filters `status = 'COMPLETED'`; `observeCompletedGraphsSince` also ranges on `performedStartInstantMs` while the index is on `performedStartLocalEpochDay`; `activity_blocks.exerciseId` is unindexed but `observeFinishedWorkingSets(exerciseId)` filters on it. |
| B4a | DB-10 | B4a | P3 | opportunity | C | — | `P/data/local/dao/PlannerDao.kt:40-43,89-90`, `P/ui/plan/PlanDayViewModel.kt:58`, `P/ui/plan/PlanViewModel.kt:169`, `P/ui/home/HomeViewModel.kt:108`, `P/ui/workout/StartOptionsViewModel.kt:118`, `P/data/repository/PlannerRepository.kt:614-628` | `observeOccurrences()` streams the whole `schedule_occurrences` table (every planned day ever generated) to four ViewModels, including Home, and re-emits on any occurrence write, although a bounded `getOccurrencesBetween` exists (`:50-54`); `getDeliveries()` reads all `reminder_deliveries` ordered by an unindexed `scheduledAtMs` on every `rebuildReminders`, and delivery rows are never pruned except by occurrence cascade. Growth is ~rules × 52 per year, so small. |
| B4a | DB-11 | B4a | P3 | opportunity | C | W2a | `P/data/local/dao/WorkoutDao.kt:22-24,386-398`, `P/data/repository/WorkoutRepository.kt:130-135,179-181`, `P/data/local/dao/ActivityDao.kt:199-200`, `P/data/repository/FoundationReset.kt:30-98` | Dead data-layer code that W2a's UI-scoped pass left: `WorkoutDao.observeFinishedSessions` and its wrappers `observeHistoryHealth`/`observeHistory`, `WorkoutDao.observeBestWorkingWeights` and its wrapper (no caller in `main` or `test`); `ActivityDao.insertBlocks`; and `FoundationReset` (no constructor call in `main`; it also opens Room with no `addMigrations`, `:35-41`, which would throw on an older file). |
| B4a | DB-12 | B4a | P3 | opportunity | C+R | ADR-010 §3 | `P/data/repository/FoundationReset.kt:96`, `P/data/local/PreMigrationSnapshot.kt:80,103-114`, `P/data/local/FoundationGeneration.kt:18` | The only code that deletes the legacy `personal_trainer.db` is the unreachable `FoundationReset` (DB-11) and no other `deleteDatabase(` call exists in `main`, so a phone that still carries the legacy file, its WAL/SHM and the `files/pre-migration/v1/` copy keeps them forever. Whether the owner's phones still have them is R. |
| B4a | DB-13 | B4a | P3 | validation gap | C | — | `T/data/local/ScheduleSlotCascadeTest.kt:31-38`, `RoutineSwapTest.kt:30-38`, `CatalogQueriesTest.kt`, `DbMaintenanceTest.kt`, `OnboardingApplierTest.kt`, `SessionNotesWriteTest.kt`, `SessionRepairRepositoryTest.kt`, `BackupV2RoundTripTest.kt` (each `TrainerDatabase::class`) | Eight data-layer tests build the legacy `TrainerDatabase` in memory rather than the production `TemperDatabase`; the shared entities make them valid for the shared tables, but none can reach `activityDao`, `plannerDao`, `goalDao` or `syncDao`, and the cascade proof for routine deletion runs against the database the app no longer opens. |
| B4a | DB-14 | B4a | P3 | validation gap | C | CURRENT_STRUCTURE "soft FKs" | `P/data/repository/PlannerRepository.kt:257-275`, `P/domain/SchedulePlannerModels.kt:128-143`, `P/data/repository/ActivityRepository.kt:248-262`, `P/data/local/dao/ActivityDao.kt:76-104` | Orphan inventory for the seven soft keys (see notes): none crashes and each has a fallback, but no test pins any of the fallbacks (a disabled rule whose routine is gone reads "Strength"; a DONE day whose activity is deleted keeps DONE; a deleted lift's activity block shows its snapshot name). |
| B4b | DB-3 | B4b | P3 | defect | C | — | `data/repository/WorkoutRepository.kt:1020-1035,1153,1192`; `data/repository/ExerciseRepository.kt:464-475`; `ui/library/ExerciseLibraryViewModel.kt:272-278` | `loadClassByExercise` is a plain `mutableMapOf` cache that is never invalidated and not synchronised; `updateCustom(loadType=…)` (a Library edit) or a restore changes a lift's class, but `recordsBrokenBy` (PR badge at log time) and `lastPerformance` (the floor's Last cell) keep the old class until the process restarts, and two first reads on Room's IO threads can race the map. |
| B4b | DB-4 | B4b | P3 | defect | C | — | `workout/FinishWorkout.kt:58-62`; `PendingOccurrence.kt:66-72`; `data/repository/PlannerRepository.kt:354-365`; `ui/workout/ActiveWorkoutViewModel.kt:2139-2141`; `ui/navigation/LiveSessionBarViewModel.kt:216-221,231` | A strength finish is two stores in sequence: `finishSession` (one UPDATE) commits, then each caller separately runs `PendingOccurrence.complete` → `markOccurrenceDone` (three DAO writes, no transaction) → DataStore clear; a process death between them leaves the workout finished and its plan row PLANNED, with the binding armed for a finished session that nothing will complete. Cardio does the DONE write inside the confirm transaction (`ActivityRepository.kt:183-195,351-362`). |
| B4b | DB-5 | B4b | P3 | architecture debt | C | — | `data/repository/PlannerRepository.kt:142,146,185,190,262,302,349,630-671` vs `511-551,564-589,691-713` | `ReminderSideEffects` defers WorkManager and NotificationManager calls until after commit in `moveOccurrenceForward` and `moveOccurrenceToDay`, but `setRuleHour`, `applyDayOrder`, `retireOrDeleteRuleLocked` (via `removeTimedRule`/`onRoutineDeleted`), `ensureWeek` and `applyMissedWork` still call `scheduler.schedule/cancel` and `ReminderNotifications.cancel` inside `withTransaction`; on a rollback the OS-side job state diverges from the rows (a cancelled job for a delivery that still exists). `ActivityRepository.kt:55-58` states the rule these five sites break. |
| B4b | DB-6 | B4b | P3 | defect | C | S-3 | `data/repository/RoutineRepository.kt:66-77`; `data/repository/ExerciseRepository.kt:490-499`; `data/repository/GoalRepository.kt:389-395` | Three delete paths enqueue the sync tombstone before the delete statement runs, outside any transaction; if the delete throws (`deleteCustom` after a set lands between `usageFor` and the delete hits `set_logs`' RESTRICT key, `SetLogEntity.kt:17-21`), the queue carries a delete for a row that still exists. |
| B4b | DB-7 | B4b | P3 | target gap | C | — | `data/repository/WorkoutRepository.kt:138-141,1314-1327`; `docs/architecture/ADR-011-time-semantics.md` §2, Consequences, review Q1 | Strength sessions store no captured zone, so `observeSessionSummaries` derives `localEpochDay` from the device zone at read (`JvmTime.civilDate(date)`), which ADR-011 §2 forbids and whose review answer ("does the session move days? No.") is false for the strength store; the code comment calls it "the documented leftover", but no ADR or the 22 Sep record names it. |
| B4b | DB-8 | B4b | P3 | defect | C | — | `data/repository/WorkoutRepository.kt:781-823,831-851` | `updateSet` reads the row and session, validates, then writes the whole row back outside a transaction; a `deleteSet`+`renumber` that lands in between has its new `setNumber` overwritten by the stale copy, leaving two sets with one number in a lift. The removal path was made atomic for this reason (`498-513`); the edit path was not. |
| B4b | DB-9 | B4b | P3 | defect | C | — | `data/repository/PreferencesRepository.kt:461-465,479-515` | `recordBodyweight` writes Room (upsert, trim, hook) and then DataStore in two separate steps; the KDoc still says it "writes both in one edit so they cannot disagree", which was true before the Room mirror; a crash between leaves `bodyweightKg` (DataStore) behind `bodyweightLog` (Room). |
| B4b | DB-10 | B4b | P3 | validation gap | C | — | `data/repository/OnboardingApplier.kt:102-118,153-170`; `app/src/test/…/data/local/OnboardingApplierTest.kt:196` | `apply` commits the routines-and-pins transaction, then writes `onboardingComplete` as a separate DataStore edit; a process death between them leaves a full program behind a false flag, so the questionnaire runs again and a re-run "adds alongside and never deletes" (class comment, 34-38). `aFailedApplyLeavesNoRoutinesForTheRetryToDuplicate` pins a failure inside the transaction only. `hasExistingProgram()` (272-273) lets the screen warn, which is the only mitigation. |
| B4b | DB-11 | B4b | P3 | defect | C | — | `data/repository/DayBlocks.kt:77-89,214-223`; `data/repository/AuxiliaryBlocks.kt:295-306`; `PersonalTrainerApp.kt:60-69` | Home/Plan block minting is a sequence of separate repository calls with no transaction: pin → `syncSlotsToRules` → `setRuleHour` → publish, or `addTimedRule` → publish → `setRuleEnabled(false)` for "just today"; a crash after publish and before the disable turns a one-off block into a weekly rule, and a crash after `pin` leaves a slot with no rule until the next Plan publish, because app start runs `ensureWeek` only. |
| B4b | DB-12 | B4b | P3 | architecture debt | C | — | `data/repository/ActivityRepository.kt:92-108`; `data/repository/FlowGuards.kt:255-280`; `grep observeLiveHealth ui/` → no caller | After one read fault `observeHealth` emits `Unavailable` and completes, so the `shareIn(WhileSubscribed)` live-activity flow stays on that value until all six subscribers (Home, bar, sheet, Plan, backup) drop; `presentValues` hides it, and no screen reads `observeLiveHealth`, so a live cardio session can vanish from Home and the bar with no message until the app is left. The unique `liveToken` (`ActivityEntities.kt:12`) still blocks a second start. |
| B4b | DB-13 | B4b | P3 | target gap | C | — | `data/repository/RoutineRepository.kt:53-63,80-116,144-172,174-184,209-212` | `updateDetails`, `addExercise`, `updateExercise` and `removeExercise` return `Unit` and silently return when the routine or item is gone, so the editor cannot tell "landed" from "refused" (ADR-021 §7: Save "leaves only when every write it is responsible for landed"); only `swapExercise` reports a refusal. `touch` and `updateDetails` also rewrite the whole routine row from a stale read, the lost-update shape `WorkoutDao.kt:166-179` documents for sessions. |
| B4b | DB-14 | B4b | P3 | defect | C | — | `data/mapper/ActivityMappers.kt:79-81,101-102,109,335-337,363-364,381`; `data/backup/BackupValidator.kt:313-341`; `app/src/test/…/data/mapper/MapperContractTest.kt:412,448` | The activity mappers use `valueOf` (throws) for status, origin, source, load type, equipment and cardio type, while every other mapper falls back; today only the backup validator (313-341) and the paused sync stand between an unknown token and a thrown `getSessionGraph().toDomain()`, which would fail `ActivityRepository.get`, `all()` and therefore backup export (`ActivityBackupIo.snapshot`). `MapperContractTest` pins the unknown-kind case only. |
| B4b | DB-15 | B4b | P3 | architecture debt | C | — | `timer/CardioTimerPersistence.kt:46-53,69`; `workout/CompleteTraining.kt:65`; `workout/StartLiveCardio.kt:56`; `ui/activity/LiveCardioViewModel.kt:298,322`; `ui/navigation/LiveSessionBarViewModel.kt:258`; `ui/workout/StartOptionsViewModel.kt:331` | `SharedPrefsCardioTimerPersistence.save/clear` use `commit()` (synchronous disk write) and every caller runs on `viewModelScope` with no `withContext(IO)`; `RestTimerController` takes an `ioDispatcher` for the same shape, `CompleteTraining` and `StartLiveCardio` do not. No `runBlocking` anywhere in `main` (confirmed by grep); this is the one main-thread write in scope. |
| B4b | DB-16 | B4b | P3 | opportunity | C | — | `insights/TrainingInsightsSource.kt:161,173-181,210` | `windowStart = nowMs() - WINDOW_MS` is captured once when the shared pipeline is first built and reused for the process lifetime, so the two `…Since(windowStart)` queries fetch a window that grows with uptime (line 180 re-filters, so numbers stay right); "today" at 210 moves only when an upstream emits. |
| B9a | UI-2 | B9a | P3 | architecture debt | C | ADR-021 §3 (13 Sep), W2a | `ui/home/HomeViewModel.kt:225-238,558-614,635-647,671-676`, `ui/home/HomeScreen.kt:80,96-100`, `ui/navigation/AppNav.kt:514`, `app/src/test/.../ui/home/HomeViewModelTest.kt:508-591` | `addDaySession`, `HomeDayAdd`, `requestWeekSuggestion`, `requestAnswerReplay`, the `navigateToEditor` flow and Home's `onOpenRoutine` wiring have no caller in `app/src/main` (grep); four tests keep them compiling. ~90 lines W2a missed. |
| B9a | UI-3 | B9a | P3 | validation gap | C (measure) / R (result) | Shared contract "reflow 3 + 2" | `ui/navigation/AppNav.kt:808-829,907-913`, `app/src/androidTest/.../FrontendShellInstrumentedTest.kt:138-164,208-222` | The bar reflows 3 + 2 when a label plus 8 dp exceeds a fifth of the width (:821-824); by glyph estimate that is already at font ≈ 1.3 on 360 dp. In the 3-wide row a label gets 112 dp (:911) and "SETTINGS" at 2.0 measures ≈ 116 dp, so a mid-word break is plausible; the only pin (`lineCount == 1`, 3 + 2, 48 dp floors, at 360×640 font 2.0) runs on the hosted non-blocking lane. No JVM render covers the shell. |
| B9a | L-4 | B9a | P3 | validation gap | C | — | `ui/permissions/LaunchPermissionsHost.kt:39-40,56-61`, `ui/settings/SettingsViewModel.kt:156-160,171-184,198-204`, `ui/navigation/AppNav.kt:353-364` | `launchPermissionsAsked` starts `false` with no loaded flag; the host's `LaunchedEffect(alreadyAsked)` sets `walking = true` and persists "asked" on the first `false` it sees. Today `postureChosen` (gated on `loaded`, which flips after two DataStore round-trips) lands later than the one read `launchAsked` needs, so the order holds — but nothing pins it, and a reorder re-runs the walk on every cold start for anyone who tapped "Not now". |
| B9a | UI-4 | B9a | P3 | defect | C | 22 Sep "process death" row | `ui/navigation/LiveSessionBarViewModel.kt:160-168,229-236` | A finish confirmed on the bar writes the session (`Written`) and completes the plan row, then holds the summary route only in `_finishedNavigation`; process death between the write and the `LaunchedEffect` (`AppNav.kt:381-388`) drops the summary. Same for `HomeViewModel.undoableSkip` (:94-95): the Skip stands, the undo offer vanishes. |
| B9a | UI-5 | B9a | P3 | validation gap | C | 22 Sep "10 of 21 ViewModels" | `ui/home/HomeScreen.kt:115,158,165,236`, `ui/home/DailyAgendaCard.kt:77`, `ui/home/ThisWeekCard.kt:67`, `ui/progress/ProgressScreen.kt:79-80`, `ui/progress/ProgressViewModel.kt:109-111`, `ui/navigation/AppNav.kt:352,441`, `ui/navigation/LiveSessionBar.kt:96-97`, `ui/workout/StartOptionsSheet.kt:95,98-100`, `ui/workout/StartOptionsViewModel.kt:195-199`, `ui/permissions/LaunchPermissionsHost.kt:39,71-73` | Inventory for the five no-`SavedStateHandle` screens: selected day, `lastToday`, start sheet, weigh-in dialog, pending confirm, fallback confirm, body view, selected muscle, Body/History/Plan start sheet, discard confirms, walk-showing and skipped-step flags are all `rememberSaveable`; the heat window is in DataStore; `menuOpen` (bar) is `remember` only; VM-only losses are the one-shot nav flows and undo offer (UI-4), plus `blockedByInProgress`, `actionError`, `reviewOccurrenceId`, `focusEpochDay`, which are harmless. `StartOptionsSheet` clears its nav flows on dispose so a stale value cannot fire on the next open. Nothing pins any of this. |
| B9a | L-5 | B9a | P3 | defect | C | ADR-023 §3 | `ui/intro/ColdStartIntro.kt:70-88`, `domain/ColdStartIntroCopy.kt:10`, `MainActivity.kt:83-92` | Under reduced motion `LaunchedEffect(reduceMotion)` calls `finishOnce()` on the first frame, so the intro is composed for one frame (a flash), fires `Haptics.tickLight` with no user action, and skips the 2,000 ms dwell; ADR-023 says animation collapses and dwell stays, which means either show it at rest for the dwell or do not compose it. `rememberInfiniteTransition` is correctly avoided (:156-165). |
| B9a | UI-6 | B9a | P3 | opportunity | C / R | Body contract "stale request never shows old totals" | `ui/progress/ProgressViewModel.kt:67-102,86`, `ui/progress/ProgressScreen.kt:92-96,306-315` | `window = insights.snapshot?.window ?: stored` keeps the chip paired with the snapshot, so old totals never sit under a new label (contract met) — but after a tap the selected chip stays on the old window until the recompute lands, with no busy state; how long is `R`. |
| B9a | UI-7 | B9a | P3 | architecture debt | C | — | `ui/navigation/AppNav.kt:164,187-195,526-533`, `app/src/test/.../ShippingTabsTest.kt:42` | The Library composable is registered as the literal `"library?muscle={muscle}"` while `Route.Library.path` is `"library"`; navigation works because the query arg is optional, but `Tab.matchPattern`'s "plain equality" doc no longer describes this route, and `ShippingTabsTest:42` asserts `routeAllowsPermissionWalk(Route.Library.path)` against a string the graph never reports, so it passes vacuously. |
| B9a | UI-8 | B9a | P3 | defect | C | Shared contract "state restoration" | `ui/navigation/AppNav.kt:408-418,494-516` | `goToTab` pops to the start destination (Home) with `saveState`; Navigation keys the saved stack by the first popped entry, which for Home is the pushed screen, so returning to `home` with `restoreState` finds nothing — Home's pushed screens are lost on a tab switch while Body/Plan/History's come back. |
| B9a | UI-9 | B9a | P3 | validation gap | R | 16 Sep acceptance "TalkBack complete journeys" | `ui/navigation/AppNav.kt:802,813,885-889,905,909`, `ui/navigation/LiveSessionBar.kt:130,144-153,187-192`, `ui/components/WeekStrip.kt:118-126,141-161` | Tab labels are `uppercase()` in the text itself with no `contentDescription` (icon is `null`), the role is `Role.Tab` in a `selectableGroup` — what TalkBack says for "PLAN" (word or letters) and whether it announces "n of 5" needs a phone. The bar's clock changes every second inside the merged clickable node; the only live region is the error line (:130), so no flood by design, but a focused bar may re-read each tick. Week cells set a `contentDescription` on a node that also has child text — the double-announce shape W1a fixed on chips. |
| B9a | UI-10 | B9a | P3 | defect | C | 22 Sep "two start sheets" | `ui/workout/StartOptionsViewModel.kt:209-218`, `ui/home/HomeStartSheet.kt:212-222`, `ui/activity/LiveCardioScreen.kt:179` | "Start cardio" on Body/History/Plan starts `CardioType.RUN` titled "Cardio" with no type pick; Home's sheet asks the type. The live screen lets you change it afterwards, so it is a wrong default, not a trap. |
| B9a | UI-11 | B9a | P3 | defect | C | — | `ui/navigation/AppNav.kt:296-299`, `MainActivity.kt:63-65,83-92`, `PersonalTrainerApp.kt:35-41` | `UNKNOWN` returns nothing until DataStore's first emission, with no timeout; on a cold start the intro (2 s) hides it, but a process-death restore is a new process too (`coldStartIntroPending` is per process), so the branded intro replays after every background kill and is what hides the blank frame. |
| B9a | UI-13 | B9a | P3 | defect | C | raised by B9b (Plan has the same shape) | `ui/home/HomeViewModel.kt:140-144,146,169,391-407`, `ui/home/HomeScreen.kt:320-346`, `domain/TrainingInsights.kt:192-210`, `domain/MissedWorkPolicy.kt:12-25`, `ui/plan/MissedWorkCard.kt:30-36` | `applyMissedWork` returns silently when `uiState.value.weekPlan` is null (:393), before the `runCatching` that would set `actionError`; but `weekPlan` is null whenever the plan stage failed (`recoverStage` → null, `failures += InsightFailure.PLAN`), and in that state Home still derives `weekStart` from preferences (:140-144), computes `overdue` from occurrences (:146) and shows the card (:169), so all four buttons (Move remaining / Adapt / Keep dates / Skip missed) do nothing: no write, no banner, the card stays. `HomeUiState.weekStartEpochDay` (:183) already holds the derived start, so the early return is unnecessary. No data can be lost. |
| B9b | UI-4 | B9b | P3 | architecture debt | C | 22 Sep Plan row d (extends) | `UI/plan/PlanViewModel.kt:108,165-175,201,248-251,253-271,295-319,360-459,461-463,575-580,608-632,658`, `UI/plan/PlanScreen.kt:312-314,318-319,385,415-429,577-587,694-804`, `ui/home/HomeViewModel.kt:635-637,645-647`, `AppDependencies.kt:79` | The whole proposal flow is unreachable: `HomeViewModel.requestWeekSuggestion`/`requestAnswerReplay` have no caller in `main`, so `pendingWeekSuggestion`/`pendingAnswerReplay` are never true, `suggestFills`/`replayStoredAnswers`/`acceptFills`/`dismissFills` never run, `proposals` is always empty, the quiet-Add-session branch and `WeekStrip(proposals=)` never fire, and `PlanRecoveryCommands`/`PlanLighterChip` are never composed; eleven more `PlanViewModel` functions (`agendaFor`, `pinFocus`, `unpin`, `swapRoutine`, `addMorningCardio`, `removeTimedRule`, `setTrainingDays`, `setWeekStart`, `setLighterWeek`, `setSessionHour`, `onErrorShown`) and `PlanDayViewModel.agendaFor` (`:82-85`) are called only by tests. ADR-017 §4 (docs/architecture/ADR-017-home-week-board.md:79-81) still says Lighter is a Plan chip and Suggest/Replay/Use this week "remain recovery"; in code Lighter is set from Body (`ui/progress/ProgressViewModel.kt:118-131`). |
| B9b | UI-5 | B9b | P3 | defect | C | — | `UI/plan/PlanViewModel.kt:183-192,217-227,582-596`, `UI/plan/PlanScreen.kt:336-338,351-377`, `domain/TrainingInsights.kt:192-210`, `domain/StageRecovery.kt:32-42` | `missedWorkPrompt` is computed from planner rows with a week-start fallback, but `applyMissedWork` returns silently when `uiState.week` (the insights `weekPlan`) is null — which is exactly the state where the "Couldn't read this week's plan" banner shows — so Keep/Move/Adapt/Skip all do nothing, no error, card stays. (Home has the same shape, `HomeViewModel.kt:391-393`, see §4.) |
| B9b | UI-6 | B9b | P3 | architecture debt | C | — | `UI/routines/SessionLiftStrip.kt:516-528` (`:523`), `:697-781`, `UI/components/NumberEntryDialog.kt:45,116`, `domain/TargetStepper.kt:18-25`, `UI/routines/RoutineEditorViewModel.kt:62-73,416-441,465-487,544-555,1277-1282`, `UI/routines/CustomWeekViewModel.kt:94-100,319-337,357-362,403-434` | The card's only entry surfaces are steppers that clamp at ≥1 and `NumberEntryDialog`, whose Set is disabled until the text parses, and `persist` always hands `invalidReason = null`; so the "unreadable box" refusal family (`ERR_TARGET_RULE`, `invalidReason`, `forgetTargetRule`, `invalidTargets`, the `DisposableEffect` at `:389-391`) and `writeTargets`' Rejected branch cannot be entered from the shipped UI, yet ~170 ViewModel lines and 13 tests (`RoutineEditorViewModelTest.kt:1118-1308`, `CustomWeekViewModelTest.kt:367-615`) keep them alive. |
| B9b | UI-7 | B9b | P3 | validation gap | C | 22 Sep cross-tab | `UI/routines/RoutineEditorViewModel.kt:911-925,1449-1454,1485-1490`, `UI/routines/RoutineEditorScreen.kt:161-170,281-289` | Paste-import results (`unmatched`, `createdFromPaste`) are in-memory `PasteUi`; after process death the "couldn't match these lines" block and the "also created" caption vanish while the routine keeps the matched lifts, so the user loses the list of what did not import. |
| B9b | UI-8 | B9b | P3 | target gap | C | — | `UI/plan/PlanDayViewModel.kt:36-43,57-76`, `UI/plan/PlanDayScreen.kt:103-114` | `PlanDayUiState` has loading, empty and write-error states but no read-error state: the `combine` of four Room flows has no `.catch` (contrast `CustomWeekViewModel.kt:157-163`), so a read fault has nowhere to land except an uncaught exception, and `error` only ever carries an action failure. |
| B9b | UI-9 | B9b | P3 | validation gap | C | CURRENT_STRUCTURE:208; D17 | `app/src/test/java/com/sinura/personaltrainer/ui/plan/PlanDayConfirmTest.kt:11-24`, `UI/plan/PlanDayViewModel.kt`, `UI/plan/PlanDayScreen.kt`, `app/src/debug/.../ui/plan/PlanPreview.kt:20-65` | PlanDay is the least-tested screen: no ViewModel test, no render test, no preview (`PlanPreview` only draws `PlanHeader` and the dead `PlanRecoveryCommands`), and the one test that names it reads the `.kt` source as text and asserts substrings (`readOwned(...).contains("ConfirmActionDialog(")`) — the pattern T1 retired for the workout floor — plus two substrings of `RestTimerScreen.kt` that are not its subject. |
| B9b | UI-10 | B9b | P3 | opportunity | C (feel R) | — | `UI/routines/SessionLiftStrip.kt:251-260` | The lift card's header is `clickable(onClick)` with no `Role`, and its expanded/collapsed state is conveyed through `selected` rather than an expand/collapse state description; a merged `contentDescription` is present. |
| B9b | UI-11 | B9b | P3 | opportunity | C | F8b (picker side) | `UI/components/ExercisePickerSheet.kt:130,308-324`, `domain/ExercisePickerContract.kt:51-52` | In the picker's empty state the branch "Nothing in the library matches. Create it as a custom lift above." (`:316-317`) is unreachable (it needs `!canCreate` with a non-empty needle and no muscle chip, which implies an exact-name hit and a non-empty list), the no-query empty case reads "Search the library" even when the library is genuinely empty, and a no-hit query under a muscle chip offers no way to clear the chip. |
| B9b | UI-12 | B9b | P3 | opportunity | C | — | `UI/routines/SessionLiftStrip.kt:530-539,550-559` | Every stepper tap calls `persist` (stage + commit) and, because the tap also flips `differsFromStored` to true, `LaunchedEffect(rowKey, differsFromStored)` fires a second `persist` for the same values; the second commit is a no-op once the first lands but is a second Room round trip per tap. |
| B10a | UI-3 | B10a | P3 | defect | C | 22 Sep cross-tab | `UI/library/ExerciseLibraryViewModel.kt:84-97, 192-198`; `UI/library/ExerciseLibraryScreen.kt:86-89, 330-339` | The editor draft lives only in the ViewModel, so process death while creating or editing a custom lift discards the typed name, muscle, notes and load; query, both filters and expanded families reset; the Body muscle re-seeds even if it had been cleared. |
| B10a | UI-4 | B10a | P3 | defect | C | — | `UI/history/SessionDetailScreen.kt:296-319, 321-349, 268-276`; `UI/history/SessionDetailViewModel.kt:181-213, 287-293` | `onSave` sets `editingSetId = null` before calling `updateSet`/`addSet`, so a repository refusal (`SetLogRules.isUserMessage`) lands as a page banner after the sheet has closed and the sheet's `rememberSaveable` values are gone. |
| B10a | UI-5 | B10a | P3 | defect | C | — | `UI/summary/WorkoutSummaryViewModel.kt:50-57, 73-81, 110-147`; `UI/navigation/AppNav.kt:651-659, 665-674`; `UI/history/SessionDetailScreen.kt:122-124` | Summary reads once; "Open session" pushes Session Detail on top (`launchSingleTop`) and its Back/`deleted` pops back to Summary, so after editing a set the hero, tiles and "N personal records" are stale, and after deleting the session the summary still celebrates it and "Open session" leads to "Session not found". |
| B10a | UI-6 | B10a | P3 | defect | C | — | `UI/library/ExerciseLibraryScreen.kt:341-350` (:344); `UI/library/ExerciseLibraryViewModel.kt:309-360`; `data/repository/ExerciseRepository.kt:210-225`; `data/local/entity/ActivityEntities.kt:51-74` | The confirm copy promises "Lifts used in routines or history can't be deleted", but `usageFor` counts routines, `set_logs` and `session_exercises` only; `activity_blocks.exerciseId` is a soft key with no `exercises` FK, so a custom lift used only in a backdated strength activity deletes silently (history keeps its snapshot; Exercise Detail then reads "Exercise missing"). `confirmDelete` (:336-360) also has no `try/catch`, unlike `requestDelete` (:315-333). |
| B10a | UI-7 | B10a | P3 | validation gap | C | — | `data/repository/ExerciseRepository.kt:66-70, 128-131`; `data/repository/FlowGuards.kt:39-43`; `UI/library/ExerciseLibraryScreen.kt:125-127`; `UI/exercise/ExerciseDetailViewModel.kt:193-241` | Library and Exercise Detail read through `observeHealth(...).presentValues()`, which drops `Unavailable`, so a first read that throws leaves `isLoading = true` and the spinner for ever, and a later throw silently keeps the old list; neither screen has a failed/retry state (Session Detail and Summary do). |
| B10a | UI-8 | B10a | P3 | defect (a11y) | C | — | `UI/components/GymSurfaces.kt:466-471, 530-541` | `SessionLogRow` is two clickable targets for one action: the identity column carries the merged description, the metrics row (`clickable` at :533, no `semantics`) is a second focusable read as "3 sets, 1240 kg, 45 min" with no title; a long list costs TalkBack two swipes per session. |
| B10a | UI-9 | B10a | P3 | defect (a11y) | C | — | `UI/history/TrainingCalendarCard.kt:223-226, 239-242` | A trained day is `clickable` with no `role`, and "today" is a hairline border only — the label says "5, trained, 1200 kg" with no "today" and no "button". |
| B10a | UI-10 | B10a | P3 | defect | C | adjacent to the refuted 22 Sep item (distinct claim) | `domain/SessionSummary.kt:11-25, 119-130` (:127); `UI/history/HistoryScreen.kt:543`; `domain/SetCopy.kt:174-184`; `UI/history/SessionDetailScreen.kt:232-236` | `SessionSummary` carries no bodyweight-rep total and `toHistoryEntry` hard-codes `bodyweightReps = 0`, so a session of only bodyweight lifts shows "— kg" in the History row and the day sheet while its Session Detail (`session.work()`) and Summary count the same session in reps. |
| B10a | UI-11 | B10a | P3 | defect (polish) | C | Foundation item 4 (rounding closed) | `UI/summary/WorkoutSummaryScreen.kt:349`; `UI/exercise/ExerciseDetailScreen.kt:257, 629-630`; `domain/WeightFormat.kt:116-117`; `util/QuantityFormat.kt:16-33` | The same whole-number tonnage prints grouped on the Summary hero and the Exercise Detail tile ("1,240") and ungrouped everywhere `SetCopy.workColumn` is used ("1240"). |
| B10a | UI-12 | B10a | P3 | target gap | C | ADR-011 d.2 | `UI/units/DateCopy.kt:37-42`; `UI/history/HistoryScreen.kt:259-260, 541`; `domain/HistoryEntries.kt:203-212`; `UI/history/SessionDetailScreen.kt:233`; `UI/summary/WorkoutSummaryScreen.kt:283-285`; `UI/exercise/ExerciseDetailScreen.kt:619-624, 671-672` | Month headers and calendar cells use the captured `localEpochDay`, but every printed date on rows, receipts and chart axes formats the instant in `ZoneId.systemDefault()`, so after a zone change a session sits under "October" with a row date of "30 Sep". |
| B10a | UI-13 | B10a | P3 | defect (copy) | C | — | `UI/exercise/ExerciseDetailScreen.kt:263-289` (:277-282); `domain/PersonalRecords.kt:85-90` | `estimatedOneRepMaxKg` is null for `weightKg <= 0`, so a bodyweight lift's "Estimated 1RM" card is a permanent ghost whose hint tells the user to "log a loaded set" of that lift. |
| B10a | UI-14 | B10a | P3 | defect | C | see B5 (upload) | `UI/summary/WorkoutSummaryScreen.kt:144-157, 252-260`; `UI/summary/WorkoutSummaryViewModel.kt:209-231` | The auto-backup caption is an item of the full-summary `LazyColumn` only; a warm-up-only session (`!summary.hasWork`) or a failed summary runs the upload but never shows RUNNING/DONE/FAILED/NEEDS_SIGN_IN. |
| B10a | UI-15 | B10a | P3 | defect | C | — | `UI/history/HistoryViewModel.kt:176, 196-202, 254-259`; `UI/history/HistoryScreen.kt:106, 192`; `UI/units/TodayTicker.kt:47-61` | `cat.today` is evaluated when the catalog recomputes, while the composition's `LocalTodayEpochDay` ticks at midnight and on resume, so a History screen left open overnight moves the calendar's today ring but keeps yesterday's Day/Week window until a write or re-subscription. |
| B10a | UI-16 | B10a | P3 | architecture debt | C | — | `UI/history/HistoryScreen.kt:92-370`; `UI/library/ExerciseLibraryScreen.kt:76-384` | One 278-line and one 309-line composable hold the whole screen, the sheets and the dialogs; neither appears in CURRENT_STRUCTURE's "Known debt". |
| B10b | UI-6 | B10b | P3 | defect | C | — | `UI/settings/BackupDialogs.kt:181-189`, `UI/settings/BackupCoordinator.kt:407-414`, `UI/settings/BackupRestoreSection.kt:133` | `ProtectBackupDialog` discards `onConfirm`'s Boolean; when `submitAutoBackupPassphrase` refuses because the Keystore would not seal, the dialog stays open with no message and the refusal is drawn as `state.error` under the dialog's scrim. |
| B10b | UI-7 | B10b | P3 | defect | C | `docs/FRONTEND_REDESIGN.md:199` | `UI/settings/BackupRestoreSection.kt:80-86,129-132`, `UI/components/GymStatus.kt:154-193`, `UI/theme/Motion.kt:169`, `UI/settings/BackupCoordinator.kt:827-835` | Every backup outcome (restore reconciliation counts, "Protected backup saved…", "Found N backups") is a `GymStatusBanner` that fades after 6 s, is remembered as dismissed so it never returns, and has no `liveRegion` (only `GymUndoHost` `:216-218` and the PR banner add one), so TalkBack is not told the result. The persistent facts (Last backup / Last restore stamps `:92-109`, safety copies `:276-298`) remain; the count sentence does not. |
| B10b | UI-8 | B10b | P3 | defect (privacy consistency) | C | — | `UI/settings/AccountSection.kt:329-330`, `UI/settings/BackupDialogs.kt:87-92` | The account password field is `rememberSaveable`, so the typed password is serialised into the Activity's saved-state Bundle; the backup dialogs deliberately use `remember` for exactly this reason and say so. |
| B10b | UI-9 | B10b | P3 | validation gap | R | 22 Sep "no loading state" | `UI/settings/SettingsViewModel.kt:55-72,150-154`, `UI/settings/AccountCoordinator.kt:61-68`, `UI/settings/SettingsViewModel.kt:163-168`, `UI/navigation/AppNav.kt:289-299` | Initial values are lb / 12-hour / signed-out / LOCAL, so the root subtitles can read "lb · Regular", "Optional cloud sign-in", "This phone · optional Drive backup" for a kg, signed-in, Account user until DataStore and the auth session emit; AppNav subscribes `uiState` before the onboarding gate opens (`:289`) so the window is small, but nothing marks "loading". |
| B10b | UI-10 | B10b | P3 | defect | C | — | `UI/reminders/ReminderPrefsSection.kt:66-72` | `editing` (which weekday the wheel edits) is `remember`, not `rememberSaveable`; a rotation falls back to the first alarm day, so the wheel silently edits a different day than the one tapped. |
| B10b | UI-11 | B10b | P3 | defect | C | — | `UI/settings/AccountSection.kt:85-90` | `signedOutStep` is `remember`, so rotation or process death on the sign-in form bounces the user back to the "Open your account" entry page (fields may or may not come back — R). |
| B10b | UI-12 | B10b | P3 | defect (copy) | C | ADR-012 d18 | `UI/settings/RestTimerPrefsSection.kt:42-44`, `domain/RestTimer.kt:172-183` | The Rest timer caption "Default rest is used after a working set if the lift has none and you haven't picked a preset" omits the first term of the order (coach's suggestion → routine's rest → last preset → default) that ADR-012 d18 fixed on 23 Sep. |
| B10b | UI-13 | B10b | P3 | opportunity | C | `docs/FRONTEND_REDESIGN.md:197` "discoverable version/build/update" | `UI/settings/SettingsScreen.kt:86-89,215-219`, `UI/update/DebugUpdateBanner.kt:82-91`, `UI/settings/AboutSections.kt:144-154` | Opening Settings triggers a silent update check and the Update row exists only while an offer exists, so there is no "up to date / last checked" state and no manual check; the Version row does show the live drop (`app/build.gradle.kts:197-198` sets `versionCode = debugLiveCode`). |
| B10b | UI-14 | B10b | P3 | validation gap | R | 22 Sep cross-tab process-death row | `UI/settings/BackupCoordinator.kt:120,136,850-870`, `UI/settings/SettingsScreen.kt:98-102`, `UI/settings/SettingsViewModel.kt:82-86,186-190` | With no `SavedStateHandle`, process death loses: the restore preview (`pendingPlan`, dialog vanishes; re-pick), the pending ciphertext (unlock dialog), the generate notice, the pending sub-page request, and the Drive consent waiter — the consent Activity's result then reaches a fresh coordinator whose `resolutionWaiter` is null (`:851`) and the sign-in ends silently with no error and no busy state. Export protection is handled (`:720-726`) and restore is journaled (`:574-583`). |
| B10b | UI-15 | B10b | P3 | opportunity | C | — | `UI/settings/BackupDialogs.kt:138,157,235`, `UI/settings/AccountSection.kt:365` | Four password fields, none with a show/hide toggle; the backup one is min 8 with a confirm field (`data/backup/BackupEnvelope.kt:42,77-81`), which is the right gate but a typo in both fields still seals a password nobody can read back without the lock-screen reveal. |
| B10b | UI-16 | B10b | P3 | defect (copy) | C | — | `UI/settings/AccountSection.kt:372,394,406`, `data/auth/SupabaseAccountAuth.kt:115-147`, `domain/AccountAuthCopy.kt:54-63` | Client validation is `isNotBlank()` only; four server cases are mapped and every other Supabase message is shown verbatim (`AccountAuthError.Message`), e.g. the server's own wording for a too-short sign-up password. |
| B10b | UI-17 | B10b | P3 | defect | C | — | `UI/settings/BackupRestoreSection.kt:379-383`, `UI/settings/BackupCoordinator.kt:312-317` | "Yesterday"/relative stamps and the 14-day staleness are computed from the clock inside `remember(atMillis, clock)` and inside `combine`, so they only refresh on a new emission or re-subscription (`WhileSubscribed(5_000)` makes that roughly each Settings visit). |
| B10b | UI-18 | B10b | P3 | defect (copy) | C | ADR-031 §3 | `domain/AccountAuthCopy.kt:8-10`, `UI/settings/AccountSection.kt:253-262,136-137` | The Account entry page's headline blurb promises "Keep … in the cloud, and bring them back on another phone" unconditionally; the paused notice is drawn further down the same page. |
| B10b | UI-19 | B10b | P3 | opportunity (copy) | C | ADR-005 | `UI/settings/BackupCoordinator.kt:340`, `data/backup/BackupJson.kt:18`, `data/repository/BackupService.kt:613` | "Backups stay in your PersonalTrainer Backups Drive folder" is accurate (the folder is named that) but carries the retired product name; renaming would orphan existing folders, so this is a naming decision, not a copy fix. |
| B10b | UI-20 | B10b | P3 | defect | C | — | `UI/update/DebugUpdateBanner.kt:54-64,94-111`, `UI/components/GymStatus.kt:320-338`, `update/DebugUpdateMonitor.kt:111-118` | The update banner is a `GymNoticeBanner` with no live region and its download bar is a bare `Box` with no `progressBarRangeInfo`, so TalkBack hears neither progress nor state changes; and because the monitor publishes `Idle` the moment the APK is handed to Android (`:118`), the "Android will ask you to install" state is visible only during that hand-off — there is no banner state for a system install sheet that was cancelled or stalled (B7 owns the cause). |
| B10c | UI-6 | B10c | P3 | validation gap (accessibility) | C | — | `ui/onboarding/OnboardingScreen.kt:296-327` | The place cards are `Modifier.clickable` with no `toggleable`/role/state; "on" is only a visible "Selected" caption (`:324-326`), "off" has no cue, unlike the Experience step's `selectable(role = RadioButton)` (`:252-259`). |
| B10c | UI-7 | B10c | P3 | validation gap (accessibility) | C / R (announcement) | — | `ui/onboarding/BodyweightWheel.kt:105-112,167-185`, `ui/components/SnapWheel.kt:254-276` | The hero numeral's `semantics { contentDescription = "Type bodyweight" }` replaces its spoken text, the wheel's pages are plain `Text` with no selected/value semantics and the pager exposes only scroll, so the chosen weight is spoken nowhere on the step; the typed dialog (`:131-145`, `TalkBackPolicy`) is the only path that reads it back. |
| B10c | UI-8 | B10c | P3 | target gap | C | 22 Sep Cardio row; ADR-011 §3 | `ui/activity/ActivityComposerViewModel.kt:171-180,368-375`, `ui/activity/ActivityComposerScreen.kt:154-177`, `domain/ComposerCopy.kt:27` | Backdating sets only the day: time is fixed at noon (honestly captioned), the zone is the device zone at save (`now.zoneId`) and is neither shown nor settable, and `Earlier` has no lower bound (`setEpochDay` clamps the upper bound only), so a 1970 activity is accepted; ADR-011 §3 asks for date, time and zone. |
| B10c | UI-9 | B10c | P3 | opportunity | C (absence) / R (phone) | — | `docs/DATA_SAFETY.md:30-31`, `app/src/main/AndroidManifest.xml:56`, `ui/navigation/LiveSessionBarViewModel.kt:116-117,138`, `timer/CardioTimerPersistence.kt` | A running cardio has no foreground service or notification (the FGS is rest-only), and the cardio bar hard-codes `stale = false` while a workout bar warns "Left open Nh"; recovery after process death is by timestamps in `cardio_timer_state` (pinned by `timer/CardioElapsedTest`), so the clock stays honest but nothing ever tells the owner a session was left running. |
| B10c | UI-10 | B10c | P3 | validation gap | C (absence) / R | — | `ui/activity/LiveCardioScreen.kt:107`, `ui/activity/ActivityComposerScreen.kt:90-94,108`, `MainActivity.kt:56`, `AndroidManifest.xml:36` | The live cardio `Scaffold` has no `imePadding`, unlike the composer's (which carries a comment and an instrumented proof, `ProductionScreensPassInstrumentedTest.kt:281-303`); with edge-to-edge and `adjustResize` the keyboard can cover the distance box and the Finish dock. |
| B10c | UI-11 | B10c | P3 | defect (polish) | C | — | `ui/activity/LiveCardioViewModel.kt:222-245`, `ui/activity/LiveCardioScreen.kt:122-135` | After the owner confirms Discard, the screen stays and shows the "No live cardio — That session is gone. Start a new one from Home." empty state with a Back button instead of leaving. |
| B10c | UI-12 | B10c | P3 | defect by design (owner decision) | C | CURRENT_STRUCTURE "six soft foreign keys" | `data/repository/ActivityRepository.kt:248-262`, `ui/activity/ActivityDetailViewModel.kt:112-129` | Deleting a completed activity leaves its Plan day `DONE` with `completedActivityId` pointing at a row that no longer exists (documented as intentional, "as deleting a finished strength session does"). |
| B10c | UI-13 | B10c | P3 | validation gap (accessibility) | C | — | `ui/activity/ActivityComposerScreen.kt:180-191,206-222,560-571` | Each line's `Remove` is a `TextButton` whose only name is "Remove"; the line title is a sibling `InstrumentRow`, not merged, so several lines announce identically. |
| B10c | DC-1 | B10c | P3 | docs truth | C | — | `docs/artifacts/onboarding-programs-review.md:1-11`, `domain/PlanReviewRenderer.kt:9,40-52,67`, `domain/OnboardingAnswers.kt:223-243`, `domain/RoutineGenerator.kt:449`, `app/src/test/.../domain/RoutineGeneratorTest.kt:615-631` | The review claims "Every program `RoutineGenerator` can produce" (and the renderer comment says "420 reachable combinations"), but the renderer iterates single places × age × days × goal plus two emphasis samples, while the screen lets places combine (`withToggledPlace`: Gym + Hyper Pro, Home + Bodyweight, Hyper Pro + Home + Bodyweight …) and those unions change the allowed catalog; the golden test pins doc = renderer, not renderer = generator space. |
| B13a | AR-3 | B13a | P3 | opportunity | C (structure) / R (cost) | C-2 | `ActiveWorkoutViewModel.kt:657-671,672-689`, `data/local/dao/WorkoutDao.kt:454-455`, `WorkoutRepository.kt:149,172,186`, `ui/navigation/LiveSessionBar.kt:274`, `LiveSessionBarViewModel.kt:88-146` | Per logged set, Room invalidation re-runs: `observeSession` (a 3-query relation load), `observeSessionActivity`, `observeLastLogged` (a `GROUP BY` over all `set_logs`) and `observeFinishedWorkGeneration` three times over (one subscription per repository flow, each with its own `distinctUntilChanged`, `WorkoutRepository.kt:149-150,172-173,186-187`); the floor's `uiState` then sorts the whole catalog (`ExerciseOrdering.pickerOrder`, 179 lifts) on every emission with no `showPicker` guard, and `LiveSessionBarHost` keeps `uiState` collected while the bar is hidden on the floor (only the 1 Hz ticker stops, `LiveSessionBarViewModel.kt:69-85,97-99`). The heavy insight reloads are gated (pinned by `TrainingInsightsSourceTest.loggingASetOnAnInProgressSessionDoesNotRecomputeInsights`). |
| B13a | AR-4 | B13a | P3 | opportunity | C | C-2 | `ActiveWorkoutViewModel.kt:576-580,1548-1557,379-381` | `microRec`, `primaryAction` and `session` are `SharingStarted.Eagerly` in `viewModelScope`, so the coach engine and `WorkoutPrimaryActions.derive` keep running on every Room re-emission while the floor is on the back stack or the app is in the background (the ViewModel outlives the screen by design; `uiState` itself is `WhileSubscribed(5_000)`). The hold/stopwatch loops tick at 250 ms (`:103,1056,1111`) but the StateFlow dedupes equal seconds, so emission is 1 Hz (C). |
| B13a | AR-5 | B13a | P3 | opportunity | C (size) / R (memory) | — | `PersonalTrainerApp.kt:109-112`, `ui/components/PoseArtwork.kt:53-92,101-110` | Every cold start decodes two 1024×1024 ARGB stills (2 × 4 MB resident, plus a transient 4 MB `IntArray` per still for `punchPit`) on `applicationScope`, for the Body tab only, and holds them for the process life whether Body is opened or not; `ThumbCache` (`ui/components/ThumbCache.kt:22-77`) by contrast is bounded (8 MB LRU, sampled, off-main via `withContext(io)`, keyed by `(id, sample)`, `produceState` in `ExerciseThumb.kt:164-168`). 177 WebPs, 1,008 KB, `res/drawable-nodpi`. **B12 hand-off, settled from code:** every `BitmapFactory.decodeResource` in `ui/` runs off the main thread — `ThumbCache.kt:52-63` under `withContext(io)`, `PoseArtwork.kt:73-92` only from `bind`, whose sole caller is `PersonalTrainerApp.kt:112` on the IO `applicationScope` (`:26-28`); the "8 MB" is that `bind` (2 × 1024×1024 ARGB, header-verified), not a main-thread decode. The only composition-time decodes are two `painterResource` calls in `ui/components/ExerciseThumbGallery.kt:223,232` (the 768×768 heat stills), and that composable has no caller in main or debug (AR-6). Thumb maths: all 143 `ex_*.webp` are 256×256 (header-verified, 467 KB on disk) = 256 KB ARGB each, 35.75 MB if all resident; the 8 MB LRU holds 32 (the comment at `ThumbCache.kt:76` says "~32"; no "~98" comment exists in `ThumbCache.kt`, `ExerciseThumb.kt` or `KeyedArtwork.kt`), so a scroll through the full Library re-decodes 3–7 KB WebPs on IO as it goes (R: cost). At 40 dp rows the 1:1 decode is 1.6–2.1× the display edge; `inSampleSize = 2` would fall below xxxhdpi, so 1:1 is the right power of two. |
| B13a | AR-6 | B13a | P3 | architecture debt | C | 22 Sep W2a | `ui/plan/PlanViewModel.kt:295,407,461,616,627`, `ui/home/HomeViewModel.kt:558,635,645`, `ui/components/GymSurfaces.kt:90,532`, `ui/components/InstrumentSelection.kt:59,106`, `ui/plan/PlanScreen.kt:540,647`, `ui/plan/PreferenceBlock.kt:23`, `ui/components/ExerciseThumbGallery.kt:223,232`, `domain/coach/CoachEngine.kt:128`, `data/repository/DbMaintenance.kt:170` | Zero production callers (direct grep, comments excluded): ViewModel functions `pinFocus`, `dismissFills`, `addMorningCardio`, `setLighterWeek`, `setSessionHour`, `addDaySession`, `requestWeekSuggestion`, `requestAnswerReplay` (three have no test either); composables `SectionHeader` (pinned in place by `ComponentInventoryTest.kt:19`, which asserts the signature exists), `Numeral`, `InstrumentToggleChip`, `InstrumentSuggestion`, `ExerciseThumbGallery` (`ui/components/ExerciseThumbGallery.kt`; no caller in main or debug, 0 test file(s) reference it; it holds the app's only composition-time `painterResource` still decodes, `:223,232`), plus the three F7a items; `CoachEngine.toCoachSuggestion` and `DbMaintenance.parseCollisions` have no caller anywhere. Test-only helpers shipped in main beyond the doc's five: `emptyInkPoints`, `poseCoversTheCatalog`, `poseAndKitPoints` (ui), `prSummary`, `previousSameRoutine`, `signedWorkDelta`, `homeWork`, `toSessionStub`, `toKgLabel`, `toKgNumber`, `toWeekday` (domain/util). `PreferenceBlock.kt:42` is also the only `items(` in main without a `key` (34 of 35 keyed; 8 `contentType`s). |
| B13a | AR-7 | B13a | P3 | architecture debt | C | — | 66 sites (grep `catch (\w+: (kotlinx.coroutines.)?CancellationException)` in main), `util/CoroutineErrors.kt:14-30`; 23 `on*Handled()`/`consume*` acks across 12 ViewModels (e.g. `HomeViewModel.kt:208-249`, `StartOptionsViewModel.kt:170-195`, `LiveSessionBarViewModel.kt:165-168`) | Two idioms for one rule: 124 `runCatchingCancellable`/`recoverWith` uses coexist with 66 hand-written rethrow clauses (`check-cancellation.py` holds the *unguarded* count at 0 but does not converge the idiom); and each ViewModel hand-rolls the same one-shot navigation ack (`MutableStateFlow<String?>` + `onXHandled()`). |
| B13a | AR-8 | B13a | P3 | validation gap | C | — | `ui/home/HomeViewModel.kt:93`, `ui/plan/PlanViewModel.kt:107`, `ui/plan/PlanDayViewModel.kt:53`, `ui/library/ExerciseLibraryViewModel.kt:97`, `ui/navigation/LiveSessionBarViewModel.kt:175`, `ui/activity/LiveCardioViewModel.kt:88`; `util/ErrorSlot.kt:17-45` | Six ViewModels still hold the raw `MutableStateFlow<String?>` error whose success-clears-everything race `ErrorSlot` was written to end (12 ViewModels adopted it); `HomeViewModel` has 14 `viewModelScope.launch` sites writing `actionError`. |
| B13a | AR-9 | B13a | P3 | validation gap | C (when) / R (how long) | X2b | `PersonalTrainerApp.kt:97-101`, `data/local/TemperPreMigrationCopy.kt:64-123,130-131,151-158`, `data/local/PreMigrationSnapshot.kt:52-58` | The whole-database copy with per-file `fsync` runs synchronously on the main thread in `Application.onCreate`, but **only on the first launch after a schema bump** (marker `KEY_CHECKED_VERSION` at `:71`, written at `:122` after a copy or when nothing needs copying `:79,91,99`), never on ordinary launches; it needs `usable ≥ 3 × (db + wal + shm + journal) + 32 MB` or it skips. The next such launch is S2b's v8. Size of `temper.db` on the owner's phone is unknown (R); a few MB copies in well under a second, tens of MB in seconds. Two exceptions leave the check un-marked so it re-runs every launch: an unreadable version (`:83-88`) and too little space (`:104-107`) — each is one read-only SQLite open per launch, cheap. Comment contradiction: `TemperPreMigrationCopy.kt:38` "never blocks launch" vs `PreMigrationSnapshot.kt:53` "Synchronous by design". |
| B13a | AR-10 | B13a | P3 | defect | C | — | `PersonalTrainerApp.kt:115-133`, `PendingOccurrence.kt:55-57,102-104,115-118` | `PendingOccurrence.restore` runs third in the startup chain, behind `recoverInterruptedRestore` and `importEncodedHistoryIfNeeded`, and assigns `pendingOccurrenceId.value` unconditionally from a DataStore read; a `write()` that lands between that read and the assignment is overwritten in memory. A null is self-healing (`stored()` falls back to prefs, `:102-104`), a stale non-null is not: `complete()` for a composer arm (`:66-72`, no session id to compare) would mark an already-done occurrence again. |
| B13a | AR-11 | B13a | P3 | validation gap | C (scope) / R (cost) | — | `ui/workout/ActiveWorkoutScreen.kt:191-201,360-365,433-439,677,838`, `ui/workout/WorkoutDock.kt:117-198`, `ui/workout/RestTimerCard.kt:83-175,291-335`, `ui/navigation/LiveSessionBar.kt:262-296`, `ui/navigation/AppNav.kt:347,366-373`, `timer/RestTimerController.kt:91-120` | Per-tick recomposition is contained by construction: the four ticking `State`s are held as `State` objects and read only inside the `bottomBar` lambda (`:436-439`), one `item(key = "entry")` (`:677`) and a `derivedStateOf` (`:360-365`), so the 795-line `ActiveWorkoutContent` and the `LazyColumn` do not re-run each second; `WorkoutDock` and `RestTimerCard` do (their `remainingSeconds` params change), the ring animates per frame in the draw phase only (`animateFloatAsState` read inside `Canvas`, `:298-334`), control widths are remembered per control set (`:167-175`); the nav root reads only `hasLiveSession` and the whole `LiveSessionBar` recomposes at 1 Hz inside its host; `remainingSeconds` is one shared boundary-aligned poll for every collector. No test counts recompositions, so FRONTEND_REDESIGN's "no timer-driven full-screen recomposition" target is held by reading, not by a gate. |
| B13a | AR-12 | B13a | P3 | opportunity | C (frequency) / R (cost) | B6 hand-off | `timer/RestTimerService.kt:186-208,252-257`, `timer/RestTimerNotifications.kt:48-90`, `timer/RestTimerController.kt:300-310`, `MainActivity.kt:102-103` | `ensureChannels` (three `deleteNotificationChannel` + `createNotificationChannel` Binder calls) runs on every SYNC command — each rest start, each ±15, each rehydrate — at `:198`, and again on the first foreground claim `:255`, on the main thread, although `PersonalTrainerApp.kt:107` already ran it once per process; it is **not** on the tick path (`handleTick`, `:227-250`). `refreshAlarmCapability` on every `onResume` costs one `canScheduleExactAlarms` query and, only while a rest runs, one alarm re-arm (`:302-304`). Per user action and per foreground, never per second. |
| B13a | AR-13 | B13a | P3 | validation gap | C | — | `tools/check-unbounded-waits.py:1-40`, `ActiveWorkoutViewModel.kt:748-750,1514`, `RestTimerViewModel.kt:63`, `PlanViewModel.kt:362,420` | The unbounded-wait checker scans `*ViewModelTest.kt` only; five `first { }` waits in main sit inside `viewModelScope` with no ceiling and no failure branch — bounded by the ViewModel's life, so a predicate that never holds (e.g. `observations` never leaving `LOADING`) makes the action silently never finish rather than fail. |
| B13b | TS-5 | B13b | P3 | target gap | C | ADR-032 §Consequences (W3 pending) | `ui/workout/WorkoutFloorRenderTest.kt:92-216,337-345` | Twelve of the floor's fourteen render tests end in `assertExists()` on three to five tags, which passes for a node scrolled off-screen, so "a control that falls out of reach fails the gate" (ADR-032 §1) is not yet true for the floor; only `:218-243` measure a touch target, and the reachability pattern exists elsewhere (`FirstLaunchRenderTest.kt:70` `performScrollTo().assertIsDisplayed()`, `LargeTextEntryRenderTest.kt:193-230` ink boxes, `FirstWorkingSetRenderTest.kt:164-179` positions). |
| B13b | TS-6 | B13b | P3 | validation gap | C | ADR-010 §7 | `data/local/TemperMigration1To2Test.kt:36-70`, `TemperMigration2To3Test.kt:33-67`; `androidTest/…/TemperMigrationDeviceTest.kt:27-61` | The 1→2 and 2→3 JVM tests seed rows and then only count them after migrating (plus an index name), never reading a value back; 3→4 reads back only the new default columns; 4→5, 5→6 and 6→7 (with the 4→7 chain, `TemperMigrationFixtures.kt:43-57`) read values back and are the data-preserving shape ADR-010 §7 intends; the device chain 1→7 is count-only plus `PRAGMA foreign_key_check`. |
| B13b | TS-7 | B13b | P3 | validation gap (flaky-prone, not observed) | C | — | `FakeAppDependencies.kt:109-111`; `insights/TrainingInsightsSourceTest.kt:195-210`; `timer/RestTimerControllerTest.kt:478-496,1040-1052`; `timer/RestTimerServiceTest.kt:486-495,670-680`; `timer/RestTimerRecoveryTest.kt:88-98` | 93 test files use `runBlocking` against 8 `runTest`; `FakeAppDependencies` defaults `ioDispatcher`/`computeDispatcher` to `Dispatchers.IO`/`Default` when no scheduler is passed; five timer tests poll with `Thread.sleep` on wall-clock deadlines (3 s, `PREFS_WAIT_NANOS`, `COMPLETION_WAIT_NANOS`) and `TrainingInsightsSourceTest` races a `delay(200)` against a 10 ms share grace on the shared `Default` pool. All are bounded and none has failed in the record (baseline 0 failures; 30/30 hosted runs green), so this is a hazard, not a flake. |
| B13b | TS-8 | B13b | P3 | validation gap | C | — | `ui/settings/SettingsViewModelTest.kt:362-370`; `timer/RestSoundTest.kt:92-118`; `reminder/ReminderReceiversAndWorkerTest.kt:128-134` | Of 52 `@Test` methods with no assertion keyword in their body, 47 delegate to helpers that assert (`showsTheSession`, `render`, `activeScenario`, `checkRemovedPendingOwner`, or a `withTimeout { flow.first { } }` that is itself the check); five are honest "does not throw" smokes; one, `actionReceiverWithoutTheAppClassIsANoOp`, claims a no-op and checks nothing. |
| B13b | TS-9 | B13b | P3 | validation gap | C | X5 | `tools/coverage-floors.txt:146-152`, `tools/check-coverage.py:106-121` | The floors cover 7 of 46 JaCoCo packages; unfloored and low: `data.sync` 48.0 % (10,334 instr; S1 will resume it), `reminder` 43.2 %, `data.auth` 12.0 %, `data.security` 1.1 %, `data.repository.prefs` 64.1 %, `update` 56.9 %, `domain.coach` 85.4 %; and the timer floor (73) sits 8.7 points under its reading. |
| B13b | TS-10 | B13b | P3 | validation gap | C | — | `tools/check-unbounded-waits.py:16-33`; `ui/workout/FloorVmContractTest.kt:121-125` | The unbounded-wait checker only reads `*ViewModelTest.kt`; ViewModel waits in render, kit and scenario files (`FloorTestKit`, `ManualRestStartTest`, `WorkoutSaveRecoveryTest`, `FloorVmContractTest`) are bounded today by `awaitFirst` (`TestWaits.kt:44-56`, 30 s) and `withTimeout` by convention, not by the checker; the one bare `.first {` I found (`FloorVmContractTest.kt:123`) is inside a `withTimeout`. |
| B13b | TS-11 | B13b | P3 | architecture debt (test) | C | — | `ui/theme/MotionPolicyTest.kt:65-75`, `:86-89` | A unit test reads `docs/architecture/ADR-023-palette-and-reduced-motion.md` and fails if the words "non-colour" or "Do not shift Warn toward orange" are edited, so a docs pass can break `testDebugUnitTest`. |

## §1 prior-status rows

B1: | S-1 | Pull replaced rows; FK cascades dropped children; ABORT stalled later tables | Closed | #383. Every parent write the pull makes is `@Upsert`: `P/data/local/dao/ActivityDao.kt:232-245`, `RoutineDao.kt:37-38`, `PlannerDao.kt:31-32,65-66`. Remaining `REPLACE` writes on the pull path are leaf tables with no child FK (`RoutineDao.kt:46-47`, `GoalDao.kt:18-19`, `CatalogDao.kt:49-50`). Pinned by `T/data/sync/SyncPullInPlaceTest.kt` (11 tests). |
B1: | S-2 | Cursor `gt` on phone-clock `updated_at_ms`, no tiebreaker; tombstones keep the old stamp; server upsert unconditional | Open, exactly as recorded | `P/data/sync/SupabaseRestClient.kt:33-34` (`gt.`, `order=updated_at_ms.asc`, no secondary key, so a tie straddling a 500-row page boundary is skipped too); `SyncEngine.kt:173-188`. Tombstone stamps: sessions `SyncOutboxWriter.kt:86` (`updatedAtMs` = last edit, fed from `P/data/repository/ActivityRepository.kt:265-269`), routines `:168`, whole-routine lift deletes `:182` via `SyncAuthoring.kt:79`, goals `:331`, weigh-ins `:309` (`recordedAtMs`, `SyncRemoteRows.kt:548`), rules/occurrences `:125,:134`. Only custom lifts and their credits stamp the delete time (`:268-288`). Server side: `SupabaseRestClient.kt:22` `resolution=merge-duplicates`. |
B1: | S-3 | Outbox written after the commit, not in the save's transaction; edits while the session is loading or offline never queue | Open, exactly as recorded | Hook after `withTransaction`: `ActivityRepository.kt:210-213,367`; before the delete transaction: `RoutineRepository.kt:67-76`, `ExerciseRepository.kt:222-223`. `enqueue` itself is delete-then-insert with no transaction (`SyncOutboxWriter.kt:396-408`). Gate `auth.session.first()?.userId ?: return` on all 15 hooks (`SyncAuthoring.kt:37…175`); `LoadingFromStorage` and `NetworkError` both map to null (`P/data/auth/SupabaseAccountAuth.kt:41,43`). A deterministic cold-start case: `PersonalTrainerApp.kt:60-69` → `PlannerRepository.ensureWeek` → `notifyScheduleSync()` (`:306`) runs before the stored session has loaded (timing R), so a newly generated week is never queued. The "retry duplicates the session" half was fixed for reminder cleanup (`ActivityRepository.kt:215-238`) but the sync hook still runs outside `afterCommit`'s guard: a throw from `onActivitySessionCommitted` (`:212`) escapes to the caller after the row is durable. |
B1: | S-4 | Four delete enqueuers have no callers | Open, exactly as recorded | `grep` over `app/src/main`: `enqueueRuleDelete`, `enqueueOccurrenceDelete`, `enqueueTemplateDelete`, `enqueueBodyweightDelete` are called nowhere. Rule/occurrence deletes go through `PlannerRepository.kt:256-275` and `:340`, which then re-enqueue only the survivors (`SyncAuthoring.kt:54-59`). Nuance: no app path deletes a template (`activityDao.deleteTemplate` is called only by `SyncEngine.kt:306`) and the only weigh-in delete is the cap trim (`PreferencesRepository.kt:497-500`), so for those two the missing hook has nothing to hook yet. |
B1: | S-5 | One bad upload blocks the queue forever; unlimited retry; no timeouts | Open, exactly as recorded | `SyncEngine.kt:94-115`: `valueOf`/`error()` at `:99-100` sit outside the per-row `try`, so an unknown type or a null payload fails the push without even marking the row; inside it, any failure marks and rethrows (`:110-111`), and `peekOutbox` (`SyncDao.kt:29-32`) returns the same row first next pass. `attempts` is written, never read. `Result.retry()` uncapped (`SyncWorker.kt:20`). No `connectTimeout`/`readTimeout` on any connection (`SupabaseRestClient.kt:16-23,36-41,56-61`). Transport half: see B2. |
B1: | S-6 | Live strength workouts not synced; copy said they were | Open as recorded (copy honest) | `P/domain/SyncEntityType.kt:4-22` has no `workout_sessions`/`set_logs` type; `SyncCopy.kt:21-25` says so, pinned by `T/domain/SyncCopyTest.kt:29`. |
B1: | S-7 | Account deletion always failed | Open as recorded (hidden) | `SupabaseAccountAuth.kt:71-77` still calls `rest.deleteAuthUser`; reachable only through `AccountCoordinator.kt:133` guard on `IN_APP_DELETE_AVAILABLE = false`. Push-path angle: nothing else. Transport: see B2. |
B1: | S-8 | DDL/RLS missing for 9 of 17 tables; pulls send no `user_id` filter | Open, exactly as recorded | `docs/supabase/` has packet-2 (3 tables) and packet-3 (5 tables); the 9 activity/schedule/routine/template tables are absent. `SupabaseRestClient.kt:33-34` sends no `user_id=eq.` filter; correctness rests on RLS alone. |
B1: | S-9 | Sign-out keeps cursors; sync not serialised with restore; fresh phone's default prefs overwrite real ones; plaintext tokens | Open, exactly as recorded; one new upload-side twin in §2 (S-14) | Cursors: `SyncCoordinator.kt:41-50` clears `sync_outbox` only; `SyncDao.kt:49-53` has no delete for `sync_table_cursors`. Restore: `LocalBackupRepository.kt:503-579` never touches cursors and takes no lock the engine shares (`DbMaintenance.kt:46-49` is unused by `SyncEngine`). Default prefs: `SyncAccountPrefs.kt:62,77,95,106` stamp `System.currentTimeMillis()` when no local stamp exists, and `bootstrapLocalSnapshot` (`SyncAuthoring.kt:174-184`) uploads them on every sign-in. Tokens: B2. |
B1: | S-10 | Governance: ADR-009 §14–15 never amended | Closed | ADR-031 accepted 23 Sep; ADR-009 §15 carries the amendment note. |
B1: | S-1 follow-up (a) | Pulled occurrence with no rule, or routine lift with no exercise, fails its FK and stops later tables | Open, exactly as recorded | `SyncEngine.kt:287-301` never checks the rule exists before `upsertOccurrence` (FK CASCADE, `PlannerEntities.kt:33-36`); `:335-350` checks the routine (`:345`) but not the exercise (FK RESTRICT, `RoutineExerciseEntity.kt:18-22`). `pullTable` (`:169-189`) has no per-row `try`. Broadened in §2 (S-12). |
B1: | S-1 follow-up (b) | A table's cursor is saved only after the whole table | Open, exactly as recorded | `SyncEngine.kt:184-188` — one `upsertCursor` after the page loop; a throw on page 3 loses pages 1–2's watermark. |
B1: | S-1 follow-up (c) | A custom lift kept on this phone can be re-uploaded and bring a server tombstone back | Open on the client (C); server effect narrower than recorded (R) | Client still queues a kept lift as a live UPSERT via `onCustomExercisesChanged` (restore, `AppContainer.kt:344`), `bootstrapLocalSnapshot` (`SyncAuthoring.kt:179`) and `updateCustom` (`ExerciseRepository.kt:205`). But `syncGson` drops nulls (`SyncJson.kt:6`, no `serializeNulls()`), so the payload carries no `deleted_at_ms` key; under `merge-duplicates` PostgREST leaves an absent column untouched, so the tombstone stamp most likely survives and only `updated_at_ms` moves. Same mechanism is a defect elsewhere: §2 S-13. |
B1: | S-1 follow-up (d) | Set timestamps use `completedAtMs`, so a late upload can fall behind another phone's cursor | Open, exactly as recorded | `SyncOutboxWriter.kt:46,208`. Weigh-ins have the same shape (`SyncRemoteRows.kt:548`) but `recordBodyweight` restamps `recordedAtMs` to now on every write (`PreferencesRepository.kt:491`), so they are not affected. |
B1: | S-1 follow-up (e) | Nothing serialises two passes | Open, narrowed | Pass-vs-pass is serialised by construction: every pass comes from one unique WorkManager chain, `APPEND_OR_REPLACE` (`WorkManagerSyncScheduler.kt:23-27`), and `SyncWorker` is `engine.run`'s only caller. No `Mutex` anywhere in `data/sync`, and nothing serialises a pass against restore (`LocalBackupRepository.kt:503-579`), `abandonOutboxOnSignOut` (`SyncCoordinator.kt:41-50`) or `bootstrapLocalSnapshot`. No test pins the chain. |
B2: | S-7 | Delete account: blocking `HttpURLConnection` on main; `DELETE /auth/v1/user` | **Open, gated** (S2a/S2b) | Code unchanged: `P/data/auth/SupabaseAccountAuth.kt:71-77` still wipes 17 tables then calls `rest.deleteAuthUser` (`P/data/sync/SupabaseRestClient.kt:70-82`, still `DELETE /auth/v1/user`) before `clearSession()`; it runs on `viewModelScope` (`P/ui/settings/SettingsViewModel.kt:94-97`) with no IO dispatcher. Gate: `P/domain/AccountSyncGate.kt:26` = false, guard `P/ui/settings/AccountCoordinator.kt:133`, button hidden `P/ui/settings/AccountSection.kt:304-318`, dialog gated `P/ui/settings/SettingsScreen.kt:477`. Pinned by `AccountCoordinatorTest.deleteAccountIsANoOpWhileInAppDeletionIsOff` (`:133-159`). |
B2: | S-9 (token sentence) | "tokens sit in plaintext SharedPreferences" | **Open** (S4), now located exactly | `P/data/auth/SupabaseRuntime.kt:19-24` installs `Auth` with no `sessionManager`; bytecode `AuthConfigDefaults.<init>`: `autoLoadFromStorage`/`autoSaveToStorage`/`alwaysAutoRefresh` = true, `sessionManager` = null → `SettingsUtilKt.createDefaultSessionManager` → `SettingsSessionManager(Settings(), "sb-<host>-session")`; `com.russhwolf.settings.NoArgKt.Settings()` = `context.getSharedPreferences("<packageName>_preferences", MODE_PRIVATE)`. So the file is `shared_prefs/com.sinura.personaltrainer.debug_preferences.xml`, one key holding the JSON `UserSession` (`access_token`, `refresh_token`, `user{email,id,…}`, via `UserSession.Companion.serializer`). Covered by the domain-wide `<exclude domain="sharedpref"/>` (`app/src/main/res/xml/backup_rules.xml:15`, `data_extraction_rules.xml:13,36`) plus `allowBackup="false"` (`app/src/main/AndroidManifest.xml:21`; device proof `BackupPolicyInstrumentedTest.debugPackageDisablesAllowBackup` `:42-52`), so no OS backup or D2D channel. Plaintext at rest = the accepted T1 posture for Room (`docs/architecture/backup-threat-model.md:174`); ADR-031 `:78` owns it under S4. Store is unnamed in the rule files, `tools/check-backup-policy.py:20-30` and threat-model §3 (→ PV-1). |
B2: | S-5 (timeouts) | HTTP calls have no timeouts | **Open** (S4) | `P/data/sync/SupabaseRestClient.kt:16-23, 36-41, 56-61, 72-76` set no `connectTimeout`/`readTimeout` (HttpURLConnection default 0 = wait forever); `P/data/backup/DriveHttp.kt:44-45` sets 20 s / 30 s. New angles in S-11. |
B2: | S-3 (queue while loading/offline) | Changes made while the session is loading or offline are never queued | **Open** (S1) | `P/data/auth/SupabaseAccountAuth.kt:41-43` maps `LoadingFromStorage` and `NetworkError` to null; every `SyncAuthoring` hook returns on null (`P/data/sync/SyncAuthoring.kt:37,43,55,…`). UI consequence is new → PV-3. |
B2: | S-8 (RLS / user filter) | 9 of 17 tables lack DDL/RLS in the repo; pulls send no `user_id` filter | Open (S2a), B1 owns | My angle only: `deleteAllRows` filter → S-12. `docs/supabase/` still holds 8 `CREATE TABLE`s. |
B2: | 23 Aug foundation item 6 | No app-layer encryption of DB/DataStore; eligible for OS backup; plaintext export | **Closed for backup and export; DB/DataStore unencrypted by decision** | OS channel closed (`AndroidManifest.xml:21-23`, rule files, `tools/check-backup-policy.py`); export is the envelope (`backup-threat-model.md` §4.2); at-rest encryption declined at T1 (`:174`). The Supabase session prefs (above) are a third plaintext store the record does not list. |
B2: | 16 Sep D01–D17 | — | none touch auth, tokens, secrets or the manifest | `docs/design-audit/2026-09-16/AUDIT.md` has no D-item on Account or privacy. |
B3: | S-8 | Server DDL and RLS missing for 9 of 17 tables; pulls send no `user_id` filter | **Open** | `docs/supabase/` holds two files defining 8 tables (packet-2: `custom_exercises`, `custom_exercise_muscles`, `bodyweight_entries`; packet-3: `measurable_goals`, `coach_prefs`, `reminder_prefs`, `display_prefs`, `account_profiles`) against 17 in `domain/SyncEntityType.kt:5-21`. The pull query is `updated_at_ms=gt.X&order=updated_at_ms.asc&limit=500` with no `user_id` term (`Rest:33-34`), and no `apply*` in `Engine` compares `remote.userId` with the signed-in user (`userId` is read only at `Engine:429,441,453,465`, to build a prefs entity id). RLS on the live project for the 9: `R` (Appendix B probes). Not regressed. |
B3: | S-2 (server half) | Server upsert is unconditional, last arrival wins; `gt` cursor with no tiebreaker skips tied rows | **Open** | `Prefer: resolution=merge-duplicates,return=minimal` on every POST (`Rest:22`); tombstones go through the same upsert (`SupabaseSyncRemote.kt:16-18`); neither SQL file declares a trigger, function or CHECK; ordering is `order=updated_at_ms.asc` alone (`Rest:34`). Fix shapes proposed as S-15 and S-16. |
B3: | S-7 (server half) | No server-side delete; `DELETE /auth/v1/user` is not offered to a user JWT | **Open (gated)** | `data/auth/SupabaseAccountAuth.kt:71-77` still wipes 17 tables client-side (`:79-100`) and then calls `rest.deleteAuthUser` (`Rest:70-82`); no function under `docs/supabase/`; the button is held off by `domain/AccountSyncGate.kt:26`. Shape proposed as S-17. |
B3: | ADR-031 §3, server item | RLS on for all 17 tables on the live server, security advisors clean, recorded in the S1 PR | **Open** | Nothing under `docs/supabase/` or in `docs/HANDOFF-NEXT.md` records an applied-on date or an advisor run; the committed SQL covers 8 tables. `R`. |
B3: | 16 Sep D01–D17 | — | n/a | None touches the server: `grep -i "sync\|supabase\|server\|RLS" docs/design-audit/2026-09-16/AUDIT.md` returns nothing. |
B5: | S-9 (part: "sync is not serialised with restore") | Restore and a sync pass can overlap; restore does not reset the pull cursors | Open (S1) | No caller of `DbMaintenance.withMaintenanceLock` under `P/data/sync/` (grep, none); `P/data/repository/LocalBackupRepository.kt:352-580` touches no sync table; ADR-031 §3 bullet "restore holds sync … resets the pull cursors" is S1's. No runtime effect while `AccountSyncGate.SYNC_PAUSED = true` (`P/domain/AccountSyncGate.kt`). The parts S1 does not list are BK-2 below. Sign-out keeping cursors is B1's. |
B5: | 22 Sep Settings row: "`BackupCoordinator` is 985 lines" | Split in F10c | Open (F10c) | `P/ui/settings/BackupCoordinator.kt` is 985 lines. |
B5: | Foundation audit item 2, clause "snapshot failure does not block replacement" | Snapshot failure must abort before Room is touched | Closed | `P/data/repository/BackupService.kt:265-266` writes the verified copy before `restoreJournal.stage` (280) and before any Room write; `P/data/backup/SafetySnapshotStore.kt:16-48` throws on write, rename, re-read or count mismatch; `T/data/local/RestorePrepareTest.kt:135` `commitRestoreAbortsWhenSafetyDirIsUnwritable`. |
B5: | Item 2, clause "retained paths have no recovery UI" | Safety copies must be listable, exportable, restorable, deletable, without paths | Closed (orchestration; screen is B10b's) | `BackupCoordinator.kt:616-624` restore-through-preview, `:628-687` export (protect-or-warn), `:689-695` delete, `:612-614` list; ids only, `SafetySnapshotStore.kt:109-120` refuses anything but `pre-restore-<ms>.json`. |
B5: | Item 2, clause "catalog-only files bypass the empty guard" (threat model T6) | Authored counts on both sides | Closed for the eight counted tables; **incomplete** for goals, templates, planner rules (BK-3, new specifics) | `P/data/backup/BackupValidator.kt:400-404`; `P/data/backup/AuthoredInventory.kt:54-73`; `T/data/local/RestorePrepareTest.kt:95,123`. |
B5: | Item 2, clause "Room/DataStore/reconciliation is not atomic" | Success must never be a mixed result | Closed by design (journaled, not atomic) | Room is one `withTransaction` (`LocalBackupRepository.kt:358-578`); prefs, reconcile and reminders are idempotent and finished from the journal (`BackupService.kt:461-487`); a failed prefs write is a *named* mixed state (`RestoreResult.settingsPending`, `:665`); `T/data/local/RestoreRecoveryTest.kt` (11 tests). Residual outside both the transaction and the journal: the sync tables and the outbox writes in `onPlanDataRestored` (BK-2). |
B5: | 16 Sep D16 (Settings backup row shows features, not status; unknown stamps) | Truthful backup state in Settings | Open (F10a; B10b owns the screen) | `BackupCoordinator.kt:56-62` computes `backupStale` / `backupCaption` / `lastVerifiedBackupAt`; rendering not audited here. |
B6: | 22 Sep Live-workout row | "Rest-timer architecture is sound (P3: non-atomic `adjust`, W2b)" | Closed | #403. `P/timer/RestTimerStore.kt:73-98` is a compare-and-set loop that writes the replacement only over the snapshot it read; `T/timer/RestTimerStoreTest.kt:12-29` (`anAdjustThatLosesTheRaceToACompletionLeavesTheRestFinished`), `T/timer/RestTimerControllerTest.kt:90-118`. |
B6: | 22 Sep Live-workout row | "rest logic duplicated with `RestTimerViewModel`. W2b, W2d" | Closed (rest half) | #411: one `RestCommands` (`P/ui/workout/RestCommands.kt:42-137`) behind the dock and the rest page; `RestTimerViewModel.kt:57,112`. W2d (session-state extraction) is still pending — B11. |
B6: | HANDOFF-NEXT:131-134 quirk (a) | "an alarm set a moment before its row lands" | Open — scheduled for the rest-alarm packet after W2d | Three arm paths do not wait for the row job: `P/timer/RestTimerController.kt:300-310` (`refreshAlarmCapability`, arms at :304 whenever the last commit was healthy, even if this rest's row is still queued), `:296-298` (`rescheduleCurrent`), and inside the job itself `:341` → `scheduleAlarmForCurrent` `:402-411` re-reads `store.current()` rather than arming the `snap` whose row it wrote at `:338`. Consequence: see §3 RT-4. |
B6: | HANDOFF-NEXT:131-134 quirk (b) | "a reschedule that ignores a failed disk write" | Open — scheduled | `RestTimerController.kt:296-298` arms with no look at `_persistenceHealthy`; the guard that exists for the same case is only in `refreshAlarmCapability` `:303-309`. Called from `RestTimerCompletion.kt:43-47` (an EARLY claim) and the instrumented test. Consequence: see §3 RT-5. |
B6: | Foundation README item 1 / risk-register:31-41 | Exact-alarm access neither declared, checked, nor degraded honestly; receiver never re-checks the deadline; no reboot handling | Closed | Declared: `AndroidManifest.xml:14-15` (`SCHEDULE_EXACT_ALARM`, never `USE_EXACT_ALARM`; `ExactAlarmCapabilityInstrumentedTest.kt:64-74`). Checked: `P/timer/ExactAlarmCapability.kt:41-45` and again at `:51` next to the exact API; `RestAlarmPlan.kt:20-31` picks EXACT/INEXACT/FAIL; `RestTimerAlarmScheduler.kt:43-80`. Degraded honestly: `BEST_EFFORT` reaches `RestCommands.kt:80` → `RestHonestyCopy.kt:11,53-55` "May be late when the phone sleeps"; Settings offers the grant only after `restAlarmEligible` (`P/ui/settings/SettingsViewModel.kt:122-127`); `MainActivity.kt:103` rechecks on resume; `ExactAlarmStateReceiver.kt:21-31` re-arms on grant. Due-ness: `P/domain/RestTimer.kt:73-77` (`decide` returns EARLY at :75; the receiver hands it the live or disk deadline, `RestTimerAlarmReceiver.kt:61-74,80-88`). Reboot: dropped by decision (ADR-012 §7) `RestTimerStatePersistence.kt:166-177`. Tests: `RestTimerAlarmSchedulerTest.kt:43,58,73,89,163`, `RestTimerViewModelTest.kt:242`, `RestTimerRehydratorTest.kt:54,145,164`. |
B6: | W2b-1 (#403) | ±15 and finish cannot undo each other across threads; a late ±15 on a finished rest does nothing | Closed | `RestTimerControllerTest.aCompletionThatLosesTheRaceToAnAdjustLeavesTheReplacementRunning:60-88`, `anAdjustThatLosesTheRaceToACompletionKeepsTheRestDoneAndSendsNoStop:90-118`, `completeIfCurrentLeavesANewerTimerAlone:367-387`; `RestTimerStoreTest.clearIfCurrentClearsItsOwnTimerOrAnIdleStoreButNeverAReplacement:32-45`, `adjustOnIdleStoreNeverStartsAPhantomTimer:151-165`. |
B6: | W2b-1b (#405) | A stop names its rest; a skipped rest never says "Rest done"; recovery never brings it back; Skip on a finished card keeps it done; a rest that ran out while the process was dead still finishes | Closed | `RestTimerControllerTest.aSkipTellsTheServiceWhichRestItStopped:153-170`, `anAlarmAlreadyOnItsWayAfterASkipIsNotAFinish:172-199`, `aSkipLandingAsTheRestFinishesLeavesItSkipped:201-222`, `aRecoveryNeverBringsBackARestThisProcessSkipped:224-251`, `aRecoveryNeverAnnouncesARestThisProcessSkipped:253-285`, `aRestThatRanOutWhileTheProcessWasDeadStillFinishes:287-299`; `RestTimerServiceTest.aStopSentForAnEarlierRestLeavesTheNewOneRunning:102-129`, `aLateStopAfterTheNextRestFinishedKeepsItsRestDone:167-200`, `aSkipOnACardLeftAfterItsRestFinishedKeepsItDone:202-232`, `anAlarmAlreadyOnItsWayAfterASkipPostsNoRestDone:390-416`; `RestTimerRecoveryTest.kt:60,76`; `RestTimerStoreTest.theStoreRemembersTheRestsItHeldAndNotOnesItNeverSaw:60-81`. |
B6: | W2b-1c (#406) | No call cancels another's disk job; a SYNC is owed until a job that writes a running rest sends it | Closed | `RestTimerControllerTest.aRestStartedAsTheLastOneFinishesStillReachesTheService:710-751`, `aFinishThatTookItsNumberFirstNeverCancelsTheNextRestsSave:753-791`, `aRestStartedJustBeforeTheFinishTakesItsNumberStillLands:833-872`, `aStartOvertakenByALaterCallStillReachesTheService:874-910`, `aJobANewerCallOvertookBeforeItRanWritesNothing:912-938`, `aSaveThatANewerChangeOvertakesArmsNothing:940-996`, smoke `aFinishOnAnotherThreadNeverCostsTheNextRestItsRowOrWakeup:998-1038`. Code: `RestTimerController.kt:326-365`. |
B6: | W2b-1d (#407) | The notification's Skip names the rest its card shows | Closed | `RestTimerNotificationsTest.theRunningCardsSkipNamesItsRest:67-89`, `aRebuiltCardsSkipNamesTheNewRestNotTheFirst:91-101`; `RestTimerServiceTest.aSkipFromACardForAnEarlierRestLeavesTheNewOneRunning:234-258`, `aSkipSentJustBeforeAPlusFifteenStillEndsTheRest:260-281`, `aSkipFromTheCardRebuiltAfterAPlusFifteenEndsTheExtendedRest:283-300`, `aSkipNamingTheRunningRestEndsIt:319-337`, `aSkipFromACardThatNamesNoRestEndsTheRunningOne:339-354`, `aSkipNamingARestThatJustFinishedKeepsItDone:356-388`; `RestTimerStoreTest.aPlusFifteenIsTheSameRestUnderANewId:95-108`. Code: `RestTimerNotifications.kt:170,261-275`, `RestTimerService.kt:149-163`, `RestTimerController.kt:172-185`. |
B6: | W2b-2 (#411) / ADR-012 decision 18 | The rest a logged set starts is the coach's length; a dock pick holds until a logged set or a lift change | Closed | `T/ui/workout/ActiveWorkoutViewModelTest.afterALoggedSetRestRunsTheCoachsLengthNotOnePickedOnTheDock:1295`, `aPickHoldsUntilTheNextLoggedSetThenTheDockShowsTheCoachsLength:1317`, `aWarmUpStartsNoRestSoAPickOnTheDockHoldsThroughIt:1356`, `aRestPickedOnOneLiftDoesNotStopTheNextLiftSeedingItsOwn:2183`; `RestTimerViewModelTest.theStoredLastPresetIsNotANewPickWhenThePageOpens:271`. Code: `ActiveWorkoutViewModel.kt:2253-2271` → `RestTimer.secondsToStart` (`P/domain/RestTimer.kt:172-183`). |
B6: | W2b-3 (#412) | The lock glance's and the rest page's Skip name their rest; the dock's Skip is unchanged | Closed | `T/timer/RestLockSkipTest.kt:69-131` (four tests, composed through the real `RestLockActivity`), `RestTimerControllerTest.aFinishLandingInsideAnInAppSkipKeepsTheRestDone:793-831`, `T/ui/workout/ManualRestStartTest.kt:136`, `RestTimerViewModelTest.kt:465`. Dock: `ActiveWorkoutViewModel.skipRest` `:2046-2049` → `RestTimerController.stop` `:210-219` (`store.clear()` unconditionally, `afterHalt` names `was.timerId`); pinned, not endorsed, by `ActiveWorkoutViewModelTest.theDocksSkipStillEndsWhateverRunsAndTakesRestDoneWithIt:1472-1493`. |
B6: | 16 Sep D11 | Rest and set timing need clearer visual language; acceptance includes "verify countdown/notification continuity and the effects of Skip and ±15" | Open (partly) | Copy and one ±15 control set landed in W1b (#393). The notification half is held: `RestTimerServiceTest.addFifteenMovesTheTicksWithTheDeadline:538`, `minusFifteenReanchorsTheTicksFromTheStoreWithoutASync:578`, `deadlineFreezesTheShadeClockAtZero:458`; `RestTimerNotificationsTest.runningNotificationFreezesAtZeroInsteadOfCountingThrough:117`. Visual sign-off of the dock and rest page is B11's. |
B7: | S-5 (WorkManager half) | `APPEND_OR_REPLACE` retries without limit; no timeouts | Open (dormant while paused) | `P/data/sync/WorkManagerSyncScheduler.kt:16-27` — `OneTimeWorkRequestBuilder` with `NetworkType.CONNECTED` only, no `setBackoffCriteria`, `ExistingWorkPolicy.APPEND_OR_REPLACE` at :25. `P/data/sync/SyncWorker.kt:18-21` — every failure is `Result.retry()`, `runAttemptCount` never read. Nothing runs today: `PausableSyncScheduler` (`P/data/sync/SyncScheduler.kt:19-27`) and `SyncCoordinator.runPass` (`P/data/sync/SyncCoordinator.kt:57-60`) both stop on `AccountSyncGate.SYNC_PAUSED`. The HTTP-timeout half belongs to the Supabase client (B-sync); the updater's own client does set them (`P/update/DebugUpdateHttp.kt:24-25`, `P/update/DebugApkFetcher.kt:34-35`). `docs/FRONTEND_REDESIGN.md:94` still lists S4 as Pending. |
B7: | 22 Sep "System" row: `SyncWorker` casts the Application (S4) | The worker reaches the graph by `applicationContext as? PersonalTrainerApp` | Open | `P/data/sync/SyncWorker.kt:14`. The same cast in `P/reminder/ReminderWorker.kt:19`, `ReminderActionReceiver.kt:16`, `WorkoutAlarmReceiver.kt:25`, `ReminderRebuildReceiver.kt:23`, `P/ui/update/DebugUpdateBanner.kt:38`. No custom WorkManager initialisation anywhere: grep of `app/src` for `Configuration.Provider|WorkManagerInitializer|InitializationProvider|WorkerFactory|androidx.startup` returns nothing; `P/PersonalTrainerApp.kt:22` is a plain `Application`; `app/src/main/AndroidManifest.xml:19-101` has no `InitializationProvider` override, so the default `androidx.startup` initialiser runs. `docs/architecture/CURRENT_STRUCTURE.md:112-117` already names all six sites. |
B7: | 16 Sep D01–D17 | — | Not in scope | A grep of `docs/design-audit/2026-09-16/AUDIT.md` for reminder/notification/update/alarm hits only rest-timer acceptance text (:135, :214, :238). No D-item touches reminders, the updater or WorkManager. |
B8: | C-1 (22 Sep) | Goal set in Settings never reaches the workout | **Closed** (W1b, #393) | Chain confirmed end to end: `P/data/repository/prefs/CoachingPrefsStore.kt:67,88` (flow + `setTrainingGoal`) → `P/data/repository/PreferencesRepository.kt:146` → `P/ui/workout/ActiveWorkoutViewModel.kt:544–574` (`coachPrefs = extras.coachPrefs`) and `:785` (rest seed) → `P/ui/workout/SetMicroRecUi.kt:14–72` (`prefs = coachPrefs`) → `P/domain/coach/CoachEngine.kt:63–65` → `explanationShort(reasonCode, prefs.goal)` → `SetMicroRec.explanation` → Why sheet. Rest page too: `P/ui/workout/RestTimerViewModel.kt:73–90,119`. Note the goal changes only the Why sentence for two codes (IN_TANK+Strength, CLIMB_REPS+Muscle), never a number — exactly what ADR-029 §3 / ADR-025 §2 allow. |
B8: | C-2 (22 Sep) | Engine runs three times per tap; `generatedAtMs` defeats equality | **Open** (W2c pending, `docs/FRONTEND_REDESIGN.md:81`) | `ActiveWorkoutViewModel.kt:544–579`: `microRec` recomputes on any of 10 upstream flows and stamps `nowMs = time.nowMillis()` (:570) → `RuleTrace.forMicroRec(... generatedAtMs = nowMs)` (`P/domain/RuleTrace.kt:214`) → `SetMicroRec` data-class equality fails → `stateIn` re-emits every time. Second run: prefill rest seed `:774–786`. Third and fourth: `RestTimerViewModel.kt:75,139`. Unchanged since 22 Sep; not regressed. |
B8: | 19 Aug audit §3a | Progression read the chronologically last set (back-off lowered next session) | **Closed** | `P/domain/ProgressionBasis.kt:57–78` (heaviest, then most reps, then later); every repository path goes through it: `P/data/repository/WorkoutRepository.kt:1091,1274,1305`. Pinned by `T/domain/ProgressionBasisTest.backOffSetAfterATopSetDoesNotBecomeTheBasis`, `theWholeChainSuggestsFromTheTopSetNotTheBackOff`, `WorkoutSummaryBuilderTest.aBackOffSessionBreaksNothing`. |
B8: | 19 Aug audit §3b | Planner deleted the last lower-body day when lower volume was low | **Closed** | The heat-driven day deletion no longer exists; `WeeklySchedulePlanner.plan` never drops a day (`P/domain/WeeklySchedulePlanner.kt:31–98`). Emphasis keeps at least one day of the other family: `P/domain/EmphasisLayout.kt:22–29` (`other(it) > 1` guard). Pinned by `RoutineGeneratorTest.fourDayUpperIsMajorityUpperAndKeepsALegDay`, `WeeklySchedulePlannerTest.fourDayUpperEmphasisIsMajorityUpperAndKeepsALowerDay`. |
B8: | Foundation audit item 4 | 100 lb × 5 shown as 501 lb on Summary, 500 lb on Session Detail (two tie-rounding paths) | **Closed** — one path, named | The single path is `WeightConverter.volumeDisplayWhole` (`P/domain/WeightFormat.kt:99–103`): `round(toDisplayValue(kg, unit))`, half-to-even. `util/QuantityFormat.formatVolumeNumber` (:16–20) wraps it. Every surface reads it: Summary hero `P/ui/summary/WorkoutSummaryScreen.kt:291,321` (the count-up at :349 prints the already-rounded Int), floor stats `P/ui/workout/ExerciseStatsRow.kt:93`, `SetCopy.kt:176`, `ActivityDetailCopy.kt:63`, `SessionSummary.kt:89`, Exercise Detail lifetime `ExerciseDetailScreen.kt:629`. Trace: 100 lb → `toKg` = 45.4 kg; ×5 = 227.0 kg; ×2.20462 = 500.4487; `roundToHalf` → 500.5; `round` half-even → **500**. Pinned by `T/domain/VolumeLabelTest.oneHundredPoundsTimesFiveIsFiveHundredNotFiveHundredOne` (:56) and `everySurfacePrintsTheSameBytesForBothUnits` (:95). Exercise Detail's chart labels (`:132` `toDisplayValue`, then `formatSeries` `:633–635` `round`) are a second copy of the same two-step arithmetic and print the same integer — but see DM-3 for what that arithmetic does to accuracy. |
B8: | 16 Sep D01–D17 | Visual findings | n/a | None of D01–D17 is a domain-rule item; nothing to re-verify here. |
B11: | D02 (16 Sep) | A recommendation looks like a selection | Closed (F3.1, W1a) | `ui/components/InstrumentChip.kt:154-163` draws a corner dot for `recommended && !selected` and speaks "recommended"; the ramp presets read `Use 40 kg · 40% · Suggested` (`ui/workout/WeightRepsEditor.kt:558-566`); `RpeSelectorRenderTest.aRecommendationIsSpokenButNeverSelected` |
B11: | D03 (16 Sep) | The footer reserves too much space; the commit must not jump when a receipt goes | Closed (F3.1) | One companion slot ≥ 56 dp (`ui/workout/WorkoutDock.kt:216`) over a 72 dp commit (`:309`); receipts live in the set-history chip, not the dock (`WorkoutDock.kt:115`, `Screen:793`); the dock is the Scaffold `bottomBar` (`Screen:433`) so the commit's bottom edge is the window's; `DockCommitRenderTest.theCommitIsNeverShorterThanSeventyTwoDp`, `WorkoutFloorRenderTest.theEntryLoopStaysWithinItsHeightBudget` |
B11: | D04 (16 Sep) | Logging scrolls the entry away | Closed (ADR-026/027, T1c-2) | No `bringIntoView`/`BringIntoViewRequester` left under `ui/workout` (grep, 25 Sep); the list scrolls to item 0 only on load or a lift switch (`Screen:261-266`) and to the entry on an edit (`:268-270`); `FloorScreenWiringRenderTest.savingASetKeepsTheNumeralsOnScreen`, `LogLoopBringIntoViewTest` |
B11: | D08 (16 Sep) | Exclusive choices exposed a checkbox role | Closed on the floor (app-wide: F8a pending) | RPE track `selectableGroup` + `Role.RadioButton` (`ui/workout/RpeSelector.kt:140,155`); Working \| Warm-up the same (`ui/workout/ExerciseHeader.kt:259-299`) |
B11: | D09 (16 Sep) | Exercise identity hides its navigation | Closed (W1a #389) | "Lift n of N ⌄" pill (`ExerciseHeader.kt:132-139`); `ExerciseHeaderRenderTest.theVisibleSwitchNamesTheLiftsPlaceAndOpensTheSwitcher` |
B11: | D10 (16 Sep) | Numbers do not invite typing | Closed (W1a #389) | Underline drawn on the enabled numeral (`WeightRepsEditor.kt:385-400`), tap opens `NumberEntryDialog` (`:287-320`); `WeightRepsEditorRenderTest.tappingTheWeightOpensItsKeypadAndATypedValueLands` |
B11: | D11 (16 Sep) | Rest / set timing language unclear | Closed (W1b #393) | "Start rest", "Planned · m:ss", "Time set" on the card (`ui/workout/RestTimerCard.kt:138-165,245-284`); `RestTimerCardRenderTest.atRestStartRestIsTheOneControlAndSaysHowLong`, `WorkoutDockTimerRenderTest.timeSetStartsTheSetClockFromTheCardAndFromTheSheet` |
B11: | D12 (16 Sep) | RPE needs persistent help | Closed (W1b #393) | "Effort · optional" with a 48 dp help mark (`RpeSelector.kt:90-110`), the chosen value's meaning under the track (`:163-176`), Clear (`:113-120`); `RpeSelectorRenderTest.helpIsOneTapAwayAndExplainsEachValue`, `theChosenEffortClearsFromClearOrASecondTap` |
B11: | D13 (16 Sep) | Live bar looks more tappable than it is | Not verified here | `ui/navigation/LiveSessionBar.kt` is not the floor; the navigation pass owns it |
B11: | 22 Sep · D09 switch / D10 typing / evidence chip < 48 dp / chips announced twice | W1a items | Closed (#389) | As D09, D10 above; chip `heightIn(min = touchMin)` (`ui/workout/EvidenceCitationChip.kt:34`); one spoken sentence per chip (`ui/workout/SetHistoryStrip.kt:239`, `ExerciseHeader.kt:301-302`); `SetHistoryStripRenderTest.eachChipSaysItsOrdinalSetAndStateOnce`, `ExerciseHeaderRenderTest.aSetTypeChipIsReadOnce` |
B11: | 22 Sep · two "Add set" buttons | W1a | Closed (#389) | The strip offers none (`SetHistoryStripRenderTest.theStripNeverOffersAnExtraSetEvenOnceThePlanIsMet`); the dock has it (`WorkoutDock.kt:271-276`); the saved-sets sheet keeps a second one as a secondary surface (`ui/workout/WorkoutSavedSets.kt:137-139`); `FloorScreenWiringRenderTest.onceThePlanIsMetTheFloorOffersOneWayToAddASet` |
B11: | 22 Sep · churn debris (W2a) | Named dead code gone? | Closed (#409) | `ui/components/SetEntryPanel.kt` is 324 lines, its compact path gone (KDoc `:52-55`), and it is History's set editor, not the floor's (`ui/history/SetEditSheet.kt:95`); `FloorInstrumentBar` keeps one caller (`ui/components/RestTimerUi.kt:270`); `domain/FloorCompactChrome.kt` has three functions, each read in production; every public `ActiveWorkoutViewModel` function has a caller (scripted grep, 25 Sep) |
B11: | 22 Sep · VM 2,541 lines, 23-field state, 13-stage combine, rest logic duplicated | W2b / W2d | Open (W2b-4, W2c, W2d pending) | Now 2,362 lines; `uiState` has 18 `combine` calls (`VM:605-717`); the rest commands are shared (`ui/workout/RestCommands.kt`, W2b-2) but the coach call is still made twice with different inputs — see note W2b-4 below |
B11: | 22 Sep · ~1,100 source-text assertions | T1 | Closed (#388/#392/#404/#408) | 107 `ownedSource`/`SourceText` uses remain across 20 files, kept as bans (for example `LogLoopBringIntoViewTest.kt:22-33`); behaviour is pinned by 51 render/ViewModel test files on `FloorTestKit.showFloor`/`showWorkoutScreen` |
B11: | 22 Sep · token ceilings | W2a | see B12 | — |
B11: | C-1 | The goal never reached the workout's coach | Closed (W1b #393) | `VM:573-574` passes `extras.coachPrefs`; `RestVM:151-152` the same; `FloorRestAndCoachWiringRenderTest.theFloorsCoachSpeaksWithTheTrainingGoal` |
B11: | C-2 | Engine runs three times per tap; `generatedAtMs` defeats equality | Open (W2c pending) | On the Log the engine now runs once per draft change (`VM:544-580`) plus `CoachEngine.fromMicroRec` in composition (`ui/workout/NextSetRecommendation.kt:71`); `RuleTrace.generatedAtMs` is still stamped from `nowMs` (`domain/RuleTrace.kt:28,214`; `domain/SetMicroRec.kt:61`) so `microRec` re-emits on every stepper tick even when nothing changed; the rest page runs it twice more while open (`RestVM:75,139`) |
B11: | W2b-4 (HANDOFF, pending) | The rest page's next-set line can differ from the Log's after Another set and on a lift's first set | Open, confirmed from code | Two call sites with different inputs: `VM:544-580` vs `RestVM:139-155` (and the seed at `:75-90`) — the exact difference is in the note below; `RestTimerViewModelTest.kt:390-393` records the divergence |
B11: | ADR-012 (W2b-3) · dock Skip unchanged | What does the dock's Skip end? | Open (owner decision pending) | `VM:2046-2049` → `RestTimerController.stop` (`timer/RestTimerController.kt:210-213`) ends whatever rest runs and clears "rest done"; `ActiveWorkoutViewModelTest.theDocksSkipStillEndsWhateverRunsAndTakesRestDoneWithIt` pins today's behaviour |
B11: | W1c / W1d | Entry wells hold still on the first working set; large-text floor | Closed (#396, #399) | `FirstWorkingSetRenderTest` (15 cases incl. `raisingTheSystemFontWithTheFloorOpenStillHoldsTheEntry`), `LargeTextEntryRenderTest` (8 cases), `ExerciseStatsRow.kt:126-131,256` reserved label lines |
B11: | W2b-1…W2b-1d | Rest timer CAS, named stops | see B6 | `RestPageSkipTest`, `RestPagesRenderTest.theRestPagesSkipNamesTheRestItDraws` cover the page's side (`ui/workout/RestTimerScreen.kt:255`) |
B11: | W2b-2 | One set of rest commands; dock pick holds until a logged set starts a rest or the lift changes | Closed (#411) | `RestCommands.kt:58-83,105-116`; `VM:2253-2270` starts the coach's length; `ActiveWorkoutViewModelTest.aPickHoldsUntilTheNextLoggedSetThenTheDockShowsTheCoachsLength`, `aWarmUpStartsNoRestSoAPickOnTheDockHoldsThroughIt`, `aRestPickedOnOneLiftDoesNotStopTheNextLiftSeedingItsOwn`, `RestTimerViewModelTest.aDockPickReachesAnOpenRestPageButNotWhileARestRuns` |
B12: | D02 | A recommendation looks like a selection | Closed | `UI/components/InstrumentChip.kt:112-117` (Volt edge only when selected), `:154-163` (corner dot for `recommended`, spoken "recommended"); `UI/components/InstrumentSelection.kt:80-114` (`InstrumentPreset` "Use 15 lbs", `InstrumentSuggestion` "Suggested ·"); W1a #389 |
B12: | D03 | Workout footer reserves too much space | Closed (per record) | `UI/components/PinnedDock.kt:22-45`; F2/F3, W1c #396, W1d #399. Dock geometry is B9a's; not re-measured here |
B12: | D08 | Exclusive filters expose a checkbox role | Open (partly closed) | Radio exists: `InstrumentSelection.kt:54` (`InstrumentChoiceChip` = `Role.RadioButton` in a `selectableGroup` `:32`); History horizon `UI/history/HistoryScreen.kt:392-394` and Body view `UI/progress/BodyMap.kt:158-160`, period `ProgressScreen.kt:306-308` use it. Bare `InstrumentChip` (`Role.Checkbox` default, `InstrumentChip.kt:84`) still on exclusive groups: picker muscle filter `UI/components/ExercisePickerSheet.kt:173-188`, create-row muscle chips `:580-592`, rest presets `UI/components/RestTimerUi.kt:787-798`, History set editor RPE `UI/history/SetEditSheet.kt:108-118`, `UI/activity/LiveCardioScreen.kt:177-194`, `UI/activity/ActivityComposerScreen.kt:463-480`, `UI/reminders/ReminderPrefsSection.kt:181-195`, `UI/library/ExerciseLibraryScreen.kt:158-165`. Packet F8a (pending) |
B12: | D09 | Lift switch hidden | Closed | `UI/workout/ExerciseHeader.kt:139` (`onClickLabel = CurrentLiftCopy.SWITCH`), `UI/components/QuietButton.kt:43-91`; W1a #389 |
B12: | D10 | Numbers do not invite typing | Closed | `UI/workout/WeightRepsEditor.kt:402` (`onClickLabel = typeLabel`), `:407-410` (Decrease/Increase/Type custom actions); W1a |
B12: | D11 | Rest/set timing language | Closed | `UI/workout/RestTimerCard.kt:138-152` (planned rest spoken apart from time left); W1b #393 |
B12: | D12 | RPE help | Closed | `UI/workout/RpeSelector.kt:99` help button; `RpeSelectorRenderTest.kt:104` pins the "Effort · optional" heading; W1b |
B12: | D13 | Live bar looks more tappable than it is | Closed | `UI/navigation/LiveSessionBar.kt:144-153`: the whole main region is one `Role.Button` with `onClickLabel = resumeLabel`; menu is a separate `IconButton` `:212-222` |
B12: | D14 | Completed planning too prominent on Home | Open | F4 pending (B-Home pass) |
B12: | D15 | Component roles, gallery covers states | Open (partly) | Typed controls exist (`InstrumentSelection.kt`, `QuietButton.kt`, `InstrumentSwitch.kt`); the gallery contract is not met — AX-6 below |
B12: | D16 | Header actions / Settings hierarchy | Open | One `ScreenHeader` (`UI/components/ScreenHeader.kt:36-104`, 11 callers) closed the "hand-built headers" half; Settings grouping is F10a-c |
B12: | D17 | Full-screen coverage as a gate | Closed as re-decided | ADR-032 (JVM renders); X3 #395. Not mine (B13b) |
B12: | 22 Sep · chips announced twice | Spoken form joined the visible label | Closed | `InstrumentChip.kt:143-151` and `QuietButton.kt:76-78` (`clearAndSetSemantics { contentDescription = spoken }` after the click modifier so role survives); `UI/workout/SetHistoryStrip.kt:238-239`; W1a #389 |
B12: | 22 Sep · evidence chip under 48 dp | | Closed | `UI/workout/EvidenceCitationChip.kt:32-37` (`heightIn(min = Metrics.touchMin)`, `Role.Button`) |
B12: | 22 Sep · every token ceiling at 100 % | | Open | W2a lowered four (`tools/checker-baselines.toml:40-56`), then re-based to the new counts. Run 25 Sep: all 15 families equal their ceiling (`alpha_copy 7/7, raw_dp 14/14, motion 11/11, stock_icons 9/9, xml_hex 12/12, xml_monospace 2/2, named_color 2/2, defaults_elevation 1/1, stock_component 1/1`, the rest 0/0). Zero headroom: AX-7 |
B12: | 22 Sep · Home week-strip cells ≈ 43 dp at 360 dp | | Open | `UI/components/WeekStrip.kt:66-83` (`weight(1f)`, 4 dp gaps, no width floor); F4. The same strip on Custom week: AX-2 |
B12: | 22 Sep · 6 unused components, churn | | Closed | W2a #409 |
B12: | 22 Sep · rest-timer architecture sound | | n/a | B6 / B9a |
B12: | DESIGN_AUDIT G-01 (≥ 48 dp) | | Closed | `UI/theme/Metrics.kt:61,86,89,121`; 98 `touchMin` uses |
B12: | DESIGN_AUDIT ACC-02 (clock must not announce every tick) | | Closed | No live region on a running clock: `RestTimerCard.kt:204-209` (only when `justFinished`), `RestTimerCardRenderTest.kt:235` `theRunningClockIsNeverALiveRegion`; see AX-11 for the focused-node caveat |
B12: | DESIGN_AUDIT ACC-03 (colour-only heat) | | Closed | `UI/theme/Color.kt:117-131` luminance ramp; `BodyMap.kt:241-275` rows speak the load |
B14: | 22 Sep System 1 | Docs contradicted the code (live code, Room version, counts) → X1 | Closed (X1); leftovers reopened as DC-1 (P2), DC-2…DC-8 (P3) | `docs/HANDOFF-NEXT.md:8-10` rewritten 23 Sep; `README.md:139` says v7; `docs/architecture/CURRENT_STRUCTURE.md:3-4` re-measured 24 Sep. Still wrong: `docs/DEVELOPMENT.md:488-489` "version 4, frozen" vs `FoundationGeneration.kt:17` `VERSION = 7` |
B14: | 22 Sep System 2 | No test ran `MIGRATION_TEMPER_6_7` | Closed (#382) | `app/src/test/java/com/sinura/personaltrainer/data/local/TemperMigration6To7Test.kt`, `TemperSchemaAssetsTest.kt` exist; 3,149 tests green (§7) |
B14: | 22 Sep System 3 | Container gate skipped `lintDebug` | Closed by process, not by mechanism → BR-8 | `docs/HANDOFF-NEXT.md:121-125` lists it; baseline `lintDebug` 0/0. Nothing in Gradle ties lint to the push gate: `app/build.gradle.kts:396,405-409` hang only `staticChecks` off `Test` tasks; hosted `ci.yml:84-85` runs it but is not a required check (ADR-024, owner) |
B14: | 22 Sep System 4 | `SyncWorker` casts the Application → S4 | Open (S4 pending, `docs/FRONTEND_REDESIGN.md` row 28) | `data/sync/SyncWorker.kt:14` `applicationContext as? PersonalTrainerApp ?: return Result.success()` |
B14: | FND-026 | Lint 64 warnings, non-gating | Closed (P4.6) | `app/build.gradle.kts:90-92` `warningsAsErrors/abortOnError/checkReleaseBuilds`; `app/lint-baseline.xml:2-4` empty; baseline run 0 errors 0 warnings; `docs/FOUNDATION_PROGRAM.md:575` done |
B14: | FND-027 | 2024 stack behind stable | Closed (P4.1–P4.6); residual BR-4 | Every catalog pin matches its signed matrix: AGP 8.9.2 (`sdk36-compatibility.md:45`), Kotlin 2.0.21, core 1.17.0 / lifecycle 2.10.0 / activity 1.12.4 / coroutines 1.10.2 / Robolectric 4.16 / test 1.7.0+1.3.0 (`core-toolchain.md:17-23`), BOM 2026.06.01 / nav 2.9.8 (`compose-toolchain.md:18-19`), Room 2.7.2 / DataStore 1.2.1 (`persistence-toolchain.md:29-30`), play-services-auth 21.6.0 (`drive-auth.md:17`), `robolectric.properties` `sdk=35` (`sdk36-compatibility.md:51-56`). No drift |
B14: | FND-029 | Release unminified/unshrunk; versionCode 1 | **Regressed** → BR-1 | Minify/shrink on (`app/build.gradle.kts:149-150`) but `assembleRelease` fails on `trunk` (§7). versionCode 1 is by decision (`docs/FOUNDATION_PROGRAM.md:796-798`, `SETUP.md:381`) |
B14: | FND-041 | README stale | Closed (P0.2); residual DC-6 | `README.md:95-107` says backup, not sync; `README.md:43` still labels the dated foundation audit "canonical current-state" |
B14: | FND-042 | `main` in operational docs | Closed for docs; residual DC-5 | `check-doc-authority` 0; `.github/ISSUE_TEMPLATE/config.yml:4` still links `blob/main/…`; `.github/` is outside the checker's list (`tools/check-doc-authority.py:20-28`) |
B14: | FND-014 | Field failures invisible | Closed (P12.1) | `PersonalTrainerApp.kt:159-197` crash capture; `DiagnosticRedaction.kt`, `LastCrashStore.kt` |
B14: | HANDOFF "Known tooling gaps" | `required_args_mixed` largest debt family | Open, unchanged (181) | `tools/checker-baselines.toml:6-13`; run prints "181 mixed-argument call(s) skipped (baseline 181)". These are call sites that mix named and positional arguments, which `check-required-args.py` declines to judge rather than guess (`tools/check-required-args.py:14-19`). Not defects; a blind spot the ratchet keeps from growing |
B14: | HANDOFF "Eight stale branches" | Owner must delete eight branches | Open — proven | `git ls-remote --heads origin` lists 37 heads: all eight named in `docs/HANDOFF-NEXT.md:191-198` (`claude/app-audit-optimization-xnqf5e`, `claude/ecstatic-galileo-pw9iub`, `claude/file-visibility-check-jraqc2`, `claude/google-signin-integration-xijk5e`, `codex/home-day-design`, `cursor/live-workout-clarity-a-35c4`, `cursor/live-workout-clarity-research-3c1f`, `probe/delete-permission-test`) plus 27 `debug-live/*` and `trunk`. No `main`. The local clone shows only two because the session fetch was narrow, not because they were deleted. ADR-002 §3 calls leftover heads a process defect |
B14: | 16 Sep D01–D17 | visual items | none touch this pass | — |
B4a: | 22 Sep "System" (migrations) | No test ran `MIGRATION_TEMPER_6_7`; device test stopped at v6; guard wanted | **Closed** (X2a, #382) | `T/data/local/TemperMigration5To6Test.kt:29,51` and `TemperMigration6To7Test.kt:25,39,69-76` call `runMigrationsAndValidate(…, true, …)`; `6To7Test.kt:66-81` chains v4→v7; device twin `app/src/androidTest/…/TemperMigrationDeviceTest.kt:34-44` chains v1→v7; guard `T/data/local/TemperSchemaAssetsTest.kt:22-40`; all nine `app/schemas/**/N.json` byte-identical to `app/src/debug/assets/**/N.json` (`cmp`, this pass) |
B4a: | 22 Sep X2b (#402) | A raw copy of `temper.db` with its WAL before every migration (ADR-010 decision 12) | **Closed** | `P/data/local/TemperPreMigrationCopy.kt:64-123`; called from `PreMigrationSnapshot.ensure` (`PreMigrationSnapshot.kt:58-67`) at `P/PersonalTrainerApp.kt:101`, before `container = AppContainer(this)` at `:103`; pinned by `T/data/local/PreMigrationStartupOrderTest.kt:21-30` and the 20 tests in `TemperPreMigrationCopyTest.kt`. Free-space rule `bytes * 3 + 32 MiB` (`TemperPreMigrationCopy.kt:130-131`), keep-2 pruning (`:51,165-176`), failure → app opens and migrates unmarked (`:115-119`) all match the ADR text |
B4a: | S-1 (local half) | REPLACE on parents cascades children; sync fixed (S0b), local callers still REPLACE | **Open, unchanged, not worse** | Sync writes use `@Upsert` (`P/data/local/dao/ActivityDao.kt:232-245`, `RoutineDao.kt:37-38`, `P/data/sync/SyncEngine.kt:331`) pinned by `T/data/sync/SyncPullInPlaceTest.kt:77`. Local REPLACE survives at `ActivityDao.kt:196,215`, `RoutineDao.kt:29,46,70,73`, `WorkoutDao.kt:160,220,223,474,477`, `ExerciseDao.kt:91`. Every local caller is proven unable to hit an existing parent with children (DB-1, DB-3 in §3), so no P1 |
B4a: | CURRENT_STRUCTURE "Six soft foreign keys" | Text says six, lists seven | **Confirmed** (docs) | `docs/architecture/CURRENT_STRUCTURE.md:194-198` lists seven columns; logged for B14 in §4 |
B4a: | 16 Sep D01–D17 | Visual findings | **None touch this scope** | grep for room/migrat/database/dao/schema/foreign in `docs/design-audit/2026-09-16/AUDIT.md` returns nothing |
B4b: | S-3 (repository half) | Outbox written after the save commits, not in the same transaction | **Open**, and uneven: 6 hooks are inside the transaction, 13 are after it, 3 fire *before* the delete they describe (see Notes, "S-3 inventory") | Inside: `RoutineRepository.kt:114,200` (`writeRoutine` txn), `ExerciseRepository.kt:443,479` (`writeExercise` txn), `PlannerRepository.kt:274` when reached via `removeTimedRule:238` / `onRoutineDeleted:248`. After: `ActivityRepository.kt:212,265-270,300-304,366-368`; `PlannerRepository.kt:85,106,111,306,351,364,392,413,552,587`; `RoutineRepository.kt:49,62,140,171,183`; `GoalRepository.kt:360,385`; `PreferencesRepository.kt:502`. Before the delete: `RoutineRepository.kt:66-67`, `ExerciseRepository.kt:496-497`, `GoalRepository.kt:390-394` |
B4b: | CURRENT_STRUCTURE "Known debt": `WorkoutRepository` 1,340 lines, two subjects | Open | `data/repository/WorkoutRepository.kt` is 1,340 lines; the analytics half is `1037-1312` (`lastFinishedWork`, `progressionFor`, `lastPerformance`, `recordsBrokenBy`, `historyBefore`, `readyForProgression`) |
B4b: | CURRENT_STRUCTURE "Known debt": `PlannerRepository` fuses rules with reminder delivery | Open | `PlannerRepository.kt:421-628` (`processDueDelivery`, `snoozeDelivery`, `markDeliveryStatus`, `rebuildReminders`) beside the rule and occurrence writes at `109-415` |
B4b: | CURRENT_STRUCTURE "Known debt": no repository interfaces; 13 ViewModels import repository outcome types | Open; count re-verified at 13 | `grep "import com.sinura.personaltrainer.data.repository\." ui/**/*ViewModel.kt` → ActivityComposer, History, SessionDetail, Home, ExerciseLibrary, Onboarding, PlanDay, Plan, CustomWeek, RoutineEditor, Settings, ActiveWorkout, StartOptions. Only `data/repository/prefs/*` and `insights/TrainingInsightsPublisher.kt` are interfaces |
B4b: | completed-training-convergence.md §5 (R17): `sessionsBetween(0, end)` full-graph read is the expected outlier | Open (known, measurement still pending) | `CompletedTrainingRepository.kt:416-422` reads every strength graph and every activity graph; called from `ui/history/HistoryViewModel.kt:123,218` and `ui/plan/PlanViewModel.kt:128` |
B4b: | 16 Sep D01–D17 | None touch this scope (all visual) | `docs/design-audit/2026-09-16/AUDIT.md:29-183` |
B9a: | D01 (16 Sep) | Home said "TRAINED TODAY" for another selected date | Closed (Q1, #384). No regression. | `domain/MastheadCopy.kt:29-52` takes `relation`; `ui/home/HomeScreen.kt:282-295` passes `DayRelation.of` and the Back-to-today target; every acceptance case (yesterday done, today done, today with another planned block, future planned, empty past/future, past rest) is pinned by `app/src/test/.../domain/MastheadCopyTest.kt:280-366` and the route back by `HomeMastheadTest`, `FirstLaunchRenderTest:77-89`. Residual: the empty-agenda `ThisWeekCard` still says "Today" on another day — that is the 22 Sep F4 row below, not a D01 regression. |
B9a: | D05 (16 Sep) | Body figure and heat do not line up | Open (F5b) | `ui/components/FigureArt.kt:131-169`: the raster is cropped to the box aspect (`stillSrc`, :141) and drawn to fill it (:142-149); the plates are hand-authored fractions of the same box (:182-183, :369-517) at `STILL_HEAT_ALPHA` 0.72 (:176). Both share one coordinate system (the figure box), so a misalignment can only come from the raster's crop or from the fractions not matching the picture; nothing pins either. Probe that would prove it: a JVM test decoding `temper_front_unlit`, applying `stillSrc`, and asserting every working plate's centroid lands on an opaque figure pixel and inside `FIGURE_OUTLINE` (:239-295). |
B9a: | D06 (16 Sep) | Body sizing ignores the live bar | Open (F5a) | `ui/progress/BodyViewport.kt:20,32-33,61` subtracts `TAB_BAR_DP` 64 only; `ui/progress/BodyMap.kt:80-82` sizes from `LocalWindowInfo.containerDpSize`, not the measured content area. With a live session the bar (≥ 56 dp, `LiveSessionBar.kt:139`) is not modelled; `BodyViewportTest:11-28` pins the arithmetic without it. |
B9a: | D13 (16 Sep) | Live bar looked more tappable than it was | Closed. | `ui/navigation/LiveSessionBar.kt:144-153` the whole weighted main Row is `clickable(role = Button, onClickLabel = resumeLabel)`; the overflow `IconButton` (:212-215) is a sibling. Pinned by `FrontendShellInstrumentedTest:166-185` (hosted, non-blocking lane only). |
B9a: | D14 (16 Sep) / 22 Sep Home "Finished blocks not tappable" | A done block is a dead readout, no link to the recorded session | Open (F4) | `ui/home/DailyAgendaCard.kt:234-256`: `canOpenStart` is false for DONE (`domain/DailyAgenda.kt:55-64`), so `action = null` and `onOpen = null`; `ui/home/DayBlock.kt:70-71` then draws it as a plain card. |
B9a: | 22 Sep Home | Week-strip cells about 43 dp wide at 360 dp | Open (F4) | (360 − 2×16 gutter `HomeScreen.kt:267-272` − 6×4 `WeekStrip.kt:71`) / 7 = 43.4 dp; only the height is floored (`ui/components/WeekStrip.kt:123`). The strip's own doc says "never scrolling" (:50) against ADR-026 §6. |
B9a: | 22 Sep Home | No read-error state | Open (F4). Mechanism now exact: it is "loading forever", not "no message". | Every flow Home combines drops `Unavailable` and hides `Degraded`: `data/repository/FlowGuards.kt:40-46` (`presentValues`), `PlannerRepository.kt:48-61`, `WorkoutRepository.kt:253-256`, `RoutineRepository.kt:26-28`, `ActivityRepository.kt:108`; `ui/home/HomeViewModel.kt:97-194` has no catch or health branch and `state.error` (:165) is only an action error. An unreadable table on first read leaves Home on `ScreenLoading` (`HomeScreen.kt:150-153`) with no retry; a later failure keeps stale data silently. |
B9a: | 22 Sep Home / F4 | `ThisWeekCard` on other days | Open (F4) | `ui/home/ThisWeekCard.kt:86-90` kicker is "Today" whenever the slot is a training day; :162-188 offers "Start this session" for the selected day's slot with no `DayRelation`; `HomeScreen.kt:388-390` starts it for that date. Reachable only when `WeekBoard.leftoverBelongsOn` (`domain/WeekBoard.kt:118-137`) is true: a rule with no occurrence on the day (before `ensureCurrentWeek` lands, or a weekday-named routine that matches). |
B9a: | 22 Sep Body | Legend does not wrap | Open (F5c) | `ui/progress/BodyMap.kt:196-208` is a `Row(SpaceBetween)`; `Kicker` is `maxLines = 1` + Ellipsis (`ui/components/GymSurfaces.kt:90-91`). By glyph estimate (kicker 11 sp, 0.88 sp tracking; `ui/theme/Type.kt:125`) the four swatches need ≈ 358 dp at font 1.6 and ≈ 447 dp at 2.0 against 328 dp, so the last swatch is squeezed and ellipsized, not wrapped. `R` for the exact font scale it starts at. |
B9a: | 22 Sep Cross-tab | Two overlapping start sheets | Open, by decision (ADR-021 §2 keeps both) | `ui/home/HomeStartSheet.kt:60-110` on Home; `ui/workout/StartOptionsSheet.kt:80-267` hosted at `AppNav.kt:745-768` for Body/History/Plan. Both offer free / routine / cardio starts; see UI-11 for the one behavioural difference. |
B9a: | 22 Sep Cross-tab | 10 of 21 ViewModels lose state on process death | Open; low impact in this scope | Home, Progress, LiveSessionBar, StartOptions, OnboardingGate have no `SavedStateHandle`, but the screen state that matters is `rememberSaveable` — see UI-5 for the full list and the two real losses. |
B9a: | 22 Sep Cross-tab | Four hand-built tab headers | Open (F10/F6 own); Body's is `ui/progress/ProgressScreen.kt:275-317` | Not re-audited here. |
B9a: | L-1 (22 Sep) | Chooser let taps, TalkBack, Back through; choices unreachable | Closed (Q1). No regression. | `ui/saveposture/SavePostureChooser.kt:76` BackHandler, :82/:191-195 touch block, :83 paneTitle, :88-93 scroll + safe-drawing padding; `MainActivity.kt:72` hides the app beneath; `SavePostureChooserTest` (7 tests) and `FirstLaunchRenderTest` (3 sizes × 3 scales, every button reachable) pin it. |
B9a: | L-2 (22 Sep) | Permission walk covered the sign-in form | Closed (Q1). No regression. | `domain/LaunchPermissions.kt:42-47`, `AppNav.kt:216-217,352-365`; `LaunchPermissionsTest:9-61`, `ShippingTabsTest:33-44`. |
B9b: | 22 Sep Plan row · a | Routines hidden behind a toggle | Open (F7a pending). Note: ADR-021 §6 (docs/architecture/ADR-021-home-start-and-day-add.md:90) says collapsed *by decision*; the 22 Sep row and F7a reverse it without amending the ADR → DC for B14 | `UI/plan/PlanScreen.kt:279` (`routinesOpen = false`), `:449-471` |
B9b: | 22 Sep Plan row · b | Delete only by long-press | Open (F7a). `combinedClickable` has `onLongClickLabel` (a TalkBack custom action) but no `role` and no visible affordance; the hint "Long-press a routine to delete it." shows only while the list is open | `UI/plan/PlanScreen.kt:453-455`, `:644-651` |
B9b: | 22 Sep Plan row · c | "Programs" vs "routines" | Open (F7a). Same row says "Show routines" and "N programs" | `UI/plan/PlanScreen.kt:452-458` |
B9b: | 22 Sep Plan row · d | Dead UI: `PreferenceBlock`, `PlanLighterChip`, `PlanRecoveryCommands` | **Open — still there, and wider than listed** (see UI-4). W2a (#409) touched 2 lines under `ui/routines` and nothing under `ui/plan` (`git show --stat 8643ba3`). `PreferenceBlock.kt` (100 lines) has no caller in `main`; `PlanLighterChip` is called only from `PlanPassInstrumentedTest.kt:82`; `PlanRecoveryCommands` only from `app/src/debug/.../PlanPreview.kt:24,43` and `PlanPassInstrumentedTest.kt:39,63` | `UI/plan/PreferenceBlock.kt:1-100`, `UI/plan/PlanScreen.kt:577-587`, `:694-804` |
B9b: | 22 Sep Plan row · e | `RoutineEditorViewModel` is 1,438 lines | Open (F7b). Now 1,490 (`wc -l`; CURRENT_STRUCTURE.md:182 agrees). The 52 added lines are R0 and X4 fixes | `UI/routines/RoutineEditorViewModel.kt` |
B9b: | 22 Sep cross-tab | 10 of 21 ViewModels lose screen state on process death | Partly closed for this scope: `PlanScreen` and `PlanDayScreen` keep selection, toggles and pending dialogs in `rememberSaveable`; the editor keeps id/name/notes/created in `SavedStateHandle` and its wells in `rememberSaveable`; custom week keeps its draft. What is still lost: editor paste results and custom-week launch seed (UI-3, UI-7) | `UI/plan/PlanScreen.kt:277-279`, `UI/plan/PlanDayScreen.kt:81-85`, `UI/routines/RoutineEditorViewModel.kt:1415-1420`, `UI/routines/SessionLiftStrip.kt:507-515`, `UI/routines/SavedStateCustomWeekDraft.kt:36-50` |
B9b: | 22 Sep Library row | "No matches" offers only "Create lift" (F8b) | Open from the picker's side too: a muscle chip plus a no-hit query shows "Nothing in X matches that name." with no way to clear the chip | `UI/components/ExercisePickerSheet.kt:308-324` |
B9b: | R0 (#391) | Second tap in the routine picker never lost | Closed | `UI/routines/RoutineEditorViewModel.kt:1064-1128`; tests `RoutineEditorViewModelTest.kt:539,564,641,702` |
B9b: | X4 (#398) | Leave anyway no longer strands the screen when a save finishes at that instant | Closed. Sibling races checked: Save twice (`:714` guard; test `:1412`), Back during Save (`:665` guard; `BackHandler` at `RoutineEditorScreen.kt:87` routes to the same guard), rotation during Save (exit is a `StateFlow`, `RoutineEditorScreen.kt:81-85`) | `UI/routines/RoutineEditorViewModel.kt:1347-1358`; test `RoutineEditorViewModelTest.kt:1365-1394` |
B9b: | W2a (#409) | Dead code removed | Not applicable to Plan: W2a was the workout floor. Plan dead UI is F7a's (still present, row d) | `git show --stat 8643ba3` |
B9b: | CURRENT_STRUCTURE known debt | `PlanDayViewModel` has no tests | Open (UI-9) | docs/architecture/CURRENT_STRUCTURE.md:208 |
B9b: | F7 (FRONTEND_REDESIGN) | Plan/routines, ordering, editing, import and failed-save recovery | Pending as a packet; import (paste) and truthful Save/Back are implemented | `UI/routines/RoutineEditorViewModel.kt:665-779`, `:868-935` |
B9b: | 16 Sep D08 | Exclusive filters expose a generic checkbox role | Picker muscle chips are `InstrumentChip` → see B12 | `UI/components/ExercisePickerSheet.kt:167-189` |
B9b: | 16 Sep D17 | Full-screen visual coverage as a gate | Open for PlanDay: no render test, no preview (UI-9) | — |
B10a: | 22 Sep History / 16 Sep D07 | Two period models on one screen | **Open** (F6a pending) | Two independent states `visibleMonth` and `horizon` (`UI/history/HistoryViewModel.kt:88-89`); `horizon` drives the readout only (:254-259, :188-229), `visibleMonth` drives the calendar only (:246-250), the list is always the whole log (:171). The caption says so on purpose: `domain/HistoryCopy.kt:11-12` "Totals for this window. Calendar and the list stay all history." Contradicts ADR-026 decision 5. Multi-session days now open a sheet (`UI/history/HistoryScreen.kt:204-214`, :498-559), so D07's "correctly handles multiple sessions" half is closed. |
B10a: | 22 Sep History | Period lost on process death | **Open** (F6a "period saved" pending) | `HistoryViewModel` has no `SavedStateHandle` (`UI/history/HistoryViewModel.kt:84-89`); `visibleMonth` and `horizon` are plain `MutableStateFlow`s. What survives: `selectedDayEpoch`, `monthExpanded` (`UI/history/HistoryScreen.kt:107-108`, `rememberSaveable`). |
B10a: | 22 Sep History | Mixed sessions titled "Cardio" in one view, "Workout" in another | **Open, latent** | Three fallback title rules disagree: list `domain/SessionSummary.kt:122` ("Cardio" if any cardio seconds, else "Workout"), day sheet `UI/history/HistoryScreen.kt:536-540` ("Cardio" for *any* activity, so a backdated strength-only activity is "Cardio" there and "Workout" in the list), and `domain/HistoryEntries.kt:184-193` ("Mixed session"), which has **no callers**. Reachable only for a blank-title row: the composer always writes one (`UI/activity/ActivityComposerViewModel.kt:392-402`), live cardio writes "Cardio" (`UI/workout/StartOptionsViewModel.kt:217`). Session Detail names an unnamed workout "Session" (`UI/history/SessionDetailScreen.kt:138`) where History and Summary say "Workout" (`SessionSummary.kt:122`, `domain/WorkoutSummary.kt:154`). Not regressed. |
B10a: | 22 Sep History | Set editor uses a different effort control | **Open** (F6b pending) | `UI/history/SetEditSheet.kt:104-117`: five `InstrumentChip`s under a plain "Effort" kicker, default `Role.Checkbox` (`UI/components/InstrumentChip.kt:84`, :123-131 `toggleable`), no ends, no help, no Clear. Floor: `UI/workout/RpeSelector.kt:55-140` — `selectableGroup` (:140), "Effort · optional" spoken (:93), help mark (:95-112), Clear (:114-122). |
B10a: | 22 Sep Library / F8b | "No matches" offers only "Create lift" | **Open** (F8b pending) | `UI/library/ExerciseLibraryScreen.kt:210-223`: `EmptyState` has one action slot (`UI/components/EmptyState.kt:24-34`), `actionLabel = "Create lift"`; the body text says "Clear search or filters" but nothing clears them. |
B10a: | 22 Sep cross-tab | Hand-built tab headers | **Open** (History) | `UI/history/HistoryScreen.kt:122-138` builds its own `Row`/`Text("History")`; Library uses `ScreenHeader` (`ExerciseLibraryScreen.kt:96-101`). |
B10a: | 22 Sep cross-tab | 10 of 21 ViewModels lose state on process death | **Open** (History, Library) | Library loses: query, muscle filter, equipment filter, expanded families, the editor draft (`ExerciseEditorDraft`: name, muscle, notes, load), pending delete, blocked-delete dialog, add-to-routine sheet, banners (`UI/library/ExerciseLibraryViewModel.kt:84-97`); and the Body muscle re-seeds because `routeMuscleSeeded` is a field (:192-198, `ExerciseLibraryScreen.kt:87-89`). Survives: `overflowExerciseId` (:86), editor's `typingGroup` (`ExerciseEditorSheet.kt:52`). Session Detail, Exercise Detail, Summary are nav-argument only (`SessionDetailViewModel.kt:61-66`, `ExerciseDetailViewModel.kt:81-86`, `WorkoutSummaryViewModel.kt:58-63`): fine. |
B10a: | 22 Sep (refuted) | History's bodyweight showing zero | **Refuted stands** | `SetCopy.workColumn` prints "—", never "0", for a summary with no tonnage (`domain/SetCopy.kt:174-184`); the block card's "Bodyweight" line is null when weight did not move (`HistoryScreen.kt:617-625`). Not re-raised. A *distinct* cross-screen claim is in §3 UI-10. |
B10a: | 16 Sep D08 | Exclusive filters expose a checkbox role | **Closed for History horizons; Open for Set editor and Library** (F8a pending) | Horizons use `InstrumentChoiceChip` with `Role.RadioButton` (`UI/components/InstrumentSelection.kt:54`; `HistoryScreen.kt:392-401`). Still `InstrumentChip` (checkbox): RPE 6–10 in `SetEditSheet.kt:108-114`; muscle and kit chips `ExerciseLibraryScreen.kt:158-190`. |
B10a: | 16 Sep item 1 / F6 | "800 min" unreadable; readable h/min | **Open** (F6 pending) | Raw minutes everywhere: readout `HistoryScreen.kt:446-451`, row `UI/components/GymSurfaces.kt:555-559`, receipt `SessionDetailScreen.kt:434-438`, summary `WorkoutSummaryScreen.kt:194-200`. No empty-range recovery either: a zero window shows "0 sessions" with no way out (`HistoryScreen.kt:407-458`). |
B10a: | Foundation item 4 | 501 lb vs 500 lb across Summary and Session Detail | **Closed by construction** (B8 owns the arithmetic) | Every volume path ends in `WeightConverter.volumeDisplayWhole` (`domain/WeightFormat.kt:99-103`). Summary hero: `volumeAnimationTarget` (:106-107) printed by `util/QuantityFormat.formatGroupedNumber` (`WorkoutSummaryScreen.kt:291`, :349). Summary lift rows, Session Detail receipt, History rows, calendar, blocks: `SetCopy.workColumn` → `WeightConverter.formatVolumeNumber` (`WorkoutSummaryScreen.kt:511`, `SessionDetailScreen.kt:403`, `GymSurfaces.kt:549`, `TrainingCalendarCard.kt:201`, `HistoryScreen.kt:603`). Exercise Detail all-time tile: `QuantityFormat.formatVolumeNumber` (`ExerciseDetailScreen.kt:257`, :629-630). Grouping differs (§3 UI-11). |
B10a: | Foundation item 5 | 360 dp history rows erase identity | **Closed** | Title on its own line, `maxLines = 2` (`GymSurfaces.kt:474-481`); metrics on a second row with fixed 48/88/48 dp columns (:530-566). Pinned by `app/src/androidTest/.../ui/components/IdentityBeforeMetricsInstrumentedTest.kt:53-97` and `.../ui/history/HistoryPassInstrumentedTest.kt:41-71` (non-blocking lane). |
B10a: | F6b | Error banner / read-error state on History | **Present** | `DataHealth` on the list (`HistoryViewModel.kt:142-180`, :398-435): unavailable → retry page (`HistoryScreen.kt:144-155`), degraded → stale caption (:166-178), repeat errors → `GymErrorBanner` (:324-332). Session Detail: `failed` before `missing` (`SessionDetailScreen.kt:189-215`). |
B10b: | 22 Sep Settings "11 rows in unnamed groups" | Root is three `GroupedList`s with no visible name | Open (F10b) | `UI/settings/SettingsHome.kt:64,107,142` (items keyed `training`/`keep`/`about`, no label); 11 rows :66-158 (+ a debug-only Update row :51-63). The `SettingsGroup` component can draw a kicker (`UI/settings/SettingsChrome.kt:56-58`) but all 8 callers pass `title = ""` (`AboutSections.kt:45,62,83,111,139`, `BackupRestoreSection.kt:88`, `BodyweightPrefsSection.kt:76`, `RestTimerPrefsSection.kt:41`) |
B10b: | 22 Sep "three save rows" | How you save · Account · Backup side by side | Open (F10a) | `SettingsHome.kt:109-131` |
B10b: | 22 Sep "static subtitles" | 4 of 11 subtitles are constants | Open (F10a) | static: Backup :127 (`BACKUP_SUMMARY` "Export, restore, Drive"), Your plan :135 (`PlanSetupCopy.ROW_TITLE`), Diagnostics :146, About :154. Live: Display, Reminders, Week generator, Rest timer, Bodyweight, How you save, Account (`SettingsScreen.kt:192-213`) |
B10b: | 22 Sep "reused icons" | Same glyph on two rows, twice | Open (F10b) | `TemperIcons.Backup` on How you save :112 and Backup :128; `TemperIcons.Bodyweight` on Bodyweight :101 and Account :120 |
B10b: | 22 Sep "two ways to regenerate a week" | Settings → Week generator → Generate a week, and Settings → Your plan → guided setup | Open, **Worse** (see UI-3) | `SettingsScreen.kt:543-547` (no caption, no confirm) vs `:361-366` + `domain/PlanSetupCopy.kt:11-17` (caption + seven-question preview). Both end in `data/repository/OnboardingApplier.kt:62-119` |
B10b: | 22 Sep "three heading styles" | Root header, subpage header, kicker | Open (F10b) | root: plain `Text` `SettingsChrome.kt:23-34`; subpage: `ScreenHeader(titleStyle = display)` `SettingsSubpage.kt:27-36`; sections: `GymSectionHeader`/`Kicker` uppercase (`UI/components/GymSurfaces.kt:118-150`), used both `compact` (TextTertiary) and bare (TextSecondary, `RestTimerPrefsSection.kt:94`, `BodyweightPrefsSection.kt:112`) |
B10b: | 22 Sep "no loading state" | Defaults are the initial state; nothing says "loading" | Open (F10a) | `UI/settings/SettingsViewModel.kt:55-72,150-154`; `AccountCoordinator.kt:61-68`; `savePostureState` :163-168. See UI-9 |
B10b: | 22 Sep "`BackupCoordinator` is 985 lines" | Unchanged | Open (F10c) | `UI/settings/BackupCoordinator.kt` is 985 lines; all 39 public functions have a caller in `ui/settings` (see §6), so none is dead |
B10b: | 22 Sep cross-tab "10 of 21 ViewModels lose screen state on process death" (Settings part) | `SettingsViewModel` has no `SavedStateHandle` | Open, mostly benign | sub-page, pending dialogs and safety ids are `rememberSaveable` (`SettingsScreen.kt:78-80,130-131`); what the VM loses is listed under UI-14 |
B10b: | 16 Sep D16 (P2) | Settings groups need visible names; "Backup" row gives features not status; "Week generator" / "Your plan" / "Add a new block" overlap | Open (F10a/b) | same lines as the first four rows; `domain/SettingsHomeCopy.kt:12,18` and `PlanSetupCopy.kt:15` still carry the overlapping words |
B10b: | 16 Sep D08 (P1) | Exclusive choices expose a generic role | Open in Settings (F8a pending) | `SettingsChoice.kt:86-92` `SettingsStrip` uses `Role.Button` for both the exclusive Training days strip and the multi-select Training weekdays strip (`SchedulePrefsSection.kt:35-39,61-65`); quiet-hour chips are exclusive but `InstrumentChip` defaults to `Role.Checkbox` (`ReminderPrefsSection.kt:181-187,195-201`; `UI/components/InstrumentChip.kt:84`). `SettingsRadioList` is correct (`InstrumentRow` → `Role.RadioButton`, `GymSurfaces.kt:374-381`) |
B10b: | S-7 | Delete account hidden; app says how to request deletion | Closed (S0a #380) | `domain/AccountSyncGate.kt:26`; `AccountSection.kt:304-318`; `SettingsScreen.kt:477`; pinned by `AccountSectionTest.kt:54-75` and `AccountCoordinatorTest.kt:133` |
B10b: | S-6 (Settings copy) | Copy says live strength workouts are not synced | Closed (S0a) | `domain/SyncCopy.kt:21-25`; `domain/SavePostureCopy.kt:30-32` |
B10b: | L-2 | Permission walk waits until Settings is left | Closed (Q1 #384) | `UI/navigation/AppNav.kt:353-358`; `domain/LaunchPermissions.kt:42-47` |
B10b: | C-1 | Coach goal set in Settings reaches the workout | Closed (W1b #393) | `UI/workout/ActiveWorkoutViewModel.kt:555-574,785` reads `coachPreferences`; B6 owns |
B10c: | 22 Sep · Cardio row | "The composer has no header or back; backdating moves one day per tap." | **Open** (F9 pending) | `ui/activity/ActivityComposerScreen.kt:107-177`: a `Scaffold` with no `ScreenHeader`; the date is `Earlier` / `Later` `TextButton`s (`:156-175`) over `shiftDay(±1)` (`ActivityComposerViewModel.kt:178-180`). Back is system Back (`:105`) and the dock's `Cancel` (`:266-276`). Date label is now readable (`DateCopy.weekdayShort` = `EEE d MMM yyyy`, `ui/units/DateCopy.kt:32-33,56`). |
B10c: | 22 Sep · Cross-tab | "10 of 21 ViewModels lose screen state on process death." | **Closed for this scope, one hole** | Onboarding: `ui/onboarding/OnboardingViewModel.kt:109-150,403-406` (step, answers, weight unit) pinned by `OnboardingViewModelTest:155`. Composer: `SavedStateComposerDraft.kt` (title, day, every line) pinned by `ActivityComposerViewModelTest:140`. Live cardio: `SavedStateCardioDraft.kt` pinned by `LiveCardioViewModelTest:162,182`. Detail: nav argument only. The hole is the custom-week hand-off, UI-3 below. |
B10c: | F8c (FRONTEND_REDESIGN #33) | Onboarding entry; stale comments | **Open** | Guided setup is reachable only from Settings (`ui/navigation/AppNav.kt:544`, `ui/settings/SettingsScreen.kt:365`); Home's empty state says "pin a day in Plan" (`domain/GetStartedCopy.kt:4-13`). Stale: `ui/onboarding/OnboardingScreen.kt:79` "Opened from Home's get-started sheet" — `GetStartedCopy.kt:4` "There is no first-launch sheet". |
B10c: | F9 (FRONTEND_REDESIGN #34) | Cardio composer: standard header, date picker; cardio details | **Open** | Header/picker as above. "Cardio details": the activity detail shows no date at all — UI-4. |
B10c: | FRONTEND_REDESIGN "Cardio/composer" contract | readable elapsed/type/distance/date/zone/units; validation; Finish vs leave-running/discard; interruption recovery | **Partly met** | Elapsed `LiveCardioScreen.kt:270-285`; type/indoor chips `:170-199`; distance in the display unit `:204-221`; validation `domain/ComposerCopy.kt:51-84`, `LiveCardioViewModel.kt:171-176`. Finish / Leave running / Discard: `LiveCardioScreen.kt:78-120`, `ui/components/LeaveCardioDialog.kt`. Recovery: `timer/CardioTimerPersistence.kt` + `SavedStateCardioDraft.kt`. Not met: zone is never shown or settable (UI-8); leaving loses the typed inputs (UI-2). |
B10c: | FRONTEND_REDESIGN "Onboarding" contract | retained answers, step progress, optional labels, full plan preview before writing | **Met**, with UI-5's caveat | Retained: `OnboardingViewModel.kt:119-150`. Progress: `OnboardingScreen.kt:180-191` "Question n of N". Optional: `:388`, `:433-444`. Preview before writing: `applyPlan` (`OnboardingViewModel.kt:361-393`) is the only plan write; `OnboardingApplier.apply` has one call site. Pinned by `OnboardingFocusTest:261`, `OnboardingViewModelTest:155`. |
B10c: | FRONTEND_REDESIGN "Summary/details" contract | saved truth, accurate totals, Done, missing/read-error cases, correction journeys | **Partly met** | Missing vs failed read: `ActivityDetailScreen.kt:100-134`, `ActivityDetailViewModel.kt:131-145`, pinned by `ActivityDetailViewModelTest:327,378`. Correction = notes and delete only (`ActivityDetailViewModel.kt:103-129`); set/interval repair is refused by design (`data/repository/ActivityRepository.kt:317-321`, `docs/architecture/completed-training-convergence.md:41,50`). No date on the receipt: UI-4. |
B10c: | 16 Sep D16 | "Log or start cardio" header action competes with Body/History titles | **Open** (B9a/B10a own) | `domain/StartOptionsCopy.kt:8` still `"Log or start cardio"`. |
B10c: | ADR-028 §2 order (L-1, L-2 fixed in Q1) | intro → chooser → permission walk | **Held** (B9a owns) | `MainActivity.kt:63-93`; `ui/saveposture/SavePostureHost.kt:60-64`; `AppNav.kt:352-360`. Nothing in onboarding contradicts it; no new `L-` item. |
B13a: | C-2 | Engine runs three times per tap; `RuleTrace.generatedAtMs` defeats equality so the flow re-emits every tap | Open (reduced; W2c pending) | Timestamp half confirmed: `domain/SetMicroRec.kt:425-451` builds `trace = RuleTrace.forMicroRec(... nowMs = inputs.nowMs)` into the data class (`:54-61`), and `ui/workout/ActiveWorkoutViewModel.kt:570` passes `nowMs = time.nowMillis()` on every `combine` run, so no two `microRec` values are ever equal and `stateIn` (`:576-580`) never dedupes; `ui/workout/NextSetRecommendation.kt:71` then re-runs `remember(rec) { CoachEngine.fromMicroRec(rec) }` on each. "Three times" is no longer the shape on the floor: one `CoachEngine.suggest` per stepper tap (`ActiveWorkoutViewModel.kt:544-580` → `SetMicroRecUi.kt:43`), one `fromMicroRec` reshape in composition (a `CoachDecision` rebuild, `domain/coach/CoachEngine.kt:76-82`, not a second `Coach.decide`), one more per lift select (`ActiveWorkoutViewModel.kt:774`). The rest page runs its own two (`ui/workout/RestTimerViewModel.kt:75,139`). |
B13a: | 22 Sep live-workout row | `ActiveWorkoutViewModel` 2,541 lines, 23-field state, 13-stage `combine`; rest logic duplicated with `RestTimerViewModel` | Open (reduced; W2d pending) | 2,362 lines (`wc`), 31 `MutableStateFlow` references, `ActiveWorkoutUiState` has 23 `val`s (`ActiveWorkoutViewModel.kt:160-`); `uiState` is now **18 `combine` calls over 24 inputs** (`:605-717`, input list in §2 notes), `microRec` 4 calls / 10 inputs (`:544-580`), `primaryAction` 2 calls / 6 inputs (`:1548-1557`). Rest commands shared by W2b-2 (#411, `ui/workout/RestCommands.kt:58`). |
B13a: | 22 Sep churn debris (W2a) | ~320 dead lines in `SetEntryPanel`, dead branches in `FloorInstrumentBar`, 6 unused components, ~10 unused ViewModel functions, `FloorCompactChrome` constants only tests read | Partly Closed | #409 (`8643ba3`): 77 files, +619/−2,446. `FloorCompactChrome` Closed: three functions remain, each read in main (`ActiveWorkoutScreen.kt`, `ActiveWorkoutViewModel.kt`, `ExerciseHeader.kt`). `SetEntryPanel.kt` is 324 lines and used by `ui/history/SetEditSheet.kt`. `FloorInstrumentBar` is called (`ui/components/RestTimerUi.kt:270`); its branches were not re-audited. **Remaining:** 7 unused composables and 8 ViewModel functions with zero production callers — AR-6 in §3. |
B13a: | 22 Sep Plan row | Dead UI: `PreferenceBlock`, `PlanLighterChip`, `PlanRecoveryCommands` | Open (F7a) | Zero call sites in main: `ui/plan/PreferenceBlock.kt:23`, `ui/plan/PlanScreen.kt:540,647` (direct grep, comments excluded). |
B13a: | 22 Sep System row | `SyncWorker` casts the Application (S4) | Open (S4) | `data/sync/SyncWorker.kt:14`. The six bypass groups CURRENT_STRUCTURE names are exactly the 13 files that cast: `timer/` (5 files, 7 sites), `reminder/` (4), `SyncWorker`, `MainActivity.kt:62,102,129`, `ui/update/DebugUpdateBanner.kt:38`, `ui/navigation/AppNav.kt:294` via `appContainer()`. `AppContainer.debugUpdate` and `syncCoordinator` are not on `AppDependencies` (`AppContainer.kt:82,138`), which is why those casts exist. |
B13a: | CURRENT_STRUCTURE "Known debt, named" | Sizes, `PlannerRepository` fusion, no repository interfaces, `PlanDayViewModel` untested, test scaffolding in main | All still true | AWVM 2,362 / REVM 1,490 / BackupCoordinator 985 / WorkoutRepository 1,340 (`wc`); `PlannerRepository.ensureWeek` schedules reminders inside the schedule transaction (`data/repository/PlannerRepository.kt:283-305`); no `interface` for any of the 10 repositories (`AppDependencies.kt:61-99` binds concrete classes); no test file references `PlanDayViewModel`; the five scaffolding objects each have one main reference (`AccessibilityMatrix`'s two extra mentions are KDoc in `domain/TalkBackPolicy.kt:7-8`). |
B13a: | ADR-003 KMP gate | `domain` standalone | Held | 206 files; six distinct outside imports (`kotlin.math.{abs,max,round,floor}`, `kotlinx.coroutines.CancellationException`, `kotlinx.coroutines.flow.Flow`); every `java.time`/`Locale` mention in `domain/` is a comment. |
B13a: | 16 Sep D01–D17 | visual | none in this scope | — |
B13b: | 22 Sep, live workout: "About 1,100 `ui/workout` test assertions check source text rather than rendered screens. T1." | Source-string pins on the floor | Closed for `ui/workout` (T1a–T1c-2, #388/#392/#404/#408); Open elsewhere → TS-4 | 20 `ui/workout` test files still read source through `ui/workout/SourceText.kt` (129 calls), but they are bans: 227 `assertFalse(…contains(` / `assertWorkoutPackageLacks(` against 16 `assertTrue(…contains(`, and those 16 sit on `notes` copy strings, not code (`FloorImageLedHeroTest.kt:85-87`, `FloorPacketHFinalPassTest.kt:166-169`, `RestIdlePresentationTest.kt:68-69`, `WorkoutLogBarTest.kt:39,47`). `SourceText.kt:5-10` states the ban-only rule. |
B13b: | Foundation audit item 3: "core workout surface has no behavioral ViewModel or Compose UI tests" | No floor tests | Closed | `ui/workout/ActiveWorkoutViewModelTest.kt` (96 tests, 5.2 s in the baseline), 44 `*RenderTest` files compose the real floor (`WorkoutFloorRenderTest.kt:297-319`); JaCoCo `ui.workout` 88.2 % (§7). |
B13b: | Foundation audit item 3: "one stale package-id smoke assertion" | Device smoke pinned the release id | Closed | `androidTest/…/InstrumentationSmokeTest.kt:19` asserts `BuildConfig.APPLICATION_ID`; `:25-28` asserts the `.debug` suffix. |
B13b: | CURRENT_STRUCTURE "Known debt": `PlanDayViewModel` has no tests | Untested ViewModel | Open (unchanged) | 0 test files reference it (grep `\bPlanDayViewModel\b` under `app/src/test`, `app/src/androidTest`); JaCoCo `PlanDayViewModel` 0 % of 496 instructions, `PlanDayScreenKt` 0 % of 2,367. Its only "test" is `ui/plan/PlanDayConfirmTest.kt:18`, a source string `viewModel.deleteSession`. |
B13b: | CURRENT_STRUCTURE "Known debt": `ui/components` ≈ 0.15 test lines per production line | Thin component tests | Open (0.18 now) | 1,588 test lines / 9,021 production; JaCoCo `ui.components` 61.5 %, but 11 component files sit at 0 % (`WeekStripKt` 902 instr, `AddToRoutineSheetKt` 990, `ExerciseThumbGalleryKt` 764, `SetTableKt` 730, `SnapWheelKt` 350, `InstrumentSwitchKt` 318, `LoadTypeChipsKt` 240, `TemperMarkKt` 228, `LeaveCardioDialogKt` 150, `ResumeOrDiscardDialogKt` 131). |
B13b: | X5 (#410): coverage counts Robolectric-loaded classes; floors re-based | Honest coverage numbers | Closed as measurement; the ratchet itself runs nowhere → TS-1 | `app/build.gradle.kts:425-428` (`isIncludeNoLocationClasses = true`, `includes = com.sinura.personaltrainer.*`); baseline `check-coverage.py` 0 findings (§7). |
B13b: | X4 (#398): the `RoutineEditorViewModelTest` 30-second wedge | Wedged JVM in CI | Closed | `ui/routines/RoutineEditorViewModelTest.kt:1082,1388,1396-1405` (`leaveAnyway` paths), comments `:351,1817,1846`; `python3 tools/check-unbounded-waits.py` → `unbounded_waits: 0 (baseline 0)`; baseline: 61 tests in 2.8 s, watchdog never fired. |
B13b: | 22 Sep System: no test ran `MIGRATION_TEMPER_6_7`; device test stopped at v6 (X2a) | Migration lane | Closed | `data/local/TemperMigration5To6Test.kt`, `…6To7Test.kt` (3 tests incl. a 4→7 chain, `:66-80`); `androidTest/…/TemperMigrationDeviceTest.kt:34-43` chains 1→7; `TemperSchemaAssetsTest.kt:29` diffs `app/schemas` against `app/src/debug/assets` (7 JSON copies present). |
B13b: | HANDOFF "What is verified": hosted *Instrumented smoke* is non-blocking; `WorkoutEntryJourneyInstrumentedTest` 955 → 997 px fixed by W1c/W1d; lane green on #399 | Hosted lane state | Closed / holds | `ci.yml:156` `continue-on-error: true`; the last 30 hosted runs (#2074–#2103) all concluded `success` at attempt 1; in the 11 runs I opened at job level (10 trunk pushes + the two 23-minute branch runs) the *Instrumented tests on emulator* step succeeded every time (7 m 16 s – 11 m 21 s) and *Print instrumented failures* was skipped. The #374–#399 history predates this 30-run window; it is HANDOFF's record, not re-verified. |
B13b: | 22 Sep System: the container gate skipped `lintDebug` | Lint not gated | Closed | `ci.yml:84-85` blocking; baseline lint 0 errors, 0 warnings (§7). |
B13b: | 22 Sep S-9: "33 sync tests, none covering these paths" | Sync test gap | Open (sync pass owns it) | My angle only: JaCoCo `data.sync` 48.0 % of 10,334 instr, `SupabaseRestClient` 0 % (646), `SyncWorker` 0 %, `SupabaseSyncRemote` 0 %; 42 `@Test` in `test/data/sync`. See §4. |
B13b: | 16 Sep D01–D17 | Visual items | None touch test posture | — |
