# Handoff — full-tree audit sessions, 29 August 2026

Written by the remote audit session for whoever continues the work (the
owner's Cursor environment). Read this top to bottom before touching
code. The paste-ready continuation prompt is the last section.

## Status, 1 September 2026

Tasks 1 and 2 are done. Task 3 still needs the owner's go-ahead.

- Pre-release **`debug-live-2026-09-01-2`** is the Obtainium drop:
  `PersonalTrainer-1.0.0-debug.apk`, versionCode 17,
  `1.0.0+debug.17`. Include pre-releases, prefer `*-debug.apk`.
- Live test 16 (`debug-live-2026-09-01`) remains on GitHub as the previous
  drop. Obtainium offers 17 because `debugLiveCode` increased.
- **`tools/released-version-code.txt` stays at 1.** That file is the
  gym-floor `appVersionCode` floor. Debug live codes live in
  `debugLiveCode` / `debug-live-*` tags only.

## Where the tree stood on 29 August 2026

- `trunk` == `claude/full-app-audit-s890pj` == `8af08f5`. Working tree
  clean. Nothing unpushed anywhere.
- Two audit rounds ran today. Round one: eight adversarial passes over
  the whole tree, ~45 defects fixed (4 P0). Round two: five fresh lenses
  over the finished tree — including a hostile review of round one's own
  diff — 11 more defects fixed. The complete ledger, including what was
  checked and found sound, is
  **`docs/foundation-program/evidence/FD-full-audit.md`**. Treat it as
  law: do not rediscover a FIXED row as live work; the DEFERRED rows are
  the queue.
- `debugLiveCode = 13` was staged in `app/build.gradle.kts` and unshipped.
  Trigger branch `debug-live/2026-08-29-10` (== trunk) is pushed and the
  `debug-live.yml` workflow fires on it — but every Actions run in this
  account dies in ~4 s with no runner assigned (billing/spending-limit,
  owner-side). The phone has not received a drop containing today's work.

## The one critical caveat

The audit environment could not reach Google's Maven, so **the Gradle
build never ran there**. Every change passed:

- the 17 static checkers + syntax gate (`tools/preflight.sh` — PASS), and
- the 1,066-test JVM lane (`PT_JARS=build/test-jars sh
  tools/run-domain-tests.sh`) — domain, timers, backup, all green.

What has NOT been executed: `./gradlew testDebugUnitTest` (the
Robolectric suites — `PendingOccurrenceTest`, `SettingsViewModel` tests,
`OnboardingViewModelTest`, `RestTimerStatePersistenceTest`, the
ViewModel suites), lint, and `assembleDebug`. The changes were written
carefully against those suites' expectations, but "carefully" is not
"compiled". Assume there may be a handful of trivial breaks (a missed
import, a test constructing a data class positionally) and zero tolerance
for shipping before the full gate is green.

## Task 1 — the gate, then the drop (done 30 August 2026)

1. `./gradlew testDebugUnitTest assembleDebug` (Windows: `.\gradlew`).
2. Fix anything that fails. Today's commits to inspect first if
   something breaks: `8af08f5` (round two — touched
   `PersistedRestTimer`/`PersistedCardioTimer` constructors, added
   `BootSession`, `SettingsViewModel` busy-guards,
   `ActivityComposerViewModel.armTransfer`, `LiveSessionBar`
   `onActionErrorShown` param), and `16b153f`/`b14c69a` (PendingOccurrence
   redesign, reminder Start rewrite).
3. Run the emulator lane for the migration/journey tests
   (`connectedDebugAndroidTest`) per `docs/DEVELOPMENT.md`.
4. Ship the drop: either fix the Actions runner (Settings → Billing →
   Spending limits → Actions; free tier includes 2,000 min/month) and
   push any `debug-live/<suffix>` branch from trunk — the workflow
   builds, tests and publishes the pre-release itself — or cut it by
   hand: `assembleDebug`, create pre-release `debug-live-13` with
   `PersonalTrainer-*-debug.apk` attached, confirm Obtainium sees it.
   After shipping, do **not** append 13 to
   `tools/released-version-code.txt` — that file is the gym-floor
   `appVersionCode` floor, currently 1. See Status above.

## Task 2 — the deferred queue (needs your compiler)

All in `FD-full-audit.md` under "Deferred" — the short version, in
recommended order:

1. **Delete the dead code** (~1,300 lines) — **done 30 August 2026.**
   PlanViewModel start spine, `ui/goals` + `GoalCopy` + tests/preview,
   unused `CompactLiftRow` composable, `LinkRow`, dead `HomeUiState`
   fields, `beginGuided()`. `goalDao` / `GoalRepository` stay for the
   backup format. `CompactTargetFields` stays with the session-lift
   strip. `startNextBlock` stays (Plan uses it).
2. **Extract one `StartOccurrence` use case** — **done 30 August 2026.**
   Home leftover relocation still sits on Home. The start sheet maps
   the sealed outcome. The cardio-start ritual is `StartLiveCardio`.
3. **Make onboarding "Use this plan" retry-safe** — **done 30 August 2026.**
   Routine creates and pins run in one Room transaction. A failed
   attempt drops the routines it just wrote so retry cannot duplicate
   the program. A finished program is still never deleted.
4. **Perf queue** — **done 30 August 2026.**
   Coach hints load in one finished-set query per insights pass, cached
   on (routines, unit, last finished work). Progress's window chip
   retargets only the heat snapshot of the shared assembly. PR detection
   uses MAX aggregates; finished summaries invalidate on finished work,
   not every in-progress set. Rest seconds `shareIn` and wake on the
   second boundary; the live bar drops its poll on routes that hide it.
5. **Tests where a defect would be silent** — **done 30 August 2026.**
   Mapper tests pin enum `.name` tokens and round-trips (unknown
   schedule focus still drops the slot; unknown activity block kinds
   still throw on the Room graph). `ExerciseRepository` covers
   create/rename/delete/search/collisions. Reminder Start/Snooze/Move/
   Skip, reboot rebuild, and the worker reread-then-notify path sit on
   shared apply/work functions the receivers call — Start still does
   not `startActivity`.
6. **Build fat** — **done 30 August 2026.** Toolchain pin files round-trip
   through Gson. kotlinx.serialization is not a shipped artifact; 1.8.1
   remains the Room 2.8 ceiling. The five outlined marks that lived only
   in `material-icons-extended` are vendored; the rest stay on
   `material-icons-core`.
7. **Small UX** — **done 30 August 2026.** BodyweightWheel rounds the
   raw conversion and does not commit the parked page on a unit toggle.
   Cardio preview no longer raises the lift-catalog error. Move-to-today
   (and Move remaining) mint a distinct id when the canonical row is
   already `MOVED`. Still open includes the previous week, so Monday
   still lists Sunday.

## Task 3 — product roadmap (owner-ranked candidates)

Fifteen ranked opportunities were compiled (top: starting-weight
suggestion for new lifts — first set opens at 0.0 kg; warm-up ramp
calculator; supersets; CSV import from Strong/Hevy + export; History
search; layoff-aware progression). Full ranked list with rationale is in
the owner's "Temper Second Pass" report. Confirm priorities with the
owner before starting any of these; they are product decisions, not
defects.

## Rules of the road (unchanged, binding)

- `docs/FOUNDATION_PROGRAM.md` is current law; ADRs 001–019 in
  `docs/architecture/`; `docs/UX_PAGE_PASS.md` product rules (one filled
  Volt per screen; Start only on Home/start-sheet/Repeat; no sixth tab;
  no LLM coach; `fallbackToDestructiveMigration` forbidden).
- `tools/preflight.sh` must pass before any commit; it now also runs the
  1,066-test JVM lane. `PT_STATIC_ONLY=1` gives the fast static-only
  variant.
- Accessibility strings are mirrored in `AccessibilityMatrixTest`; keep
  them in sync.
- Do not re-audit anything listed as "Verified sound" in the ledger
  without new evidence.

## Continuation prompt (paste into Cursor)

> You are taking over the Temper (PersonalTrainer) codebase from a
> completed two-round, full-tree audit. Before anything else, read
> `docs/HANDOFF-2026-08-29.md` and
> `docs/foundation-program/evidence/FD-full-audit.md` in full — they are
> the authoritative record of what is fixed, what is verified sound, and
> what is deliberately deferred. Do not rediscover fixed items; do not
> re-litigate items marked sound.
>
> Task 1 and Task 2 are done (gate green, live test 15 on
> `debug-live-2026-08-31`, `released-version-code.txt` still 1).
> Task 3 (product roadmap) requires the owner's go-ahead per item.
>
> Constraints: follow `docs/FOUNDATION_PROGRAM.md`, the ADRs, and
> `docs/UX_PAGE_PASS.md`; never use `fallbackToDestructiveMigration`;
> keep `AccessibilityMatrixTest` in sync with UI strings; small,
> reviewable commits with the repo's commit-message style; if a change
> and the ledger disagree, trust the code, then correct the ledger in
> the same commit.
