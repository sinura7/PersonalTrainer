# P4.6 evidence — local lint and supply-chain gate

- **Implementation commit:** `9fe54b9`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Lint is a local gate.** `warningsAsErrors` stays on.
`app/lint-baseline.xml` is empty.

**Only signed waivers remain.** Project-level disables are
`AndroidGradlePluginVersion`, `GradleDependency`, and `UseKtx`.
Durable `commit()` stays on the rest timer and schema marker
(`@Suppress("ApplySharedPref")`). Adaptive-icon XML stays in
`mipmap-anydpi-v26` (`app/lint.xml`).

**Supply chain is closed.** Repositories are `google()` /
`mavenCentral()` / plugin portal. Catalog versions are exact.
`gradle/verification-metadata.xml` holds 1081 sha256 checksums
across 604 components.

**Hosted CI is still not the test lane.**

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 705 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 997 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 new lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 18.2% / domain 91.9% / workout 23.0% / util 31.3% / backup 71.9% |
| full `connectedDebugAndroidTest` | 33/33, 0 skipped, 0 failed |

`LintPolicyTest` (3) is the new targeted proof. Device count is
unchanged.

## Finding disposition

FND-026 closed. FND-027 closed. Phase 4 Supported Platform Gate is
green: target SDK 36, Drive auth without Google Sign-In, local lint
policy.

## Limitations

Kotlin stays 2.0.21. Versions that need AGP 9 / compileSdk 37 stay
refused. Phone OAuth UI was not exercised on this VM.
