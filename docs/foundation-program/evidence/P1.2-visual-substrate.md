# P1.2 evidence — preview profiles and deterministic goldens

- **Implementation commit:** `8bace08`
- **Evidence date:** 24 August 2026
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application under test:** `com.sinura.personaltrainer.debug`
- **Golden:** `foundation-state-gallery-api29.png`

## Changed UI surface

The debug-only foundation gallery renders the required fixtures:

- loading;
- empty;
- populated;
- error;
- long identity;
- large metrics;
- active;
- resting;
- notification permission denied.

Reusable IDE annotations cover 360, 412, and 600 dp; font scale 2.0; RTL;
and reduced motion. The gallery baseline is a 360 × 800 dp viewport.

## Commands and results

| Command | Result |
|---|---|
| record targeted golden with `recordGoldens=true` | passed; PNG persisted and reviewed |
| verify `FoundationGoldenTest` without record flag | passed 4/4 |
| `unchangedCapturesArePixelStable` | zero differing pixels |
| fresh APK reinstall versus committed golden | zero differing pixels |
| Volt → Warn probe | visible, bounded 0.1–2% diff with non-empty bounds |
| `tools/preflight.sh` | passed; 647 plain-JVM tests |
| `./gradlew testDebugUnitTest assembleDebug lintDebug jacocoTestReport` | passed |
| `python3 tools/check-coverage.py` | zero findings |

## Review

The committed PNG was opened after recording. It shows Instrument typography,
surface ladder, one Volt rule/action, stable tabular metrics, and meaningful
identity before metrics. The selected tool and renderer scope are documented
in [VISUAL_TESTING.md](../VISUAL_TESTING.md).

## Known limitations

- This packet establishes the substrate, not every page’s baseline. Page
  goldens land with feature packets and P9.6/P9.7.
- A device PNG is intentionally scoped to the recorded API/density/font
  renderer. Cross-profile screenshots use separate names and evidence.
- `LocalReducedMotion` is now an explicit seam; mapping every animation to it
  remains P9.2.

## Finding disposition

P1.2 establishes the visual-regression foundation for FND-043. That finding
closes only after critical page/state coverage in P9.6/P9.7.
