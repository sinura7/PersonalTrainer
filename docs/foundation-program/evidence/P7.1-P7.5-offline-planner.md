# P7.1–P7.5 evidence — Offline Planner Beta

- **Implementation commit:** *(see follow-up evidence commit)*
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**P7.1.** `TemperDatabase` migrates v2 → v3. `schedule_rules`,
`schedule_occurrences`, `missed_work_decisions`, and
`reminder_deliveries` are empty after migrate. `schedule_slots` is
kept. Empty rules plus existing pins import as evening-strength rules
at 18:00. User-added cardio defaults to 07:00. Two occurrences on one
date complete independently: cardio through the activity row,
strength through the persisted pending-occurrence binding.
`fallbackToDestructiveMigration` is absent. Generated `3.json` is
committed (`identityHash` `89413b6feee594abff6411a440ddf1b9`).
`FoundationGeneration.VERSION` is 3. `FROZEN` stays true. Export
version is 4; v3 files still decode with empty planner arrays.

**P7.2.** The Plan day sheet lists dated occurrences and can add
morning cardio. Four tabs remain Home · Body · Plan · History.

**P7.3 / FND-017.** One persisted missed-work decision per week:
Keep dates, Skip missed, Move remaining, Adapt week. Recurrence
rules do not change. Read paths do not mutate the week. Occurrence
generation keeps DONE / SKIPPED / MISSED / MOVED.

**P7.4 / FND-007 reminders.** WorkManager delivers reminders. Actions
are Start, Snooze, Move, Skip. Quiet hours default 22:00–07:00.
Opt-out exists. Rebuild after boot and time-zone change marks
past-due PENDING rows STALE (no catch-up spam). Not exact alarms.
Reminder permission is not requested during onboarding. Start opens
the occurrence through `MainActivity`.

**P7.5.** Home lists today's occurrences. Morning cardio and evening
strength are independently visible and startable. One live activity
still holds.

## Production change

Reviewed pass:
[foundation-generation.md](../../architecture/foundation-generation.md),
[ADR-012](../../architecture/ADR-012-rest-and-reminders.md),
[activity-contract.md](../../architecture/activity-contract.md).
Slot-week derivation remains for the leftover ThisWeekCard fallback.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 768 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1101 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 19.3% / domain 90.6% / workout 23.0% / util 68.7% / backup 70.4% |
| full `connectedDebugAndroidTest` | 34/34, 0 skipped, 0 failed |

Targeted proofs: `TemperMigration2To3Test`, `TemperMigrationDeviceTest`,
`OccurrenceGeneratorTest`, `MissedWorkPolicyTest`, `DailyAgendaTest`,
`ReminderPolicyTest`, `SlotRuleImportTest`, `BackupV4FormatTest`,
`PlannerRepositoryTest`, `PendingOccurrenceTest`,
`ReminderNotificationsTest`, `HomeViewModelTest`, `PlanViewModelTest`.

## Finding disposition

FND-007 closed for reminders and timed occurrences. FND-017 closed.
FND-018 Home agenda is proven; one live activity still holds.

## Limitations

Legacy `schedule_slots` and `WeekDerivation` remain for pin editing
and the leftover ThisWeekCard week strip. That strip may still shift
a missed slot; occurrence UI does not. Strength completion binds
through DataStore, not a column on `workout_sessions`. This debug
install (`com.sinura.personaltrainer.debug`) is a separate id from a
release phone. The owner's release app was not wiped. Phase 8 is not
opened.
