# P1.1 evidence — debug device lane and local gate

- **Implementation commit:** `4669411`
- **Evidence date:** 24 August 2026
- **Host:** Cursor Linux VM, JDK 17
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application under test:** `com.sinura.personaltrainer.debug`

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 647 plain-JVM tests |
| `tools/verify.sh` | passed preflight, Gradle JVM, lint baseline, coverage floors |
| `./gradlew connectedDebugAndroidTest` | passed; 15/15, 0 skipped, 0 failed |
| deliberate broken `TestSeamsTest` | gate failed, then source restored |
| deliberate domain coverage floor 99% | gate failed at 91.9%, then floor restored |
| deliberate unused string resource | lint failed as an error, then resource deleted |

## Durable assertions

- Instrumented target package equals `BuildConfig.APPLICATION_ID`.
- Connected debug target ends in `.debug`.
- Existing package floors are explicit in `tools/coverage-floors.txt`.
- Existing lint findings are in `app/lint-baseline.xml`; a new warning fails.
- Shared test fixtures provide deterministic wall clock, IDs, and elapsed
  realtime without sleeps.

## Known limitations

- This packet proves the local API 29 lane. API 31/34/35 behavior is owned by
  exact-alarm and platform packets.
- The VM’s advertised nested KVM path stalled with the device offline.
  Software acceleration booted in 108 seconds and completed the suite. This
  is an environment limitation, not an omitted test.
- No physical-device behavior was required by P1.1.

## Finding disposition

FND-004 is closed: the stale release-package assertion is gone and the full
checked-in device suite passes against the generated debug application ID.
