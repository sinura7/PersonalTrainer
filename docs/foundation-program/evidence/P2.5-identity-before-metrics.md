# P2.5 evidence — identity before metrics

- **Implementation commit:** `7331ecc`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 17
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**History row (360 dp, font 1.0 and 2.0):** title still contains
"Lower" and "squat"; date still contains "24 Aug"; metrics remain
present on a second line.

**Routine row (360 dp, font 1.0 and 2.0):** a long lift name still
contains "Romanian"; the expanded field is labeled Target weight;
the unit suffix is `lbs`, not the label.

## Production change

`SessionLogRow` is no longer an `InstrumentRow` with 184 dp of
fixed trailing columns. Identity owns the first line; metrics wrap
below. `CompactLiftRow` keeps the name on its own line (two lines
allowed) and moves sets × reps plus reorder to a secondary line.
Target load uses `CompactLiftCopy.TARGET_WEIGHT` with the unit as
`OutlinedTextField` suffix.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 664 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 916 tests |
| `./gradlew lintDebug jacocoTestReport` | passed |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 91.9% / workout 23.6% / util 31.3% |
| full `connectedDebugAndroidTest` | 29/29, 0 skipped, 0 failed |
| `tools/verify.sh` | passed, including the API 29 device lane |

## Finding disposition

FND-006 and FND-016 are closed: identity remains readable at 360 dp
through font 2.0, and the target-weight field names the quantity.

## Limitations

These are component-level constrained Compose tests, not a full
History or Routine Editor journey. TalkBack row announcement is
unchanged (the row still merges descendants via clickable).
