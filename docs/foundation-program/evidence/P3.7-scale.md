# P3.7 evidence — backup scale measurement

- **Implementation commit:** `202dea2`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 17
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Fixture.** 500 finished sessions, 15,000 sets (30 per session), one
authored exercise.

**Signed ceilings.** Encode ≤ 2 s, decode ≤ 2 s, Room snapshot+encode
≤ 4 s, encoded UTF-8 ≤ 8 MiB. Times are JVM-host ceilings, not phone
SLAs.

**Measured on this host.** Encode 230 ms / 4,189,069 bytes. Decode
218 ms. Snapshot+encode 280 ms / 4,189,089 bytes.

**No streaming rewrite.** The whole-document path stays. P8.5 streams
only if a later run exceeds these ceilings.

## Production change

`BackupScaleBudget` plus encode and Room-snapshot tests. No encode
rewrite.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 702 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 982 tests |
| `./gradlew lintDebug jacocoTestReport` | passed |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 92.0% / workout 23.0% / util 31.3% / backup 71.5% |
| full `connectedDebugAndroidTest` | 32/32, 0 skipped, 0 failed |
| `tools/verify.sh` | passed, including the API 29 device lane |

## Finding disposition

FND-038 measurement half is closed: the 500 / 15,000 fixture is
measured and under budget. Streaming remains a later option (P8.5)
if the ceilings fail.

## Limitations

Phase 3's Trustworthy Strength Gate still wants before/after
fingerprints under the full fault matrix. That is the phase exit,
not this packet.
