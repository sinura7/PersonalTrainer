# P9.6 audit follow-up — window boundary

- **Implementation commit:** `189d923`
- **Evidence date:** 25 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

A finished session older than 30 days no longer blanks Body. Heat
bands stay on the windowed graph. `hasAnyWorkingSets` also reads
all-time summaries. The window-empty line is honest when the graph
has no recency: it does not claim every muscle still shows last
trained.

Activities that feed heat/coach use the same 30-day cutoff as
workouts. Restore rejects unknown measurable-goal `kind` / `period`,
non-positive targets, and implausible timestamps instead of mapping
them to `SESSION_COUNT` / `WEEK`. Week-strip day letters, dates, and
labels use `TextSecondary` or stronger. Reduced motion on resume and
Home last-session were already closed in `6ef2802`.

Per-muscle recency older than 30 days still needs a dedicated query.
That is not invented from summaries. Physical TalkBack remains P9.7.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 817 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1157 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 19.3% / domain 90.9% / workout 23.0% / util 68.7% / backup 70.7% |
| full `connectedDebugAndroidTest` | 38/38, 0 skipped, 0 failed |

Targeted proofs: `TrainingInsightsCalculatorTest`
lifetimeSummariesKeepTheBodyMapFromLookingUntrained,
`ActivityInsightTest`, `BackupValidatorTest` goal cases.

## Finding disposition

The 30-day graph no longer answers “have I ever trained.” FND-024
week-strip cells no longer use decorative-only contrast. Backup goal
decode no longer silently remaps. P9.6 Body/Plan/History/Library
page passes and P9.7 physical TalkBack remain open.
