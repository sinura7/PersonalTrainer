# P3.1 evidence — backup threat-model inventory

- **Implementation commit:** `PENDING`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 17
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Policy is not reopened.** Auto Backup will be disabled; user-controlled
backup is authoritative; Drive is backup, not sync.

**Coverage:** the signed inventory names every store in §3, the four
required channels (device theft, Auto Backup, file leak, Drive), and the
owning packet for each remaining defect.

**Authority:** `tools/check-doc-authority.py` requires the inventory file
and the four coverage needles.

## Production change

None. Shipping `allowBackup="true"` stays until P3.5.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 669 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 932 tests |
| `./gradlew lintDebug jacocoTestReport` | passed |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 92.0% / workout 23.0% / util 31.3% / backup 64.8% |
| `python3 tools/check-doc-authority.py` | 0 findings |
| full `connectedDebugAndroidTest` | 29/29, 0 skipped, 0 failed |
| `tools/verify.sh` | preflight/unit/lint/coverage passed; first connected pass flaked on `ActiveWorkoutJourneyInstrumentedTest` `assertIsDisplayed`; isolated rerun 2/2 and full rerun 29/29 |

## Finding disposition

FND-011 inventory half is signed. Manifest and encryption remain P3.5
and P3.6. FND-014A–C and FND-038 are mapped, not closed.

## Limitations

This packet does not change restore, snapshots, or the manifest.
The journey `assertIsDisplayed` flake is pre-existing timing, not a
P3.1 production change.
