# P5.3–P5.7 evidence — TemperDatabase, export v3, reset, freeze

- **Implementation commit:** `81e62f4`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**New generation.** `TemperDatabase` (`temper.db`, v1,
identityHash `a07cac89e03d333eff3c9566d8c5a637`) sits beside legacy
`TrainerDatabase` v2. Not a silent v2→v3 patch.
`fallbackToDestructiveMigration` is absent from both.

**One live is transactional.** `liveToken` unique index plus
`ActivityRepository.confirm` inside `withTransaction`.

**Export v3.** `activities` and `activityTemplates` round-trip.
v1/v2 files still decode.

**Signed reset.** Requires off-device export acknowledgement and
irreversible copy. Integrity-check and catalog seed happen before
legacy DB deletion. Weight unit and rest sound / vibration /
default duration are preserved.

**Frozen.** `FoundationGeneration.FROZEN` is true. `AppContainer`
opens `TemperDatabase` only. A second wipe is refused.

## Production change

Reviewed pass:
[foundation-generation.md](../../architecture/foundation-generation.md).
`TrainerDatabase` remains for historical v1→v2 migration tests.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 735 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1040 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 18.2% / domain 90.3% / workout 23.0% / util 68.7% / backup 72.8% |
| full `connectedDebugAndroidTest` | 33/33, 0 skipped, 0 failed |

`ActivityContractTest`, `TemperDatabaseTest`, `ActivityRepositoryTest`,
`FoundationExportTest`, `FoundationResetTest`, `FoundationFreezeTest`,
and `BackupActivityFormatTest` are the targeted proofs.

## Finding disposition

FND-002 is persistable. UI/query close is P6.6. FND-019 storage
direction is established; the move is P6.1.

## Limitations

Current production UI still writes legacy `workout_sessions` /
`set_logs` on `TemperDatabase`. Activity tables are the durable
spine; History and Home become activity-aware in Phase 6. This
debug install (`com.sinura.personaltrainer.debug`) is a separate
id from a release phone. The owner's release app was not wiped.
