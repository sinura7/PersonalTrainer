# P9.6 page depth — remaining packets and lifetime recency

- **Implementation commit:** `499c972`
- **Evidence date:** 25 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Lifetime recency.** Heat stays on the 30-day / current-week graph.
A muscle last trained 40 days ago is no longer "Not trained yet."
`WorkoutRepository.observeLastLogged()` overlays
`BodyHeatSnapshot.rememberLifetimeRecency`. No schema change.

**Remaining pages / FND-021 / FND-022.** Routine editor, custom week,
summary, session detail, exercise detail, activity detail, live cardio,
composer, goals, onboarding, and settings keep a named Volt act and
named Back at 360 dp through font 2.0.

**Session delete.** The confirm dialog names the session
(`Delete Upper strength?`), not a generic "this session."

**Live cardio.** Elapsed time is a readout (`Elapsed 7:42`). It is not
a live announcement stream.

FND-044 is unchanged: stock Switch/FAB/menus are not skinned.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 824 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1172 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; domain 91.1% / backup 70.7% / workout 23.0% |
| full `connectedDebugAndroidTest` | 53/53, 0 skipped, 0 failed |

Targeted proofs: `MuscleRecencyTest`, `RemainingPagesPassInstrumentedTest`,
`SessionDetailCopyTest`.

## Limitations

Physical TalkBack is P9.7. Android Public Candidate is not claimed.
