# P9.1–P9.5 evidence — Product coherence

- **Implementation commit:** `7bfb34c`
- **Evidence date:** 25 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**P9.1 / FND-031 / FND-032 / FND-033 / FND-046.** Canonical tasks T1–T7
have named first-click landings. Tabs remain Home · Body · Plan ·
History. Library is a Home and Plan push, not a tab. Goals stay pushed.
Body recommendations sit above the map so coach actions are not
below-fold. Plan command stacking is unchanged: Replay is the empty-week
recovery; Suggest is quiet; Tune/Lighter stay in Tune. The ADR-006
reconsideration gate is encoded and closed without representative-user
sessions — four tabs stay.

**P9.2 / FND-024 / FND-045.** `TextPrimary` and `TextSecondary` meet WCAG
AA on Pit and Surface2. `TextTertiary` fails normal AA and is not
load-bearing. Unselected tab labels use `TextSecondary`. Dark-only
Instrument remains. Reduced motion reads the system animator scale and
collapses token durations to zero. No light theme.

**P9.3 / FND-034.** Live session notes and program notes share
`NotesBlock` with `NotesKind`. Copy stays different on purpose. Swap
sheet remains a family list, not the full picker. Stock Switch/FAB/menus
are not skinned (FND-044 waits on golden mismatch).

**P9.4 / FND-035.** `ExercisePickerState` + `ExercisePickerEvent` replace
the eleven-parameter sheet. Modes are SINGLE_ADD, SWAP, MULTI_ADD.

**P9.5 / FND-036.** `RecommendationIntents.from` emits a typed intent.
The card labels from the same mapping. Progress applies the intent.

## Production change

Reviewed pass:
[ADR-005](../../architecture/ADR-005-instrument-identity.md),
[ADR-006](../../architecture/ADR-006-information-architecture.md).
No fifth tab. No heat-window change.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 808 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1147 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 19.3% / domain 90.9% / workout 23.0% / util 68.7% / backup 70.3% |
| full `connectedDebugAndroidTest` | 34/34, 0 skipped, 0 failed |

Targeted proofs: `InformationArchitectureTest`, `RecommendationIntentTest`,
`ExercisePickerContractTest`, `ContrastPolicyTest`, `MotionPolicyTest`,
`NotesKindTest`.

## Finding disposition

FND-024 closed as a tested contrast policy. FND-035 closed. FND-036
closed. FND-045 remains the permanent dark-only decision, verified.
FND-031 / FND-032 / FND-033 have first-click and disclosure evidence;
P9.6 page passes may still refine copy. FND-034 has a shared notes
contract; remaining row/numeric unification stays for P9.6.

## Limitations

No representative beginner/intermediate user sessions were run. The
reconsideration gate therefore cannot fire. P9.6 page-by-page passes
and P9.7 physical TalkBack remain open. Phase 10/11 stay gated.
