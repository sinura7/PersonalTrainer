# P4.3 evidence — Compose / Material / Navigation / compiler

- **Implementation commit:** `21fc4f4`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Signed matrix.** Compose BOM 2026.06.01 (UI / runtime / foundation
1.11.4, Material3 1.4.0), Navigation Compose 2.9.8, Kotlin Compose
compiler plugin 2.0.21.

**Refused newer Compose.** BOM 2026.08.00 resolves Compose UI 1.12.0,
which fails `checkDebugAarMetadata` on compileSdk 36 / AGP 8.9.2
(wants compileSdk 37 and AGP ≥ 9.1.0). Navigation 2.10.0 is still an
RC and was left.

**Locale is observable.** `TrainingCalendarCard` weekday labels read
`LocalLocale.current.platformLocale`. The new Compose
`NonObservableLocale` lint is an error, not a baseline dump.

**String routes stay.** `NavHost` still uses the existing `Route`
paths. Type-safe navigation is not this packet.

**Goldens unchanged.** `FoundationGoldenTest` matched the committed
API 29 pixels after the BOM bump. No re-record.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 702 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 989 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 new lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 91.9% / workout 23.0% / util 31.3% / backup 72.1% |
| full `connectedDebugAndroidTest` | 33/33, 0 skipped, 0 failed |

`ComposeToolchainTest` (2) is the new targeted proof. Device count is
unchanged.

## Finding disposition

FND-027 continues. Room, Sign-In, and lint-baseline cleanup remain
P4.4–P4.6.

## Limitations

Kotlin stays 2.0.21. Room stays 2.6.1. DataStore stays 1.1.1.
Google Sign-In remnants stay until P4.5. `createComposeRule` stays on
the junit4 API; the BOM’s v2 rule would switch tests to
`StandardTestDispatcher`.
