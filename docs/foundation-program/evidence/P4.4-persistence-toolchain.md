# P4.4 evidence — Room / DataStore persistence matrix

- **Implementation commit:** `d00403e`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Signed matrix.** Room 2.7.2, DataStore 1.2.1.

**Refused newer Room.** Room 2.8.0–2.8.4 fail KSP with
`AbstractMethodError` on
`GeneratedSerializer.typeParametersSerializers()`. That ABI is newer
than kotlinx.serialization 1.8.1 (P4.2). DataStore 1.3 is still
alpha.

**Schema hashes unchanged.** `1.json` remains
`6d58ad40d5c03785ab29aaf61157f369`. `2.json` remains
`3eedd5301f0344b7802f5d0da2f68b3e`. `MIGRATION_1_2` is still the only
production migration. `fallbackToDestructiveMigration` is still
absent.

**Native DataStore counter.** DataStore 1.2 ships
`libdatastore_shared_counter.so`. The debug packager cannot strip it
on this host; it is packaged as-is.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 702 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 991 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 new lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 91.9% / workout 23.0% / util 31.3% / backup 72.1% |
| full `connectedDebugAndroidTest` | 33/33, 0 skipped, 0 failed |

`PersistenceToolchainTest` (2) is the new targeted proof. Device
migrations stayed in the existing 33.

## Finding disposition

FND-027 continues. Sign-In and lint-baseline cleanup remain P4.5–P4.6.

## Limitations

Kotlin stays 2.0.21. Google Sign-In remnants stay until P4.5.
