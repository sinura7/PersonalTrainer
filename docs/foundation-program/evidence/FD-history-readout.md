# History readout — evidence

Packet: History is an information page. Day / week / month / year / all
chips retotal a hero sessions numeral. Empty still shows the calendar.
There is no Start Volt on this tab.

## What changed

- `AnalyticsHorizon.DAY` is today only. Chip labels are Day / Week /
  Month / Year / All. Spoken window titles stay Today / This week / …
- Horizon picker leads with a sessions readout in a card, then days /
  sets / min. Four equal zeros are gone.
- Empty history no longer uses `EmptyState` + Start. Calendar stays.
  Copy is `Finished sessions land here.` Repeat from a session row is
  unchanged. Start lives on Home.
- Fault still offers Retry.

## Commands

- `tools/preflight.sh` — 986 domain tests
- `./gradlew testDebugUnitTest` — 1446 tests, 0 failures
- `./gradlew assembleDebug` — SUCCESS

## Known limitations

- Hours / a clock picker are not this packet.
- Phone judges Temper Debug via Obtainium.
