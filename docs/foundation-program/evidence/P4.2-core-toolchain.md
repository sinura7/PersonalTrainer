# P4.2 evidence — core AndroidX / Kotlin families

- **Implementation commit:** `f26ffec`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Signed matrix.** Core KTX 1.17.0, Lifecycle 2.10.0, Activity 1.12.4,
coroutines 1.10.2, kotlinx.serialization 1.8.1, Robolectric 4.16,
AndroidX Test 1.7.0 / ext-junit 1.3.0.

**Refused newer cores.** Core 1.19.0 requires compileSdk 37 and AGP
9.1. Lifecycle 2.11.0 requires AGP 9.2. Coroutines 1.11.0 is the
Kotlin 2.2 companion.

**Robolectric emulates API 36.** `robolectric.properties` pins
`sdk=36`. The P4.1 API 35 pin is historical.

**Serialization is a signed seam, not a backup rewrite.**
`CoreToolchain` round-trips through kotlinx.serialization. Gson still
encodes BackupJson. The class lives outside `domain/` so the
plain-JVM domain lane stays plugin-free.

**Quality pass on earlier phases.** Production contracts from P1–P3
remain wired (`allowBackup=false`, `BackupEnvelope`,
`StartSessionOutcome.Blocked`, `volumeDisplayWhole`,
`RestTimerClaimLedger`). Phase headings that still said “next” /
“pending” after those packets were closed are corrected.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 702 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 987 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 new lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 18.1% / domain 91.9% / workout 23.0% / util 31.3% / backup 72.1% |
| full `connectedDebugAndroidTest` | 33/33, 0 skipped, 0 failed |

`CoreToolchainTest` (2) is the new targeted proof. Device count is
unchanged because the SDK target assertion already landed in P4.1.

## Finding disposition

FND-027 continues. Compose, Room, Sign-In, and lint-baseline cleanup
remain P4.3–P4.6.

## Limitations

Kotlin stays 2.0.21. Compose BOM stays 2024.12.01. Room stays 2.6.1.
Google Sign-In remnants stay until P4.5.
