# P6.1–P6.7 evidence — Fitness Recorder Alpha

- **Implementation commit:** *(see follow-up evidence commit)*
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 21 (source/target 17)
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration, on-screen nav
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**P6.1 / FND-019.** `TemperDatabase` migrates v1 → v2. `bodyweight_entries`
and `training_blocks` are empty after migrate. DataStore encoded strings
import once after open. Room is the source of truth. Writers still echo
current kg / current block keys for backup compatibility.
`fallbackToDestructiveMigration` is absent. Generated `2.json` is
committed (`identityHash` `34e0a87449598c56fcfe27137d6a863e`).

**P6.2 / FND-008 strength.** Backdated composer writes
`ActivityDraft(origin=BACKDATED)` through `ConfirmActivity`. Future
dates clamp to today. Nothing is written until Save.

**P6.3.** Cardio-only sessions write zero `exercises` / `set_logs` rows.

**P6.4.** Live cardio persists elapsedRealtime plus a boot marker.
Process death keeps the clock. Reboot keeps the last honest elapsed.
One live activity at a time across the workout and activity lanes.

**P6.5.** Mixed sessions keep ordered typed blocks. Insights and
calendar project strength work only; cardio does not invent kilograms.

**P6.6 / FND-002 / FND-008 / FND-018.** History, calendar, insights,
detail, and summary read completed activities. Cardio-only days appear
without a fake lift row. A live cardio session blocks a strength start
and the reverse.

**P6.7.** Onboarding asks Strength / Cardio / Both after the fork.
Nothing is written until “Use this plan.” Cardio focus generates an
empty lift week. No notification permission during onboarding.

## Production change

Reviewed pass:
[foundation-generation.md](../../architecture/foundation-generation.md),
[activity-contract.md](../../architecture/activity-contract.md).
`TrainerDatabase` remains for historical v1→v2 migration tests.

## Commands and results

| Command | Result |
|---|---|
| `tools/preflight.sh` | passed; 742 plain-JVM tests |
| `./gradlew testDebugUnitTest` | passed; 1064 tests |
| `./gradlew lintDebug jacocoTestReport` | passed; 0 lint issues |
| `python3 tools/check-coverage.py` | zero findings; timer 19.3% / domain 90.6% / workout 23.0% / util 68.7% / backup 72.8% |
| full `connectedDebugAndroidTest` | 34/34, 0 skipped, 0 failed |

Targeted proofs: `TemperMigration1To2Test`, `TemperMigrationDeviceTest`,
`PreferencesRoomHistoryTest`, `EncodedHistoryImportTest`,
`ActivityComposerViewModelTest`, `OneLiveCrossLaneTest`,
`CardioElapsedTest`, `ActivityHistoryProjectionTest`,
`OnboardingFocusTest`.

## Finding disposition

FND-019 closed. FND-002 closed. FND-008 closed. FND-018 one-live is
proven across both live lanes; Home agenda remains P7.5.

## Limitations

Legacy `workout_sessions` / `set_logs` remain for the live strength
logger. New and backdated work can also be an `ActivitySession`.
This debug install (`com.sinura.personaltrainer.debug`) is a separate
id from a release phone. The owner's release app was not wiped.
Phase 7 is not opened.
