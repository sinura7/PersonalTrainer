# P2.3 evidence — compact notification-denial state

- **Implementation commit:** `PENDING`
- **Evidence date:** 24 August 2026
- **JVM host:** Cursor Linux VM, JDK 17
- **Device:** `temper-tests-api29`, API 29, x86_64, 1080 × 1920,
  420 dpi, software acceleration
- **Application:** `com.sinura.personaltrainer.debug`

## Behavior contracts

**Copy:** the one explanation and the recovery row never say
"reliable" and never reuse the old "Notifications are blocked" banner
body. Recovery title and action stay one short line each.

**Constrained Active Workout (360 dp, font 2.0):** recovery row is
visible; the fat banner body is absent; Log stays on screen and names
the payload (`Log … × …`); current lift and set-entry wells are
reachable by scroll.

## Production change

`RestNotificationsDisabledBanner` is gone. After the existing
why-dialog, denial recovery is `RestNotificationRecoveryRow`: one
`InstrumentRow` (`Rest alerts off` / `Turn on`) pinned above RestDock.
The OS notification-settings deep link is unchanged.

## Commands and results

Recorded after the packet gate. See the following commit for counts.

## Finding disposition

FND-015 is closed: set entry remains discoverable and recovery is not
hidden. The one full explanation still happens before the OS prompt.

## Limitations

API 33+ POST_NOTIFICATIONS grant/deny on a physical phone is an owner
check. This lane forces the recovery row through
`restNotificationsEnabledOverride` because the API 29 emulator has no
runtime notification permission.
