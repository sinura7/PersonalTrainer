# Handoff — 3 September 2026, for the Cursor environment

Read this top to bottom before touching code. It replaces
[HANDOFF-2026-09-02.md](HANDOFF-2026-09-02.md). The 2 September document
described `trunk` as `0063de6` with CI red and Dependabot `#114`/`#116` as
Cursor's to merge. That is a day behind the repo.

Two agents split by capability. Claude Code has no Android SDK, so it
takes only what `tools/preflight.sh` can prove. Cursor has the SDK and
takes every Gradle loop and every device packet. Do not redo §2.

## 1. Where the tree stands

| Thing | State |
|---|---|
| `trunk` | `52c2320` "J4: freeze civil today through AppDependencies.time" |
| Cursor JVM | **1654 tests, 0 failures** on that commit (TimePort +1 vs K2's 1653) |
| `appVersionCode` / `appVersionName` | `1` / `1.0.0` |
| `debugLiveCode` | `18` |
| `tools/released-version-code.txt` | `1` (gym-floor ratchet; there is no `v*` tag yet) |
| Dependabot | `#114` and `#116` **closed as refuse**. `#121` ignore rules are on `trunk`. Open: `#125` AGP 9.3.2, `#126` play-services-auth 22 — do not merge; they are K2 / P4.5. |
| Repair packets | Phase A done. B3 done. J4 TimePort done. J4 scheduler polish in this packet. Policy tests still owed. J3 ledger slice done. |
| Owner decisions | 6 still, see §6 |
| Test count | **1654** — a change means behaviour moved, not waits |

GitHub Actions is not the test lane. `mergeable_state: unstable` is
noise. Cursor `./gradlew testDebugUnitTest` + `assembleDebug` is the
push gate.

`claude/app-audit-optimization-xnqf5e` is still on the remote
(`ahead_by: 1` vs `trunk`). Do not delete it. Other leftover heads from
the 2 September list are gone.

## 2. Already done — do not redo

On `trunk`:

1. Phase A (A1–A6).
2. J4 first half (`#120`) and the two barrier tests (`#122`).
3. J4 second half: `AppDependencies.ioDispatcher` / `computeDispatcher`,
   `FakeAppDependencies.scheduler`, poll helpers replaced with `first { }`.
   Room query = fixed pool of 2 (`room-query-test`); transaction = single
   thread (`room-txn-test`). **Do not put Room on a test dispatcher.**
   `UnconfinedTestDispatcher.dispatch` throws unless the caller is
   `yield`; `StandardTestDispatcher.asExecutor()` queues work
   `runBlocking` never pumps.
4. Dependabot ignore rules (`#121`). Signed P4.2–P4.4 refuse the
   `#114`/`#116` versions. Regenerating the ledger is not enough.
5. B3: same-boot rest expiry is always `Expired`. `playCue` on
   `RestTimerCompletion.completeOnce` is a B1-file seam (Floor findings
   2026-09-03).
6. K2 tripwire: `check-sdk-target.py` fails if a Kotlin 2.2 stdlib
   enters the ledger. The compiler train is still held.
7. J4 TimePort: `AppDependencies.time`. Civil today freezes.
   `ProgressViewModelTest.markLighterWeekUsesFrozenCivilTodayNotWallClock`.
   Rest stays on `elapsedRealtime`. Count 1654.

J4's delivered shape is Robolectric + `runBlocking` +
`UnconfinedTestDispatcher` + value waits. **`runTest` is struck.** Do
not convert the suite as a drive-by.

## 3. Next packets, in order

One packet, one `cursor/<slug>-a14c` branch, one PR, squash-merge,
delete the remote head. Do not wait for a phone check.

1. **`scheduler=` polish** (this packet). Pass `scheduler = dispatcher`
   into remaining ViewModel tests that hop IO/compute. Do not convert
   `TrainingInsightsSourceTest`. Leave the three named bounded waits.
2. **B4** timer surfaces, then **B1** rest service. They share
   `RestTimerService.kt` and `RestTimerController.kt`. B4 first. B4 must
   Floor-find `RestTimerCompletion.kt` (`lastCompletedTimerId`) and
   amend Owns for `Common.kt` / `RestTimerScreen.kt`.
3. **J3** size, then **J2** ratchet (there is no `v*` tag — keep floor
   1 until the first gym-floor tag), then **J5** skip baselines. K2
   tripwire before J5. J2 before J1 (`release.yml`).
4. **J1** redact-by-default + mapping upload + `assembleRelease`.
   Settings toggle waits for H2. Phone walk is owner decision 6.

**Do not start B2** until owner decision 3 (Alarm). Ready-to-cut:
four-arg `MediaPlayer.create` + `USAGE_ALARM`.

**Do not start Phase C** until this house/timer leftover train is on
`trunk`.

Policy tests into `tools/` stay held: they drop the 1652 count.

## 4. Constraints

1. Never weaken `gradle/verification-metadata.xml` or
   `tools/check-supply-chain.py`. `verify-metadata` stays true.
2. Never skip, disable, or quarantine a test.
3. `tools/released-version-code.txt` is the gym-floor ratchet. Do not
   write `debugLiveCode` into it.
4. Packet Owns: a deviation is a Floor finding, newest first,
   76-character wrap.
5. Robolectric `sdk=35`, Java 17. Raise only with the JDK.
6. Commit style: sentence subject plus why, with `file:line` evidence.

## 5. How to verify

```
sh tools/syntax-check.sh app/src/test/java
PT_JARS=build/test-jars sh tools/preflight.sh
./gradlew testDebugUnitTest lintDebug assembleDebug assembleDebugAndroidTest
```

`tools/preflight.sh` does not compile `app/src/test`. Type errors in
the test tree surface only under Gradle. The count is **1652**.

## 6. The owner's

Six decisions still block six packets: 1 start-sheet host (D1), 2 how
late is "missed" (C3), 3 rest cue channel (B2), 4 Settings Export
prominence (F2), 5 unpinning erases history (C2), 6 signed release on a
phone (J1).

Phone: Obtainium, include pre-releases, `PersonalTrainer-*-debug.apk`.
That install is **Temper Debug**. Gym-floor **Temper** stays on the
signed APK. After a timer packet the owner cares about, bump
`debugLiveCode` and cut `debug-live-YYYY-MM-DD`.

## 7. Continuation prompt

> Read `docs/HANDOFF-2026-09-03.md` and `docs/REPAIR_PROGRAM.md`. Do not
> rediscover §2. Fetch `origin/trunk`. Cursor JVM is the gate; ignore
> Actions. Next packet is B4, then B1, then J3, J2, J5, J1. Do not
> start B2 without decision 3. Do not start Phase C. Never
> weaken the ledger. Never skip a test. Never put Room on a test
> dispatcher. Never convert the suite to `runTest`.
