# Handoff — 3 September 2026, for the Cursor environment

Read this top to bottom before touching code. It replaces
[HANDOFF-2026-09-02.md](HANDOFF-2026-09-02.md). The 2 September document
described `trunk` as `0063de6` with CI red and Dependabot `#114`/`#116` as
Cursor's to merge. That is a day behind the repo.

Two agents split by capability. Claude Code has no Android SDK, so it
takes only what `tools/preflight.sh` can prove. Cursor has the SDK and
takes every Gradle loop and every device packet. Do not redo §2.

## 1. Where the tree stands

| Thing | State |
|---|---|
| `trunk` | J4 remainder this packet; fetch `origin/trunk` after merge |
| Cursor JVM | **1736 tests, 0 failures** |
| `appVersionCode` / `appVersionName` | `1` / `1.0.0` |
| `debugLiveCode` | `20` |
| `tools/released-version-code.txt` | `1` (fallback until the first `v*` tag; git is source of truth after that) |
| Dependabot | `#114` and `#116` **closed as refuse**. `#121` ignore rules are on `trunk`. Open: `#125` AGP 9.3.2, `#126` play-services-auth 22 — do not merge; they are K2 / P4.5. |
| Repair packets | Phase A done. B3–B1 done. B2 done. C1–C4 done. D1–D3 done. E1–E4 done. F1–F6 done. G1–G6 done. H1–H3 done. J3 done. J2 done. J5 done. J4 TimePort done. J4 scheduler polish done. J4 remainder done. J1 code done. |
| Owner decisions | 6 still, see §6 |
| Test count | **1736** — a change means behaviour moved, not waits |

GitHub Actions is not the test lane. `mergeable_state: unstable` is
noise. Cursor `./gradlew testDebugUnitTest` + `assembleDebug` is the
push gate.

`claude/app-audit-optimization-xnqf5e` is still on the remote
(`ahead_by: 1` vs `trunk`). Do not delete it. Other leftover heads from
the 2 September list are gone.

## 2. Already done — do not redo

On `trunk`:

1. Phase A (A1–A6).
2. J4 first half (`#120`) and the two barrier tests (`#122`).
3. J4 second half: `AppDependencies.ioDispatcher` / `computeDispatcher`,
   `FakeAppDependencies.scheduler`, poll helpers replaced with `first { }`.
   Room query = fixed pool of 2 (`room-query-test`); transaction = single
   thread (`room-txn-test`). **Do not put Room on a test dispatcher.**
   `UnconfinedTestDispatcher.dispatch` throws unless the caller is
   `yield`; `StandardTestDispatcher.asExecutor()` queues work
   `runBlocking` never pumps.
4. Dependabot ignore rules (`#121`). Signed P4.2–P4.4 refuse the
   `#114`/`#116` versions. Regenerating the ledger is not enough.
5. B3: same-boot rest expiry is always `Expired`. `playCue` on
   `RestTimerCompletion.completeOnce` is a B1-file seam (Floor findings
   2026-09-03).
6. K2 tripwire: `check-sdk-target.py` fails if a Kotlin 2.2 stdlib
   enters the ledger. The compiler train is still held.
7. J4 TimePort: `AppDependencies.time`. Civil today freezes.
   `ProgressViewModelTest.markLighterWeekUsesFrozenCivilTodayNotWallClock`.
   Rest stays on `elapsedRealtime`. Count 1654.
8. J4 scheduler polish: remaining ViewModel tests that hop IO/compute
   pass `scheduler = dispatcher`. `TrainingInsightsSourceTest` and the
   three bounded waits stay. Count unchanged.
9. B4: gold flash and lock glance key on `lastCompletedTimerId`, not
   `running == false`. `completeOnce` → `completeIfCurrent` (id then
   halt). `RestLockActivity.onNewIntent`. Null-intent sticky restart
   rehydrates before foreground. `replayExpirationMillis = 0`.
   `FLAG_NO_CREATE` on alarm cancel. Floor-find
   `RestTimerCompletion.kt` (B1 file). Count **1662** (+8).
10. B1: `RestTimerService` collects `snapshot` and `stopNow()` after a
    rest has run. `handleDeadline` keeps a +15s mint. Dropped
    `cancel(RUNNING_ID)` from `completeOnce`. `debugLiveCode` 19.
    Count **1664** (+2). Owner phone: force-idle shade on Temper
    Debug live test 19 (`debug-live-2026-09-03`).
11. J3: `localeFilters("en")`, `dependenciesInfo` out of the APK,
    packaging excludes, `checkReleaseBuilds`, Robolectric
    `android-all-instrumented` under the ledger with `offline=true`,
    explicit `kotlinx-coroutines-android`. Count unchanged.
12. J2: one ratchet in `tools/version_ratchet.py`. First `v*` may
    equal 1; later `v*` must be strictly above the previous `v*`
    tag. No `v*` tag yet — file floor stays 1. Actions SHA-pinned.
    Drop/release yaml run static + `lintDebug`. Count unchanged.
13. J5: skip ceilings in `tools/checker-baselines.toml`. Advisory
    token families from the 1 September audit. Debug + XML scanned.
    Syntax-check captures compiler status. Jars by catalogue
    version + contents. Inherited `todayEpochDay()` is in scope.
    Preflight static is green. Count unchanged.
14. J1: `AppLog.redactMessages = true` in both build types
    (`PersonalTrainerApp.onCreate` and the `AppLog` default).
    `-keeppackagenames com.sinura.personaltrainer.**`. `release.yml`
    archives `mapping.txt`. Settings debug Log switch is H2.
    `assembleRelease` on Cursor is unsigned without secrets — it
    cannot update gym-floor Temper. Count **1665** (+1). Phone walk
    is owner decision 6.
15. B2: rest cue is Alarm. Four-arg `MediaPlayer.create` with
    `USAGE_ALARM` before prepare. Silent ringer is not a mute.
    Done channel `rest_timer_done_v3` bypasses DND. `debugLiveCode`
    20. Count **1667** (+2). Phone: media volume 0 then DND.
16. C1: `generateWeek` skips a rule created after that day began
    when the slot is already behind now. Last month's rule still
    mints Monday. Plan `addCardio` goes through `DayBlocks`.
    Same-day hour clamps to `currentHour + 1`. Replay drops days
    behind today. Count **1672** (+5).
17. C2: ADAPT keeps disabled once-rules and relocated ids. A rule
    with history is retired (`enabled = false`) instead of
    `deleteRule`. `RoutineRepository.delete` calls
    `onRoutineDeleted` in the same transaction. Count **1676** (+4).
18. C3: overdue is the civil day, not the session hour. A 19:00
    row is not overdue at 19:01 and is overdue tomorrow morning.
    Today-MISSED stays startable. `TodayTicker` re-reads on resume
    and at local midnight. Count **1678** (+2).
19. C4: move writes sit in one transaction; WorkManager runs after
    commit. Discard is `DELETE … AND finishedAt IS NULL`. Count
    **1680** (+2).
20. D1: start sheet hosted on Body, History, Plan. Never Home.
    Quiet "Log or start cardio" openers. Composer `past` is STRENGTH.
    Unplanned live cardio from an empty week. Cardio discard is
    guarded like the live bar. Count **1686** (+6).
21. D2: reminder Start switches to Home first, then starts. Body tap
    uses `EXTRA_REVIEW_OCCURRENCE_ID` and opens the ADR-018 confirm.
    A vanished row surfaces `ReminderCopy.GONE`. Count **1694** (+8).
22. D3: live cardio hides Home's filled Volt. Error banners
    dismiss. Routine editor, custom week, and `editingSetId`
    survive process death. Double-tap `logSet` writes one set.
    Count **1699** (+5).
23. E1: insights recency is finished-only and generation-gated.
    `pref()` equality on all 26 DataStore maps. Equal `Sources`
    skip compute; refresh still nudges. Count **1703** (+4).
24. E2: catalog thumbs are 256px, decoded on IO through
    `ThumbCache`. Body unlit pair is 1024. Heat mark samples 2.
    Count **1705** (+2).
25. E3: nav root collects `hasLiveSession` only.
    `LiveSessionBarHost` owns elapsed ticks. Rest snapshot
    publishes immediately; persist then arm is one IO job.
    Halt cancels the alarm before the clear job. Count **1709**
    (+4).
26. E4: `ensureWeek` upserts only generated rows. Plan day is a
    thin VM. History month/horizon derive from a catalog.
    `historyBefore` is one batched query. Count **1711** (+2).
27. F1: after a log, `bringIntoView` targets the entry wells.
    Log is disabled while `logging`. Last-time chips apply that
    set with a tick. Count **1714** (+3).
28. F2: leftover Start is secondary when a recovery Volt is present.
    Plan empty is compact. Custom-week filled days are a check.
    Settings Export is the Volt (decision 4). Count **1717** (+3).
29. F3: TextTertiary is #7F8B93. TextDisabled is the old grey.
    Load-bearing text on every reading surface clears 4.5:1.
    Count **1718** (+1).
30. F4: `tileNumeral` keeps painted 36 sp once wells stack. Home /
    Summary / Activity tiles stack at 1.6. Live-bar rest and sets
    hide at that scale. Body and onboarding chips wrap. Count
    **1720** (+2).
31. F5: Library says lift. Resume is `LiveBarCopy.RESUME`. Receipt
    cardio captions speak to the user. Count **1722** (+2).
32. F6: Keep is the Volt. Move / Adapt / Skip sit behind Other
    choices. Home Add is inside Today's list. Count **1723** (+1).
33. G1: Kickers are headings. Rows publish Button / Radio / Switch.
    Rest-finished and the record banner are polite live regions.
    Bodyweight numeral opens the number dialog. Physical TalkBack
    stays false. Count **1726** (+3).
34. G2: comma decimals parse on every typed path. Sets → Reps → Rest →
    Weight is Next then Done. Custom rest uses the number pad. Count
    **1727** (+1).
35. G3: `ScreenHeader` and `PinnedDock` are the chrome. Ten headers,
    six docks. Count **1728** (+1).
36. G4: `InstrumentSwitch`, `InstrumentMenu` on Surface3 with a
    hairline, snackbars through Gym banners, field border ≥ 3:1.
    Count **1731** (+3).
37. G5: figure is 45% of the screen clamped 300–440; legend above
    the silhouette; calendar cell 48; Plan-day Remove confirms in
    Danger ink; idle Start rest is Volt. Count **1735** (+4).
38. G6: remaining motion sites collapse to zero; dwells live on
    `Motion`; palette collisions stay with a non-colour channel
    (ADR-023). Count **1738** (+3).
39. H1: HorizonPicker scrolls with the list. Calendar opens on this
    week; Month discloses the grid. Horizon readout adds PRs and
    Moved most via `BlockReviewBuilder.overRange`. Home last-session
    tile signs the delta versus the previous visit of the same
    routine. Count **1744** (+6).
40. H2: `DateCopy` is the stamp grammar (`ui/units`, not
    `domain/` — seam policy). Hours chips drive History, session,
    composer, and backup stamps. Distance follows weight (pounds →
    mi, kilograms → km). Debug Settings Log switch writes
    `AppLog.redactMessages` for this process only. Count **1748**
    (+4).
41. H3: landscape workout/rest chrome (`LandscapeChrome`),
    `WorkoutLiftCard` state+events, public `DangerGymButton` /
    `CountBadge` / `Numeral`, six gym-floor goldens in the catalog,
    component gallery, tabular measure. Count **1752** (+4).
42. J4 remainder: sixteen source-reading policy tests live in
    `tools/` checkers (`test_policy_move.py` is the fixture proof).
    Gson toolchain round-trips, Drive About parse, and TimePort
    runtime types stay on the JVM. Count **1736** (−16).

J4's delivered shape is Robolectric + `runBlocking` +
`UnconfinedTestDispatcher` + value waits. **`runTest` is struck.** Do
not convert the suite as a drive-by.

## 3. Next packets, in order

One packet, one `cursor/<slug>-a14c` branch, one PR, squash-merge,
delete the remote head. Do not wait for a phone check.

1. **Stop.** K1 and K2 stay held. Leave Dependabot `#125` / `#126`
   unmerged. J1 signed phone walk is the owner's.

**Do not start K1 or K2.** Leave Dependabot `#125` / `#126` unmerged.

## 4. Constraints

1. Never weaken `gradle/verification-metadata.xml` or
   `tools/check-supply-chain.py`. `verify-metadata` stays true.
2. Never skip, disable, or quarantine a test.
3. `tools/released-version-code.txt` is the fallback floor until the
   first `v*` tag. After that, git is source of truth. Do not write
   `debugLiveCode` into the file.
4. Packet Owns: a deviation is a Floor finding, newest first,
   76-character wrap.
5. Robolectric `sdk=35`, Java 17. Raise only with the JDK.
6. Commit style: sentence subject plus why, with `file:line` evidence.

## 5. How to verify

```
sh tools/syntax-check.sh app/src/test/java
PT_JARS=build/test-jars sh tools/preflight.sh
./gradlew testDebugUnitTest lintDebug assembleDebug assembleDebugAndroidTest
```

`tools/preflight.sh` does not compile `app/src/test`. Type errors in
the test tree surface only under Gradle. The count is **1736**.

## 6. The owner's

Six decisions: 3 (Alarm) is applied on `trunk` as B2. 5 (history
survives unpin) is applied as C2. 2 (missed = end of the civil day)
is applied as C3. 1 (restore the start sheet) is applied as D1.
Still open: 6 signed release on
a phone (J1). Decision 4 (Settings Export) is applied as F2.

Phone: Obtainium, include pre-releases, `PersonalTrainer-*-debug.apk`.
That install is **Temper Debug**. Gym-floor **Temper** stays on the
signed APK. After a timer packet the owner cares about, bump
`debugLiveCode` and cut `debug-live-YYYY-MM-DD`.

## 7. Continuation prompt

> Read `docs/HANDOFF-2026-09-03.md` and `docs/REPAIR_PROGRAM.md`. Do not
> rediscover §2. Fetch `origin/trunk`. Cursor JVM is the gate; ignore
> Actions. Next packet is none — K1 and K2 stay held. Leave `#125` /
> `#126` unmerged. Never weaken the ledger. Never skip a test. Never
> put Room on a test dispatcher. Never convert the suite to `runTest`.
